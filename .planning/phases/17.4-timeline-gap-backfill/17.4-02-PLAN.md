---
phase: 17.4-timeline-gap-backfill
plan: 02
type: execute
wave: 2
depends_on: [17.4-01]
files_modified:
  - src/main/java/com/lostark/tracker/collect/dto/MarketItem.java
  - src/main/java/com/lostark/tracker/collect/ItemFetchResult.java
  - src/main/java/com/lostark/tracker/collect/ItemFetchService.java
  - src/main/java/com/lostark/tracker/backfill/BackfillCaptureService.java
  - src/main/java/com/lostark/tracker/collect/PriceCollector.java
  - src/test/java/com/lostark/tracker/backfill/BackfillCaptureServiceIT.java
autonomous: true
requirements: [BACKFILL-01, BACKFILL-04]

must_haves:
  truths:
    - "D-01(소스①): 매 수집 시 리스트 응답(`POST /markets/items`)의 `YDayAvgPrice`(전일 평균, **각인서 포함 전 품목**, 추가 API 호출 0·무료)를 `item_daily_stats`에 `source=YDAY_AVG`로 멱등 upsert해 going-forward 일별 평균 시리즈를 전 품목으로 축적한다. YDayAvgPrice는 이미 페치된 리스트 응답에 동봉 — 신규 API 호출 0"
    - "BACKFILL-01: YDayAvgPrice 캡처의 `stat_date`는 **전일(KST)** = `LocalDate.now(Asia/Seoul).minusDays(1)` — YDayAvgPrice가 전일 평균이므로. 하루 48틱이 같은 (item·전일·YDAY_AVG)로 upsert해도 멱등(행 1개·값 덮어쓰기)"
    - "BACKFILL-04(Core Value 가드): `price_snapshot` 저장 경로·`min_price` 수집 semantics·`LatestPriceCache` evict·레이트리밋·`RetryPolicy`는 **무변경** — YDayAvgPrice 캡처는 이미 페치된 데이터를 읽어 별도 테이블에 쓰는 **additive fail-open 후킹**(캡처 실패가 수집/스냅샷을 절대 깨지 않음, 캐시 evict의 fail-open 철학 준수)"
    - "지표 정직성: `YDayAvgPrice`(일별 **거래** 평균가)는 `price_snapshot.min_price`(실시간 최저 **호가**)와 다른 지표 — 같은 테이블/라인에 혼합하지 않고 `item_daily_stats`에 `source`로 분리 적재(표시 구분은 17.4-04 책임)"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/collect/dto/MarketItem.java"
      provides: "리스트 응답의 YDayAvgPrice 매핑(@JsonProperty) — 기존 Id/Name/CurrentMinPrice에 additive 추가"
    - path: "src/main/java/com/lostark/tracker/backfill/BackfillCaptureService.java"
      provides: "수집 편승 캡처: 성공 fetch의 YDayAvgPrice → item_daily_stats(YDAY_AVG, 전일 KST) 멱등 upsert, fail-open (BACKFILL-01)"
    - path: "src/main/java/com/lostark/tracker/collect/ItemFetchResult.java"
      provides: "fetch 결과가 YDayAvgPrice를 수집 계층까지 운반(additive 필드)"
  key_links:
    - from: "BackfillCaptureService.captureYDayAvg(item, result)"
      to: "17.4-01 ItemDailyStatRepository.upsertDailyStat(...)"
      via: "멱등 upsert 계약(tracked_item_id, 전일 statDate, avgPrice, YDAY_AVG)"
      pattern: "upsertDailyStat"
    - from: "PriceCollector settle 루프(persistSnapshot 직후)"
      to: "BackfillCaptureService"
      via: "additive fail-open 후킹 — 스냅샷 저장 경로 무변경"
      pattern: "captureYDayAvg"
---

<objective>
리스트 응답의 `YDayAvgPrice`(전일 평균·전 품목·무료)를 매 수집 시 `item_daily_stats`에 `source=YDAY_AVG`로 멱등 upsert해 **going-forward 일별 평균 시리즈를 전 품목으로 축적**한다(D-01 소스①, BACKFILL-01) — `MarketItem` DTO에 `YDayAvgPrice` 매핑을 추가하고, `ItemFetchResult`가 그 값을 수집 계층까지 운반하며, 신규 `BackfillCaptureService`가 성공 fetch의 YDayAvgPrice를 전일(KST) stat_date로 upsert한다. `PriceCollector`는 settle 루프에서 `persistSnapshot` 직후 이 캡처를 **fail-open 후킹**으로 호출한다.

Purpose: 각인서 포함 전 품목의 일별 평균을 **추가 API 호출 0**으로 확보(리스트 응답에 무료 동봉)해, 서버 상시 운영 시 전 품목이 매일 한 점씩 쌓이는 백필 시리즈를 만든다. `price_snapshot` 저장·`min_price` 수집 semantics·캐시·레이트리밋은 0줄 무변경(BACKFILL-04) — 캡처 실패는 수집을 절대 깨지 않는다.

Output: `MarketItem`(+YDayAvgPrice)·`ItemFetchResult`(+yDayAvgPrice)·`ItemFetchService`(성공 결과에 값 운반)·`BackfillCaptureService`(신규)·`PriceCollector`(fail-open 후킹) + BackfillCaptureServiceIT. price_snapshot·캐시·레이트리밋·event-impact diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.4-timeline-gap-backfill/17.4-CONTEXT.md
@.planning/phases/17.4-timeline-gap-backfill/17.4-SPIKE-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- MarketItem(com.lostark.tracker.collect.dto) record 확장: 기존 (@JsonProperty("Id") long id, @JsonProperty("Name") String name, @JsonProperty("CurrentMinPrice") Long currentMinPrice)에 @JsonProperty("YDayAvgPrice") Double yDayAvgPrice 추가(전일 거래 평균가·전 품목, 스파이크 실측: 유물 원한 각인서 147007.4). @JsonIgnoreProperties(ignoreUnknown=true) 유지. Javadoc의 "YDayAvgPrice ... ignored" 문구 갱신(이제 매핑됨). -->
<!-- ItemFetchResult(com.lostark.tracker.collect) record에 Double yDayAvgPrice 추가: success(Long trackedItemId, Long minPrice, Double yDayAvgPrice, OffsetDateTime fetchedAt) 팩토리 시그니처 확장, failed(...)는 yDayAvgPrice=null. minPrice(실측 호가) semantics 무변경 — yDayAvgPrice는 별개 필드로 병렬 운반. -->
<!-- ItemFetchService.fetch 성공 리턴만 변경: ItemFetchResult.success(trackedItemId, match.currentMinPrice(), match.yDayAvgPrice(), OffsetDateTime.now(ZoneOffset.UTC)). 그 외(레이트리밋·retry·AuthApiException 등) 무변경. 신규 API 호출 없음(같은 match에서 필드 하나 더 읽음). -->
<!-- BackfillCaptureService(com.lostark.tracker.backfill) @Service: 생성자(ItemDailyStatRepository repo, Clock clock). private static final ZoneId KST = ZoneId.of("Asia/Seoul"). void captureYDayAvg(TrackedItem item, ItemFetchResult result): result.yDayAvgPrice()==null이면 no-op; 아니면 LocalDate statDate = LocalDate.ofInstant(clock.instant(), KST).minusDays(1); try { repo.upsertDailyStat(item.getId(), statDate, BigDecimal.valueOf(result.yDayAvgPrice()), DailyStatSource.YDAY_AVG.name()); } catch (RuntimeException e) { log.warn 후 삼킴 — fail-open, 수집 안 깸 }. 요청/키 미로깅. -->
<!-- PriceCollector(com.lostark.tracker.collect): 생성자에 BackfillCaptureService 주입. settle 루프에서 result가 SUCCESS일 때 persistSnapshot(itf.item(), collectedAt, result) 직후 backfillCaptureService.captureYDayAvg(itf.item(), result) 호출(1줄). persistSnapshot·evict·레이트리밋·run 카운트 로직 무변경. -->
<!-- IT 베이스: PostgresRedisContainers(Testcontainers), Clock은 Clock.fixed(instant, ZoneOffset.UTC) 주입 가능. TrackedItem 시드 후 ItemDailyStatRepository로 결과 조회. 실 API 미호출(ItemFetchResult 직접 구성). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarketItem + ItemFetchResult + ItemFetchService — YDayAvgPrice 운반</name>
  <read_first>
    - src/main/java/com/lostark/tracker/collect/dto/MarketItem.java (record 확장 대상 — 현재 Id/Name/CurrentMinPrice)
    - src/main/java/com/lostark/tracker/collect/ItemFetchResult.java (success/failed 팩토리 원형)
    - src/main/java/com/lostark/tracker/collect/ItemFetchService.java (성공 리턴 라인 — success(...) 호출부)
  </read_first>
  <files>src/main/java/com/lostark/tracker/collect/dto/MarketItem.java, src/main/java/com/lostark/tracker/collect/ItemFetchResult.java, src/main/java/com/lostark/tracker/collect/ItemFetchService.java</files>
  <action>
    `MarketItem` record에 `@JsonProperty("YDayAvgPrice") Double yDayAvgPrice` 필드를 추가한다(기존 3필드 뒤). `@JsonIgnoreProperties(ignoreUnknown=true)` 유지, Javadoc의 "YDayAvgPrice ... ignored"를 "YDayAvgPrice → item_daily_stats(YDAY_AVG) 백필 캡처(D-01)"로 갱신. `ItemFetchResult` record에 `Double yDayAvgPrice` 필드를 추가하고 `success` 팩토리를 `success(Long trackedItemId, Long minPrice, Double yDayAvgPrice, OffsetDateTime fetchedAt)`로 확장(내부 new에 필드 전달), `failed`는 yDayAvgPrice=null로 생성. `minPrice`/`fetchedAt`/`failureReason` semantics 무변경. `ItemFetchService.fetch`의 **성공 리턴만** `ItemFetchResult.success(trackedItemId, match.currentMinPrice(), match.yDayAvgPrice(), OffsetDateTime.now(ZoneOffset.UTC))`로 바꾼다 — 레이트리밋·retry·예외 분기·NOT_FOUND는 무변경, 신규 API 호출 없음(같은 `match`에서 필드 하나 더 읽음).
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - MarketItem이 @JsonProperty("YDayAvgPrice") Double yDayAvgPrice를 매핑 — `grep -q "YDayAvgPrice" src/main/java/com/lostark/tracker/collect/dto/MarketItem.java`
    - ItemFetchResult.success가 yDayAvgPrice 인자를 받고, ItemFetchService 성공 리턴이 match.yDayAvgPrice()를 전달
    - 레이트리밋/retry/예외 분기·minPrice 운반 semantics 무변경, `./gradlew compileJava` 그린
    - 신규 API 호출 0(searchMarketItems 호출 횟수 무변경 — 같은 match 재사용)
  </acceptance_criteria>
  <done>YDayAvgPrice가 DTO→fetch 결과→수집 계층까지 운반되고, min_price 수집 semantics·API 호출 횟수는 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: BackfillCaptureService + PriceCollector fail-open 후킹 + IT</name>
  <read_first>
    - src/main/java/com/lostark/tracker/collect/PriceCollector.java (settle 루프·persistSnapshot·evict fail-open 철학)
    - src/main/java/com/lostark/tracker/repository/ItemDailyStatRepository.java (17.4-01 산출 — upsertDailyStat 시그니처)
    - src/main/java/com/lostark/tracker/domain/DailyStatSource.java (YDAY_AVG)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (Testcontainers 베이스)
  </read_first>
  <files>src/main/java/com/lostark/tracker/backfill/BackfillCaptureService.java, src/main/java/com/lostark/tracker/collect/PriceCollector.java, src/test/java/com/lostark/tracker/backfill/BackfillCaptureServiceIT.java</files>
  <action>
    `BackfillCaptureService`(`com.lostark.tracker.backfill`) `@Service`: 생성자로 `ItemDailyStatRepository`·`Clock` 주입, `private static final ZoneId KST = ZoneId.of("Asia/Seoul")`. `void captureYDayAvg(TrackedItem item, ItemFetchResult result)`: `result.yDayAvgPrice()==null`이면 no-op(return); 아니면 `LocalDate statDate = LocalDate.ofInstant(clock.instant(), KST).minusDays(1)`(YDayAvgPrice=전일 평균) 계산 후 `try { repo.upsertDailyStat(item.getId(), statDate, BigDecimal.valueOf(result.yDayAvgPrice()), DailyStatSource.YDAY_AVG.name()); } catch (RuntimeException e) { log.warn("YDayAvgPrice backfill capture failed for item {}", item.getId(), e); }` — **fail-open**(캡처 실패가 수집을 깨지 않음, item id 외 민감정보/키 미로깅). `PriceCollector` 생성자에 `BackfillCaptureService` 주입, settle 루프에서 `persistSnapshot(itf.item(), collectedAt, result);` **직후** `backfillCaptureService.captureYDayAvg(itf.item(), result);` 1줄 추가. `persistSnapshot`·`latestPriceCache.evict`·레이트리밋·run 카운트 로직은 무변경. `BackfillCaptureServiceIT`(PostgresRedisContainers 상속, @SpringBootTest, @ActiveProfiles("test"), `Clock.fixed` 주입): (a) yDayAvgPrice=147007.4인 성공 결과 캡처 → item_daily_stats에 (item, 전일, YDAY_AVG, 147007.4) **1행**·avg_price 소수 보존 검증; (b) 같은 결과 2회 캡처 → **1행**(멱등); (c) yDayAvgPrice=null → **0행**(no-op); (d) fail-open — repo 오류를 던지는 스텁/시나리오로 캡처가 예외를 **전파하지 않음** 검증(간단히: null item 등으로 내부 예외 유발 시 throw 안 함). 실 API 미호출.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - captureYDayAvg가 전일(KST) stat_date·YDAY_AVG로 upsert하고 avg_price 소수(147007.4) 보존 — IT (a) 통과
    - 2회 캡처 시 1행(멱등), yDayAvgPrice=null 시 0행(no-op) — IT (b)(c) 통과
    - 캡처 내부 예외가 호출부로 전파되지 않음(fail-open) — IT (d) 통과
    - PriceCollector가 persistSnapshot 직후 captureYDayAvg를 호출하고 스냅샷 저장/evict/레이트리밋 로직 무변경 — `./gradlew build` 그린
    - price_snapshot 저장·LatestPriceCache·EventImpactService diff 0줄(BACKFILL-04)
  </acceptance_criteria>
  <done>YDayAvgPrice가 매 수집 시 전 품목 전일 평균으로 item_daily_stats에 멱등·fail-open 적재되고, 수집 핵심 경로(스냅샷/캐시/레이트리밋)는 0줄이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 API 리스트 응답 → item_daily_stats | 이미 신뢰 검증된 수집 응답의 필드 하나 추가 소비(신규 외부 호출 0) |
| 백필 캡처 → 수집 파이프라인 | fail-open 후킹 — 캡처 실패가 price_snapshot 수집을 절대 중단시키지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-02-01 | Denial of Service | 백필 캡처 오류가 수집 틱을 중단 | mitigate | captureYDayAvg는 try/catch fail-open — IT (d)가 예외 비전파 검증. persistSnapshot 이후 호출이라 스냅샷은 이미 커밋 |
| T-174-02-02 | Tampering | 백필 캡처가 min_price 수집 semantics 변경 | mitigate | success 팩토리에 필드만 추가(minPrice 운반 불변), 성공 리턴 1줄 변경 외 fetch 분기 무변경 — verify가 diff 확인 |
| T-174-02-03 | Information Disclosure | 캡처 실패 로그에 키/민감정보 유입 | mitigate | log.warn에 item id만, API 키/요청 바디 미로깅(수집 로깅 컨벤션 준수) |
| T-174-02-04 | Spoofing/DoS | YDayAvgPrice 캡처가 레이트리밋 초과 유발 | mitigate | 신규 API 호출 0 — 이미 페치된 리스트 응답 재사용(추가 콜 없음) |
| T-174-02-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 수집/JPA 스택 재사용 |
</threat_model>

<verification>
- 매 수집 시 YDayAvgPrice(전 품목·전일)를 item_daily_stats(YDAY_AVG)에 멱등 upsert — BackfillCaptureServiceIT (a)(b)(c), avg_price 소수 보존·중복 없음(D-01, BACKFILL-01).
- 캡처는 추가 API 호출 0(리스트 응답 재사용)이고 fail-open(수집 비중단) — IT (d) + fetch diff 검토.
- price_snapshot 저장 경로·min_price 수집 semantics·LatestPriceCache·레이트리밋·EventImpactService diff 0줄(BACKFILL-04, Core Value 가드).
- `./gradlew build` 그린(Testcontainers Postgres/Redis, 실 API·키 미사용).
</verification>

<success_criteria>
- 리스트 응답의 YDayAvgPrice(각인서 포함 전 품목·무료)가 매 수집 시 item_daily_stats(YDAY_AVG, 전일 KST)에 멱등 축적된다(BACKFILL-01, D-01 소스①).
- 추가 API 호출 0(이미 페치된 응답 재사용) — 레이트리밋 여유 유지.
- price_snapshot 수집·캐시·event-impact 핵심 경로 0줄, 캡처 실패가 수집을 깨지 않음(BACKFILL-04, fail-open).
</success_criteria>

<output>
Create `.planning/phases/17.4-timeline-gap-backfill/17.4-02-SUMMARY.md` when done
</output>