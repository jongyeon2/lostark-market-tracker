---
phase: 17-real-data-transition
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified: []
autonomous: false
requirements: [REALDATA-01, REALDATA-02]
user_setup:
  - service: lostark-open-api
    why: "실 API 키로 dev 프로파일 수집기를 로컬 1회 구동해 collection_run SUCCESS + 실스냅샷 적재를 검증(REALDATA-01, D-01) — Claude가 소유/커밋할 수 없는 사용자 비밀"
    env_vars:
      - name: LOSTARK_API_KEY
        source: "로스트아크 개발자 포털 JWT 키(재발급 가능) → 로컬 .env 에만 기재(gitignore됨, 커밋 금지)"

must_haves:
  truths:
    - "D-01: 실 API 키로 dev 프로파일 수집기를 로컬 1회 구동해 GET /api/health/collection의 status가 SUCCESS(또는 PARTIAL_SUCCESS) + itemsSucceeded > 0 + summaryMessage가 null(AUTH_ERROR/RATE_LIMITED 아님)로 확인되고, 실스냅샷이 적재된다(실키 수집 SUCCESS = REALDATA-01 성공기준 충족). 상시 축적·공개 배포는 Phase 18"
    - "D-02: 라이브 실행 프로파일은 기존 dev 재사용(application-dev.yml, initial-delay-ms=10000) — 새 prod/live 프로파일을 신설하지 않는다(신설은 Phase 18)"
    - "D-07: 실데이터 검증은 깨끗한 볼륨에서 시작한다(docker compose down -v → up -d) — 과거 seed/합성 collection_run·snapshot이 남아 '실수집만' 증명을 오염시키지 않게(11-04 gap closure 전례)"
    - "D-06: SeedDataRunner의 @Profile(\"seed\") 코드 격리가 그대로 유지되고(별도 런타임 감지/거부 코드 미추가), dev 인스턴스는 seed를 심지 않아 프로파일 수준에서 자연 격리된다(REALDATA-02 'seed는 로컬/테스트 전용')"
    - "D-08: 불변 가드 — 페이즈 전체에서 수집(PriceCollector)/캐시/event-impact/seed 등 백엔드 src/ diff 0줄. Phase 17 델타는 프론트 read-path 문구·문서·검증 절차에 한정된다(git diff로 src/ 0줄 검증)"
    - "3화면(Dashboard·Timeline·Impact)이 dev 실수집 데이터를 표시하고, 첫 수집 전/직후 초기 상태에서도 빈 화면 없이 '수집 중'을 정직히 표시한다(REALDATA-02/03 통합 라이브 확인, Plan 01 산출물 위에서)"
  artifacts:
    - path: ".planning/phases/17-real-data-transition/17-03-SUMMARY.md"
      provides: "실키 로컬 수집 검증 결과(관측된 status/카운트) + 불변 가드 통과 기록"
  key_links:
    - from: "dev 프로파일 수집기(PriceCollector, 실 LOSTARK_API_KEY)"
      to: "collection_run SUCCESS + price_snapshot 적재 → GET /api/health/collection"
      via: "실키 로컬 1회 구동(깨끗한 볼륨) → 헬스 엔드포인트로 SUCCESS·실스냅샷 확인"
      pattern: "health/collection"
---

<objective>
실데이터 전환의 **검증 게이트**: 실 API 키로 dev 프로파일 수집기를 로컬에서 1회 구동해 `collection_run` SUCCESS + 실스냅샷 적재를 확인하고(REALDATA-01, D-01), 수집/캐시/event-impact/seed 백엔드가 **0줄**임을 검증한다(D-08 불변 가드). seed는 `@Profile("seed")` 격리로 dev에 자연 미유입(D-06). 코드 변경 없음 — 검증·확인 전용 plan.

- **D-01/D-02 — 실키 로컬 수집 검증:** 기존 dev 프로파일(재사용, 새 프로파일 미신설)로 `./gradlew bootRun --args='--spring.profiles.active=dev'`을 실키(.env `LOSTARK_API_KEY`)로 구동 → `GET /api/health/collection` status SUCCESS(또는 PARTIAL_SUCCESS) + itemsSucceeded > 0 + summaryMessage null + 실스냅샷 적재.
- **D-07 — 깨끗한 볼륨:** `docker compose down -v` → `up -d`로 시작해 과거 seed 합성 run/snapshot 잔재가 증명을 오염시키지 않게 한다(11-04 gap 전례).
- **D-06 — seed 격리:** `SeedDataRunner`의 `@Profile("seed")`가 유지되어 dev 인스턴스엔 seed가 심기지 않는다(런타임 거부 코드 미추가).
- **D-08 — 불변 가드:** 페이즈 전체 백엔드 `src/` diff 0줄 검증.

Purpose: Core Value(수집 신뢰성)를 실키로 실제 증명하고, 실데이터 전환이 백엔드 코어를 건드리지 않았음을 못 박는다. REALDATA-01의 "실키 수집 SUCCESS"는 이 로컬 1회 구동으로 충족한다.

Output: 관측된 status/카운트·불변 가드 통과를 기록한 `17-03-SUMMARY.md`. 코드 diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-real-data-transition/17-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- 페이즈 베이스 커밋(가드 기준): fa863e2 "docs(state): record phase 17 context session" — Phase 17 실행은 이 이후 커밋들. -->
<!-- 가드 대상 백엔드 패키지: src/main/java/com/lostark/tracker/{collect,cache,seed,read,health,web,domain,repository,...}, src/main/resources/. D-08 = 이들 src/ diff 0줄. -->
<!-- dev 프로파일: application-dev.yml — 실수집, collection.initial-delay-ms=10000(첫 tick 10s 지연; WatchlistSeeder가 ApplicationRunner로 15품목 upsert 후 수집), 이후 fixedDelay 10분 기본. datasource/redis는 .env 또는 localhost compose 기본. -->
<!-- seed 프로파일: application-seed.yml — collection.initial-delay-ms=3600000(수집기 1h 밀어냄, 합성만). SeedDataRunner=@Profile("seed") @Order(2). dev엔 seed 미유입(자연 격리). -->
<!-- 헬스 계약: GET /api/health/collection → { status: SUCCESS|PARTIAL_SUCCESS|FAILED|NO_RUNS, itemsAttempted/Succeeded/Failed, lastRunAt, summaryMessage: AUTH_ERROR|RATE_LIMITED|null }. 키/시크릿 필드 없음(구성상 안전). -->
<!-- 실스냅샷 확인: GET /api/items/{id}/latest → 200 + minPrice(실수집), 또는 DB price_snapshot count > 0. GET /api/items → 워치리스트 15품목. -->
<!-- .env는 gitignore됨(git check-ignore .env = IGNORED) — 실키는 .env에만, 커밋/로그/문서 미기재(상시 가드). AUTH_ERROR/RATE_LIMITED 디버깅: .env 키 유효성/재발급 확인 + collection_run 재확인(seed=합성·dev=실수집 구분). -->
<!-- Windows/PowerShell: docker compose·gradlew·curl 사용. bootRun은 장기 실행 프로세스 — 검증 후 Ctrl+C로 종료. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 불변 가드 검증 — 백엔드 src/ 0줄 + seed @Profile 격리 유지(D-06/D-08)</name>
  <read_first>
    - src/main/java/com/lostark/tracker/seed/SeedDataRunner.java (@Profile("seed") 격리가 그대로인지 — 무변경 확인)
    - src/main/resources/application-dev.yml (dev 프로파일 무변경 확인 — 새 프로파일 미신설, D-02)
    - .planning/phases/17-real-data-transition/17-CONTEXT.md (D-06/D-08 불변 가드)
  </read_first>
  <files></files>
  <action>
    페이즈 전체가 백엔드를 건드리지 않았음을 git으로 검증한다(D-08). 페이즈 베이스 커밋은 `fa863e2`(Phase 17 CONTEXT 세션 기록; 실행 커밋은 그 이후). (1) `git diff --name-only fa863e2 -- src/`가 **비어 있음**을 확인한다 — 수집(collect/PriceCollector)/캐시(cache)/event-impact(read)/seed/health/web 등 백엔드 코드·리소스 0줄. 비어 있지 않으면 D-08 위반이므로 STOP하고 어떤 백엔드 파일이 바뀌었는지 보고. (2) `git diff --name-only fa863e2 -- ':(exclude).planning'`가 프론트(frontend/) + README.md + frontend/README.md에만 한정됨을 확인한다. (3) `SeedDataRunner.java`에 `@Profile("seed")`가 그대로 있고 새 런타임 감지/거부 코드가 추가되지 않았음을 확인(D-06). (4) `application-dev.yml`이 무변경이고 새 prod/live 프로파일 파일이 생기지 않았음을 확인(D-02). 베이스 커밋이 다르면(그 사이 새 커밋) Phase 17 첫 실행 커밋 직전 커밋을 베이스로 재산정해 동일 검증.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only fa863e2 -- src/)" && grep -q '@Profile("seed")' src/main/java/com/lostark/tracker/seed/SeedDataRunner.java && test -z "$(git diff --name-only fa863e2 -- src/main/resources)"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only fa863e2 -- src/`가 빈 출력 — 백엔드 src/ diff 0줄(D-08)
    - `git diff --name-only fa863e2 -- ':(exclude).planning'`가 frontend/·README.md·frontend/README.md에만 한정
    - `SeedDataRunner.java`에 `@Profile("seed")`가 유지되고 새 격리/거부 코드 미추가(D-06)
    - `application-dev.yml` 무변경, 새 prod/live 프로파일 파일 미생성(D-02)
  </acceptance_criteria>
  <done>Phase 17이 백엔드 코어(수집·캐시·event-impact·seed)를 0줄도 바꾸지 않았고, seed는 @Profile 격리로 dev에 미유입됨이 git으로 검증된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실키 로컬 수집 검증 — 깨끗한 볼륨 dev 1회 구동(REALDATA-01, D-01/D-02/D-07)</name>
  <files></files>
  <what-built>
    Plan 01(프론트 빈 상태)·Plan 02(문서) 위에서 실데이터 전환을 라이브로 검증하는 절차. Claude는 실키를 소유/커밋할 수 없으므로, 사용자가 자신의 .env LOSTARK_API_KEY로 깨끗한 볼륨에서 dev 프로파일을 1회 구동해 collection_run SUCCESS + 실스냅샷 + 3화면 실데이터/빈 상태 정직성을 확인한다(REALDATA-01/02/03, D-01/D-02/D-07).
  </what-built>
  <action>Claude는 아래 how-to-verify 절차를 사용자에게 제시하고 실행을 안내한다. 실키(.env LOSTARK_API_KEY)는 사용자만 소유하므로 사용자가 깨끗한 볼륨(docker compose down -v → up -d, D-07)에서 dev 프로파일(기존 재사용, D-02)을 1회 구동하고, GET /api/health/collection의 SUCCESS·실스냅샷·3화면 정직 표시를 확인한다. Claude는 사용자 보고(관측 status·itemsSucceeded·실스냅샷 유무·3화면 상태)를 17-03-SUMMARY.md에 기록하되 실키 값은 절대 기재하지 않는다.</action>
  <how-to-verify>
    1. **깨끗한 볼륨(D-07):** 레포 루트에서
       `docker compose down -v`  → `docker compose up -d`
       (과거 seed/합성 collection_run·snapshot 잔재 제거 — 11-04 gap 전례).
    2. **실키 주입:** `.env`의 `LOSTARK_API_KEY`에 로스트아크 개발자 포털 JWT(재발급 가능)를 채운다. `.env`는 gitignore됨 — 커밋/로그/문서에 값 기재 금지(상시 가드).
    3. **dev 기동(D-02, 기존 프로파일 재사용):**
       `./gradlew bootRun --args='--spring.profiles.active=dev'`
       (WatchlistSeeder가 15품목 upsert 후 initial-delay 10초 뒤 첫 수집 tick).
    4. **수집 SUCCESS 확인(D-01, REALDATA-01):** 첫 tick 이후(약 15~40초)
       `curl http://localhost:8080/api/health/collection`
       → `status`가 `SUCCESS`(또는 `PARTIAL_SUCCESS`), `itemsSucceeded > 0`, `summaryMessage`가 `null`(AUTH_ERROR/RATE_LIMITED 아님)인지 확인.
    5. **실스냅샷 확인:** `curl http://localhost:8080/api/items/1/latest` → 200 + `minPrice`(실수집가), 또는 DB `price_snapshot` count > 0.
    6. **3화면 확인(REALDATA-02/03):** 별 터미널에서 `cd frontend && npm install && npm run dev` → http://localhost:5173
       - 첫 수집 전/직후: Dashboard 카드·Timeline·Impact가 빈 화면 대신 '수집 중'을 정직히 표시(Plan 01).
       - 첫 수집 후: 3화면이 dev 실수집 데이터를 표시.
    7. **문제 시(AUTH_ERROR/RATE_LIMITED):** .env 키 유효성/재발급을 확인하고 collection_run을 재확인(seed=합성·dev=실수집 구분). 레이트리밋이면 잠시 후 재시도.
    8. 검증 후 bootRun은 Ctrl+C로 종료(상시 축적은 Phase 18).
  </how-to-verify>
  <verify>
    <human-check>사용자가 GET /api/health/collection status = SUCCESS(또는 PARTIAL_SUCCESS) + itemsSucceeded > 0 + summaryMessage = null 및 실스냅샷 적재(/latest 200 또는 snapshot count > 0), 3화면 실데이터/'수집 중' 정직 표시를 확인</human-check>
  </verify>
  <done>깨끗한 볼륨 dev 실키 구동으로 collection_run SUCCESS + 실스냅샷 적재(REALDATA-01/D-01) + 3화면 실데이터/'수집 중' 정직 표시(REALDATA-02/03)가 사용자에 의해 승인된다.</done>
  <resume-signal>관측된 status·itemsSucceeded·최신가(또는 스냅샷 유무)와 3화면 상태를 보고하고 "approved" 입력 (문제 시 관측값 서술)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env LOSTARK_API_KEY(실키) → 로컬 앱 JVM | 실키는 사용자 .env에만(gitignore) — 커밋/로그/문서에 값 미기재(상시 가드/DEPLOY-04). Claude는 실키를 소유/커밋하지 않음 |
| dev 수집기 → 외부 Lostark Open API | 백엔드만 실키로 외부 호출(프론트 직접 호출 금지, 상시 가드). 레이트리밋 키당 분당 한도 내 |
| GET /api/health/collection → 검증자 | 응답은 카운트·status·categorical marker만 — 키/시크릿 필드 없음(구성상 안전) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17V-01 | Information Disclosure | 검증 중 실 LOSTARK_API_KEY가 커밋/로그/SUMMARY에 유출 | mitigate | 실키는 .env(gitignore)에만; SUMMARY/보고엔 status·카운트만 기록하고 키 값 미기재. health 응답은 키 필드 없음(구성상 안전) |
| T-17V-02 | Tampering | 오염된 볼륨(과거 seed 합성 run)이 '실수집만' 증명을 훼손 | mitigate | D-07 깨끗한 볼륨(`docker compose down -v`)으로 시작 — 11-04 gap 전례 근거. 헬스의 lastRunAt/summaryMessage로 실수집 run인지 확인 |
| T-17V-03 | Spoofing/Denial | 유효하지 않은 키(AUTH_ERROR)·레이트리밋(RATE_LIMITED)으로 SUCCESS 오판 | mitigate | summaryMessage가 null인지(AUTH_ERROR/RATE_LIMITED 아님) + itemsSucceeded > 0을 명시 검증. 문제 시 키 재발급/재시도 절차 안내 |
| T-17V-SC | Tampering | 패키지/공급망 | accept | 코드/의존성 변경 0(검증 전용). `npm install`은 기존 프론트 lockfile 기준 재설치만 |
</threat_model>

<verification>
- Task 1 자동 가드: `git diff --name-only fa863e2 -- src/` 빈 출력(백엔드 0줄, D-08); frontend/·README 2문서에만 diff 한정; `@Profile("seed")` 유지(D-06); application-dev.yml 무변경(D-02).
- Task 2 체크포인트: 깨끗한 볼륨 dev 구동(실키) → GET /api/health/collection status SUCCESS/PARTIAL_SUCCESS + itemsSucceeded > 0 + summaryMessage null(D-01, REALDATA-01); 실스냅샷 적재(/latest 200 또는 snapshot count > 0); 3화면이 실수집 데이터/'수집 중' 정직 표시(REALDATA-02/03).
- SUMMARY에 관측된 status/카운트/실스냅샷 유무를 기록(실키 값은 미기재).
</verification>

<success_criteria>
- 실 API 키로 수집기가 구동되어 실데이터가 축적된다 — collection_run SUCCESS(성공기준 1, REALDATA-01/D-01).
- 3화면이 실수집 데이터를 표시하고 seed 프로파일은 @Profile 격리로 로컬/테스트 전용으로만 남는다(성공기준 2, REALDATA-02/D-06).
- 데이터 미축적 초기에도 3화면이 빈 화면 없이 '수집 중'을 정직히 표시(성공기준 3, REALDATA-03 — Plan 01 위 라이브 확인).
- 수집/캐시/event-impact 핵심 경로 회귀 없음 — 백엔드 src/ 0줄(성공기준 4, Core Value 가드/D-08).
- 검증은 깨끗한 볼륨에서(D-07), 기존 dev 프로파일 재사용(D-02, 새 프로파일 미신설).
</success_criteria>

<output>
Create `.planning/phases/17-real-data-transition/17-03-SUMMARY.md` when done
</output>
