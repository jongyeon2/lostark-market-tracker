---
phase: 17.1-demo-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/dashboard/DashboardPage.tsx
  - frontend/src/features/dashboard/HealthCard.tsx
  - frontend/src/features/dashboard/ItemCard.tsx
  - frontend/src/features/admin/CollectionSection.tsx
autonomous: true
requirements: [POLISH-01, POLISH-04]

must_haves:
  truths:
    - "D-07: DashboardPage에서 <HealthCard/>를 완전 제거한다 — 클라이언트 대시보드는 '무엇을·지금 얼마'만 담고, 수집 헬스는 관리자 콘솔 CollectionSection에 존치(이미 재사용 중, Phase 15)"
    - "D-08: 헬스 라벨을 'API 상태' 단일 제목으로 통일 — HealthCard CardTitle '수집 헬스'→'API 상태', 관리자 CollectionSection 바깥 '수집 상태' 래퍼 제목 제거(섹션>카드 중첩 해소, 관리자에서도 'API 상태' 카드 하나)"
    - "D-09: HealthCard 제거로 대시보드 정직성 별도 대체 표시를 만들지 않는다 — per-card pending(ItemCard) + 아이템 그리드 AsyncBoundary 빈/로딩 카피로 충분(집계 ops 상태는 관리자 관심사)"
    - "D-10: 대시보드를 반응형 그리드(sm:2 lg:3열)에서 카테고리별 세로 섹션 2개로 재구성 — 상위 각인(DEALER+SUPPORT) → 상위 재료(MATERIAL), roleGroup 버킷 재사용, 섹션 간 각인→재료 순"
    - "D-11: 각인 섹션 내부는 딜러→서포터 순(sortByRole 정합) + 각 카드 RoleBadge로 구분(무거운 서브헤더 없이), 섹션 내 이름(ko-KR) 정렬"
    - "D-12: 카드를 정사각 그리드→가로 전폭 행으로 전환하되 딥링크(/timeline?item={id})·ItemIcon·RoleBadge·골드 가격(minPrice)·수집 시각(formatKst)을 보존한다(회귀 금지)"
  artifacts:
    - path: "frontend/src/features/dashboard/DashboardPage.tsx"
      provides: "세로 섹션(각인→재료) 재구성 + HealthCard import/렌더 제거 (D-07/D-10/D-11)"
    - path: "frontend/src/features/dashboard/HealthCard.tsx"
      provides: "CardTitle 'API 상태' (D-08)"
    - path: "frontend/src/features/admin/CollectionSection.tsx"
      provides: "'수집 상태' 래퍼 제목 제거 → 단일 'API 상태' 카드 (D-08)"
    - path: "frontend/src/features/dashboard/ItemCard.tsx"
      provides: "가로 전폭 행 레이아웃, 딥링크/ItemIcon/RoleBadge/minPrice/formatKst 보존 (D-12)"
  key_links:
    - from: "DashboardPage (섹션 분할)"
      to: "sortByRole (frontend/src/features/_shared/roleGroup.ts)"
      via: "역할 버킷으로 각인(DEALER+SUPPORT)/재료(MATERIAL) 섹션 분할·정렬(D-10/D-11, roleGroup.ts는 읽기만)"
      pattern: "sortByRole"
    - from: "ItemCard <Card>"
      to: "/timeline?item={id}"
      via: "react-router Link 딥링크 보존(D-12/CARD-02)"
      pattern: "timeline\\?item="
---

<objective>
대시보드 정보 배치를 **관점 분리**(수집 헬스=운영자 관심사→관리자, 클라이언트 대시보드=무엇을·지금 얼마)로 정리하고, 바둑판 그리드를 **카테고리별 세로 섹션**(각인→재료)으로 재구성한다(POLISH-01/04). 프론트 read-path만 변경 — 백엔드 0줄.

- **D-07/D-08/D-09 — 헬스카드 이관 + 라벨 통일:** DashboardPage에서 `<HealthCard/>` 제거(관리자엔 존치), HealthCard 제목 '수집 헬스'→'API 상태', 관리자 CollectionSection 바깥 '수집 상태' 래퍼 제목 제거(단일 'API 상태' 카드). 대시보드 정직성은 기존 자산(ItemCard pending·AsyncBoundary)으로 충분 — 별도 대체 표시 미신설.
- **D-10/D-11 — 세로 섹션:** 그리드를 각인(DEALER+SUPPORT) 섹션 → 재료(MATERIAL) 섹션 2개로. 각인 섹션 내부 딜러→서포터→이름 순(sortByRole 재사용), RoleBadge로 구분.
- **D-12 — 가로 행 카드:** ItemCard를 정사각→가로 전폭 행으로 재배치하되 딥링크·ItemIcon·RoleBadge·minPrice·formatKst 보존.

Purpose: 카테고리로 시선을 정리하고 넓은 행으로 품목명·가격을 한 줄에 스캔하기 쉽게 만들며, 수집 헬스는 운영자(관리자)에게 두어 정보 관점을 분리한다.

Output: 세로 섹션·가로 행으로 재구성된 대시보드, 'API 상태' 단일 라벨의 헬스 카드/관리자 섹션. 백엔드 diff 0줄, 딥링크/enrichment 회귀 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-demo-polish/17.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- DashboardPage.tsx: useItems() → sorted = sortByRole(data). 현재 <HealthCard/>(L30 full-width) + grid grid-cols-1 sm:2 lg:3 안에 sorted.map(ItemCard). AsyncBoundary(status,isEmpty,onRetry) 유지. ★ HealthCard import/렌더 제거(D-07), grid → 각인/재료 세로 섹션(D-10/11). -->
<!-- sortByRole<T extends {roleGroup:RoleGroup|null; displayName:string}>(items): T[] — [role 버킷(DEALER0/SUPPORT1/MATERIAL2/null3), 그다음 displayName ko-KR] 정렬. 섹션 분할은 item.roleGroup으로 각인(DEALER|SUPPORT) vs 재료(MATERIAL) 필터. roleGroup.ts는 읽기만(수정은 17.1-02). -->
<!-- HealthCard.tsx: HealthContent의 <CardTitle className="text-xl">수집 헬스</CardTitle> ★ → 'API 상태'. 나머지(useCollectionHealth·AsyncBoundary·StatusBadge·SummaryMarker·NO_RUNS 분기) 무변경. 대시보드/관리자 공용. -->
<!-- CollectionSection.tsx(관리자): <Card><CardHeader><CardTitle>수집 상태</CardTitle></CardHeader><CardContent><HealthCard/></CardContent></Card>. ★ 바깥 Card/CardHeader/CardTitle('수집 상태') 래퍼 제거 → <HealthCard/>만 렌더(HealthCard 자체가 Card라 중첩 해소, 관리자에서 'API 상태' 카드 하나). -->
<!-- ItemCard.tsx: <Link to={`/timeline?item=${item.id}`} className="group block rounded-xl focus-visible:ring-2 ..."> <Card hover:border/shadow> <CardHeader><CardTitle className="flex items-center gap-2"><ItemIcon iconUrl roleGroup size="sm"/><span>{displayName}</span><RoleBadge roleGroup/></CardTitle></CardHeader> <CardContent> pending→Skeleton, error→'최신가 수집 중'|'데이터 없음'(deriveCollectionEmptyKind), success→ minPrice.toLocaleString('ko-KR') + '수집 시각 '+formatKst(collectedAt) </CardContent></Card></Link>. ★ 세로 카드→가로 전폭 행(아이콘/이름/배지 좌 · 가격/시각 우), 딥링크/enrichment/focus-visible 링/accent 규칙 보존(D-12/CARD-02/Phase16 D-05). -->
<!-- 보존(회귀 금지, D-12): Link to /timeline?item={id}, ItemIcon, RoleBadge, minPrice(골드), formatKst(수집 시각), focus-visible ring(accent blue-600은 focus-visible에만). 고유번호는 Phase 16에서 이미 제거됨(재도입 금지). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 헬스카드 이관 + 'API 상태' 라벨 통일(D-07/D-08/D-09)</name>
  <read_first>
    - frontend/src/features/dashboard/DashboardPage.tsx (HealthCard import L5·렌더 L30 — 제거 대상)
    - frontend/src/features/dashboard/HealthCard.tsx (CardTitle '수집 헬스' L41 — 'API 상태'로)
    - frontend/src/features/admin/CollectionSection.tsx (바깥 '수집 상태' 래퍼 — 제거 대상)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-07/D-08/D-09)
  </read_first>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/dashboard/HealthCard.tsx, frontend/src/features/admin/CollectionSection.tsx</files>
  <action>
    (1) **DashboardPage(D-07):** `import { HealthCard } from './HealthCard'`와 `<HealthCard />` 렌더(그 위 주석 ①) 제거. 아이템 그리드/섹션과 AsyncBoundary는 유지(Task 2에서 재구성). 대시보드 정직성 대체 표시는 **신설하지 않는다**(D-09 — ItemCard pending·AsyncBoundary가 담당). (2) **HealthCard(D-08):** `HealthContent`의 `<CardTitle className="text-xl">수집 헬스</CardTitle>`를 `API 상태`로 변경(className·StatusBadge·나머지 무변경) — 대시보드/관리자 공용이므로 한 곳 변경이 양쪽에 반영. (3) **CollectionSection(D-08):** 바깥 `<Card><CardHeader><CardTitle>수집 상태</CardTitle></CardHeader><CardContent>...</CardContent></Card>` 래퍼를 제거하고 `<HealthCard />`만 반환(HealthCard 자체가 Card → 섹션>카드 중첩 해소, 관리자에서 'API 상태' 카드 하나). 미사용이 된 Card/CardHeader/CardTitle import 정리.
  </action>
  <verify>
    <automated>cd frontend && npm run build && ! grep -q "HealthCard" src/features/dashboard/DashboardPage.tsx && grep -q "API 상태" src/features/dashboard/HealthCard.tsx && ! grep -q "수집 헬스" src/features/dashboard/HealthCard.tsx && ! grep -q "수집 상태" src/features/admin/CollectionSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage에 HealthCard import/렌더가 없다(D-07) — 클라이언트 화면에서 헬스 제거
    - HealthCard 제목이 'API 상태'('수집 헬스' 없음), 나머지 로직 무변경(D-08)
    - CollectionSection에 '수집 상태' 래퍼 제목이 없고 <HealthCard/>만 렌더 → 관리자 'API 상태' 카드 하나(D-08)
    - 대시보드에 헬스 대체 표시를 새로 만들지 않았다(D-09)
    - `cd frontend && npm run build` 그린(미사용 import 없음)
  </acceptance_criteria>
  <done>대시보드에서 헬스가 사라지고 관리자엔 'API 상태' 카드 하나로 존치되며, 라벨이 'API 상태'로 통일되고 빌드가 그린이다.</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 세로 섹션(각인→재료) + 가로 행 카드 재구성(D-10/D-11/D-12)</name>
  <read_first>
    - frontend/src/features/dashboard/DashboardPage.tsx (Task 1 후 상태 — grid → 섹션 재구성 대상)
    - frontend/src/features/dashboard/ItemCard.tsx (Link 딥링크·ItemIcon·RoleBadge·minPrice·formatKst — 가로 행 재배치, 보존 요소)
    - frontend/src/features/_shared/roleGroup.ts (sortByRole·RoleGroup — 읽기만)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-10/D-11/D-12 + Discretion 레이아웃)
  </read_first>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/dashboard/ItemCard.tsx</files>
  <action>
    (1) **DashboardPage(D-10/D-11):** 반응형 그리드를 **세로 섹션 2개**로 재구성한다. `sortByRole(data)` 결과를 `roleGroup`으로 분할 — 각인 섹션 = DEALER+SUPPORT(딜러→서포터→이름 순은 sortByRole가 이미 보장), 재료 섹션 = MATERIAL. 섹션 순서: **각인 → 재료**. 각 섹션은 경량 제목(리터럴 '각인'/'재료' — ROLE_LABEL 참조 금지, 각인은 DEALER+SUPPORT 합집합이라 단일 라벨 아님)과 그 아래 카드들을 **세로 스택**(가로 전폭 행)으로 렌더. 빈/로딩/에러는 기존 `AsyncBoundary`로 감싼 채 유지(D-09 — 별도 표시 신설 금지). 섹션 제목 타이포·스페이싱은 07-UI-SPEC 상태 컴포넌트 패턴과 8pt 스페이싱에 정합(Discretion). (2) **ItemCard(D-12):** 카드를 정사각 그리드 셀에서 **가로 전폭 행**으로 재배치 — 좌측에 [ItemIcon][이름][RoleBadge], 우측에 [minPrice 골드][수집 시각 formatKst] 정렬(구분선/간격은 Discretion, 8pt). **보존(회귀 금지):** `<Link to={/timeline?item=${item.id}}>` 딥링크·ItemIcon·RoleBadge·minPrice(`toLocaleString('ko-KR')`)·`formatKst`·per-card useLatestPrice/useCollectionHealth 상태 매핑·focus-visible ring(accent blue-600은 focus-visible에만, hover는 elevation/border만). pending→Skeleton, error→'최신가 수집 중'|'데이터 없음' 분기 유지. 고유번호(카테고리 코드) 재도입 금지(Phase 16 제거).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "timeline?item=" src/features/dashboard/ItemCard.tsx && grep -q "RoleBadge" src/features/dashboard/ItemCard.tsx && grep -q "formatKst" src/features/dashboard/ItemCard.tsx && grep -q "sortByRole" src/features/dashboard/DashboardPage.tsx && ! grep -q "lg:grid-cols-3" src/features/dashboard/DashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage가 각인(DEALER+SUPPORT) → 재료(MATERIAL) 세로 섹션 2개로 렌더(바둑판 lg:grid-cols-3 없음, D-10)
    - 각인 섹션 내부가 딜러→서포터→이름(ko-KR) 순, RoleBadge로 구분(D-11), sortByRole 재사용
    - ItemCard가 가로 전폭 행이고 딥링크(/timeline?item=)·ItemIcon·RoleBadge·minPrice·formatKst 보존(D-12)
    - pending/error/success 상태 매핑·focus-visible ring 보존, 고유번호 미재도입
    - `cd frontend && npm run build` 그린
  </acceptance_criteria>
  <done>대시보드가 각인→재료 세로 섹션 + 가로 전폭 행으로 재구성되고, 카드 딥링크/아이콘/배지/가격/시각과 상태 매핑이 모두 보존되며 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 read API → 프론트 렌더 | 대시보드/카드는 백엔드 read DTO(공개 시세·enrichment)만 소비 — 프론트는 Lostark API 직접 호출 금지(상시 가드), 백엔드 0줄 |
| 사용자 입력 → 라우팅 | 카드 딥링크가 item.id를 /timeline?item= 쿼리로 전달 — id는 백엔드 발급 값 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | Information Disclosure | 헬스 카드 이관 중 관리자 전용 집계 상태가 클라이언트에 남음 | mitigate | DashboardPage에서 HealthCard 완전 제거(verify가 부재 강제), 헬스는 관리자 CollectionSection에만(D-07) |
| T-173-02 | Tampering | displayName/id 렌더 시 XSS | accept | React 기본 이스케이프 + 값은 백엔드 read DTO(자유 입력 아님), dangerouslySetInnerHTML 미사용 |
| T-173-03 | Denial of Service | 22개 카드 per-card fan-out 요청 증가 | accept | React Query 캐시·dedupe(useCollectionHealth queryKey 공유), 규모 22개로 무의미 — 수집 로직 무변경 |
| T-173-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 컴포넌트/훅 재배치만, 설치 태스크 없음 |
</threat_model>

<verification>
- `git diff --name-only`가 dashboard 3파일 + admin CollectionSection 4개 프론트 파일만 — 백엔드 src/ diff 0줄.
- DashboardPage: HealthCard import/렌더 부재(D-07), 각인→재료 세로 섹션(lg:grid-cols-3 부재, D-10), sortByRole 재사용(D-11).
- HealthCard 'API 상태' / CollectionSection '수집 상태' 래퍼 부재 → 관리자 'API 상태' 카드 하나(D-08).
- ItemCard 가로 행 + 딥링크(/timeline?item=)·ItemIcon·RoleBadge·minPrice·formatKst 보존(D-12), 상태 매핑 유지.
- 대시보드 헬스 대체 표시 미신설(D-09).
- `cd frontend && npm run build` 그린.
</verification>

<success_criteria>
- 대시보드에서 수집 헬스가 제거되고 관리자 콘솔 'API 상태'로만 확인된다(POLISH-01, D-07/D-08).
- 대시보드가 각인→재료 세로 섹션 + 가로 전폭 행으로 재구성된다(POLISH-04, D-10/D-11/D-12).
- 카드 딥링크·아이콘·역할배지·가격·시각 회귀 0(보존), 백엔드 0줄(D-06 정합).
- 라벨 'API 상태' 단일화로 정보 관점(운영자/클라이언트)이 분리된다.
</success_criteria>

<output>
Create `.planning/phases/17.1-demo-polish/17.1-03-SUMMARY.md` when done
</output>
