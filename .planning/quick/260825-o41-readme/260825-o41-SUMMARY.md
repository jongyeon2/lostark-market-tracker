---
quick_id: 260825-o41
slug: readme
description: README 다듬기 — 중복 제거·논문체 정리·용어 평이화·엔지니어링 핵심 본문 승격
date: 2026-08-25
status: complete
---

# SUMMARY — README 다듬기 (quick-260825-o41)

## 무엇을 했나

`README.md` 1파일. **292행 → 235행**(−57행), `<details>` 밖 본문 154행(그중 mermaid 44행 →
실질 텍스트 약 104행). 코드·설정·프론트엔드 무변경.

## Task 1 — 본문 재구성

- **상단 경고 박스 삭제**(구 47–51행). 이벤트 인과 부인이 경고 박스·설계 결정·API 설명 3곳에
  흩어져 있었고, 그중 경고 박스가 셋째 문단이라 읽는 사람이 "안 되는 것"부터 배우는 구조였다.
  → "설계에서 고민한 것" 항목 **1회**로 통합(`데이터 부족` 언급 1회로 실측 확인).
- **AI 협업 2곳 → 1곳**. 구 41–42행("AI로 일관된 결과물이…")을 삭제하고 하단 섹션으로 합쳐 3줄로 압축.
- **중복 링크 제거**. 구 57행의 `loaket.kr` 재등장과 "직접 보기" 섹션을 없애고, 화면 표를
  "무엇을 하나"로 흡수. 남은 5회는 히어로 링크 1 + 화면별 개별 경로 4(모두 다른 URL)로 중복 아님.
- **`<details>` → 본문 승격**: 아키텍처 mermaid, 설계 결정 3개, 대표 API 1개.
  🔑 GitHub `<details>`는 클릭해야 열리고 모바일에선 더 안 열려, 가장 보여줄 것이 서랍에 있었다.
- **설계 결정 5개 → 본문 3개 + 접기 2개**. 본문에 남긴 것은 호출 제한 / 인과 부인 / 점진적 구축
  (각각 이력서 bullet ①·⑥과 개발 프로세스 판단에 대응). Redis 선택·아이콘 폴백은 접기로 내렸다 —
  덜 차별적이고, Redis 항목은 호출 제한 항목과 내용이 겹친다.

## Task 2 — 문장·용어

- **논문체 3곳 재작성** — 구 33행(구조 예고 "…둘 + …하나"), 44행(되짚는 요약 "즉 소재는…"),
  202행(자의식적 제목 "정직한 '왜'"). "왜 만들었나"는 3개 bullet 각 1~2줄로 축약해 스캔 가능하게.
- **용어 평이화** (실측 잔존 0건): `레이트리밋`→`호출 제한`으로 통일(같은 개념을 두 이름으로
  부르고 있었음), `토큰버킷`→다이어그램 라벨 "호출 횟수 계산기", `하네스` 삭제,
  `모험의 서`→"모험의 서(대륙별 수집품)".
- 🔑 **`품목` → `아이템` 전면 통일**(0건 확인). 제목이 "아이템 시세 트래커"인데 본문 전체가
  "품목"이었다. 반대 방향으로 맞추면 제목·URL·이력서 문구까지 흔들리므로 제목 쪽으로 맞췄다.

## 검증

| 항목 | 결과 |
|---|---|
| `<details>` 밖 본문 | 154행 (mermaid 44 제외 시 ~104행) |
| `레이트리밋`·`하네스`·`품목`·`토큰버킷` | 각 0건 |
| `데이터 부족`(인과 부인) | 1회 |
| AI 섹션 | 1개 |
| `loaket.kr` | 5회 — 전부 서로 다른 경로 |
| mermaid 블록 | L62–L105, 라벨 문구만 변경(구조 무변경) |

⚠️ **미검증**: mermaid 실제 렌더링과 표 정렬은 GitHub 프리뷰에서 눈으로 확인 필요.
IDE 린터가 표 5곳에 "테이블 서식" 정보를 띄우지만 한글 전각 폭 정렬 지적이고, 원본도 같은
스타일이라 회귀가 아니다(헤더+구분행+앞 빈 줄 구조 정상 확인).

## 남긴 것 (의도적)

- **"기간 — 2026.06 ~ 2026.07 (약 1개월)" 무변경.** 저장소 최근 커밋이 2026-08-12라 불일치
  가능성이 있으나, **이력서 문구와 같은 값이어야 하므로** 사용자 확인 전에는 건드리지 않았다.
  면접에서 "언제까지 하셨어요"에 이력서와 다르게 답하는 게 이 파일의 어떤 문장보다 위험하다.
- 배지·면책 문구·스크린샷 경로·기술 스택 내용·기능 설명은 사실이므로 무변경. 배치와 문장만 손댔다.
