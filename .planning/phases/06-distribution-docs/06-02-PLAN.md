---
phase: 06-distribution-docs
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
autonomous: true
requirements: [DIST-04]
user_setup: []

must_haves:
  truths:
    - "README.md is rewritten into the single reviewer entry point: following ONLY the README, a reviewer can `docker compose up` (Postgres+Redis), run the app under the `seed` profile, and `curl` a non-empty `event-impact` response — the literal ROADMAP success criterion 4 reproduction, with no other doc required (DIST-04)."
    - "README carries the CI build badge `![CI](https://github.com/jongyeon2/lostark-market-tracker/actions/workflows/ci.yml/badge.svg)` near the top, pointing at the 06-01 workflow, so the green/red test state is visible at a glance (ROADMAP success criterion 1)."
    - "README includes an architecture diagram (ASCII or mermaid) of the pipeline: rate-limited `@Scheduled` collector → Postgres `price_snapshot` time series; a read API with Redis cache-aside (latest price) + admin event CRUD; and the event-impact temporal correlation layered on top (DIST-04, design §251)."
    - "README states the design trade-offs and the honest 'why': why Redis (cache-aside latest + token-bucket rate limit), why a per-key rate limiter (the external ~100/min budget), why `@Async` (explicitly acknowledged as a showcase), and the Approach A→B story (walking skeleton → decoupled collection); plus the correlation-≠-causation stance on event-impact (design §251/§253; PROJECT premise 5)."
    - "README documents local setup end to end: copy `.env.example`→`.env`, `docker compose up` for Postgres+Redis, then run with the dev profile (live collection needs `LOSTARK_API_KEY`) OR the **seed mode** (`--spring.profiles.active=seed`) which fills synthetic history and needs NO key (ROADMAP success criterion 3; ties to the 06-01 seed profile)."
    - "README shows ≥3 `curl` examples WITH sample JSON responses across the headline read surface — at minimum `GET /api/items`, `GET /api/items/{id}/prices?from=&to=`, and `GET /api/items/{id}/event-impact?window=24` — plus the admin write path (`POST /api/admin/events` with the `X-Admin-Secret` header), so the JSON IS the UI since there is no frontend (ROADMAP success criterion 3; design §255)."
    - "README contains the retention-policy one-liner in intent (design §256): the MVP keeps original snapshots with no deletion policy because composite indexes handle the expected volume, and rollup/partitioning/long-term retention is deferred to v2."
    - "Every documented route and JSON field matches the real code — paths come from ItemController / PricesController / EventImpactController / HealthController / Admin*Controller and the `{timestamp,status,error,message}` error contract; no invented endpoints or fields. curl examples use placeholder/env secrets (`$ADMIN_API_SECRET`), never a committed real key."
  artifacts:
    - path: "README.md"
      provides: "The reviewer-facing demo surface: CI badge, architecture diagram, design trade-offs + honest why (Redis / rate limit / @Async) + correlation≠causation, local setup + seed mode, ≥3 curl + sample JSON, admin X-Admin-Secret example, API-limit note, retention one-liner, and a clone→seed→curl reproduction block"
      contains: "event-impact"
  key_links:
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "CI badge URL actions/workflows/ci.yml/badge.svg (06-01 workflow)"
      pattern: "ci.yml/badge.svg"
    - from: "README.md"
      to: "the seed profile (06-01)"
      via: "seed-mode run instructions --spring.profiles.active=seed"
      pattern: "profiles.active=seed"
    - from: "README.md"
      to: "the read API controllers"
      via: "curl examples against the real endpoint paths"
      pattern: "/api/items"
---

<objective>
Rewrite `README.md` into the **single reproducible entry point** for a hiring reviewer (DIST-04). A reviewer who reads only the README must be able to: see at a glance that tests pass (CI badge), understand the architecture and the honest engineering trade-offs, and walk from clone → `docker compose up` → `seed` mode → a **non-empty `event-impact` curl response** — the literal ROADMAP success criterion 4 — without reading the source or asking a question.

Because there is no frontend, **the JSON is the UI**: the README sells the headline event-impact feature through `curl` examples + captured sample JSON. The content is already specified by the design doc's *README / Demo Artifact Requirements* (§251–257) — architecture diagram, design trade-offs, the honest "왜 Redis / 왜 레이트리밋 / 왜 @Async" rationale (with `@Async` admitted as a showcase), local run + seed mode, curl + sample JSON, the retention-policy one-liner (§256), and the CI badge. This plan depends on **06-01** for both the workflow the badge points at and the `seed` profile the demo run uses.

Output: a rewritten `README.md`. No code, schema, or test changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@docs/specs/2026-06-19-project-design.md

<interfaces>
Endpoints to document (verify each against its controller — do NOT invent routes or fields):
- GET  /api/items                                   — ItemController.list (active watchlist; List<TrackedItemResponse>)
- GET  /api/items/{id}/latest                       — ItemController.latest (LatestPriceResponse, Redis cache-aside)
- GET  /api/items/{id}/prices?from=&to=             — PricesController.prices (TimelineResponse: snapshots[] + events[], server-side downsample; from/to are UTC ISO-8601; to<=from -> 400; missing item -> 404)
- GET  /api/items/{id}/event-impact?window=N        — EventImpactController (EventImpactResponse: events[] with status "ok"/"insufficient_data" + changeRate; window required int 1..168, else 400; missing item -> 404)
- GET  /api/health/collection                       — HealthController (CollectionHealthResponse: last run timestamps + success/fail counts)
- GET  /actuator/health                             — Spring Boot Actuator (infra liveness; management exposure=health, show-details=always)
- POST /api/admin/items   (X-Admin-Secret)          — AdminItemController.create (TrackedItemRequest; 201/200/409)
- DELETE /api/admin/items/{id} (X-Admin-Secret)     — AdminItemController.delete (204 soft-delete)
- POST /api/admin/events  (X-Admin-Secret)          — AdminEventController.create (GameEventRequest; 201 + GameEventResponse)
- PUT/DELETE /api/admin/events/{id} (X-Admin-Secret)— AdminEventController.replace/delete (200 / 204)

Error contract (all 4xx): ApiErrorResponse record {OffsetDateTime timestamp, int status, String error, String message}; timestamp UTC ISO-8601 (...Z).

Setup facts (from the repo — match exactly):
- docker-compose.yml: services `postgres` (postgres:16, 5432) + `redis` (redis:7, 6379); credentials from `.env` (POSTGRES_DB/USER/PASSWORD). Copy `.env.example` -> `.env`.
- application.yml: datasource/redis from env with localhost compose defaults; `lostark.api.key` from `LOSTARK_API_KEY` (blank ok); `admin.api.secret` from `ADMIN_API_SECRET` (blank => /api/admin/** fails closed 401); actuator health exposed.
- Profiles: `dev` (live collection vs docker-compose; needs LOSTARK_API_KEY to fetch), `seed` (06-01 — synthetic history, NO key needed), `test` (Testcontainers). Run seed mode: `./gradlew bootRun --args='--spring.profiles.active=seed'` (or the equivalent `java -jar … --spring.profiles.active=seed`).
- Build/test: `./gradlew build` (CI runs this); local tests need Docker and `-PdockerApiVersion=1.44`.
- SecurityConfig: /api/** read surface is public (permitAll); only /api/admin/** is behind the X-Admin-Secret filter.

This plan modifies ONLY README.md — no code, schema, dependency, or test changes.
</interfaces>
</context>

## Phase Goal

**As** a hiring reviewer, **I want** one README that explains the architecture and trade-offs and walks me from clone to a non-empty `event-impact` response, **so that** I can judge the engineering and reproduce the headline feature without reading the source or asking any questions.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md into the reproducible demo surface (badge, architecture, trade-offs, setup + seed, curl + JSON, retention)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 success criteria 1, 3, 4 — badge, README content list, README-only reproduction)
    - docs/specs/2026-06-19-project-design.md (§103 Distribution Plan; §251–257 README/Demo Artifact Requirements — architecture, trade-offs, "왜 Redis/레이트리밋/@Async", curl+JSON §255, retention one-liner §256, badge §257; §45–54 Approaches A/B/C for the trade-off narrative)
    - README.md (the current placeholder being replaced)
    - src/main/java/com/lostark/tracker/web/ItemController.java + PricesController.java + EventImpactController.java + HealthController.java (exact GET paths/params for the curl examples)
    - src/main/java/com/lostark/tracker/web/admin/AdminEventController.java + AdminItemController.java (the X-Admin-Secret write paths + request bodies)
    - src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java + EventImpactResponse.java + EventImpactItem.java + LatestPriceResponse.java + CollectionHealthResponse.java + GameEventResponse.java + ApiErrorResponse.java (the exact field names for the sample JSON)
    - docker-compose.yml + .env.example + src/main/resources/application.yml (local-setup commands, env vars, profiles, actuator) + src/main/java/com/lostark/tracker/security/SecurityConfig.java (which paths need X-Admin-Secret vs public read)
  </read_first>
  <files>README.md</files>
  <behavior>
    - The README opens with the project one-liner, the CI badge, and "what this is / why it's interesting".
    - An architecture section shows a diagram and explains data flow (collect → store → serve → correlate).
    - A design-decisions section gives the honest "why" for Redis, the rate limiter, and @Async (showcase), plus correlation ≠ causation for event-impact.
    - A local-setup section walks: clone → `.env` → `docker compose up` → run (dev with key OR seed mode without key).
    - A demo section shows ≥3 curl commands with captured sample JSON, including the event-impact headline and an admin X-Admin-Secret example.
    - A short "Reproduce in 3 steps" block proves success criterion 4 (clone → seed → curl event-impact returns non-empty).
    - A retention-policy one-liner and an "API limits / scope" note are present.
  </behavior>
  <action>
    Replace `README.md` with the reviewer-facing demo surface. Required sections (derive every path/field from the read_first sources — do NOT invent):
    1. **Title + badge + summary** — project name, one-line what/why, and the CI badge `![CI](https://github.com/jongyeon2/lostark-market-tracker/actions/workflows/ci.yml/badge.svg)` (exact URL so it resolves to the 06-01 workflow).
    2. **Architecture** — an ASCII or ```mermaid``` diagram of: rate-limited `@Scheduled` collector (RestClient → Lostark markets API) → Postgres `price_snapshot` time series; read API (`/api/items…`) with Redis cache-aside for latest price; admin event CRUD behind X-Admin-Secret; event-impact correlation over snapshots+events. One paragraph on the collect→store→serve→correlate flow.
    3. **Design decisions & trade-offs** — the honest "why": why Redis (cache-aside latest + token-bucket rate limiter for the ~100/min external budget), why a per-key rate limiter, why `@Async` decoupled collection (explicitly state it is a showcase of the pattern, not strictly required at this scale), the Approach A→B story (walking skeleton → decoupled collection + Redis), and that event-impact `change_rate` is a **temporal correlation, not causation** (PROJECT premise 5).
    4. **Local setup** — copy `.env.example`→`.env`; `docker compose up` (Postgres 16 + Redis 7); run the app two ways: (a) **dev** (`--spring.profiles.active=dev`, needs `LOSTARK_API_KEY` for live collection), (b) **seed mode** (`--spring.profiles.active=seed`, fills synthetic 7-day history + demo events, **no API key needed**). State that `ADMIN_API_SECRET` blank => `/api/admin/**` is 401 (fails closed).
    5. **Demo — curl + sample JSON (≥3)** — at minimum `GET /api/items`, `GET /api/items/{id}/prices?from=&to=`, and `GET /api/items/{id}/event-impact?window=24`, each with a ```bash curl``` and a ```json``` captured response using the real DTO field names; plus an admin example `curl -H "X-Admin-Secret: $ADMIN_API_SECRET" -X POST .../api/admin/events` with a `GameEventRequest` body. Note the validation/error contract briefly (e.g. window 1..168 else 400 `{timestamp,status,error,message}`; missing item 404).
    6. **API limits / scope** — markets-only source, ~100/min rate-limit budget, downsampling for large ranges; a pointer to v2 backlog.
    7. **Data retention** — the one-liner (design §256): MVP keeps original snapshots with no deletion policy (composite indexes handle the expected volume); rollup/partitioning/long-term retention is v2.
    8. **Reproduce in 3 steps** — a copy-paste block: `docker compose up -d` → run app with `--spring.profiles.active=seed` → `curl .../api/items/1/event-impact?window=24` returns a non-empty `events` array with a `status:"ok"`. Link to `docs/specs/…` and the Phase docs for depth.
    Use placeholder/env secrets in all curl examples (`$ADMIN_API_SECRET`) — never a committed real key. Keep the badge filename `ci.yml` consistent with 06-01.
  </action>
  <verify>
    <automated>test "$(grep -c 'curl ' README.md)" -ge 3 && grep -q "actions/workflows/ci.yml/badge.svg" README.md && grep -Eq "compose up" README.md && grep -q "profiles.active=seed" README.md && grep -q "X-Admin-Secret" README.md && grep -q "event-impact" README.md && test "$(grep -c '```json' README.md)" -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - README contains the CI badge URL: `grep -n "actions/workflows/ci.yml/badge.svg" README.md` matches
    - README contains an architecture diagram (a fenced ``` or ```mermaid block) and a trade-offs/"why" section naming Redis, the rate limiter, and `@Async`
    - README has ≥3 `curl ` invocations and ≥3 ```json fenced sample responses: `grep -c "curl " README.md` ≥ 3 and `grep -c '```json' README.md` ≥ 3
    - README documents `docker compose up` (or `docker-compose up`) AND the seed mode `--spring.profiles.active=seed`: `grep -nE "compose up|profiles.active=seed" README.md` shows both
    - README contains an admin example with the `X-Admin-Secret` header and the retention one-liner (no-deletion MVP / rollup·partitioning v2) and a correlation-≠-causation statement
    - Every documented route exists in a controller (`/api/items`, `/api/items/{id}/prices`, `/api/items/{id}/event-impact`, `/api/admin/events`) — no invented route; no real secret committed
  </acceptance_criteria>
  <done>README.md is the single reproducible entry point — badge, architecture, honest trade-offs, local + seed setup, ≥3 curl + JSON, an admin X-Admin-Secret example, an API-limit note, the retention one-liner, and a clone→seed→curl reproduction block — all matching the real endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (README) → public reader | The README is public documentation; it must not leak secrets and must not mislead about the API contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0602-01 | Information Disclosure | secrets in README/curl examples | mitigate | curl examples use placeholder/env secrets (`X-Admin-Secret: $ADMIN_API_SECRET`) and reference `.env`/env vars — never a committed real `LOSTARK_API_KEY` or admin secret. Acceptance criteria forbid committed real secrets. |
| T-0602-02 | Misrepresentation (correlation as causation) | event-impact wording | mitigate | README explicitly frames `change_rate` as a temporal correlation, not causation (PROJECT premise 5), matching the API field semantics. |
| T-0602-03 | Tampering (stale docs drift from code) | curl paths / JSON fields | accept | Acceptance criteria pin every documented route to a real controller and JSON fields to real DTOs at write time; ongoing drift after future code changes is a v2 docs-maintenance concern. |
| T-0602-SC | n/a | docs only | accept | No code, dependency, or executable surface is changed by this plan. |
</threat_model>

<verification>
- A `grep` battery proves README contains the CI badge, an architecture/diagram block, ≥3 `curl` + ≥3 ```json sample responses, `docker compose up` + `--spring.profiles.active=seed` instructions, the admin `X-Admin-Secret` example, the retention one-liner, and a correlation-≠-causation statement.
- A manual read confirms a reviewer can follow the README from clone → `docker compose up` → seed mode → a non-empty `event-impact` curl response (ROADMAP success criterion 4).
- No code/test/schema changed, so the build is unaffected; documented routes/fields are cross-checked against the controllers and DTOs listed in `read_first` (no invented endpoints).
</verification>

<success_criteria>
- README carries a green CI badge pointing at the 06-01 `ci.yml` workflow (ROADMAP success criterion 1).
- README includes the architecture diagram, design trade-offs, ≥3 curl examples + sample JSON, local setup, seed mode, and the retention one-liner (DIST-04, ROADMAP success criterion 3).
- A reviewer following only the README reproduces a non-empty `event-impact` response via `docker compose up` + seed mode (ROADMAP success criterion 4).
- All documented endpoints and JSON fields match the real controllers/DTOs; no secret is committed in any example.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-docs/06-02-SUMMARY.md` when done
</output>
</content>
