---
phase: 18-free-deploy-security
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - docker-compose.prod.yml
  - .env.prod.example
  - .gitignore
  - src/main/resources/application.yml
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04]

must_haves:
  truths:
    - "D-05: `docker-compose.prod.yml`에서 **postgres·redis·app 모두 host 포트를 publish하지 않는다**(내부 `internal` 네트워크 전용). **오직 caddy만 80:80/443:443 publish** — DB/Redis/app은 인터넷에서 도달 불가(DEPLOY-03)."
    - "D-05: redis는 `--requirepass ${REDIS_PASSWORD}`로 기동(심층 방어). 앱이 AUTH할 수 있도록 base `application.yml`에 `spring.data.redis.password: ${REDIS_PASSWORD:}` 추가 — **빈 기본값이라 dev/test(비밀번호 없는 redis)는 무영향**."
    - "D-06: 모든 시크릿은 `.env.prod`(gitignore) → 컨테이너 env 주입. `.env.prod.example`(커밋)은 빈 값 + `openssl rand -hex 32` 생성 가이드만. `.gitignore`는 `.env.*`가 example까지 무시하므로 `!.env.prod.example` 예외 추가."
    - "D-05: app은 `depends_on: postgres/redis service_healthy`로 헬스 대기 후 기동, 전 서비스 `restart: unless-stopped`(부팅/크래시 복구). postgres 볼륨 영속. Core Value 수집/캐시/event-impact 로직 0줄."
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "단일 박스 prod 스택(app+postgres+redis+caddy) — 포트 최소 노출·헬스 게이트·영속 볼륨. 04 런북이 up 대상으로 사용"
    - path: ".env.prod.example"
      provides: "prod 시크릿 템플릿(빈 값·생성 가이드) — 04에서 .env.prod로 복사해 채움"
    - path: "src/main/resources/application.yml"
      provides: "redis password 플레이스홀더(빈 기본) — prod requirepass와 정합, dev 무영향"
  key_links:
    - from: "docker-compose.prod.yml app.build → Dockerfile"
      to: "18-01 Dockerfile"
      via: "app 이미지 빌드"
      pattern: "dockerfile: Dockerfile"
    - from: "docker-compose.prod.yml caddy.build → frontend/Dockerfile (context .)"
      to: "18-02 frontend/Dockerfile + Caddyfile"
      via: "web 이미지 빌드(80/443 publish)"
      pattern: "dockerfile: frontend/Dockerfile"
    - from: ".env.prod (SITE_ADDRESS·ACME_EMAIL·시크릿)"
      to: "18-04 런북(.env.prod 작성 단계)"
      via: "env_file/environment 주입"
      pattern: ".env.prod"
---

<objective>
**보안 네트워킹 prod 스택**을 정의한다: `docker-compose.prod.yml`에서 app·postgres·redis는 host 포트를 열지 않고 오직 caddy만 80/443을 publish한다. redis는 requirepass로 기동하고(앱은 base yml의 redis password로 AUTH), 모든 시크릿은 `.env.prod`(gitignore)에서 주입하며 `.env.prod.example` 템플릿을 커밋한다.

Purpose: DEPLOY-03(DB/Redis 포트 비공개)과 DEPLOY-04(시크릿 env-only)를 인프라로 강제한다. 공격 표면을 caddy(80/443) 한 곳으로 줄이고, DB/Redis/app을 내부망에 격리한다.

Output: `docker-compose.prod.yml`·`.env.prod.example` 신규 + `.gitignore` 예외 1줄 + base `application.yml` redis password 1줄(dev 무영향). 수집/캐시/event-impact 로직 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-free-deploy-security/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- docker-compose.prod.yml (repo 루트):
  services:
    app:
      build: { context: ., dockerfile: Dockerfile }
      environment:
        SPRING_PROFILES_ACTIVE: prod
        POSTGRES_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        REDIS_HOST: redis
        REDIS_PORT: "6379"
        REDIS_PASSWORD: ${REDIS_PASSWORD}
        LOSTARK_API_KEY: ${LOSTARK_API_KEY}
        ADMIN_API_SECRET: ${ADMIN_API_SECRET}
      depends_on: { postgres: {condition: service_healthy}, redis: {condition: service_healthy} }
      restart: unless-stopped
      networks: [internal]
      # ★ NO ports: — caddy 경유로만 도달
    postgres:
      image: postgres:16
      environment: { POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD }
      volumes: [pgdata:/var/lib/postgresql/data]
      healthcheck: pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}
      restart: unless-stopped
      networks: [internal]
      # ★ NO ports:
    redis:
      image: redis:7
      command: ["redis-server","--requirepass","${REDIS_PASSWORD}"]
      healthcheck: ["CMD","redis-cli","-a","${REDIS_PASSWORD}","ping"]
      restart: unless-stopped
      networks: [internal]
      # ★ NO ports:
    caddy:
      build: { context: ., dockerfile: frontend/Dockerfile }
      ports: ["80:80","443:443"]      # ★ 유일한 publish
      environment: { SITE_ADDRESS: ${SITE_ADDRESS}, ACME_EMAIL: ${ACME_EMAIL} }
      volumes: [caddy_data:/data, caddy_config:/config]
      depends_on: [app]
      restart: unless-stopped
      networks: [internal]
  networks: { internal: {} }
  volumes: { pgdata: {}, caddy_data: {}, caddy_config: {} }
  ★ env_file 또는 셸 env로 ${...} 해석 — compose는 같은 디렉터리 .env.prod을 자동으로 읽지 않으므로 `--env-file .env.prod` 사용(04 런북 명시). -->
<!-- application.yml: spring.data.redis 블록에 password: ${REDIS_PASSWORD:} 추가(host/port 아래). 빈 기본값 → dev/test 무영향(비밀번호 없는 redis는 blank=no-auth). -->
<!-- .env.prod.example (커밋): 헤더 주석 "cp .env.prod.example .env.prod 후 실값 채움 — .env.prod은 절대 커밋 금지". 키:
     POSTGRES_DB=lostark / POSTGRES_USER=lostark / POSTGRES_PASSWORD=   # openssl rand -hex 32
     REDIS_PASSWORD=            # openssl rand -hex 32
     LOSTARK_API_KEY=           # 개발자 포털 JWT
     ADMIN_API_SECRET=          # openssl rand -hex 32 (dev의 123456789 절대 금지)
     SITE_ADDRESS=your-sub.duckdns.org
     ACME_EMAIL=you@example.com
     POSTGRES_URL·REDIS_HOST·REDIS_PORT은 compose가 서비스명으로 세팅하므로 example엔 주석으로만 안내. -->
<!-- .gitignore: `!.env.example` 아래에 `!.env.prod.example` 추가(.env.* 무시에서 예외). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.prod.yml (보안 네트워킹) + redis password 배선</name>
  <read_first>
    - docker-compose.yml (dev — 서비스 정의·healthcheck·볼륨 원형. prod는 포트 미publish로 하드닝)
    - src/main/resources/application.yml (spring.data.redis 블록 위치 — password 추가 지점)
    - Dockerfile (18-01 산출 — app build 대상)
    - frontend/Dockerfile (18-02 산출 — caddy/web build 대상, context .)
  </read_first>
  <files>docker-compose.prod.yml, src/main/resources/application.yml</files>
  <action>
    `docker-compose.prod.yml`을 만든다(인터페이스 명세대로). 핵심: **app·postgres·redis에 `ports:` 없음**(내부 `internal` 네트워크 전용), **caddy만 `ports: ["80:80","443:443"]`**. app은 `build: {context: ., dockerfile: Dockerfile}` + `SPRING_PROFILES_ACTIVE=prod` + env(POSTGRES_URL은 `jdbc:postgresql://postgres:5432/${POSTGRES_DB}`, REDIS_HOST=redis, REDIS_PASSWORD 등) + `depends_on` 헬스 조건. postgres는 pgdata 볼륨 + pg_isready 헬스체크. redis는 `command: ["redis-server","--requirepass","${REDIS_PASSWORD}"]` + `-a` 인증 헬스체크. caddy는 `build: {context: ., dockerfile: frontend/Dockerfile}` + SITE_ADDRESS/ACME_EMAIL env + caddy_data/config 볼륨. 전 서비스 `restart: unless-stopped`, `networks: [internal]`. 그다음 `application.yml`의 `spring.data.redis` 블록에 `password: ${REDIS_PASSWORD:}`(빈 기본값)를 추가한다 — dev/test는 빈 값이라 무영향.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.prod.yml --env-file .env.prod.example config >/dev/null 2>&1 && echo COMPOSE_OK; ! grep -E "^\s*-\s*\"?(5432|6379|8080):" docker-compose.prod.yml && echo NO_DB_PORTS; ./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - postgres·redis·app 서비스에 host 포트 publish **없음** — `grep -E '(5432|6379|8080):' docker-compose.prod.yml`가 caddy 외 매칭 0(오직 caddy만 80/443)
    - caddy만 `ports: ["80:80","443:443"]` 보유
    - redis가 `--requirepass ${REDIS_PASSWORD}`로 기동
    - `application.yml`에 `password: ${REDIS_PASSWORD:}` 추가 — `grep -q "password: ..REDIS_PASSWORD" src/main/resources/application.yml` 성립
    - `docker compose -f docker-compose.prod.yml --env-file .env.prod.example config` 유효(구문 통과) — Docker 미가용 시 수동 검증 표기
    - `./gradlew build` 그린 — redis password 추가가 dev/test(비밀번호 없는 Testcontainers redis) 무영향임을 IT로 확인
  </acceptance_criteria>
  <done>DB/Redis/app이 인터넷에서 도달 불가한 내부망에 격리되고 caddy만 공개되며, redis requirepass가 앱 AUTH와 정합하고 dev/test는 무영향이다.</done>
</task>

<task type="auto">
  <name>Task 2: .env.prod.example (시크릿 템플릿) + .gitignore 예외</name>
  <read_first>
    - .env.example (기존 시크릿 env 컨벤션·주석 스타일 원형)
    - .gitignore (env 패턴 `.env.*` / `!.env.example` — 예외 추가 지점)
  </read_first>
  <files>.env.prod.example, .gitignore</files>
  <action>
    `.env.prod.example`을 만든다: 헤더 주석에 "cp .env.prod.example .env.prod 후 실값 입력 — `.env.prod`은 절대 커밋 금지". 키와 가이드 주석: `POSTGRES_DB=lostark`, `POSTGRES_USER=lostark`, `POSTGRES_PASSWORD=`(주석 `# openssl rand -hex 32`), `REDIS_PASSWORD=`(`# openssl rand -hex 32`), `LOSTARK_API_KEY=`(`# 개발자 포털 JWT`), `ADMIN_API_SECRET=`(`# openssl rand -hex 32 — dev의 123456789 절대 금지`), `SITE_ADDRESS=your-sub.duckdns.org`, `ACME_EMAIL=you@example.com`. `POSTGRES_URL`/`REDIS_HOST`/`REDIS_PORT`은 compose가 서비스명으로 세팅함을 주석으로 안내(실값 없음). 그다음 `.gitignore`의 `!.env.example` 다음 줄에 `!.env.prod.example`을 추가한다.
  </action>
  <verify>
    <automated>git check-ignore -v .env.prod.example; test $? -ne 0 && echo EXAMPLE_TRACKABLE; git check-ignore .env.prod >/dev/null && echo PROD_IGNORED</automated>
  </verify>
  <acceptance_criteria>
    - `.env.prod.example`이 커밋 가능 — `git check-ignore .env.prod.example`가 매칭 없음(예외 규칙 적용)
    - `.env.prod`은 여전히 무시됨 — `git check-ignore .env.prod`가 매칭(시크릿 커밋 방지)
    - `.env.prod.example`에 실 시크릿 값 부재(모두 빈 값 또는 플레이스홀더) — `ADMIN_API_SECRET=`, `POSTGRES_PASSWORD=` 등 빈 우변
    - `.env.prod.example`에 `123456789` 같은 dev 시크릿 미포함
    - `.gitignore`에 `!.env.prod.example` 존재
  </acceptance_criteria>
  <done>prod 시크릿 템플릿이 안전하게 커밋되고 실제 `.env.prod`은 계속 무시돼 시크릿이 저장소에 유입되지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → 스택 | caddy(80/443)만 공개. postgres/redis/app은 내부 docker 네트워크 격리 |
| 시크릿 저장 | 실 시크릿은 .env.prod(gitignore)만. 저장소엔 빈 템플릿(.env.prod.example)만 |
| redis 접근 | 내부망 + requirepass 이중 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Info Disclosure | 공개된 5432/6379로 DB/Redis 직접 접근 | mitigate | 포트 미publish(내부망 전용) — 인터넷 도달 불가(DEPLOY-03) |
| T-18-03-02 | Elevation of Privilege | 인증 없는 redis에 내부망 접근 | mitigate | requirepass + 미publish 이중 방어 |
| T-18-03-03 | Info Disclosure | 시크릿이 저장소에 유입 | mitigate | .env.prod gitignore 유지 + example엔 빈 값만, `git check-ignore`로 검증 |
| T-18-03-04 | Spoofing | 약한/기본 시크릿(123456789·lostark) | mitigate | openssl rand 강생성 가이드, dev 기본값 폐기 명시 |
</threat_model>

<verification>
- `docker-compose.prod.yml`: postgres/redis/app에 `ports:` 없음, caddy만 80/443. redis `--requirepass`. depends_on 헬스 게이트. restart 정책.
- `application.yml`에 `password: ${REDIS_PASSWORD:}` — `./gradlew build` 그린(dev/test 무영향).
- `git check-ignore`: `.env.prod` 무시 O, `.env.prod.example` 무시 X.
- 수집/캐시/event-impact 로직 diff 0줄.
</verification>

<success_criteria>
- DB/Redis 포트가 비공개되고 시크릿이 서버 env로만 주입된다 — 배포 직전 보안 게이트의 포트·시크릿 항목 선반영(DEPLOY-03/04).
- prod 스택이 헬스 게이트·영속 볼륨·재시작 정책으로 단일 박스에서 안정 기동한다.
</success_criteria>

<output>
Create `.planning/phases/18-free-deploy-security/18-03-SUMMARY.md` when done
</output>
