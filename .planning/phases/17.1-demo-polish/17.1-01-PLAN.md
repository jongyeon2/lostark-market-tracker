---
phase: 17.1-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java
  - .planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md
autonomous: false
requirements: [POLISH-02, POLISH-03]

must_haves:
  truths:
    - "D-02: 신규 10개(딜러 각인 3 질량 증가·슈퍼 차지·바리케이드 + 서포터 각인 5 구슬동자·마나의 흐름·폭발물 전문가·분쇄의 주먹·중갑착용 + 재료 2 운명의 파괴석 결정·운명의 수호석 결정)의 실 API Id/Icon/CategoryCode를 라이브 1회 실측해 17.1-SPIKE-FINDINGS.md에 실키로 잠근다 — 추정/플레이스홀더 0건(Phase 12 spike-then-lock 동형)"
    - "D-03: 신규 각인 8종을 전부 '유물' 등급 각인서(displayName '유물 X 각인서', CategoryCode=40000)로 실측·잠그고, 유물 각인서는 공유 글리프(use_9_25.png)를 쓰므로 아이콘이 아닌 라벨로 식별됨을 findings에 명시한다"
    - "17.1-SPIKE-FINDINGS.md에 신규 10개 각각의 6필드(external_item_id·display_name·category_code·icon_url·item_group·role_group)가 실측값으로 기재되고, 유물 등급 미존재/시세 희소 품목은 fallback 처분(등급 조정 또는 목록 제외)이 휴먼 비준으로 확정된다"
    - "실 JWT 키·원본 가격·계정 식별자가 findings·커밋 어디에도 기재되지 않는다 — env LOSTARK_API_KEY만 사용, `bearer`/`eyJ...` 토큰 grep 0건(Phase 12 (f) 키-안전 계승)"
    - "스파이크 하니스(MarketsApiSpikeTest)는 실측 후에도 @Disabled·spike 프로파일을 유지 — `./gradlew build`가 키 없이 그린(CI 미실행)"
  artifacts:
    - path: ".planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md"
      provides: "신규 10개 실측 6필드 잠금표 + 세트 델타(제거 3·신규 10) + 재련 재료 CategoryCode 실측 + 키-안전 노트 (D-02/D-03)"
  key_links:
    - from: "17.1-SPIKE-FINDINGS.md (신규 10개 external_item_id/icon_url/category_code)"
      to: "WatchlistSeeder.WATCHLIST (17.1-02 전사 대상)"
      via: "spike-then-lock 전사 — findings의 실측값을 seeder에 그대로 옮김"
      pattern: "external_item_id"
---

<objective>
Phase 17.1 큐레이션 갱신(POLISH-02/03)에 필요한 **신규 10개 품목의 실 API 메타데이터를 라이브 1회 실측**해 `17.1-SPIKE-FINDINGS.md`에 잠근다(D-02). Phase 12 spike-then-lock 방법론(12-SPIKE-FINDINGS.md)을 재적용 — 기존 잠긴 12종(유지)은 재실측하지 않고, **세트 델타로 새로 들어오는 10종만** 실측한다. 런타임 키는 불필요하며 데이터만 잠근다.

- **D-02 — 신규 10개 실측:** 딜러 각인 3(질량 증가·슈퍼 차지·바리케이드) + 서포터 각인 5(구슬동자·마나의 흐름·폭발물 전문가·분쇄의 주먹·중갑착용) + 재료 2(운명의 파괴석 결정·운명의 수호석 결정). 각 품목의 실 `Id`/`Icon`(CDN 파일명)/`CategoryCode`를 `POST /markets/items`로 실측.
- **D-03 — 유물 등급 통일:** 신규 각인 8종은 전부 **유물 각인서**(`유물 X 각인서`, category=40000)로 실측. 유물 각인서 공유 글리프(`use_9_25.png`)이므로 라벨로 식별(fallback 미대체)임을 findings에 명시.
- **재련 재료 CategoryCode:** 운명의 파괴석/수호석 결정은 융화재료(50010)가 아닌 **재련 재료** — 실 `CategoryCode`(50010 아님, 예: 50020 등)를 실측으로 확정한다(D-04 다운스트림 전제).

Purpose: WatchlistSeeder 재잠금(17.1-02) 전에 신규 품목의 실 거래·아이콘·카테고리 존재를 실 API로 검증해, "큐레이션 = 안목의 선택(덤프 아님)"과 도메인 안목 서사를 실데이터로 뒷받침한다.

Output: 신규 10개 6필드 잠금표를 담은 `17.1-SPIKE-FINDINGS.md`(공개 메타데이터만). MarketsApiSpikeTest는 @Disabled 유지, 코드/시드/read-path 변경 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-demo-polish/17.1-CONTEXT.md
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@CLAUDE.md

<interfaces>
<!-- 스파이크 하니스(Phase 12): src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (@Disabled, spike 프로파일) + src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java + src/test/resources/application-spike.yml. LostarkSpikeClient가 POST /markets/items(CategoryCode + ItemName)로 검색, 콘솔 출력만 사용(raw 디스크 미덤프, Phase 12 D-10). -->
<!-- 실측 방법(12-SPIKE-FINDINGS (a)(b)(c) 계승): (1) 각인서=CategoryCode 40000(leaf), 등급 필터로 '유물'만. (2) Icon 필드=파일명, CDN base=https://cdn-lostark.game.onstove.com/efui_iconatlas/use/ . (3) 재련 재료는 강화 재료(50000) 하위 leaf에서 검색 — 50010(융화재료)이 아님. GET /markets/options로 leaf CategoryCode 후보(50020 재련 추가 재료 등) 확인 후 ItemName으로 실검색. -->
<!-- 잠금 스키마(전사 대상): external_item_id / display_name / category_code / icon_url / item_group / role_group. 신규 각인 8 → item_group=각인서, role_group=DEALER 또는 SUPPORT. 신규 재료 2 → item_group=강화재료(또는 재련재료 — 카드 미표기이므로 재량, CONTEXT Discretion), role_group=MATERIAL. -->
<!-- 키-안전(Phase 12 (f) 상시 가드): 실 JWT는 env LOSTARK_API_KEY에서만. .env는 gitignored. findings·커밋엔 공개 메타(id/name/grade/category/iconUrl)만 — 가격 원문·키·계정 식별자 금지. 커밋 전 grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" → 0건. -->
<!-- AUTH_ERROR 시: .env LOSTARK_API_KEY 유효 확인(로아 포털 재발급 가능) — 인증 실패는 실행 시점 checkpoint로 처리(사전 계획 아님). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 신규 10개 실 API 실측(MarketsApiSpikeTest 확장 + 라이브 1회)</name>
  <read_first>
    - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (Phase 12 스파이크 진입점 — @Disabled/spike 프로파일, 확장 대상)
    - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java (POST /markets/items·GET /markets/options 호출 클라이언트 — 시그니처 재사용)
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md ((a) CategoryCode·(b) Icon·(c) 6필드 표 — 실측 절차 원형)
    - src/test/resources/application-spike.yml (spike 프로파일 설정)
  </read_first>
  <files>src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java</files>
  <action>
    MarketsApiSpikeTest에 **신규 10개 검색 케이스**를 추가한다. (1) 각인서 8종 — ItemName으로 각각 검색(질량 증가·슈퍼 차지·바리케이드·구슬동자·마나의 흐름·폭발물 전문가·분쇄의 주먹·중갑착용), CategoryCode=40000, 등급 '유물'만 채택해 실 `Id`/`Icon` 확보(displayName은 '유물 {이름} 각인서'). (2) 재료 2종 — 운명의 파괴석 결정·운명의 수호석 결정: 먼저 `GET /markets/options`로 재련 재료 leaf CategoryCode를 확인(50010 융화재료 아님)한 뒤 그 CategoryCode + ItemName으로 실검색해 실 `Id`/`Icon`/`CategoryCode` 확보. 실행은 **spike 프로파일 + env LOSTARK_API_KEY**로 1세션만(`./gradlew test --tests MarketsApiSpikeTest -Dspring.profiles.active=spike` 형태, @Disabled는 실행 시에만 임시 해제하거나 spike 전용 진입 사용). **콘솔 출력만** 사용(raw 응답 디스크 미덤프 — Phase 12 D-10). 실측 종료 후 테스트는 **@Disabled·spike 프로파일 상태로 되돌려** CI가 키 없이 그린이도록 한다. AUTH_ERROR가 나면 [[lostark-dev-collection-key-debug]] 절차로 .env LOSTARK_API_KEY 유효를 확인(실행 시점 인증 checkpoint). PriceCollector/캐시/event-impact·WatchlistSeeder·SyntheticDemodata는 이 태스크에서 **일절 건드리지 않는다**(D-06).
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - MarketsApiSpikeTest가 신규 10개(각인 8 + 재료 2)의 ItemName 검색 케이스를 포함한다
    - 실행 후 MarketsApiSpikeTest는 @Disabled·spike 프로파일 상태 — `./gradlew build`가 env 키 없이 그린(스파이크는 CI 미실행)
    - 실측 세션이 각 신규 품목의 실 Id/Icon/CategoryCode를 콘솔로 반환(다음 태스크 findings 입력)
    - 백엔드 수집/캐시/event-impact 및 WatchlistSeeder/SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>신규 10개를 실 API로 실측할 수 있는 스파이크 케이스가 준비·실행되고, 하니스는 키 없이 그린인 @Disabled 상태로 복귀하며, 실측값이 콘솔로 확보된다.</done>
</task>

<task type="auto">
  <name>Task 2: 17.1-SPIKE-FINDINGS.md 작성 — 신규 10개 6필드 잠금 + 세트 델타 + 키-안전</name>
  <read_first>
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md (표 포맷·(c) 6필드·(f) 키-안전 노트 계승)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-01 세트 델타·D-02/D-03 실측 범위·Discretion fallback)
  </read_first>
  <files>.planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md</files>
  <action>
    Task 1 실측 콘솔 출력을 근거로 `17.1-SPIKE-FINDINGS.md`를 작성한다(12-SPIKE-FINDINGS 포맷 계승). 포함: (1) **세트 델타 요약** — 제거 3(정밀 단도 `65204305`·상급 오레하 `6861009`·최상급 오레하 `6861011`), 신규 10(각인 8 + 재료 2), 유지 12. (2) **신규 10개 6필드 잠금표**(external_item_id·display_name·category_code·icon_url·item_group·role_group) — 각 행은 **실측 external_item_id·icon 파일명** 보유(추정/플레이스홀더 0건). icon_url은 CDN base + 파일명으로 표기. (3) **유물 각인서 아이콘 식별 노트**(D-03) — 신규 유물 각인서 8종도 `use_9_25.png` 공유 가정 검증, 라벨 병기로 식별. (4) **재련 재료 CategoryCode 실측**(운명의 결정 = 융화재료 50010 아님 — 실 leaf code 기재, D-04 전제). (5) **fallback 처분**(Discretion) — 실측 시 유물 등급 미존재/시세 희소 품목이 있으면 그 처분(등급 조정 or 목록 제외)을 표로 명시(없으면 '전 품목 유물·거래 확인'). (6) **키-안전 노트** — 공개 메타만 기재, 실키/가격 원문 미기재(Phase 12 (f)). 커밋 전 `grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/17.1-demo-polish/` → 0건 확인.
  </action>
  <verify>
    <automated>test -f .planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md && grep -c "role_group" .planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md | grep -qvx 0 && ! grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/17.1-demo-polish/</automated>
  </verify>
  <acceptance_criteria>
    - 17.1-SPIKE-FINDINGS.md에 신규 10개 각각의 6필드가 실측값으로 기재된다(external_item_id·icon_url 추정 0건)
    - 세트 델타(제거 3·신규 10·유지 12)와 재련 재료 실 CategoryCode(50010 아님)가 기재된다
    - 유물 각인서 라벨-식별 노트(D-03)와 fallback 처분표(Discretion)가 포함된다
    - findings·phase 디렉토리에 `bearer`/`eyJ...` 토큰·실 가격 원문이 없다(grep 0건)
  </acceptance_criteria>
  <done>신규 10개 실측값이 6필드로 잠긴 17.1-SPIKE-FINDINGS.md가 존재하고, 세트 델타·재련 CategoryCode·라벨 식별·fallback·키-안전이 문서화되며 실키/가격이 새지 않는다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 스파이크 실측 결과 휴먼 비준(잠금 전 게이트)</name>
  <action>17.1-SPIKE-FINDINGS.md의 신규 10개 실측값(특히 유물 등급 존재·거래 가능 여부, 재련 재료 CategoryCode, fallback 처분)을 사용자가 검토·비준한다. 비준 전에는 17.1-02(WatchlistSeeder 재잠금)로 진행하지 않는다.</action>
  <what-built>신규 10개(각인 8 + 재료 2)의 실 API Id/Icon/CategoryCode를 실측해 17.1-SPIKE-FINDINGS.md에 6필드로 잠갔고, 세트 델타·유물 라벨 식별·재련 CategoryCode·fallback 처분을 문서화했다.</what-built>
  <how-to-verify>
    1. `.planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md`의 신규 10개 6필드 표를 확인한다.
    2. 신규 각인 8종이 전부 '유물' 등급으로 실재·거래 가능한지(등급 미존재/시세 희소 시 fallback 처분이 타당한지) 검토한다.
    3. 운명의 파괴석/수호석 결정의 실 CategoryCode가 50010(융화재료)이 아닌 재련 재료 leaf인지 확인한다.
    4. `bearer`/`eyJ...` 토큰·실 가격 원문이 문서에 없는지 확인한다.
  </how-to-verify>
  <verify>
    <human-check>신규 10개 실측값이 정확하고, fallback 처분이 타당하며, 실키/가격이 새지 않았음을 사용자가 확인</human-check>
  </verify>
  <resume-signal>Type "approved" 또는 수정할 품목/처분을 알려주세요</resume-signal>
  <done>사용자가 17.1-SPIKE-FINDINGS.md의 신규 10개 실측값과 fallback 처분을 비준했다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 셸(env) → 외부 Lostark API | 실 JWT(LOSTARK_API_KEY)로 실 마켓 API 호출 — 키는 env에서만 공급, 소스/findings/CI 진입 금지 |
| 실측 결과 → 커밋(공개 저장소) | 17.1-SPIKE-FINDINGS.md는 커밋되어 공개 — 공개 메타데이터(id/name/category/icon)만, 실키·가격 원문 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-01 | Information Disclosure | 실측 중 실 JWT 키 / 원본 가격 / 계정 식별자가 findings·커밋에 유입 | mitigate | 키는 env LOSTARK_API_KEY에서만, findings는 공개 메타(id/name/category/icon)만 — verify가 `bearer`/`eyJ...` 토큰 부재를 grep로 강제. 콘솔 출력만 사용(raw 디스크 미덤프) |
| T-171-02 | Tampering | 잘못된/추정 Id·CategoryCode를 잠그면 17.1-02 seeder가 존재하지 않는 품목을 수집 시도 | mitigate | 신규 10개 전부 실 API 실측값만 기재(추정 0건) + Task 3 휴먼 비준 게이트로 잠금 전 검증 |
| T-171-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 스파이크 하니스(LostarkSpikeClient)만 재사용, 설치 태스크 없음 |
</threat_model>

<verification>
- `17.1-SPIKE-FINDINGS.md`에 신규 10개(각인 8 + 재료 2) 6필드가 실측값으로 잠김 — 추정/플레이스홀더 0건, 세트 델타(제거 3·신규 10) 명시.
- 신규 각인 8종 = 유물 등급, 라벨 식별 노트(use_9_25 공유) 기재; 재련 재료 실 CategoryCode(50010 아님) 기재.
- `./gradlew build`가 env 키 없이 그린 — MarketsApiSpikeTest는 @Disabled·spike 프로파일 유지.
- phase 디렉토리에 `bearer`/`eyJ...` 토큰·실 가격 원문 없음(grep 0건).
- 백엔드 수집/캐시/event-impact·WatchlistSeeder·SyntheticDemoData diff 0줄(D-06).
- Task 3 휴먼 비준 완료.
</verification>

<success_criteria>
- 신규 10개 실 API 메타데이터가 17.1-SPIKE-FINDINGS.md에 실키로 잠긴다(D-02, POLISH-02/03 전제).
- 신규 각인 8종이 유물 등급으로 실측·확정되고 라벨 식별이 문서화된다(D-03).
- 재련 재료 CategoryCode가 실측으로 확정된다(D-04 다운스트림 전제).
- 실키/가격 미기재, 스파이크 하니스 CI-안전(@Disabled), 코드/시드 회귀 0줄.
</success_criteria>

<output>
Create `.planning/phases/17.1-demo-polish/17.1-01-SUMMARY.md` when done
</output>
