---
phase: 17.4-timeline-gap-backfill
plan: 02
subsystem: api
tags: [collection, jackson, ydayavgprice, backfill, fail-open, testcontainers]

requires:
  - phase: 17.4-01
    provides: ItemDailyStatRepository.upsertDailyStat idempotent write contract + DailyStatSource.YDAY_AVG
provides:
  - "MarketItem.yDayAvgPrice mapping (@JsonProperty YDayAvgPrice) — free on the already-fetched list response"
  - "ItemFetchResult carries yDayAvgPrice as a separate metric from minPrice"
  - "BackfillCaptureService.captureYDayAvg — fail-open going-forward daily-average capture (source=YDAY_AVG, prev KST day)"
  - "PriceCollector additive hook: capture runs right after persistSnapshot on each success"
affects: [17.4-04]

tech-stack:
  added: []
  patterns:
    - "Additive fail-open hook on the collection tick — capture swallows RuntimeException so it never breaks price collection"
    - "Reuse an already-fetched API field (zero extra API calls) instead of a new request"

key-files:
  created:
    - src/main/java/com/lostark/tracker/backfill/BackfillCaptureService.java
    - src/test/java/com/lostark/tracker/backfill/BackfillCaptureServiceIT.java
  modified:
    - src/main/java/com/lostark/tracker/collect/dto/MarketItem.java
    - src/main/java/com/lostark/tracker/collect/ItemFetchResult.java
    - src/main/java/com/lostark/tracker/collect/ItemFetchService.java
    - src/main/java/com/lostark/tracker/collect/PriceCollector.java

key-decisions:
  - "Existing collection ITs (PriceCollectionIT, CollectionResilienceIT, LatestPriceCacheIT) autowire the real BackfillCaptureService bean and pass it to their hand-built PriceCollector — their MarketItems carry yDayAvgPrice=null so capture is a no-op, needing no cleanup"
  - "Fail-open IT triggers a real RuntimeException via an unsaved item (null id → null tracked_item_id bind) instead of a mock"

patterns-established:
  - "YDayAvgPrice (prev-day traded avg) stamped on the previous KST day, kept separate from min_price (real-time ask)"

requirements-completed: [BACKFILL-01, BACKFILL-04]

duration: ~25min
completed: 2026-07-07
---

# Phase 17.4-02: YDayAvgPrice 무료 캡처 Summary

**리스트 응답의 `YDayAvgPrice`(전 품목·전일 평균)를 매 수집 성공마다 `item_daily_stats`(YDAY_AVG, 전일 KST)에 fail-open 멱등 upsert — 추가 API 호출 0, 수집 핵심 경로 무변경.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-07
- **Tasks:** 2
- **Files created:** 2, modified: 4 (+3 기존 IT 생성자 갱신)

## Accomplishments
- `MarketItem`에 `@JsonProperty("YDayAvgPrice") Double yDayAvgPrice` 매핑(이미 페치된 응답에서 필드 하나 더 읽음 — 신규 호출 0).
- `ItemFetchResult`가 `yDayAvgPrice`를 `minPrice`(실측 호가)와 **별개 지표**로 운반; `success` 팩토리 확장, `failed`는 null.
- `BackfillCaptureService.captureYDayAvg`: 성공 결과의 YDayAvgPrice를 전일(KST) `stat_date`로 `source=YDAY_AVG` 멱등 upsert. null이면 no-op, 예외는 **fail-open**으로 삼킴(item id만 로깅).
- `PriceCollector`가 `persistSnapshot` 직후 캡처 후킹 1줄 호출 — 스냅샷 저장·evict·레이트리밋·run 카운트 로직 무변경.
- `BackfillCaptureServiceIT`: 전일 KST 스탬핑·소수 보존(147007.4)·멱등·null no-op·fail-open을 Testcontainers로 증명.

## Task Commits

1. **Task 1: MarketItem/ItemFetchResult/ItemFetchService — YDayAvgPrice 운반** — `99ff1c8` (feat)
2. **Task 2: BackfillCaptureService + PriceCollector 후킹 + IT** — `75e7982` (feat)

## Files Created/Modified
- `backfill/BackfillCaptureService.java` — 수집 편승 fail-open 캡처(신규)
- `collect/dto/MarketItem.java` — YDayAvgPrice 매핑 추가
- `collect/ItemFetchResult.java` — yDayAvgPrice 운반 필드
- `collect/ItemFetchService.java` — 성공 리턴에 match.yDayAvgPrice() 전달(1줄)
- `collect/PriceCollector.java` — BackfillCaptureService 주입 + persistSnapshot 직후 후킹(additive)
- `backfill/BackfillCaptureServiceIT.java` — 캡처 계약 IT(신규)
- (기존 IT 3건) MarketItem 4-arg + PriceCollector 생성자 인자 컴파일 갱신

## Decisions Made
- **기존 IT의 PriceCollector 생성자** — 실제 `BackfillCaptureService` 빈을 autowire해 전달. 그들의 `MarketItem`은 `yDayAvgPrice=null`이라 capture가 no-op → item_daily_stats 미기록, 정리 불필요.
- **Fail-open 검증** — mock 없이 미저장 TrackedItem(id=null → null tracked_item_id 바인딩)으로 실제 RuntimeException을 유발해 비전파 검증.

## Deviations from Plan

### Auto-fixed Issues

**1. [Compile] 기존 수집 IT의 시그니처 갱신**
- **Found during:** Task 1/2 (레코드·생성자 확장)
- **Issue:** `MarketItem` 4-arg화 + `PriceCollector` 생성자 확장으로 3개 기존 IT 컴파일 불가
- **Fix:** `new MarketItem(..., null)` + `BackfillCaptureService` 빈 autowire·전달
- **Verification:** `./gradlew build` 그린(회귀 0)
- **Committed in:** `99ff1c8`(MarketItem), `75e7982`(PriceCollector 생성자)

---

**Total deviations:** 1 auto-fixed (compile)
**Impact on plan:** 시그니처 확장에 따른 필수 컴파일 수정. 기존 테스트 의미 불변(no-op capture). 스코프 확장 없음.

## Issues Encountered
None — 계획대로 진행.

## User Setup Required
None.

## Next Phase Readiness
- **17.4-04**(read 병합)이 소비할 going-forward YDAY_AVG 시리즈가 매 수집 시 전 품목으로 축적된다(서버 상시 운영 전제).
- 17.4-03(상세 Stats 소급)과 병렬 소스 — 같은 `item_daily_stats`에 `source`로 구분 적재.
- Core Value 가드: `price_snapshot` 저장·`min_price` semantics·`LatestPriceCache`·레이트리밋·`RetryPolicy` diff 0줄.

---
*Phase: 17.4-timeline-gap-backfill*
*Completed: 2026-07-07*
