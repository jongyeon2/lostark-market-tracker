---
phase: 19-cicd-pipeline
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - deploy/lostark.service
  - docker-compose.prod.yml
  - docs/deploy/oracle-vm-runbook.md
  - README.md
  - .github/workflows/ci.yml
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/19-cicd-pipeline/19-02-SUMMARY.md
autonomous: true
requirements: [CICD-04]

must_haves:
  truths:
    - "실배포 발생(2026-07-14): DEPLOY_ENABLED=true 전환 후 GitHub Actions run #30(커밋 916bd3a)에서 backend·frontend·images·deploy 전 job 성공. VM에 ghcr.io/jongyeon2/lostark-app:sha-916bd3a·lostark-web:sha-916bd3a 배포, 4컨테이너 Up(postgres·redis healthy), 공개 HTTPS `/actuator/health`={\"status\":\"UP\"}. 파이프라인은 검증됨 — 문서/설정을 이 실사에 맞춘다."
    - "운영 VM은 소스 빌드 금지 — GHCR 이미지 pull만. `deploy/lostark.service`와 런북 수동 경로의 `up -d --build`에서 `--build`를 제거한다(compose `build:` 블록은 오프라인 폴백으로 유지). 이미 성공한 CI/CD 흐름(ci.yml deploy job)은 로직 무변경으로 보존한다."
    - "저장소는 PRIVATE(jongyeon2/lostark-market-tracker) — 문서/주석의 'public 저장소' 문구는 부정확하므로 정정한다(패키지 private + VM docker login 유지 근거)."
    - "런북에 운영 Runbook 추가: 정상(자동) 배포 흐름·상태 확인·로그·HTTPS health·특정 sha 롤백·배포 실패 진단 순서·GHCR 인증 실패 대응·Tailscale 연결 실패 대응·SSH 복구 경로. 공개 SSH 22 완전 폐쇄는 아직 하지 않음 — Windows Tailscale 경유 운영자 SSH 검증 후 후속 보안 작업으로 문서에만 남긴다."
    - "앱 로직(수집/캐시/event-impact/서빙) 0줄 — 문서·systemd·compose 주석·ci.yml 주석만. STATE/ROADMAP/19-02-SUMMARY의 'deploy skip·사용자 사전조치 대기' stale 문구 제거 + 첫 실배포 성공 기록."
  artifacts:
    - path: "deploy/lostark.service"
      provides: "부팅 시 GHCR 이미지 pull 기반 기동(--build 제거)"
    - path: "docs/deploy/oracle-vm-runbook.md"
      provides: "자동 배포(CI/CD) 운영 Runbook — 배포·상태·로그·health·롤백·장애진단·복구 + pull 기반 수동 경로"
    - path: "README.md"
      provides: "배포 섹션에 GitHub Actions→GHCR→Tailscale SSH→VM pull+스모크 자동 배포 흐름 반영"
  key_links:
    - from: "docs/deploy/oracle-vm-runbook.md (자동 배포 섹션)"
      to: ".github/workflows/ci.yml (deploy job, 19-02)"
      via: "런북이 실제 파이프라인 단계·롤백·진단을 운영자 관점으로 문서화"
      pattern: "IMAGE_TAG=sha-"
---

<objective>
Phase 19의 마지막 플랜 — 첫 실배포가 성공한 CI/CD 파이프라인을 **운영 문서·설정에 정직하게 반영**한다. 운영 VM의 소스 빌드 잔재(`--build`)를 제거하고(GHCR pull 전용), 런북에 자동 배포 운영 Runbook(배포·상태·로그·health·롤백·장애진단·복구)을 추가하며, README 배포 섹션과 planning 상태(STATE/ROADMAP/19-02-SUMMARY)를 실사에 맞춘다. 이미 성공한 ci.yml deploy job 로직은 보존한다.

Purpose: CICD-04(배포 검증·롤백)의 운영 문서면을 완성하고 Phase 19를 종료한다.

Output: systemd/compose `--build` 제거, 런북 운영 Runbook, README 자동 배포 설명, planning 갱신. 앱 로직 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<task name="systemd·compose·ci.yml 주석 정정(--build 제거·private 정정)">
<what-to-build>
`deploy/lostark.service` ExecStart에서 `--build` 제거(부팅 시 GHCR 이미지 pull 기반) + 헤더 주석에 GHCR 배포 명시. `docker-compose.prod.yml` 헤더 주석의 `up -d --build`를 pull 기반으로 갱신. `ci.yml` images job 주석의 부정확한 'public repo 무료'를 정정(GitHub-hosted arm64). compose `build:` 블록은 오프라인 폴백으로 유지, ci.yml 로직은 무변경.
</what-to-build>
<how-to-verify>
`docker compose -f docker-compose.prod.yml config -q`(더미 env) 파싱 OK · actionlint(ci.yml) exit 0 · `git diff --check` 무경고 · systemd ExecStart에 `--build` 없음.
</how-to-verify>
</task>

<task name="런북 운영 Runbook + README 자동 배포 반영">
<what-to-build>
`docs/deploy/oracle-vm-runbook.md`: §6/§9 수동 경로를 GHCR pull 기반으로 갱신, 신규 "자동 배포(CI/CD)" 운영 섹션 추가(정상 흐름·상태 확인·로그·HTTPS health·특정 sha 롤백·배포 실패 진단 순서·GHCR 인증 실패·Tailscale 실패·SSH 복구 경로·공개 22 폐쇄는 후속). §4 SSH 주석에 Tailscale CI 경로 + /32는 비상 복구용 유지 명시. `README.md` "어떻게 배포했나"에 자동 배포 흐름 1~2줄 추가(클라이언트 친화·짧게). 실키/개인키/시크릿 값 미기재.
</what-to-build>
<how-to-verify>
런북에 롤백(특정 sha)·장애 진단·GHCR/Tailscale/SSH 복구 항목 존재 · README에 GitHub Actions→GHCR→Tailscale→스모크 흐름 명시 · 시크릿 값 0.
</how-to-verify>
</task>

<task name="planning 상태 갱신(STATE·ROADMAP·19-02-SUMMARY) + 19-03-SUMMARY">
<what-to-build>
STATE.md·ROADMAP.md·19-02-SUMMARY.md의 'deploy skip / 사용자 사전조치 대기' stale 문구 제거 + 첫 실배포 성공(run #30, 916bd3a, 2026-07-14) 기록. ROADMAP 19-03 체크·Phase 19 3/3·v1.4 완료 반영 + 'public 저장소' 정정. 19-03-SUMMARY 작성. STATE는 hand-edit(gsd-sdk STATE 쿼리 실패 known-issue).
</what-to-build>
<how-to-verify>
STATE/ROADMAP에 'skip·사전조치 대기' 잔존 0 · Phase 19 3/3 complete · 19-03-SUMMARY status: complete.
</how-to-verify>
</task>
