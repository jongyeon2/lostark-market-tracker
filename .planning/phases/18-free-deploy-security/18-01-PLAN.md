---
phase: 18-free-deploy-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - src/main/resources/application-prod.yml
  - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java
autonomous: true
requirements: [DEPLOY-01, DEPLOY-04]

must_haves:
  truths:
    - "D-04: `application-prod.yml` 신설 — `management.endpoint.health.show-details: never`(base는 `always`, 공개 노출 하드닝) + `collection.initial-delay-ms: 10000`(dev 미러: WatchlistSeeder가 첫 스케줄 tick 전에 22개 upsert 완료). datasource/redis는 base env 플레이스홀더 그대로."
    - "D-04: `WatchlistSeeder`의 `@Profile({\"dev\",\"seed\"})`를 `@Profile({\"dev\",\"seed\",\"prod\"})`로 확장 — prod에서 큐레이션 22개가 시드돼 `PriceCollector`가 수집 대상을 갖는다. `SeedDataRunner`(`@Profile(\"seed\")`)는 prod 미구동(합성 seed 제외), `PriceCollector`(프로파일 무제한)는 prod 24/7 구동."
    - "DEPLOY-01/04: 멀티스테이지 `Dockerfile`(빌드 temurin:21-jdk → 런타임 temurin:21-jre)이 **비루트 유저**로 bootJar를 실행하고, 시크릿을 이미지에 굽지 않는다(런타임 env 주입). `.dockerignore`가 `.env*`·`build`·`.git`·`frontend`를 빌드 컨텍스트에서 제외."
    - "Core Value 가드: `PriceCollector`·`price_snapshot`·`LatestPriceCache`·`EventImpactService` 로직 0줄. 이 플랜은 신규 파일(Dockerfile·.dockerignore·application-prod.yml) + WatchlistSeeder 프로파일 애노테이션 1줄만 변경."
  artifacts:
    - path: "Dockerfile"
      provides: "멀티스테이지 앱 이미지(비루트·JRE 런타임·env 주입) — 03의 compose가 build 대상으로 참조"
    - path: "src/main/resources/application-prod.yml"
      provides: "prod 프로파일(actuator 하드닝 + 수집 부트스트랩 미러) — compose가 SPRING_PROFILES_ACTIVE=prod로 활성화"
    - path: "src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java"
      provides: "prod 프로파일에서 큐레이션 22개 시드 — 실 수집의 대상 목록"
  key_links:
    - from: "application-prod.yml (SPRING_PROFILES_ACTIVE=prod)"
      to: "18-03 docker-compose.prod.yml (app 서비스 env)"
      via: "프로파일 활성화 + env 플레이스홀더 주입"
      pattern: "SPRING_PROFILES_ACTIVE"
    - from: "Dockerfile (bootJar 런타임)"
      to: "18-03 docker-compose.prod.yml (app build:)"
      via: "compose가 이 Dockerfile을 빌드"
      pattern: "Dockerfile"
---

<objective>
앱을 **프로덕션 컨테이너화**하고 **prod 프로파일**을 신설한다: (1) 멀티스테이지 `Dockerfile`(비루트 유저·JRE 런타임·시크릿 미굽기) + `.dockerignore`, (2) `application-prod.yml`(actuator `show-details: never` 하드닝 + dev의 수집 부트스트랩 미러), (3) `WatchlistSeeder`의 `@Profile`에 `"prod"` 추가로 prod에서 큐레이션 22개가 시드되게 한다.

Purpose: Oracle VM에서 `SPRING_PROFILES_ACTIVE=prod`로 뜨는 이미지를 만든다. prod는 실데이터 수집을 위해 워치리스트 시드가 필요(WatchlistSeeder)하고, 공개 노출을 위해 actuator 상세를 숨긴다(show-details never). 실 키/시크릿은 이미지에 굽지 않고 런타임 env로만 주입(DEPLOY-04).

Output: `Dockerfile`·`.dockerignore`·`application-prod.yml` 신규 + `WatchlistSeeder` 프로파일 1줄 확장. `PriceCollector`·`price_snapshot`·`EventImpactService` diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-free-deploy-security/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- application-prod.yml (src/main/resources): spring.config.activate.on-profile 불필요(파일명 프로파일). 내용: management.endpoint.health.show-details: never (+ management.endpoints.web.exposure.include: health 재확인) / collection.initial-delay-ms: 10000 (application-dev.yml과 동일 근거) / 로깅 level.root: INFO. datasource·redis·lostark·admin은 base(application.yml)의 ${...} 플레이스홀더가 유효하므로 재선언 불필요 — 오버라이드가 필요한 키만 둔다. 헤더 주석에 "prod = 공개 배포 프로파일, 실데이터 수집(WatchlistSeeder+PriceCollector), 시크릿은 env only" 명시. -->
<!-- WatchlistSeeder.java: 35행 @Profile({"dev", "seed"}) → @Profile({"dev", "seed", "prod"}). Javadoc/주석에 prod 추가 이유(실 배포에서 수집 대상 22개 시드) 한 줄 보강. 그 외 로직 무변경. -->
<!-- Dockerfile (repo 루트): 멀티스테이지.
     [build] FROM eclipse-temurin:21-jdk AS build / WORKDIR /app / COPY gradlew build.gradle settings.gradle ./ / COPY gradle ./gradle / COPY src ./src / RUN ./gradlew --no-daemon clean bootJar -x test  (frontend·테스트 제외, 이미지 슬림)
     [run]   FROM eclipse-temurin:21-jre / RUN useradd -r -u 1001 appuser (비루트) / WORKDIR /app / COPY --from=build /app/build/libs/*.jar app.jar / USER appuser / EXPOSE 8080 / ENV JAVA_OPTS="-XX:MaxRAMPercentage=75" / ENTRYPOINT ["sh","-c","java $JAVA_OPTS -jar app.jar"].
     ★ 시크릿/ .env COPY 금지 — 런타임 env로만. gradlew에 실행권한 없으면 build 스테이지에서 chmod +x gradlew. -->
<!-- .dockerignore (repo 루트): .env, .env.*, !.env.example 계열 제외(단 .dockerignore는 부정패턴보다 단순 나열 안전) → .env / .env.prod / build / .git / .gradle / frontend / node_modules / .planning 나열. 빌드 컨텍스트 축소 + 시크릿 유출 방지. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: application-prod.yml + WatchlistSeeder prod 프로파일</name>
  <read_first>
    - src/main/resources/application.yml (base — actuator show-details: always·env 플레이스홀더 확인)
    - src/main/resources/application-dev.yml (collection.initial-delay-ms: 10000 원형·주석 근거)
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (@Profile 위치·Javadoc)
  </read_first>
  <files>src/main/resources/application-prod.yml, src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</files>
  <action>
    `application-prod.yml`을 신설: `management.endpoint.health.show-details: never`(base의 `always` 오버라이드), `management.endpoints.web.exposure.include: health`(health만 노출 재확인), `collection.initial-delay-ms: 10000`(dev 미러 — WatchlistSeeder가 첫 tick 전 22개 upsert 완료), `logging.level.root: INFO`. datasource/redis/lostark/admin은 base의 `${...}` 플레이스홀더가 유효하므로 재선언하지 않는다. 헤더 주석에 "prod = 공개 배포 프로파일, 실데이터 수집, 시크릿은 env only" 명시. 그다음 `WatchlistSeeder.java`의 `@Profile({"dev", "seed"})`를 `@Profile({"dev", "seed", "prod"})`로 변경하고, 클래스 Javadoc에 prod 추가 이유(실 배포에서 수집 대상 22개 시드)를 한 줄 보강. 시더 로직·순서(@Order(1))·데이터는 무변경.
  </action>
  <verify>
    <automated>./gradlew compileJava && grep -q "show-details: never" src/main/resources/application-prod.yml && grep -q '"prod"' src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</automated>
  </verify>
  <acceptance_criteria>
    - `application-prod.yml`이 `management.endpoint.health.show-details: never` 포함 — `grep -q "show-details: never" src/main/resources/application-prod.yml` 성립
    - `application-prod.yml`이 `collection.initial-delay-ms: 10000` 포함(첫 tick 지연으로 시드 선행)
    - `WatchlistSeeder`의 `@Profile`가 `{"dev", "seed", "prod"}` — `grep -q 'seed", "prod"' src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java` 성립
    - `SeedDataRunner`(@Profile("seed"))는 무변경 — prod에서 합성 seed 미구동
    - `./gradlew compileJava` 그린
  </acceptance_criteria>
  <done>prod 프로파일이 actuator를 하드닝(show-details never)하고 수집 부트스트랩을 미러하며, WatchlistSeeder가 prod에서 큐레이션 22개를 시드하도록 확장됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 멀티스테이지 Dockerfile(비루트) + .dockerignore</name>
  <read_first>
    - build.gradle (bootJar 태스크·산출 경로 build/libs 확인, Java 21 toolchain)
    - gradlew (래퍼 실행 — 빌드 스테이지에서 사용)
    - .gitignore (제외 대상 참고 — .dockerignore가 최소한 .env 계열·build·.git 제외)
  </read_first>
  <files>Dockerfile, .dockerignore</files>
  <action>
    repo 루트에 멀티스테이지 `Dockerfile`을 만든다. [build] `FROM eclipse-temurin:21-jdk AS build`, `WORKDIR /app`, `COPY gradlew build.gradle settings.gradle ./`, `COPY gradle ./gradle`, `COPY src ./src`, `RUN chmod +x gradlew && ./gradlew --no-daemon clean bootJar -x test`. [run] `FROM eclipse-temurin:21-jre`, `RUN useradd -r -u 1001 appuser`, `WORKDIR /app`, `COPY --from=build /app/build/libs/*.jar app.jar`, `USER appuser`(비루트), `EXPOSE 8080`, `ENV JAVA_OPTS="-XX:MaxRAMPercentage=75"`, `ENTRYPOINT ["sh","-c","java $JAVA_OPTS -jar app.jar"]`. 시크릿/`.env` COPY 금지(런타임 env 주입). `.dockerignore`(repo 루트)에 `.env`, `.env.prod`, `.env.*`, `build`, `.gradle`, `.git`, `frontend`, `node_modules`, `.planning`를 나열(빌드 컨텍스트 축소 + 시크릿 유출 방지).
  </action>
  <verify>
    <automated>grep -q "USER appuser" Dockerfile && grep -q "eclipse-temurin:21-jre" Dockerfile && grep -q "^.env" .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile`이 멀티스테이지(jdk 빌드 → jre 런타임)이고 **비루트** 실행 — `grep -q "USER appuser" Dockerfile` 성립
    - `Dockerfile`에 `.env`/시크릿을 COPY하는 라인 부재(런타임 env 주입만)
    - `.dockerignore`가 `.env` 계열·`build`·`.git`을 제외 — `grep -q "^.env" .dockerignore` 성립
    - (Docker 사용 가능 시) `docker build -t lostark-app .`가 성공하고 이미지가 bootJar를 실행 — 미가용 시 실행자가 수동 검증으로 표기
  </acceptance_criteria>
  <done>비루트 멀티스테이지 이미지가 bootJar를 실행하고, 빌드 컨텍스트에서 시크릿/불필요 파일이 제외된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이미지 빌드 → 런타임 | 시크릿을 이미지 레이어에 굽지 않음 — 런타임 env 주입만(DEPLOY-04) |
| actuator 공개 노출 | health만 노출·show-details never — 내부 구성/버전/DB 상태 비노출 |
| 컨테이너 프로세스 권한 | 비루트(appuser 1001) — 컨테이너 탈출 시 권한 최소화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Info Disclosure | actuator health 상세가 DB/구성 노출 | mitigate | prod `show-details: never` + health만 exposure — 18-05 게이트가 확인 |
| T-18-01-02 | Info Disclosure | 시크릿이 이미지 레이어/빌드 컨텍스트에 유출 | mitigate | .dockerignore로 .env 제외 + Dockerfile에 시크릿 COPY 금지, 런타임 env 주입 |
| T-18-01-03 | Elevation of Privilege | 컨테이너 root 실행 | mitigate | USER appuser 비루트 런타임 |
| T-18-01-SC | Supply Chain | 베이스 이미지 신뢰 | accept | 공식 eclipse-temurin 태그 고정, 신규 앱 의존성 0 |
</threat_model>

<verification>
- `./gradlew compileJava` 그린 — prod yml/시더 변경이 컴파일 통과.
- `application-prod.yml`: `show-details: never` + `collection.initial-delay-ms: 10000` 존재.
- `WatchlistSeeder` `@Profile`에 `"prod"` 포함.
- `Dockerfile` 비루트·멀티스테이지, `.dockerignore` `.env` 제외.
- `PriceCollector`·`price_snapshot`·`EventImpactService` diff 0줄(Core Value 가드).
</verification>

<success_criteria>
- prod 프로파일로 뜨는 앱이 실데이터 수집(WatchlistSeeder 22개 시드 + PriceCollector 24/7)을 하고 actuator를 하드닝한다(DEPLOY-01).
- 실 키/시크릿이 이미지에 미기재되고 런타임 env로만 주입된다(DEPLOY-04).
- 컨테이너가 비루트로 실행되고 빌드 컨텍스트에서 시크릿이 제외된다.
</success_criteria>

<output>
Create `.planning/phases/18-free-deploy-security/18-01-SUMMARY.md` when done
</output>
