# Phase 17.4 Verification — 타임라인 gap 백필 (일별 Stats)

**Verdict: ✅ PASS** — 4 plans / 3 waves 모두 완료, 요구사항 BACKFILL-01..04 충족, Core Value 가드(BACKFILL-04) 증명, 전체 스위트 그린(백엔드 127 tests · 프론트 tsc/vite).

방식: gsd-verifier 서브에이전트는 이 환경에서 permission-denied → 오케스트레이터가 인라인 goal-backward 검증(메모리 [[gsd-subagents-inline]]).

## Phase Goal (ROADMAP)

> 서버 off 수집 공백을 로스트아크가 보관하는 일별 평균가로 백필해, 사용자가 어떤 기간을 봐도 시세가 연속으로 보이게 한다. min(호가)/avg(거래) 지표를 정직하게 구분하고, 수집·캐시·event-impact 핵심 경로는 0줄.

## Goal-backward: 요구사항별 증거

| 요구사항 | 상태 | 증거 |
|---|---|---|
| **BACKFILL-01** (YDayAvgPrice going-forward, 추가 호출 0, 멱등) | ✅ | 17.4-02 `BackfillCaptureService.captureYDayAvg` — 전일(KST)·`YDAY_AVG` 멱등 upsert, `PriceCollector` persistSnapshot 직후 fail-open 후킹. `BackfillCaptureServiceIT` 4/4: 전일 스탬핑·소수 보존(147007.4)·멱등·null no-op·fail-open. `ItemFetchService`는 같은 `match`에서 필드만 더 읽음(신규 API 호출 0). |
| **BACKFILL-02** (상세 Stats 재료 14일 소급, 기동+일1회, 재료만·레이트리밋·fail-open) | ✅ | 17.4-03 `DetailStatsBackfillRunner`(ApplicationRunner+@Scheduled cron) — `findByActiveTrueAndRoleGroup("MATERIAL")`·`getItemDetail`·`AvgPrice>0`만 `DETAIL_STATS` 멱등 upsert·tryAcquire 존중·품목별 fail-open·AUTH break. `DetailStatsBackfillRunnerIT` 3/3: 0 skip(14→13)·멱등·각인서 미호출. |
| **BACKFILL-03** (타임라인 백필 연속 라인 + 실측 min 구분, 혼합 금지, 정직 라벨) | ✅ | 17.4-04 `TimelineResponse.backfill`(snapshots와 분리)·`PricesController` KST 윈도우 병합. `PricesControllerIT` 2/2: 분리·윈도우·오름차순·빈 윈도우 200·[]. 프론트 `PriceTimelineChart` 백필=연속선(주황)·실측 min=마커(파랑) 별도 dataKey·범례/툴팁 정직 구분. |
| **BACKFILL-04** (Core Value 가드: 수집/캐시/event-impact/price_snapshot 0줄) | ✅ | phase 전체 diff(4bf0010..HEAD) 검증: `cache/`·`WindowQueryService`·`DownsampleService`·`EventImpact*`·V1 스키마·`PriceSnapshot` **0줄**. `PriceCollector`(+8)·`ItemFetchService`(+3)는 의도된 additive 후킹/운반뿐(persist·evict·레이트리밋·min_price semantics 로직 무변경). 신규 테이블 `item_daily_stats`(V6)·러너·read 병합만. |

## 산출물 (파일 존재 + 컴파일/테스트 확인)

- 저장 계층(01): `V6__add_item_daily_stats.sql`·`ItemDailyStat`·`DailyStatSource`·`ItemDailyStatRepository`(멱등 upsert+윈도우 finder) — `ItemDailyStatRepositoryIT` 3/3.
- 소스①(02): `MarketItem.yDayAvgPrice`·`ItemFetchResult`·`BackfillCaptureService`·`PriceCollector` 후킹.
- 소스②(03): `getItemDetail`·`ItemDetailResponse`/`MarketStat`·재료 finder·`DetailStatsBackfillRunner`.
- read/차트(04): `DailyStatPoint`·`TimelineResponse.backfill`·`PricesController`·`schemas.ts`·`PriceTimelineChart`·`TimelinePage`.

## 검증 명령 결과 (evidence)

- `./gradlew build` → **BUILD SUCCESSFUL** (백엔드 127 tests, 0 failures/errors).
- `cd frontend && npm run build` → **tsc -b + vite build 그린** (chunk-size 경고는 기존, 오류 아님).
- Core Value diff 가드 → 금지 경로 0줄(위 표 BACKFILL-04).

## 결정/편차 (SUMMARY 상세)

- 01: `upsertDailyStat`에 `@Transactional`(네이티브 @Modifying 자족성); IT는 유니크 시드+@AfterEach로 공유 DB FK-safe.
- 02: 기존 수집 IT 3건 시그니처 컴파일 갱신(실 BackfillCaptureService 빈 주입, no-op).
- 03: 러너 `@Profile("!test")`(WatchlistSeeder 선례, 테스트 auto-fire 차단); Clock 미사용 → 제거.
- 04: `PricesControllerIT` 신규 생성(플랜 파일 부재); `TimelinePage` 배선+빈 상태 게이트 완화(성공 기준=서버 off 렌더에 필수).

모든 편차는 정확성/성공기준/청결도 목적, must_have·요구사항 위반 없음, 스코프 확장 없음.

## 한계 (스파이크 확정, Phase 18로 보완)

- 상세 Stats는 14일만 보관 → 14일 초과 다일 gap 미복구.
- 각인서는 상세 Stats=0(API 한계) → 과거 gap 소급 불가, going-forward `YDAY_AVG`만.
- → Phase 18 상시 배포로 going-forward 시리즈 축적이 근본 보완.

---
*Verified inline (orchestrator) 2026-07-07 · Phase 17.4-timeline-gap-backfill*
