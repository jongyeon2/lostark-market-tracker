---
phase: 17.1-demo-polish
plan: 03
subsystem: ui
tags: [react, dashboard, layout, health, roleGroup]

requires:
  - phase: 08-dashboard
    provides: DashboardPage / HealthCard / ItemCard (반응형 그리드 대시보드)
  - phase: 15-admin-console
    provides: CollectionSection (관리자 HealthCard 재사용)
  - phase: 16-card-deeplink
    provides: ItemCard 딥링크 /timeline?item= + 고유번호 제거
provides:
  - 대시보드에서 HealthCard 제거(관리자 콘솔로 이관) — 정보 관점 분리
  - 'API 상태' 단일 라벨 통일 (HealthCard 제목 + CollectionSection 래퍼 제거)
  - 대시보드 각인(DEALER+SUPPORT)→재료(MATERIAL) 세로 섹션 2개 재구성
  - ItemCard 정사각→가로 전폭 행, 딥링크·아이콘·배지·가격·시각 보존
affects: [demo-polish, admin-console, dashboard]

tech-stack:
  added: []
  patterns:
    - "roleGroup 버킷(sortByRole)으로 대시보드 카테고리 세로 섹션 분할 — ROLE_LABEL 미참조(각인=DEALER+SUPPORT 합집합)"
    - "가로 전폭 행 카드: CardContent flex justify-between (identity 좌 · latest-price 우), 상태 인라인 매핑 유지"

key-files:
  created: []
  modified:
    - frontend/src/features/dashboard/DashboardPage.tsx
    - frontend/src/features/dashboard/HealthCard.tsx
    - frontend/src/features/dashboard/ItemCard.tsx
    - frontend/src/features/admin/CollectionSection.tsx

key-decisions:
  - "D-07: DashboardPage에서 HealthCard import/렌더 완전 제거 — 관리자 CollectionSection에만 존치"
  - "D-08: 'API 상태' 단일 제목 — HealthCard CardTitle 변경 + CollectionSection 바깥 래퍼 Card 제거(HealthCard 자체가 Card)"
  - "D-09: 대시보드 정직성 대체 표시 미신설 — per-card pending + 그리드 AsyncBoundary가 담당"
  - "D-10/11: 각인→재료 세로 섹션, sortByRole 재사용(딜러→서포터→이름), 섹션 제목은 리터럴 '각인'/'재료'"
  - "D-12: ItemCard 가로 전폭 행 — 딥링크/ItemIcon/RoleBadge/minPrice/formatKst/focus-visible 보존, 고유번호 미재도입"

patterns-established:
  - "카테고리 세로 섹션(ItemSection): 빈 버킷은 null 렌더로 바 헤더 숨김"
  - "공용 HealthCard 제목 한 곳 변경이 대시보드 제거 후에도 관리자 화면에 반영"

requirements-completed: [POLISH-01, POLISH-04]

duration: 12min
completed: 2026-07-06
---

# Phase 17.1 Plan 03: 대시보드 재구성 + 'API 상태' 통일 Summary

**대시보드 수집 헬스를 관리자 콘솔로 이관하고 'API 상태'로 라벨 통일, 바둑판 그리드를 각인→재료 세로 섹션 + 가로 전폭 행 카드로 재구성(백엔드 0줄)**

## Performance

- **Duration:** ~12 min
- **Tasks:** 2 (DashboardPage 공유로 플랜 단위 구현 커밋 1개)
- **Files modified:** 4 (전부 프론트, 백엔드 0줄)

## Accomplishments
- **정보 관점 분리(D-07/08/09):** DashboardPage에서 `<HealthCard/>` 완전 제거, HealthCard 제목 '수집 헬스'→'API 상태', CollectionSection 바깥 '수집 상태' 래퍼 Card 제거 → 관리자 'API 상태' 카드 하나. 대시보드 헬스 대체 표시 미신설.
- **세로 섹션 재구성(D-10/11):** `sortByRole(data)`를 roleGroup으로 분할 — 각인(DEALER+SUPPORT) → 재료(MATERIAL) 2개 세로 섹션. 경량 섹션 제목(리터럴 '각인'/'재료'), 각 카드 RoleBadge로 딜러/서포터 구분. 빈 섹션은 null 렌더.
- **가로 행 카드(D-12):** ItemCard를 정사각 그리드 셀 → 가로 전폭 행(identity 좌 · 가격/시각 우). 딥링크 `/timeline?item=`·ItemIcon·RoleBadge·minPrice(`toLocaleString('ko-KR')`)·formatKst·per-card 상태 매핑·focus-visible ring 보존, 고유번호 미재도입.

## Task Commits

1. **Task 1+2 구현** — `86a05a0` (feat) — DashboardPage.tsx가 두 태스크(HealthCard 제거 + 섹션 재구성)를 공유하여 플랜 단위 단일 구현 커밋

## Files Created/Modified
- `frontend/src/features/dashboard/DashboardPage.tsx` — HealthCard 제거, 각인→재료 세로 섹션 + ItemSection 헬퍼
- `frontend/src/features/dashboard/HealthCard.tsx` — CardTitle 'API 상태' (대시보드/관리자 공용)
- `frontend/src/features/dashboard/ItemCard.tsx` — 가로 전폭 행 레이아웃, CardHeader/CardTitle 미사용 import 정리
- `frontend/src/features/admin/CollectionSection.tsx` — 바깥 래퍼 Card 제거, `<HealthCard/>`만 반환

## Decisions Made
- 각인 섹션에 딜러/서포터 소제목 미포함(D-11 재량) — RoleBadge로 충분.
- 섹션 제목 타이포: `text-muted-foreground text-sm font-semibold tracking-wide`(경량, 8pt 스페이싱 `space-y-3`/`space-y-8`).
- 빈 카테고리 섹션은 렌더하지 않음(바 헤더 방지).

## Deviations from Plan
- **커밋 입도:** DashboardPage.tsx가 Task 1(HealthCard 제거)과 Task 2(세로 섹션)에 동시에 걸쳐 파일 단위 원자 분리가 불가 → 플랜 단위 단일 구현 커밋(`86a05a0`)으로 처리. 스코프 크리프 없음.

## Issues Encountered
- 초기 grep 가드 실패: DashboardPage 상단 주석·CollectionSection 주석에 남은 옛 문자열('HealthCard', '수집 상태')이 verify grep에 걸림 → 주석 정리로 해소.

## Next Phase Readiness
- POLISH-01/04 충족. 세로 섹션은 17.1-02 큐레이션 22개(각인 18 + 재료 4)를 그대로 수용 — 재료 섹션에 운명 결정 2종 자동 배치.

---
*Phase: 17.1-demo-polish*
*Completed: 2026-07-06*
