---
phase: 13-backend-enrichment-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V4__add_item_enrichment.sql
  - src/main/java/com/lostark/tracker/domain/TrackedItem.java
  - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java
  - src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java
autonomous: true
requirements: [ITEM-01, ITEM-02, SEED-01, SEED-02, SEED-04]

must_haves:
  truths:
    - "Flyway V4__add_item_enrichment.sql이 tracked_item에 icon_url/item_group/role_group을 전부 NULLABLE로 추가하고 V1–V3 마이그레이션은 무변경이며, 부팅 시 ddl-auto=validate가 통과한다 (ITEM-01)"
    - "TrackedItem 엔티티가 icon_url/item_group/role_group을 @Column(nullable) 필드로 매핑하고 getter를 노출하며, 기존 3-arg 생성자는 보존한 채 enrichment용 6-arg 생성자를 추가한다 — ddl-auto=validate 그린 (ITEM-02)"
    - "WatchlistSeeder가 12-SPIKE-FINDINGS.md의 큐레이션 15개(융화재료 4 MATERIAL + 딜러 9 DEALER + 서포터 2 SUPPORT)로 WATCHLIST를 교체해, icon_url(CDN base + 실측 파일명)·item_group·role_group과 함께 external_item_id 멱등 upsert로 등록한다 (SEED-01)"
    - "SyntheticDemoData는 코드 무변경으로 신규 활성 15개 품목의 합성 스냅샷을 findByActiveTrue 경유로 키 없이 멱등 생성한다(품목 비의존 설계) (SEED-02)"
    - "seed/watchlist 소스에 실 API 키·계정 식별자·가격 원문이 없다 — 공개 메타데이터(external_item_id/display_name/category/icon_url/group)만 (SEED-04)"
  artifacts:
    - path: "src/main/resources/db/migration/V4__add_item_enrichment.sql"
      provides: "tracked_item에 icon_url/item_group/role_group nullable 컬럼 추가 (V1–V3 불변)"
      contains: "icon_url"
    - path: "src/main/java/com/lostark/tracker/domain/TrackedItem.java"
      provides: "enrichment 3필드 @Column 매핑 + getter + 6-arg 생성자(3-arg 보존)"
      contains: "iconUrl"
    - path: "src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java"
      provides: "큐레이션 15개로 교체된 WATCHLIST + enrichment 동반 멱등 upsert"
      contains: "role_group"
    - path: "src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java"
      provides: "seed 후 tracked_item 15행 + enrichment 채워짐 + 멱등(2회=동일) 단언"
      contains: "MATERIAL"
  key_links:
    - from: "12-SPIKE-FINDINGS.md (c) 6필드 큐레이션 표"
      to: "WatchlistSeeder.WATCHLIST 상수"
      via: "external_item_id/display_name/category_code/icon_url/item_group/role_group 전사"
      pattern: "6861009"
    - from: "WatchlistSeeder"
      to: "tracked_item enrichment 컬럼"
      via: "6-arg TrackedItem 생성자 → JPA persist"
      pattern: "iconUrl"
---

<objective>
Phase 12가 잠근 6필드 큐레이션 15개를 데이터 계층에 전사한다. (1) Flyway `V4__add_item_enrichment.sql`로 `tracked_item`에 `icon_url`/`item_group`/`role_group`을 **nullable**로 추가하고(V1–V3 불변), (2) `TrackedItem` 엔티티에 동일 3필드를 매핑해 `ddl-auto=validate`가 통과하게 하며, (3) `WatchlistSeeder`의 미검증 12개 WATCHLIST를 스파이크 검증 큐레이션 **15개(융화재료 4 + 딜러 9 + 서포터 2)**로 교체하되 `icon_url`(CDN base + 실측 파일명)·`item_group`·`role_group`을 함께 멱등 등록한다. `SyntheticDemoData`는 `findByActiveTrue()` 기반이라 코드 변경 없이 신규 품목의 합성 history를 키 없이 재생성한다.

Purpose: 이 데이터 계층이 채워져야 Wave 2(13-02 read DTO 패스스루)가 enrichment를 노출할 수 있다. 큐레이션 상수는 12-SPIKE-FINDINGS.md에서 그대로 전사하므로 **런타임 키 불필요**.

Output: V4 마이그레이션 + enrichment 매핑된 TrackedItem + 큐레이션 15개로 교체된 WatchlistSeeder + seed/멱등/enrichment를 단언하는 Testcontainers IT. 수집 스케줄러·캐시·event-impact는 0줄 무변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@.planning/phases/12-api-spike-data-lock/12-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- TrackedItem (domain): @Entity @Table("tracked_item"). 필드 id/externalItemId(NOT NULL)/displayName(NOT NULL)/category(nullable)/active/createdAt. public 생성자 TrackedItem(externalItemId, displayName, category) — 시더·admin이 사용(보존 필수). @PrePersist로 createdAt UTC. JPA는 필드 접근 — setter 불필요, getter만 추가하면 됨. -->
<!-- 기존 마이그레이션: V1(4테이블 init, tracked_item: external_item_id VARCHAR(100) NOT NULL / display_name VARCHAR(200) NOT NULL / category VARCHAR(100) / active / created_at), V2(collection_run_summary), V3(tracked_item.external_item_id UNIQUE 제약). ddl-auto=validate(D-02/D-14) — 컬럼 추가는 마이그레이션+엔티티가 동시에 일치해야 그린. -->
<!-- WatchlistSeeder (collect): @Component @Profile({"dev","seed"}) @Order(1) implements ApplicationRunner. private record SeedItem(externalItemId, displayName, category). static List<SeedItem> WATCHLIST(현재 12개, 재련 재료 50010/50020 — 큐레이션과 무관·미검증). run(): findByExternalItemId(id).orElseGet(() -> save(new TrackedItem(...))) 멱등 upsert. -->
<!-- SyntheticDemoData (seed): @Component(프로파일 없음) — findByActiveTrue()로 모든 활성 품목에 10분 간격 8일치 스냅샷 + 데모 이벤트 2건 멱등 생성. 품목 id 비의존. 신규 품목은 active=true로 들어오면 자동 포함 → SEED-02는 WATCHLIST 교체만으로 충족(이 클래스 무변경). -->
<!-- 통합 테스트는 test 프로파일에서 실행 → WatchlistSeeder(@Profile dev/seed)는 비활성. 따라서 WATCHLIST 교체는 기존 IT 스위트를 깨지 않음(테스트는 자체 픽스처 사용). WatchlistSeederIT는 dev/seed 프로파일을 명시 활성화해 시더를 직접 검증. -->
<!-- 12-SPIKE-FINDINGS.md (c): CDN base = https://cdn-lostark.game.onstove.com/efui_iconatlas/use/ . 큐레이션 15개 = 융화재료 4(MATERIAL, item_group=강화재료, category 50010) + 딜러 9 + 서포터 2(DEALER/SUPPORT, item_group=각인서, category 40000). 각인서 11종 icon 파일명 전부 use_9_25.png(동일) — 라벨 병기는 Phase 14 처리. -->
</interfaces>
</context>

## Phase Goal

**As a** 백엔드 개발자(로아 유저), **I want to** 스파이크가 잠근 큐레이션 15개의 enrichment(아이콘 URL·품목군·역할군)를 nullable 컬럼·엔티티·시더에 베이크하고, **so that** 키 없이 seed만으로 신규 품목과 아이콘 메타데이터가 DB에 채워져 read DTO·프론트가 소비할 수 있게 한다 — 수집·캐시·event-impact는 한 줄도 건드리지 않고.

<tasks>

<task type="auto">
  <name>Task 1: Flyway V4 nullable enrichment 컬럼 + TrackedItem 엔티티 매핑 (ddl-auto=validate 그린)</name>
  <read_first>
    - src/main/resources/db/migration/V1__init_schema.sql (tracked_item 컬럼 정의·타입 컨벤션 — VARCHAR, TIMESTAMPTZ; PostgreSQL 전용)
    - src/main/resources/db/migration/V3__add_tracked_item_external_id_unique.sql (최신 마이그레이션 번호·주석 톤·CONSTRAINT 스타일)
    - src/main/java/com/lostark/tracker/domain/TrackedItem.java (기존 @Column 필드·생성자·@PrePersist — 보존 대상)
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md ((c) 6필드 스키마: icon_url/item_group/role_group 의미·값 도메인)
  </read_first>
  <files>src/main/resources/db/migration/V4__add_item_enrichment.sql, src/main/java/com/lostark/tracker/domain/TrackedItem.java</files>
  <action>
    `V4__add_item_enrichment.sql`을 새로 만든다(V1–V3 파일은 절대 수정 금지). 단일 `ALTER TABLE tracked_item`로 세 컬럼을 **NULLABLE**로 추가: `icon_url VARCHAR(300)`, `item_group VARCHAR(50)`, `role_group VARCHAR(20)`. 기본값·NOT NULL·제약 없이 순수 additive. 주석으로 "read-path additive enrichment, V1–V3 불변, ddl-auto=validate(D-02)"를 명시(V3 주석 톤 계승). PostgreSQL 타입만 사용.
    `TrackedItem`에 세 필드를 추가한다: `@Column(name="icon_url") private String iconUrl;`, `@Column(name="item_group") private String itemGroup;`, `@Column(name="role_group") private String roleGroup;` (전부 nullable — `nullable=false` 미부여). 기존 `TrackedItem(externalItemId, displayName, category)` 3-arg 생성자는 그대로 두고, enrichment를 받는 6-arg 생성자 `TrackedItem(externalItemId, displayName, category, iconUrl, itemGroup, roleGroup)`를 추가한다(3-arg는 6-arg에 null 3개를 위임하거나 독립 유지 — 기존 호출부 무파손). `getIconUrl()`/`getItemGroup()`/`getRoleGroup()` getter를 추가한다(setter는 불필요 — enrichment는 seed 시점 immutable, JPA는 필드 접근). protected 기본 생성자 보존.
  </action>
  <verify>
    <automated>test -f src/main/resources/db/migration/V4__add_item_enrichment.sql && grep -q "icon_url" src/main/resources/db/migration/V4__add_item_enrichment.sql && grep -q "item_group" src/main/resources/db/migration/V4__add_item_enrichment.sql && grep -q "role_group" src/main/resources/db/migration/V4__add_item_enrichment.sql && grep -q "getIconUrl" src/main/java/com/lostark/tracker/domain/TrackedItem.java && ./gradlew compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `V4__add_item_enrichment.sql`이 존재하고 `icon_url`/`item_group`/`role_group`을 모두 nullable로 ADD COLUMN 한다(NOT NULL·DEFAULT 토큰 없음)
    - V1/V2/V3 마이그레이션 파일은 `git diff`에서 0줄 변경
    - `TrackedItem`이 `@Column(name="icon_url")`/`@Column(name="item_group")`/`@Column(name="role_group")` 필드와 3개 getter를 가지며, 기존 3-arg 생성자와 protected 기본 생성자가 보존됨
    - 신규 6-arg 생성자가 enrichment를 받는다
    - `./gradlew build`가 그린 — 부팅 IT가 ddl-auto=validate로 V4 적용 후 엔티티↔스키마 일치를 통과(불일치면 컨텍스트 로드 실패로 잡힘)
  </acceptance_criteria>
  <done>tracked_item에 nullable enrichment 3컬럼이 V4로 추가되고 TrackedItem이 이를 매핑해 ddl-auto=validate가 그린이며, V1–V3와 기존 생성자/호출부는 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: WatchlistSeeder를 큐레이션 15개로 교체 + enrichment 동반 멱등 등록</name>
  <read_first>
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (SeedItem record·WATCHLIST·멱등 upsert 패턴 — 구조 보존, 데이터·필드만 확장)
    - src/main/java/com/lostark/tracker/domain/TrackedItem.java (Task 1에서 추가된 6-arg 생성자 — 시더가 호출)
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md ((c) 큐레이션 15개 전체 표: external_item_id/display_name/category_code/icon_url 파일명/item_group/role_group)
  </read_first>
  <files>src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</files>
  <action>
    `SeedItem` record를 `SeedItem(String externalItemId, String displayName, String category, String iconUrl, String itemGroup, String roleGroup)`로 확장한다. CDN base 상수 `ICON_BASE = "https://cdn-lostark.game.onstove.com/efui_iconatlas/use/"`를 선언한다.
    `WATCHLIST`를 기존 12개(재련 재료)를 **제거하고** 12-SPIKE-FINDINGS.md (c)의 큐레이션 **15개로 교체**한다 — 융화재료 4(role_group=MATERIAL, item_group=강화재료, category="50010"): 6861009 상급 오레하 융화 재료/use_8_109.png, 6861011 최상급 오레하 융화 재료/use_11_29.png, 6861012 아비도스 융화 재료/use_12_86.png, 6861013 상급 아비도스 융화 재료/use_13_252.png; 딜러 각인서 9(role_group=DEALER, item_group=각인서, category="40000", icon=use_9_25.png): 65200505 유물 원한, 65201005 유물 예리한 둔기, 65202805 유물 저주받은 인형, 65203905 유물 아드레날린, 65204305 유물 정밀 단도, 65203705 유물 타격의 대가, 65203005 유물 기습의 대가, 65203305 유물 돌격대장, 65201505 유물 결투의 대가; 서포터 각인서 2(role_group=SUPPORT, item_group=각인서, category="40000", icon=use_9_25.png): 65203405 유물 각성, 65204105 유물 전문의. 각 SeedItem의 iconUrl은 `ICON_BASE + "<파일명>.png"`로 구성한다.
    `run()`의 멱등 upsert를 보존하되, insert 분기에서 6-arg `new TrackedItem(seed.externalItemId(), seed.displayName(), seed.category(), seed.iconUrl(), seed.itemGroup(), seed.roleGroup())`를 저장하도록 한다. 기존 항목이 있으면 그대로 둠(admin CRUD가 후속 편집 소유 — 시더는 clobber 금지) 원칙 유지. javadoc의 "오직 66102101만 검증됨" 류 옛 주석을 큐레이션 15개 전부 12-SPIKE-FINDINGS 실측 검증분이라는 사실로 갱신한다. 어떤 키·가격·식별자도 하드코딩하지 않는다(공개 메타데이터만).
  </action>
  <verify>
    <automated>grep -q "efui_iconatlas/use/" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && grep -q "MATERIAL" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && grep -q "DEALER" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && grep -q "SUPPORT" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && grep -q "6861009" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && grep -q "65203405" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && ! grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java && ./gradlew compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `WATCHLIST`가 정확히 큐레이션 15개(MATERIAL 4 + DEALER 9 + SUPPORT 2)이고 기존 재련 재료 12개(66102101 등)는 제거됨
    - 모든 항목의 iconUrl이 `https://cdn-lostark.game.onstove.com/efui_iconatlas/use/`로 시작하고, role_group ∈ {MATERIAL, DEALER, SUPPORT}, item_group ∈ {강화재료, 각인서}
    - 융화재료는 category="50010", 각인서는 category="40000"
    - `run()`의 external_item_id 멱등 upsert가 보존되고 6-arg TrackedItem 생성자로 enrichment를 저장
    - WatchlistSeeder 소스에 JWT/`bearer <token>`/가격 원문 0건
    - `./gradlew compileJava` 그린
  </acceptance_criteria>
  <done>WatchlistSeeder가 스파이크 검증 큐레이션 15개를 icon_url·item_group·role_group과 함께 멱등 등록하도록 교체되었고, 비밀·가격은 없다.</done>
</task>

<task type="auto">
  <name>Task 3: WatchlistSeederIT — seed 15행·enrichment·멱등 + 신규 품목 합성 스냅샷(SEED-02) Testcontainers 검증</name>
  <read_first>
    - src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java (PostgresRedisContainers 베이스·프로파일·Clock 주입·멱등 단언 패턴 — 동일 스타일 계승)
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (Task 2 결과 — 검증 대상 15개·enrichment)
    - src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java (findByActiveTrue 기반 합성 — 신규 품목 자동 포함 검증 대상)
    - src/main/java/com/lostark/tracker/repository/TrackedItemRepository.java (findByActiveTrue·findByExternalItemId 시그니처)
  </read_first>
  <files>src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java</files>
  <action>
    `WatchlistSeederIT`를 새로 만든다(`extends PostgresRedisContainers`, `@SpringBootTest`). WatchlistSeeder가 `@Profile({"dev","seed"})`이므로 `@ActiveProfiles("seed")` 또는 시더 빈을 직접 주입해 `run()`을 실행한다(기존 IT가 PostgresRedisContainers를 쓰는 방식 그대로). 단언:
    (1) seed 후 `trackedItemRepository.count()`(또는 findByActiveTrue 크기)가 15이고, role_group별 개수 = MATERIAL 4 / DEALER 9 / SUPPORT 2.
    (2) 표본 품목(예: 6861009, 65203405)의 `iconUrl`이 `https://cdn-lostark.game.onstove.com/efui_iconatlas/use/`로 시작하고 `itemGroup`/`roleGroup`이 기대값과 일치.
    (3) 멱등: `run()`을 2회 실행해도 count가 15로 불변(중복 insert 없음).
    (4) SEED-02 — `SyntheticDemoData.seed()`를 호출하면 신규 활성 15개 전부에 대해 합성 스냅샷이 생성됨을 단언(예: 임의 신규 item_id의 PriceSnapshot count > 0), 그리고 2회차 seed가 멱등(스냅샷 count 불변). `SyntheticDemoData`는 코드 변경 없이 동작해야 함(이 테스트가 그 사실의 증거).
    테스트는 키 없이 그린이어야 한다(네트워크 호출 0 — 시더·합성기는 외부 API 미접촉).
  </action>
  <verify>
    <automated>test -f src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java && grep -q "MATERIAL" src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java && ./gradlew test --tests "*WatchlistSeederIT" -q</automated>
  </verify>
  <acceptance_criteria>
    - `WatchlistSeederIT`가 Testcontainers(PostgresRedisContainers)에서 그린이며 키·네트워크 불필요
    - seed 후 활성 품목 15개 + role_group 분포(4/9/2)를 단언
    - 표본 품목의 iconUrl/itemGroup/roleGroup이 스파이크 상수와 일치함을 단언
    - 시더 2회 실행 멱등(count 15 불변) 단언
    - SyntheticDemoData.seed()가 신규 품목에 합성 스냅샷을 생성하고 2회차가 멱등임을 단언 — SyntheticDemoData 소스는 0줄 변경
    - 전체 `./gradlew build` 그린
  </acceptance_criteria>
  <done>Testcontainers IT가 큐레이션 15개의 seed·enrichment·멱등과 신규 품목의 합성 스냅샷(SEED-02, 합성기 무변경)을 키 없이 증명한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 12-SPIKE-FINDINGS 상수 → seed 소스 | 큐레이션 상수를 코드에 베이크 — 공개 메타데이터만 진입해야 함(키·가격 금지) |
| seed 코드 → DB(tracked_item) | enrichment write-path; 수집/캐시/event-impact 경로와 격리되어야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | seed 상수에 비밀 유입 | mitigate | WATCHLIST는 공개 메타데이터(id/name/category/iconUrl/group)만; Task 2 AC가 `bearer <token>`/`eyJ` grep 0건 강제. 키는 어떤 seed 소스에도 없음(SEED-04). |
| T-13-02 | Tampering | enrichment 변경이 수집/캐시/event-impact로 번짐 | mitigate | 이 plan은 마이그레이션·엔티티·시더·테스트만 수정; PriceCollector/RedisTokenBucket/LatestPriceCache/CacheConfig/EventImpactService 무접촉(13-02의 ITEM-04 가드가 0줄 단언). |
| T-13-03 | Tampering | V1–V3 마이그레이션 改변으로 이력 깨짐 | mitigate | V4는 순수 신규 파일; V1–V3 `git diff` 0줄(Task 1 AC). Flyway 체크섬 불변. |
| T-13-04 | Elevation/Injection | DDL/seed에 동적·외부 입력 | accept | V4는 정적 DDL, 시더는 컴파일타임 상수 — 사용자 입력·동적 SQL 없음. |
| T-13-SC | Tampering | 의존성 공급망 | accept | 신규 패키지 0 — 기존 Spring/JPA/Flyway만. legitimacy 게이트 불필요. |
</threat_model>

<verification>
- `./gradlew build` 전체 그린(Testcontainers): V4 적용 후 ddl-auto=validate 통과 + WatchlistSeederIT 그린.
- `git diff --name-only`에 `V1__`/`V2__`/`V3__` 마이그레이션 0건(불변).
- `git diff --stat`에 collect/PriceCollector.java · ratelimit/RedisTokenBucket.java · cache/LatestPriceCache.java · cache/CacheConfig.java · read/EventImpactService.java · seed/SyntheticDemoData.java 변경 0줄.
- WatchlistSeeder가 큐레이션 15개(role_group 4/9/2)를 enrichment와 함께 등록, iconUrl 전부 CDN base 접두.
- 시더·합성기 소스에 키/가격/식별자 0건(grep).
</verification>

<success_criteria>
- V4가 icon_url/item_group/role_group을 nullable로 추가하고 ddl-auto=validate 통과, V1–V3 불변 (ITEM-01, ITEM-02).
- TrackedItem이 enrichment 3필드를 매핑(getter 노출, 기존 생성자 보존) (ITEM-02).
- WatchlistSeeder가 큐레이션 15개를 icon_url/item_group/role_group과 함께 멱등 등록 (SEED-01).
- SyntheticDemoData가 무변경으로 신규 15개 품목의 합성 스냅샷을 키 없이 멱등 생성 (SEED-02).
- seed/watchlist 코드에 키·민감정보 0건 (SEED-04).
</success_criteria>

<output>
Create `.planning/phases/13-backend-enrichment-seed/13-01-SUMMARY.md` when done
</output>
