---
phase: 11-demo-surface-docs
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/docs/screenshots/README.md
  - frontend/docs/screenshots/.gitkeep
autonomous: false
requirements: [DEMO-02]

must_haves:
  truths:
    - "**수동 캡처 — 슬롯·파일명·alt 텍스트·재현 딥링크만 plan/실행이 마련하고 실제 PNG 삽입은 사용자가 수행한다(D-02).** 다운스트림 에이전트는 브라우저 바이너리 캡처가 불가하므로 본 plan은 (a) `frontend/docs/screenshots/` 디렉터리, (b) 3장의 **확정 파일명**, (c) 각 장의 **alt 텍스트**, (d) '가장 잘 나온 seed 상태'를 재현하는 **딥링크 URL**, (e) 캡처 프로토콜(뷰포트·seed 기동 전제)을 README로 마련한다. Playwright 자동 캡처는 일회성 산출물에 과한 인프라라 채택하지 않는다."
    - "**핵심 3화면 각 1장(총 3장)을 `frontend/docs/screenshots/`에 커밋(D-03, D-04).** 확정 파일명은 `dashboard.png`(Dashboard 헬스+워치리스트), `item-timeline.png`(Item Timeline 차트+이벤트 마커), `event-impact.png`(Event Impact 결과 표)이다. README/문서는 이 PNG들을 **상대경로**로 참조해 GitHub·클론에서 바로 렌더·오프라인 재현되게 한다(외부 링크/CDN 금지 — 링크 깨짐 시 데모 표면 손상). 상태 변형(insufficient_data·다운샘플 등) 추가 캡처는 최신화 부담 대비 ROI 낮아 **3장으로 한정**한다."
    - "**스크린샷 경로는 git 추적 대상이다(D-03).** `frontend/.gitignore`는 `dist`/`dist-ssr`만 무시하고 `docs/`는 무시하지 않으므로 `frontend/docs/screenshots/`의 PNG는 기본 추적된다. 본 plan은 `git check-ignore`로 추적 가능함을 **검증**하되 `.gitignore`를 수정하지 않는다(이미 별개 경로). 빈 디렉터리가 사라지지 않도록 `.gitkeep`을 두고, README가 캡처 전에도 폴더와 규약을 문서화한다."
    - "각 스크린샷 딥링크는 **URL searchParams 단일 소스**(Phase 9/10) 정책을 이용해 특정 seed 상태를 재현·고정한다(예: `/timeline?item=<id>&from=&to=`, `/impact?item=<id>&window=24`). 캡처 프로토콜은 **seed 프로파일 백엔드 기동 + `npm run dev`** 를 전제로 명시한다(스크린샷이 담는 데이터의 출처 = seed 합성 데이터, 실데이터 아님 — honest-data 에토스)."
  artifacts:
    - path: "frontend/docs/screenshots/README.md"
      provides: "캡처 프로토콜 — 3장 확정 파일명·alt 텍스트·재현 딥링크·뷰포트/seed 전제. 사용자가 PNG를 채워 넣는 단일 안내서(D-02/D-03/D-04)"
      contains: "event-impact.png"
    - path: "frontend/docs/screenshots/.gitkeep"
      provides: "PNG 커밋 전에도 frontend/docs/screenshots/ 경로를 git에 유지하는 placeholder"
      contains: ""
  key_links:
    - from: "frontend/docs/screenshots/README.md"
      to: "frontend/src/features/impact/useImpactParams.ts"
      via: "재현 딥링크(?item=&window=)가 URL-as-state 훅 규약을 그대로 따른다 — 캡처 장면을 결정적으로 고정"
      pattern: "window"
    - from: "frontend/docs/screenshots/README.md"
      to: "frontend/.gitignore"
      via: "docs/screenshots는 dist/dist-ssr 무시 규칙과 별개 경로 — 추적 대상임을 git check-ignore로 검증(파일 무변경)"
      pattern: "dist"
---

<objective>
포트폴리오 데모의 **스크린샷 표면(DEMO-02)** 을 잠그되, 바이너리 캡처는 사용자 몫이라는 제약(D-02)에 맞춰 **에이전트가 할 수 있는 전부 — 디렉터리·확정 파일명·alt 텍스트·재현 딥링크·캡처 프로토콜 —** 을 마련한다. 실제 PNG 3장은 사용자가 seed 백엔드 + `npm run dev`를 띄우고 브라우저에서 직접 캡처해 채운다.

이 plan이 11-01(시각 마감) 뒤(wave 2)에 오는 이유: 스크린샷은 **마감된 최종 화면**을 담아야 하고, 11-03 README가 이 **확정 파일명**을 상대경로로 참조하기 때문이다. 파일명·alt·딥링크를 여기서 한 번 확정하면 11-03이 다시 정하지 않고 그대로 소비한다.

Output: `frontend/docs/screenshots/{README.md,.gitkeep}` + 사용자 캡처 체크포인트(autonomous: false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-demo-surface-docs/11-CONTEXT.md

<interfaces>
<!-- 3화면 라우트(딥링크 재현용) — react-router-dom:
     Dashboard = "/", Item Timeline = "/timeline?item=<id>&from=&to=", Event Impact = "/impact?item=<id>&window=24"
     URL searchParams가 단일 상태 소스(Phase 9 D-04 / Phase 10 D-01) — 캡처 장면을 결정적으로 고정 -->

<!-- frontend/.gitignore (무변경): dist·dist-ssr만 무시, docs/ 미무시 → docs/screenshots/*.png 기본 추적 -->

<!-- 확정 파일명(D-04, 3장):
     dashboard.png       — Dashboard: 수집 헬스 카드 + 활성 품목 워치리스트(최신가 카드)
     item-timeline.png   — Item Timeline: 라인 차트 + 이벤트 세로 마커(+ 다운샘플 배지 가능 상태)
     event-impact.png    — Event Impact: window별 이벤트 전후 변화율 결과 표 + 상관≠인과 배너 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스크린샷 디렉터리 + 캡처 프로토콜 README 작성</name>
  <files>frontend/docs/screenshots/README.md, frontend/docs/screenshots/.gitkeep</files>
  <read_first>frontend/.gitignore, frontend/src/features/timeline/useTimelineParams.ts, frontend/src/features/impact/useImpactParams.ts, .planning/phases/11-demo-surface-docs/11-CONTEXT.md</read_first>
  <action>`frontend/docs/screenshots/` 디렉터리를 만들고 빈 상태에서도 git에 유지되도록 `.gitkeep`(빈 파일)을 둔다. 같은 폴더에 `README.md`(캡처 프로토콜)를 작성한다. 내용:
1) **목적** 한 줄 — 이 폴더는 README가 상대경로로 참조하는 데모 스크린샷 3장을 담는다(D-03/D-04).
2) **전제** — `SPRING_PROFILES_ACTIVE=seed`로 백엔드 기동 후 `cd frontend && npm install && npm run dev`. 스크린샷은 **seed 합성 데이터**이며 실데이터가 아님을 명시(honest-data).
3) **캡처 대상 표** — 3행, 컬럼 = `파일명 | 화면 | 재현 딥링크 | alt 텍스트`:
   - `dashboard.png` | Dashboard | `http://localhost:5173/` | `Dashboard 화면 — 수집 파이프라인 헬스 카드와 활성 품목 최신가 워치리스트`
   - `item-timeline.png` | Item Timeline | `http://localhost:5173/timeline?item=<품목ID>` | `Item Timeline 화면 — 시세 라인 차트와 게임 이벤트 세로 마커`
   - `event-impact.png` | Event Impact | `http://localhost:5173/impact?item=<품목ID>&window=24` | `Event Impact 화면 — 이벤트 전후 변화율 결과 표와 상관≠인과 안내`
   (`<품목ID>`는 `GET /api/items`에서 '가장 잘 나온' seed 품목을 골라 채우라고 안내. 실제 라우트 prefix는 `useTimelineParams`/`useImpactParams` 규약과 일치시킨다.)
4) **뷰포트 가이드** — 데스크톱 폭(~1440px) 권장, 브라우저 100% 배율, 화면당 1장(D-04).
5) **커밋 안내** — PNG 3장을 이 폴더에 두고 커밋(상대경로 추적). 외부 링크/CDN 사용 금지.
`.gitignore`는 **수정하지 않는다**(docs/는 이미 추적 경로).</action>
  <verify>
    <automated>cd frontend && test -f docs/screenshots/README.md && test -f docs/screenshots/.gitkeep && grep -q "dashboard.png" docs/screenshots/README.md && grep -q "item-timeline.png" docs/screenshots/README.md && grep -q "event-impact.png" docs/screenshots/README.md && grep -q "seed" docs/screenshots/README.md && test -z "$(git check-ignore docs/screenshots/README.md)" && echo "screenshot scaffold OK + git-tracked"</automated>
  </verify>
  <done>frontend/docs/screenshots/에 .gitkeep과 캡처 프로토콜 README(3장 파일명·alt·딥링크·seed 전제)가 생성되고, git check-ignore가 빈 결과(추적 가능)를 반환한다.</done>
</task>

<task type="manual">
  <name>Task 2: 사용자 스크린샷 캡처 (체크포인트)</name>
  <files>frontend/docs/screenshots/dashboard.png, frontend/docs/screenshots/item-timeline.png, frontend/docs/screenshots/event-impact.png</files>
  <read_first>frontend/docs/screenshots/README.md</read_first>
  <action>**사용자 수행(에이전트는 바이너리 캡처 불가, D-02).** `frontend/docs/screenshots/README.md`의 프로토콜대로 seed 백엔드 + `npm run dev`를 띄우고, 3개 딥링크에서 각 1장씩 데스크톱 폭으로 캡처해 확정 파일명(`dashboard.png`·`item-timeline.png`·`event-impact.png`)으로 폴더에 저장한다. 캡처가 끝나면 PNG 3장을 커밋한다. **이번에 캡처를 미루면** 11-03 README는 슬롯(상대경로 참조)만 유지된 채 진행되며, 실제 PNG는 후속에 채워도 데모 표면 구조는 완성된다(슬립 허용 — DEMO-02의 PNG 삽입은 사용자 액션).</action>
  <verify>
    <manual>사용자가 3개 딥링크에서 캡처한 dashboard.png / item-timeline.png / event-impact.png가 frontend/docs/screenshots/에 존재하고 현재 화면과 일치하는지 확인. (캡처를 미룬 경우: 슬롯/파일명/딥링크가 README에 준비됐음을 확인하고 후속으로 이월.)</manual>
  </verify>
  <done>3장 PNG가 확정 파일명으로 frontend/docs/screenshots/에 존재·커밋됐다 — 또는 사용자가 의도적으로 슬립을 선택해 슬롯만 준비된 상태로 이월됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 스크린샷 콘텐츠 → 공개 레포 | 캡처 이미지가 공개 GitHub에 커밋된다. 화면에 실제 시크릿(API 키·Admin secret)이 보이면 노출이 된다. seed 화면은 공개 read 데이터만 표시하므로 위험은 낮으나 검토 대상. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Information Disclosure | 커밋되는 PNG 스크린샷 | mitigate | 캡처 대상은 공개 read 화면(Dashboard/Timeline/Impact)뿐 — 관리자 쓰기 UI·시크릿 입력 화면은 v1.1 범위 밖(read-only). README 프로토콜에 'seed 합성 데이터, 시크릿 없음' 전제를 명시하고, 사용자 캡처 시 토큰/키가 화면에 없음을 확인하도록 안내. |
| T-11-02-02 | Tampering | 외부 이미지 링크 | accept | D-03에 따라 상대경로 PNG만 사용(외부 CDN/링크 금지) → 제3자 콘텐츠 주입 표면 없음. |

**고(high) 심각도 위협 없음** — 공개 read 화면의 정적 이미지 자산만 추가한다(security_block_on: high 게이트 통과).
</threat_model>

<verification>
- `frontend/docs/screenshots/`에 `.gitkeep`과 캡처 프로토콜 `README.md`가 존재한다.
- README가 3장 확정 파일명(`dashboard.png`/`item-timeline.png`/`event-impact.png`)·각 alt 텍스트·재현 딥링크·seed 기동 전제·데스크톱 뷰포트 가이드를 담는다.
- `git check-ignore frontend/docs/screenshots/README.md`가 빈 결과(추적 가능) — `.gitignore` 무변경.
- (사용자 캡처 시) 3장 PNG가 확정 파일명으로 존재하고 현재 화면과 일치한다. (슬립 시) 슬롯/딥링크가 준비된 상태로 11-03에 이월된다.
- Java `src/`·`frontend/src/` 무변경(문서/자산 전용 plan).

## 회귀 안전 (기존 기능 보존)
- 신규 디렉터리/문서/이미지 자산만 추가하고 소스·빌드·`.gitignore`를 건드리지 않으므로 앱 동작·기존 데모 표면에 영향이 없다.
</verification>

<success_criteria>
- DEMO-02의 스크린샷 축이 잠긴다: 디렉터리·확정 파일명·alt·재현 딥링크·캡처 프로토콜이 준비돼 사용자가 PNG만 채우면 된다.
- 파일명·딥링크가 확정돼 11-03 README가 상대경로로 결정적으로 참조한다(외부 링크 없음).
- 소스/빌드 무변경, seed 합성 데이터 honest-data 전제 명시.
</success_criteria>

<output>
Create `.planning/phases/11-demo-surface-docs/11-02-SUMMARY.md` when done.
</output>
