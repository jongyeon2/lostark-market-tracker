---
phase: 09-item-timeline
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/features/_shared/ItemSelect.tsx
  - frontend/src/features/_shared/LatestPriceCard.tsx
autonomous: true
requirements: [TIME-01]

must_haves:
  truths:
    - "`ItemSelect` is a **dropdown `select`** (D-05) built on the shadcn `select` block (09-01) — the compact selector that leaves room for the chart and latest-price card on a single-item screen. It loads options from `useItems()` and renders each option's `displayName`; the trigger shows the selected item's `displayName` (placeholder '품목 선택' shown only before a selection, which the 09-05 auto-select makes near-invisible)."
    - "`ItemSelect` is **controlled** (props `value: number | null`, `onChange: (id: number) => void`) and holds NO URL/router state — so 09-05 wires it to `useTimelineParams.setItem` and Phase 10 (Event Impact) reuses the identical component (D-07: selector extracted to a shared location so the layer dependency never flows impact→timeline)."
    - "`ItemSelect` handles its own async: while `useItems()` is pending it shows a disabled trigger / skeleton; on error it shows a disabled trigger with a small '품목을 불러오지 못했어요' hint (it does NOT mount the big screen ErrorState — selector failure must not blank the page)."
    - "`LatestPriceCard` is a **timeline-specific lightweight card** (D-07 — NOT the Dashboard `ItemCard`, which is a grid-unified card of different shape). It takes `itemId: number` + `displayName: string` props, runs its OWN `useLatestPrice(itemId)`, and wraps itself in its OWN `<AsyncBoundary>` (Phase-7 D-08 / Phase-8 D-07 isolation) so a latest-price failure never hides the chart and vice-versa."
    - "`LatestPriceCard` shows, vertically (gap `sm`), per 09-UI-SPEC Latest Price Card Contract: 품목명 (`displayName`, Heading 20/600), 최신가 (`minPrice` with `tabular-nums`, full digits grouped + ' G'), 수집 시각 (`collectedAt` via `formatKst`, Label muted 'YYYY-MM-DD HH:mm KST'). Gold is rendered full-precision (`toLocaleString('ko-KR')`), never abbreviated, on the card (abbreviation is the chart y-axis's job)."
    - "Both components live under `frontend/src/features/_shared/` (D-07 shared location) and are purely consumed via props for identity; only `LatestPriceCard` fetches (its own latest query). `cd frontend && npm run build` passes with zero type errors; no Java `src/` file changes."
  artifacts:
    - path: "frontend/src/features/_shared/ItemSelect.tsx"
      provides: "Controlled dropdown item selector over shadcn select + useItems() — shared by Phase 9 & 10 (D-05/D-07, TIME-01)"
      contains: "onChange"
    - path: "frontend/src/features/_shared/LatestPriceCard.tsx"
      provides: "Timeline-specific lightweight latest-price card (useLatestPrice + own AsyncBoundary) — D-07, TIME-01"
      contains: "useLatestPrice"
  key_links:
    - from: "frontend/src/features/_shared/ItemSelect.tsx"
      to: "frontend/src/components/ui/select.tsx"
      via: "ItemSelect renders Select/SelectTrigger/SelectContent/SelectItem from the 09-01 block"
      pattern: "SelectTrigger|SelectContent"
    - from: "frontend/src/features/_shared/ItemSelect.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "options come from useItems()"
      pattern: "useItems"
    - from: "frontend/src/features/_shared/LatestPriceCard.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "card fetches useLatestPrice(itemId), wrapped in its own AsyncBoundary"
      pattern: "useLatestPrice"
---

<objective>
Build the two **shared, single-item surfaces** that both Phase 9 and Phase 10 reuse (D-07): the dropdown **`ItemSelect`** (D-05) and the timeline-specific lightweight **`LatestPriceCard`**. Together they deliver the "selected item → latest-price card" half of TIME-01.

The engineering signal is the **dividend of the pre-built data layer**: because Phase 7 already locked the `useItems`/`useLatestPrice` hooks and the `<AsyncBoundary>` wrapper, these components write only success paths and inherit per-unit failure isolation for free. Extracting them to `features/_shared/` (rather than under `timeline/`) is the deliberate D-07 move so Phase 10's Event-Impact screen imports the exact same selector and card — the dependency points timeline→shared and impact→shared, never impact→timeline.

Output: `frontend/src/features/_shared/ItemSelect.tsx` + `frontend/src/features/_shared/LatestPriceCard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-item-timeline/09-CONTEXT.md
@.planning/phases/09-item-timeline/09-UI-SPEC.md
@.planning/phases/08-dashboard/08-CONTEXT.md

<interfaces>
<!-- From frontend/src/lib/queries.ts -->
export function useItems()                  // -> TrackedItem[]
export function useLatestPrice(id: number)  // -> { itemId, minPrice, collectedAt }
<!-- From frontend/src/lib/schemas.ts -->
export type TrackedItem = { id: number; externalItemId: string; displayName: string; category: string; active: boolean }
export type LatestPrice = { itemId: number; minPrice: number; collectedAt: string }
<!-- From frontend/src/components/state/AsyncBoundary.tsx -->
export function AsyncBoundary(props: { status: 'pending'|'error'|'success'; isEmpty?: boolean; onRetry?: () => void; emptyBody?: string; children: ReactNode })
<!-- From frontend/src/lib/formatKst.ts -->
export function formatKst(value: string | number): string
<!-- 09-01 select block: Select, SelectTrigger, SelectValue, SelectContent, SelectItem from @/components/ui/select -->
<!-- Pattern reference (do NOT reuse as-is — D-07): frontend/src/features/dashboard/ItemCard.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemSelect (controlled dropdown selector)</name>
  <files>frontend/src/features/_shared/ItemSelect.tsx</files>
  <read_first>frontend/src/components/ui/select.tsx, frontend/src/lib/queries.ts, frontend/src/lib/schemas.ts</read_first>
  <action>Create `ItemSelect({ value, onChange }: { value: number | null; onChange: (id: number) => void })`. Call `useItems()` for options. Render the shadcn `Select` with `value={value != null ? String(value) : undefined}` and `onValueChange={(v) => onChange(Number(v))}` (radix select works on string values — convert at the boundary). SelectTrigger shows SelectValue with placeholder '품목 선택'; SelectContent maps `data` to `<SelectItem key={item.id} value={String(item.id)}>{item.displayName}</SelectItem>`. While `status === 'pending'` render a disabled trigger (or a `Skeleton` sized like the trigger); while `status === 'error'` render a disabled trigger plus a small muted '품목을 불러오지 못했어요' line — do NOT mount the screen-level ErrorState (a selector failure must not blank the page). No URL/router code — selection is reported only via `onChange`.</action>
  <verify>
    <automated>cd frontend && grep -q "useItems" src/features/_shared/ItemSelect.tsx && grep -q "onValueChange" src/features/_shared/ItemSelect.tsx && grep -q "onChange" src/features/_shared/ItemSelect.tsx && npm run build</automated>
  </verify>
  <done>ItemSelect is a controlled dropdown over the shadcn select + useItems(); converts string↔number at the radix boundary; handles its own pending/error inline; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: LatestPriceCard (timeline lightweight card)</name>
  <files>frontend/src/features/_shared/LatestPriceCard.tsx</files>
  <read_first>frontend/src/features/dashboard/ItemCard.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/state/AsyncBoundary.tsx, frontend/src/lib/formatKst.ts</read_first>
  <action>Create `LatestPriceCard({ itemId, displayName }: { itemId: number; displayName: string })`. Call `useLatestPrice(itemId)`. Render a shadcn `Card` and wrap the price area in its OWN `<AsyncBoundary status={status} onRetry={() => refetch()}>` (per D-07/D-08 isolation — distinct from the chart's boundary in 09-05). Inside, lay out vertically (gap `sm`) per 09-UI-SPEC Latest Price Card Contract: 품목명 `displayName` (Heading, `text-xl font-semibold`); 최신가 = `data.minPrice.toLocaleString('ko-KR')` + ' G' (`tabular-nums`, Heading 20/600, full digits — never abbreviated); 수집 시각 = `formatKst(data.collectedAt)` + ' KST' (Label, `text-sm text-muted-foreground`). The identity (displayName) stays visible regardless of the price-area state (it is passed in, already loaded). Keep it lighter than Dashboard's ItemCard — no category line, single selected item, not a grid cell. Read-only: no polling, no write surface.</action>
  <verify>
    <automated>cd frontend && grep -q "useLatestPrice" src/features/_shared/LatestPriceCard.tsx && grep -q "AsyncBoundary" src/features/_shared/LatestPriceCard.tsx && grep -q "formatKst" src/features/_shared/LatestPriceCard.tsx && grep -q "tabular-nums" src/features/_shared/LatestPriceCard.tsx && npm run build</automated>
  </verify>
  <done>LatestPriceCard renders 품목명/최신가(+G, tabular-nums, full digits)/수집시각(KST) inside its own AsyncBoundary using useLatestPrice; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → screen | useItems/useLatestPrice responses are rendered (already zod-validated in api.ts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | error rendering in ItemSelect/LatestPriceCard | mitigate | Error branches render fixed Korean copy only ('품목을 불러오지 못했어요' / inherited ErrorState) — no exception/stack/raw body, matching the Phase-7/8 honest-fail pattern. |
| T-09-02-02 | Tampering | API response shape | accept | Responses cross the zod `.parse` boundary in api.ts (existing) before reaching these components — drift throws and surfaces as the error branch, never a silent bad render. |
</threat_model>

<verification>
- `cd frontend && npm run build` passes (tsc -b + vite build), zero type errors.
- `ItemSelect` is a controlled dropdown (value/onChange) over the 09-01 select block + useItems(); selector failure stays inline (no screen ErrorState).
- `LatestPriceCard` runs its own useLatestPrice + AsyncBoundary; shows 품목명/최신가(+G, tabular-nums)/수집시각(KST) per 09-UI-SPEC.
- Both files live under `frontend/src/features/_shared/` (D-07).
- No Java `src/` file changed.
</verification>

<success_criteria>
- Shared selector + latest-price card delivered to D-05/D-07; TIME-01 latest-card half done.
- Per-unit AsyncBoundary isolation preserved; build green.
</success_criteria>

<output>
Create `.planning/phases/09-item-timeline/09-02-SUMMARY.md` when done.
</output>
