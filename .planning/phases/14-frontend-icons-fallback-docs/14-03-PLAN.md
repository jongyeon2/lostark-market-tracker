---
phase: 14-frontend-icons-fallback-docs
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - README.md
  - frontend/README.md
autonomous: true
requirements: [ICON-08]

must_haves:
  truths:
    - "루트 README는 포트폴리오 진입점으로 데이터 출처(API Icon URL→DB)·API 실측 요약·fallback 전략·도메인 안목(role_group=금융 자산 섹터 비유, 고변동 융화재료 서사)을 기록하고, frontend/README는 아이콘 소비·<ItemIcon>·fallback의 구현·실행 안내를 기록한다 — 둘 다 ICON-08 충족, 중복 최소(D-09)"
    - "findings 노출은 요약 + 링크 + 서사 수준 — 출처(API Icon URL→DB), 실측 요약(각인서 11종 단일 글리프=동일·융화재료 4종 구별·CDN 도메인 cdn-lostark.game.onstove.com/efui_iconatlas/use/), fallback 전략을 README에 요약하고 12-SPIKE-FINDINGS.md로 링크한다(D-10)"
    - "3화면 스크린샷(frontend/docs/screenshots/dashboard.png·item-timeline.png·event-impact.png)을 아이콘·역할 배지가 반영된 새 캡처로 교체하도록 README가 참조하며, 실제 이미지 캡처는 수동 사용자 액션(앱 실행+브라우저)으로 명시된다(D-11)"
    - "README는 실 API 키·계정 식별자·가격 원문을 기재하지 않고 공개 메타데이터·키 없는 seed 재현 절차만 담는다(상시 가드)"
  artifacts:
    - path: "README.md"
      provides: "데이터 출처·API 실측 요약·fallback 전략·자산 섹터 서사 + 신규 스크린샷 참조(D-09/D-10/D-11)"
      contains: "fallback"
    - path: "frontend/README.md"
      provides: "<ItemIcon> 아이콘 소비·fallback 구현·실행 안내(D-09)"
      contains: "ItemIcon"
  key_links:
    - from: "README 데이터 출처·실측·fallback 섹션"
      to: ".planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md"
      via: "요약 + 상대경로 링크(D-10)"
      pattern: "12-SPIKE-FINDINGS"
---

<objective>
Phase 14의 시각 enrichment를 **docs로 기록**한다 — 코드 변경 없이 README 2개만 갱신(백엔드 `src/` 0줄, 프론트 코드 0줄).

- **루트 README(D-09 진입점):** 데이터 출처(로스트아크 Open API `Icon` URL → DB enrichment 컬럼), API 실측 요약(각인서 11종 단일 글리프=동일·융화재료 4종 구별, CDN 도메인), fallback 전략(`<ItemIcon>` null/onError → 역할색 글리프), 도메인 안목 서사(`role_group`=금융 "자산 섹터" 비유 + 고변동 융화재료). 12-SPIKE-FINDINGS로 링크(D-10).
- **frontend/README(D-09 구현·실행):** 아이콘 소비 방식·`<ItemIcon>`/RoleBadge·역할군 셀렉터·fallback의 구현/실행 안내.
- **스크린샷(D-11):** `frontend/docs/screenshots/`의 기존 3장(아이콘 없는 구버전)을 아이콘·역할 배지 반영 새 캡처로 교체하도록 README가 참조 — 단, **실제 이미지 캡처는 수동 사용자 액션**(앱 실행 + 브라우저).

Purpose: "루트/frontend README만 따라 클린 재현 시 아이콘까지 뜬다"(ROADMAP 검증)를 문서로 보증하고, 큐레이션이 단순 덤프가 아닌 도메인 안목임을 포트폴리오 서사로 연결한다.

Output: 갱신된 `README.md`·`frontend/README.md`(출처·실측·fallback·서사 + 신규 스크린샷 참조). 코드 diff 0(docs만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-icons-fallback-docs/14-CONTEXT.md
@.planning/phases/14-frontend-icons-fallback-docs/14-UI-SPEC.md
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- README.md(루트, 기존 ~12KB): 포트폴리오 진입점. "한눈에 3단계 재현"(seed·키 불필요) 섹션 보유. 데이터 출처·API 실측·fallback·자산 섹터 서사 섹션을 추가/보강. 기존 구조·CI 배지·curl 예시 보존하고 append/섹션 보강(전체 재작성 금지). -->
<!-- frontend/README.md(기존 ~3.6KB): 프론트 데모 안내(사전조건 seed 백엔드, 실행 npm run dev). <ItemIcon>·역할 배지·역할군 셀렉터·fallback 구현/실행 섹션 추가. -->
<!-- frontend/docs/screenshots/{dashboard,item-timeline,event-impact}.png: 기존 3장(아이콘 없는 구버전). README가 이 경로를 참조 — 캡처는 수동 교체(D-11). -->
<!-- 12-SPIKE-FINDINGS.md: iconUrl CDN base, 각인서 11종 identical 글리프(d), 융화재료 구별, fallback 전략(e). README는 요약+링크만(D-10) — 전체 전재 금지. -->
<!-- 상시 가드: 실 API 키·계정 식별자·가격 원문 미기재(공개 메타데이터만). 키 없는 seed 재현만 기술. -->
<!-- 14-01/14-02 산출물: <ItemIcon>(고정 슬롯·null/onError fallback), RoleBadge(딜러/서포터/융화재료 solid), 역할군 셀렉터 헤더, 역할 정렬 — README가 정확히 기술해야 함. -->
</interfaces>
</context>

## Phase Goal

**As a** 신입 백엔드 채용 면접관·로아 유저(README만 보고 프로젝트를 이해·재현하는 사람), **I want to** 아이콘 데이터가 어디서 와서(API Icon URL→DB) 어떻게 표시되고 없을 때 어떻게(역할색 글리프 fallback) 대체되는지, 그리고 역할군이 왜 "자산 섹터"처럼 의미 있는지를 README에서 읽고, 키 없이 seed로 아이콘까지 재현하기를, **so that** 시각 enrichment가 정직한 실측 데이터 위에 얹혔음과 큐레이션의 도메인 안목이 문서로 증명된다.

<tasks>

<task type="auto">
  <name>Task 1: 루트 README — 데이터 출처·API 실측·fallback 전략·자산 섹터 서사 + 신규 스크린샷 참조(D-09/D-10/D-11)</name>
  <read_first>
    - README.md (기존 구조·"3단계 재현"·CI 배지·curl 예시 — 보존하고 섹션 보강)
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md (출처·실측·fallback 요약 + 링크 대상)
    - .planning/phases/14-frontend-icons-fallback-docs/14-CONTEXT.md (D-09/D-10/D-11 정확 문구·서사)
    - frontend/docs/screenshots (기존 3장 경로 — 참조 갱신)
  </read_first>
  <files>README.md</files>
  <action>
    루트 `README.md`에 **시각 enrichment** 섹션을 추가/보강한다(기존 섹션·curl·CI 배지·3단계 재현은 보존 — 전체 재작성 금지):
    (1) **데이터 출처:** 품목 아이콘은 로스트아크 Open API 응답의 `Icon` URL(CDN `cdn-lostark.game.onstove.com/efui_iconatlas/use/`)을 Phase 12 스파이크가 실측해 DB enrichment 컬럼(icon_url/item_group/role_group)에 잠갔고, 4개 read 응답이 패스스루함을 1~2문단으로.
    (2) **API 실측 요약(D-10):** 각인서 11종이 전부 동일 글리프(identical), 융화재료 4종은 서로 구별됨 — 그래서 각인서는 실아이콘 + 한글 라벨 + 역할 배지로 식별(fallback 강제 대체 안 함). 상세는 `.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md`로 **링크**(전재 금지).
    (3) **fallback 전략:** 공용 `<ItemIcon>`이 `iconUrl==null` 또는 `<img> onError`(CDN 차단 포함)에서 역할색 글리프 타일(딜러/서포터=ScrollText, 융화재료=FlaskConical)로 대체 — 고정 슬롯이라 레이아웃 시프트 0, "미완성"이 아닌 의도된 디자인.
    (4) **도메인 안목 서사(D-10):** `role_group`(딜러/서포터/융화재료)을 금융 마켓의 "자산 섹터"에 비유; 고변동·고가 융화재료(오레하/아비도스)가 로아온·시즌말·대형 업데이트에 시세 변동이 가장 크다는 큐레이션 안목을 짧게.
    (5) **스크린샷(D-11):** 3화면 스크린샷 참조를 아이콘·역할 배지 반영 새 이미지로 갱신(경로 `frontend/docs/screenshots/dashboard.png`·`item-timeline.png`·`event-impact.png` 유지 또는 명시). 실 캡처는 아래 <verification>의 수동 절차로 사용자가 교체함을 README 캡션/주석에 표기하지 말고(사용자용 문서이므로) 자연스러운 스크린샷 섹션으로 둔다.
    실 API 키·계정·가격 원문은 기재하지 않는다(공개 메타데이터·키 없는 seed 재현만).
  </action>
  <verify>
    <automated>grep -q "efui_iconatlas" README.md && grep -q "12-SPIKE-FINDINGS" README.md && grep -qE "fallback|글리프" README.md && grep -qE "자산 섹터|role_group|역할" README.md && grep -q "screenshots/dashboard.png" README.md && ! grep -iE "bearer |LOSTARK_API_KEY=[A-Za-z0-9]" README.md</automated>
  </verify>
  <acceptance_criteria>
    - 루트 README가 데이터 출처(Icon URL→DB, CDN `efui_iconatlas`)·API 실측 요약·fallback 전략·자산 섹터 서사를 담음(D-09/D-10)
    - `12-SPIKE-FINDINGS.md`로 링크(요약+링크, 전재 아님 — D-10)
    - 3화면 스크린샷 경로 참조(`frontend/docs/screenshots/*.png`) 존재(D-11)
    - 실 API 키(`bearer `/`LOSTARK_API_KEY=<값>`)·계정 식별자·가격 원문 0건(상시 가드)
    - 기존 README 구조(CI 배지·3단계 재현·curl)는 보존
  </acceptance_criteria>
  <done>루트 README가 아이콘 출처·실측·fallback·도메인 서사를 포트폴리오 진입점으로 기록하고 스파이크 findings로 링크한다.</done>
</task>

<task type="auto">
  <name>Task 2: frontend/README — 아이콘 소비·&lt;ItemIcon&gt;·fallback 구현·실행 안내(D-09)</name>
  <read_first>
    - frontend/README.md (기존 사전조건 seed·실행 npm run dev 구조 — 보존하고 섹션 추가)
    - frontend/src/features/_shared/ItemIcon.tsx (구현 정확 기술 대상)
    - frontend/src/features/_shared/RoleBadge.tsx, frontend/src/features/_shared/roleGroup.ts
    - frontend/src/features/_shared/ItemSelect.tsx (역할군 헤더 셀렉터)
  </read_first>
  <files>frontend/README.md</files>
  <action>
    `frontend/README.md`에 **아이콘 + 역할 배지** 섹션을 추가한다(기존 사전조건·실행 섹션 보존):
    (1) **소비 방식:** 백엔드 4개 read 응답의 `iconUrl`/`itemGroup`/`roleGroup`을 zod 스키마(`schemas.ts`, 단일 출처)가 `.nullable()`로 검증해 받고, 공용 `<ItemIcon>`/`RoleBadge`가 4곳(Dashboard 카드·품목 셀렉터·Timeline 최신가·Event Impact 정체성)에서 소비함을 기술. 프론트는 Lostark API를 직접 부르지 않고 백엔드 DTO만 소비함을 명시(상시 가드).
    (2) **`<ItemIcon>` 구현:** 고정 px 슬롯(md 32/sm 20) → 레이아웃 시프트 0; `iconUrl==null` 또는 `onError`(CDN 차단)에서 역할색 글리프(딜러/서포터=ScrollText, 융화재료=FlaskConical, 미상=Package) fallback; lucide-react만 사용(외부 아이콘 라이브러리 0).
    (3) **역할 배지·셀렉터:** RoleBadge(딜러/서포터/융화재료 solid 색배지), 셀렉터는 역할군 그룹 헤더 + 역할군→이름 정렬(필터 컨트롤은 v2).
    (4) **fallback 확인 방법:** devtools를 offline로 두거나 iconUrl을 손상시키면 역할색 글리프로 대체되고 레이아웃이 유지됨을 한 줄.
    키 없는 seed 재현(`npm run dev`)으로 아이콘까지 뜬다는 점을 사전조건 섹션과 연결.
  </action>
  <verify>
    <automated>grep -q "ItemIcon" frontend/README.md && grep -qE "fallback|글리프" frontend/README.md && grep -qE "역할|RoleBadge" frontend/README.md && grep -qE "셀렉터|역할군" frontend/README.md && ! grep -iE "bearer |LOSTARK_API_KEY=[A-Za-z0-9]" frontend/README.md</automated>
  </verify>
  <acceptance_criteria>
    - frontend/README가 `<ItemIcon>` 소비·고정 슬롯·null/onError fallback·역할색 글리프를 기술(D-09)
    - RoleBadge·역할군 셀렉터(그룹 헤더·정렬, 필터 v2 제외)를 기술
    - 프론트=백엔드 DTO만 소비(API 직접호출 금지) 명시
    - 실 API 키·계정·가격 원문 0건; 기존 사전조건·실행 섹션 보존
  </acceptance_criteria>
  <done>frontend/README가 아이콘 소비·ItemIcon·fallback·역할 배지/셀렉터의 구현·실행 안내를 담는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 내부 findings/실측 → 공개 README | 스파이크 findings를 요약해 공개 문서에 노출 — 민감정보(키·계정·가격 원문) 누출 위험 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Information Disclosure | README에 실 API 키·계정 식별자·가격 원문 유출 | mitigate | 공개 메타데이터(아이콘 URL·분류·역할군)·키 없는 seed 재현만 기재; Task 1/2 verify가 `bearer `/`LOSTARK_API_KEY=<값>` grep 0건 강제(상시 가드). |
| T-14-07 | Tampering | findings 전재로 문서 비대·드리프트 | accept | 요약 + 상대경로 링크만(D-10) — 단일 출처는 12-SPIKE-FINDINGS.md 유지. |
| T-14-BE | Tampering | docs 변경이 코드 경로 오염 | mitigate | README 2개만 수정 — 코드(`src/`·`frontend/src/`) diff 0줄(verification 게이트). |
</threat_model>

<verification>
- `git diff --name-only`이 `README.md`·`frontend/README.md` 2개만 — 코드(`src/`·`frontend/src/`) 0건.
- 루트 README에 데이터 출처(efui_iconatlas)·실측 요약·fallback·자산 섹터 서사 + `12-SPIKE-FINDINGS.md` 링크 + 3화면 스크린샷 참조 존재.
- frontend/README에 `<ItemIcon>`·fallback·역할 배지/셀렉터 안내 존재.
- README 2개에 실 API 키(`bearer `/`LOSTARK_API_KEY=<값>`)·계정 식별자·가격 원문 0건(grep).
- **수동 사용자 액션(D-11 스크린샷 — 자동 증명 불가):** `docker compose up -d postgres redis` → `./gradlew bootRun --args='--spring.profiles.active=seed'` → `cd frontend && npm run dev` → 브라우저에서 3화면 캡처해 `frontend/docs/screenshots/dashboard.png`·`item-timeline.png`·`event-impact.png` 교체(아이콘·역할 배지 반영). + ROADMAP 사용자 확인 포인트: 아이콘·역할 배지가 3화면에서 일관·식별적이고 fallback이 "미완성"으로 안 보이는지 직접 확인.
</verification>

<success_criteria>
- 루트 README·frontend/README에 데이터 출처·API 실측 결과·fallback 전략이 기록된다 (ICON-08·D-09).
- findings는 요약+링크+서사로 노출되고 12-SPIKE-FINDINGS.md로 링크된다 (D-10).
- 3화면 스크린샷이 아이콘·역할 배지 반영 새 캡처로 교체되도록 참조된다 — 캡처는 수동 (D-11).
- 실 API 키·계정·가격 원문 미기재(공개 메타데이터만), 코드 diff 0.
</success_criteria>

<output>
Create `.planning/phases/14-frontend-icons-fallback-docs/14-03-SUMMARY.md` when done
</output>
