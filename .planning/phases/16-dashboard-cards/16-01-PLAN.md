---
phase: 16-dashboard-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/dashboard/ItemCard.tsx
autonomous: true
requirements: [CARD-01, CARD-02]

must_haves:
  truths:
    - "D-01: ItemCard.tsx CardHeader의 item.category 코드 줄(현재 line 32의 <p className=\"text-muted-foreground text-sm font-semibold\">{item.category}</p>, 예 \"50010\")이 완전히 제거되고 itemGroup 등 다른 코드/숫자로 대체하지 않는다 — 제거 후 카드는 아이콘·품목명·역할 배지·골드 가격·수집 시각만 표시(CARD-01, Goal 문구 정합)"
    - "D-02: 아이콘(<ItemIcon>)·역할 배지(<RoleBadge>)·골드 가격(data.minPrice toLocaleString)·수집 시각(formatKst, CardContent의 수집 시각 <p>)·per-card useLatestPrice 팬아웃·pending/error/success 인라인 상태 매핑이 회귀 없이 그대로 유지된다(성공기준 3, v1.2 enrichment 무회귀)"
    - "D-03: <Card> 전체가 react-router-dom <Link>로 감싸져 /timeline?item={item.id} 로 이동하는 시맨틱 <a>가 되고, Ctrl/중클릭 새 탭·키보드 Enter·우클릭 메뉴가 <a> 기본 동작으로 지원된다 — onClick+useNavigate는 채택하지 않는다(CARD-02, 접근성/새 탭 우위)"
    - "D-04: 딥링크 URL은 /timeline?item={item.id}(item.id는 z.number) 하나뿐 — from/to는 넘기지 않아 타임라인 기본 최근 30일이 처리하고, useTimelineParams가 ?item= 을 단일 출처(Phase 7 D-04)로 읽어 자동 선택하므로 타임라인 화면/선택 메커니즘 변경 0"
    - "D-05: hover 피드백은 카드 elevation(shadow 강화)+border 강조로 처리하고 accent fill을 쓰지 않으며, accent blue-600(--ring/--primary)은 focus-visible 링에만 예약된다(cursor-pointer 포함) — 기존 카드 미감/8pt 스페이싱과 일관(UI-SPEC accent 규약 준수)"
    - "D-06: <Link> 래핑 후 focus-visible 포커스 링이 카드 외곽(Card의 rounded-xl 반경에 정합)에 자연스럽게 뜨고, 카드 내부 RoleBadge/ItemIcon/텍스트가 중첩 인터랙티브 요소가 되지 않는다 — 단일 <a> 안에 비인터랙티브 자식만 남는다"
    - "변경은 frontend/src/features/dashboard/ItemCard.tsx 1개 파일에 한정 — 백엔드 src/ diff 0줄, 타임라인/대시보드 그리드 등 다른 파일 무변경, npm run build(tsc -b && vite build) 그린"
  artifacts:
    - path: "frontend/src/features/dashboard/ItemCard.tsx"
      provides: "category 코드 줄 제거(CARD-01) + 카드 전체를 /timeline?item={id} 딥링크 <Link>로 래핑(CARD-02) + hover elevation/focus-visible ring 피드백(D-05/D-06)"
      contains: "/timeline?item="
  key_links:
    - from: "ItemCard <Link to=/timeline?item={item.id}>"
      to: "TimelinePage (useTimelineParams가 ?item= 을 읽어 자동 선택)"
      via: "react-router-dom <Link> → URL searchParam ?item= (Phase 7 D-04 URL-as-state 단일 출처)"
      pattern: "/timeline?item="
---

<objective>
대시보드 품목 카드(`ItemCard`)를 **의미 없는 고유 번호 없이, 클릭하면 그 품목이 선택된 채 타임라인으로 딥링크되는** 카드로 만든다. 순수 프론트엔드 변경으로 대상 파일은 사실상 `ItemCard.tsx` 하나다(백엔드 0줄, 타임라인 0줄).

- **CARD-01 — 고유 번호 제거(D-01):** CardHeader 안의 `item.category` 코드 줄(현재 `<p className="text-muted-foreground text-sm font-semibold">{item.category}</p>`, 예 "50010")을 **완전 제거**한다. `itemGroup` 등으로 대체하지 않는다. 제거 후 카드는 **아이콘 · 품목명 · 역할 배지 · 골드 가격 · 수집 시각**만 남는다.
- **CARD-02 — 클릭 딥링크(D-03/D-04):** `<Card>` 전체를 react-router-dom `<Link to={\`/timeline?item=${item.id}\`}>`로 감싼다. 시맨틱 `<a>`이므로 Ctrl/중클릭 새 탭·키보드 Enter·우클릭 메뉴가 공짜로 따라온다. URL은 `?item={id}` 하나만 — 기간은 타임라인 기본(최근 30일)이 처리하고, `useTimelineParams`가 `?item=`을 단일 출처로 읽어 자동 선택하므로 **타임라인 쪽 변경/새 선택 메커니즘은 불필요**.
- **무회귀(D-02):** `<ItemIcon>`·`<RoleBadge>`·골드 가격·수집 시각·per-card `useLatestPrice` 팬아웃·pending/error/success 인라인 상태 매핑은 **그대로 유지**한다(v1.2 enrichment 무회귀 = 성공기준 3).
- **인터랙션 마감(D-05/D-06, Claude 재량):** hover는 카드 elevation(그림자)+보더 강조로(accent fill 아님), `cursor-pointer`, accent `blue-600`은 **focus-visible 링에만** 예약. `<Link>` 래핑 시 포커스 링이 카드 외곽에 자연스럽게 뜨고 내부에 중첩 인터랙티브 요소가 생기지 않게 마감.

Purpose: 카드에서 본 그 품목을 추가 클릭 없이 타임라인 차트로 곧바로 잇고, 카드에서 의미 없는 숫자를 지워 Goal("아이콘·이름·골드·수집시각만")에 정합시킨다.

Output: category 줄이 제거되고 `/timeline?item={id}`로 딥링크되는 `ItemCard.tsx`. `npm run build`(tsc -b && vite build) 그린, 백엔드 `src/` diff 0줄, 변경 파일 1개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dashboard-cards/16-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- 대상 파일 현재 구조(frontend/src/features/dashboard/ItemCard.tsx): -->
<!-- return ( <Card> <CardHeader> <CardTitle>[<ItemIcon>][이름][<RoleBadge>]</CardTitle> <p ...>{item.category}</p> ← ★제거 대상 </CardHeader> <CardContent>{pending→Skeleton / error→'최신가 아직 없음' / success→가격+수집시각}</CardContent> </Card> ) -->
<!-- 제거 대상은 CardHeader 안 {item.category} <p> 한 줄. CardContent의 '수집 시각 {formatKst(...)}' <p>는 동일 className을 쓰지만 유지 대상 — item.category를 렌더하는 <p>만 지운다. -->
<!-- item.id: z.number()(schemas.ts line 39, TrackedItem). useTimelineParams는 searchParams.get('item')을 Number(...)로 파싱 → ?item={number}면 자동 선택. from/to 없으면 최근 30일 기본(useTimelineParams.ts line 29~41). -->
<!-- 라우터: frontend/src/main.tsx가 createBrowserRouter, 'timeline' 라우트는 AppLayout(Outlet) 하위. Link/NavLink는 'react-router-dom'에서 import(TopNav.tsx가 NavLink 선례). -->
<!-- Card 프리미티브(components/ui/card.tsx): 기본 className 'rounded-xl border shadow-sm ...' — className으로 hover/전환 오버라이드, 블록 0줄 변경. --ring/--primary=blue-600 accent는 focus 전용 예약(D-05). -->
<!-- 카드 내부 자식은 전부 비인터랙티브(ItemIcon=<img alt="">, RoleBadge=<Badge> span, 텍스트) → 단일 <Link> 래핑 시 중첩 <a>/버튼 없음(D-06 안전). -->
<!-- npm run build = tsc -b && vite build (frontend/package.json). 타입체크 게이트. 백엔드는 별도 gradle — 이 plan은 frontend/만 건드림. -->
</interfaces>
</context>

## Phase Goal

**As a** 로아 유저이자 이 대시보드를 보는 면접관, **I want to** 카드에 의미 없는 고유 번호가 사라지고 카드를 클릭하면 그 품목이 선택된 채 타임라인이 뜨기를, **so that** 카드에서 관심 품목을 본 즉시 추가 조작 없이 그 품목의 시세 시계열로 넘어갈 수 있고, 카드가 아이콘·이름·골드·수집시각만으로 깔끔하게 읽힌다.

<tasks>

<task type="auto">
  <name>Task 1: category 줄 제거 + 카드 전체를 /timeline?item={id} 딥링크 <Link>로 래핑</name>
  <read_first>
    - frontend/src/features/dashboard/ItemCard.tsx (유일한 변경 파일 — 현재 return 구조·유지할 요소 전부 확인 후 편집)
    - frontend/src/features/timeline/useTimelineParams.ts (?item= 을 Number로 읽어 자동 선택; from/to 없으면 30일 기본 — 딥링크가 왜 곧바로 동작하는지 근거, 수정 불필요)
    - frontend/src/components/ui/card.tsx (Card 기본 className 'rounded-xl border shadow-sm' — hover/focus는 className 오버라이드로, 블록 0줄)
    - frontend/src/components/layout/TopNav.tsx (react-router-dom Link/NavLink import 선례)
    - .planning/phases/16-dashboard-cards/16-CONTEXT.md (D-01~D-06 결정)
  </read_first>
  <files>frontend/src/features/dashboard/ItemCard.tsx</files>
  <action>
    **(1) category 줄 제거(CARD-01/D-01):** `CardHeader` 안에서 `{item.category}`를 렌더하는 `<p className="text-muted-foreground text-sm font-semibold">{item.category}</p>` 한 줄을 **삭제**한다. `CardContent` 안의 `수집 시각 {formatKst(data.collectedAt)}` `<p>`(동일 className이지만 다른 내용)는 **건드리지 않는다**. `itemGroup` 등 대체 줄을 새로 넣지 않는다(완전 제거). `<ItemIcon>`·`<RoleBadge>`·`<CardTitle>` 이름·가격/수집시각 블록·`useLatestPrice` 훅 호출·pending/error/success 분기는 그대로 둔다(D-02 무회귀).

    **(2) 딥링크 래핑(CARD-02/D-03·D-04):** `react-router-dom`에서 `Link`를 import한다. 반환 JSX의 최상위 `<Card>...</Card>`를 `<Link to={\`/timeline?item=${item.id}\`}>`로 감싼다(`item.id`는 number, `?item=` 하나만 — from/to 미전달). `onClick`+`useNavigate`는 쓰지 않는다.

    **(3) hover/focus 마감(D-05·D-06, 재량):** `<Link>`에 `className="group block rounded-xl focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background"`처럼 카드 외곽(Card의 `rounded-xl`에 정합) focus-visible 링을 준다 — accent는 여기(focus)에만. `<Card>`에는 `className="cursor-pointer transition-shadow hover:shadow-md hover:border-foreground/20"`처럼 hover elevation+보더 강조를 준다(accent fill 금지). 정확한 hover 세기/보더 색은 기존 미감과 일관되게 재량으로 조정하되, hover에서 배경을 accent 색으로 채우지 말고 accent는 focus-visible 링에만 남긴다. 내부 자식에 새 인터랙티브 요소(a/button)를 추가하지 않는다(D-06 단일 <a> 유지).
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "{item.category}" src/features/dashboard/ItemCard.tsx && grep -q "from 'react-router-dom'" src/features/dashboard/ItemCard.tsx && grep -q "/timeline?item=" src/features/dashboard/ItemCard.tsx && grep -q "focus-visible:ring" src/features/dashboard/ItemCard.tsx && grep -q "ItemIcon" src/features/dashboard/ItemCard.tsx && grep -q "RoleBadge" src/features/dashboard/ItemCard.tsx && grep -q "formatKst" src/features/dashboard/ItemCard.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ItemCard.tsx`에서 `{item.category}`가 더 이상 등장하지 않는다(`grep -c "{item.category}"` = 0) — 고유 번호 줄 완전 제거(CARD-01)
    - `수집 시각`/`formatKst` `<p>`·`<ItemIcon>`·`<RoleBadge>`·`data.minPrice.toLocaleString`·`useLatestPrice`·pending/error/success 분기는 그대로 존재(무회귀, D-02)
    - `import { Link } from 'react-router-dom'`가 있고, 반환 최상위가 `<Link to={...}>`로 `<Card>`를 감싼다 — `to` 값이 `/timeline?item=${item.id}` 형태(CARD-02, D-03)
    - `<a>` 기본 동작 확인: 렌더된 카드가 `<a href="/timeline?item={id}">`로 나와 Ctrl/중클릭 새 탭·Enter가 동작(onClick+useNavigate 미사용 — `useNavigate` import 없음)
    - hover 시 배경이 accent 색으로 채워지지 않고 elevation/border만 변한다(`hover:shadow`/`hover:border` 존재, hover에 accent bg 클래스 없음); `cursor-pointer` 존재; accent(`ring-ring`)는 `focus-visible:ring-*`에만 사용(D-05)
    - 카드 내부에 `<a>`/`<button>` 등 중첩 인터랙티브 요소가 새로 생기지 않는다(단일 <Link> 안 비인터랙티브 자식만, D-06)
    - `npm run build`(tsc -b && vite build) 그린; `git diff --name-only`가 `frontend/src/features/dashboard/ItemCard.tsx` 1개만 — 백엔드 `src/` 0줄
  </acceptance_criteria>
  <done>카드에서 고유 번호가 사라지고(아이콘·이름·역할·골드·수집시각만), 카드를 클릭하면 `/timeline?item={id}`로 그 품목이 선택된 채 이동하며(새 탭/키보드/우클릭 지원), enrichment·가격 정보는 무회귀로 유지되고 전체 프론트 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `item.id`(number) → `<Link to>` URL searchParam | id는 우리 백엔드가 발급하고 zod `z.number()`로 파싱된 내부 엔티티 id — 외부 사용자 입력 아님 |
| 프론트 → 외부 API | 프론트는 Lostark Open API 직접 호출 금지(상시 가드) — 이 plan은 라우팅 링크만 추가, fetch 0 |
| 백엔드 경로 | 이 plan은 `frontend/` 1파일만 수정 — 백엔드 `src/` diff 0줄(수집·저장·서빙 코어 경로 불변) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | 딥링크 URL에 조작된 `?item=` 값 도착 | accept | `useTimelineParams`가 `?item=`을 `Number(...)`로 파싱해 NaN이면 `itemId=null`(자동 선택 없이 기본 화면) — 임의 문자열이 쿼리/렌더 오염 불가. `item.id`는 내부 number라 링크 생성부에서 주입 위험 없음(href 문자열, 스크립트 실행 불가). |
| T-16-02 | Elevation of Privilege | 중첩 인터랙티브 요소로 인한 접근성/포커스 트랩 | mitigate | 카드 내부 자식(ItemIcon=img, RoleBadge=span, 텍스트)은 전부 비인터랙티브 → 단일 `<a>` 안에 중첩 링크/버튼 없음(D-06). focus-visible 링이 카드 외곽 한 곳에만. |
| T-16-BE | Tampering | 프론트 변경이 백엔드 핵심 경로 오염 | mitigate | 변경 파일 1개(ItemCard.tsx)로 한정 — 아래 verification의 `git diff --name-only` 게이트로 백엔드 `src/` 0줄 강제. |
</threat_model>

<verification>
- `cd frontend && npm run build`(tsc -b && vite build) 그린.
- `git diff --name-only`가 `frontend/src/features/dashboard/ItemCard.tsx` 1개 파일만 — 백엔드 `src/`·타임라인·대시보드 그리드 변경 0.
- `ItemCard.tsx`에 `{item.category}` 부재(고유 번호 제거), `import { Link } from 'react-router-dom'` + `<Link to={\`/timeline?item=${item.id}\`}>` 존재(딥링크).
- `<ItemIcon>`·`<RoleBadge>`·`formatKst`·`useLatestPrice`·`data.minPrice.toLocaleString` 잔존(무회귀).
- accent(`ring-ring`)는 `focus-visible:ring-*`에만; hover에 accent 배경 fill 클래스 없음(D-05). `useNavigate` import 없음(D-03).
- (수동 UAT 후보) 대시보드에서 카드 클릭 → `/timeline?item={id}`로 이동해 그 품목이 선택된 차트가 뜸; Ctrl+클릭 새 탭·Tab 포커스 링·우클릭 메뉴 동작.
</verification>

<success_criteria>
- 대시보드 카드에 물품 고유 번호(`item.category` 코드)가 없고 아이콘·품목명·골드 가격·수집 시각만 표시된다(성공기준 1, CARD-01/D-01).
- 카드를 클릭하면 `/timeline?item={id}`로 이동해 해당 품목이 선택된 상태로 타임라인이 뜬다(성공기준 2, CARD-02/D-03·D-04).
- v1.2 enrichment(아이콘·역할 배지)와 기존 카드 정보(골드·수집시각)가 회귀 없이 유지된다(성공기준 3, D-02).
- accent(blue-600)는 focus-visible 링에만, hover는 elevation/보더로(D-05); 중첩 인터랙티브 요소 없음(D-06).
- `npm run build` 그린, 변경 파일 1개(ItemCard.tsx), 백엔드 `src/` 0줄.
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-cards/16-01-SUMMARY.md` when done
</output>
