---
phase: 17.2-news-panel
plan: 03
type: execute
wave: 3
depends_on: [17.2-02]
files_modified:
  - frontend/src/lib/schemas.ts
  - frontend/src/lib/queries.ts
  - frontend/src/features/dashboard/NewsPanel.tsx
  - frontend/src/features/dashboard/DashboardPage.tsx
autonomous: true
requirements: [NEWS-02, NEWS-03]

must_haves:
  truths:
    - "D-03: 프론트 useNews()(React Query, staleTime ~5분) + NewsPanel.tsx(이벤트 표 + 공지 표, AsyncBoundary) + DashboardPage lg 2-column — 컨텐츠 max-w-6xl, lg:grid lg:grid-cols-[minmax(0,1fr)_20rem] lg:gap-8(좌 물품 / 우 NewsPanel ≈320px). 기존 단독 max-w-3xl 제거(그리드가 폭 제어). 모바일(< lg) 물품 아래 세로 스택"
    - "D-04: 이벤트 표(제목·기간)·공지 표(타입·제목·날짜), 각 ≤6개. 행 클릭 → 로아 공식 link 새 탭(target=_blank rel=noopener noreferrer). 로딩/빈/에러 = 기존 AsyncBoundary"
    - "D-06: Core Value 가드 — 프론트는 백엔드 GET /api/news만 소비(로아 직접 호출 금지). 백엔드·수집/캐시/event-impact 0줄"
    - "D-07: 정직성 — NewsPanel이 로딩(스켈레톤)·빈('진행중 이벤트/공지 없어요')·에러(다시 불러오기)를 빈 화면 없이 표시(useNews 독립 boundary, 물품 그리드와 분리)"
  artifacts:
    - path: "frontend/src/features/dashboard/NewsPanel.tsx"
      provides: "이벤트/공지 표 패널 + AsyncBoundary 상태 + 새 탭 링크 (D-04/D-07)"
    - path: "frontend/src/features/dashboard/DashboardPage.tsx"
      provides: "lg 2-column(좌 물품 / 우 NewsPanel), 모바일 세로 스택 (D-03)"
  key_links:
    - from: "useNews() → GET /api/news"
      to: "NewsController(17.2-02) NewsResponse"
      via: "read DTO 계약 소비(zod newsResponseSchema로 boundary loud-fail)"
      pattern: "/api/news"
    - from: "NewsPanel 링크"
      to: "로아 공식 페이지"
      via: "target=_blank rel=noopener 새 탭"
      pattern: "target=\"_blank\""
---

<objective>
대시보드 우측 여백에 로스트아크 진행중 이벤트·공지사항을 표 형식으로 노출하는 `NewsPanel`을 추가하고, `DashboardPage`를 lg 2-column(좌 물품 / 우 뉴스)으로 재구성한다(D-03/D-04). 프론트는 백엔드 `GET /api/news`만 소비(로아 직접 호출 금지) — 백엔드 0줄(D-06). 로딩/빈/에러는 기존 `AsyncBoundary`로 정직히 표시(D-07).

Purpose: "물품 옆 공간"을 로아 이벤트·공지로 채워 데모 완성도를 높이되, 뉴스 패널이 물품 그리드와 독립 boundary라 한쪽 실패가 다른 쪽을 가리지 않게 한다.

Output: `useNews()` 훅 + `newsResponseSchema` + `NewsPanel.tsx` + 2-column `DashboardPage`. 백엔드/수집/캐시/event-impact diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-news-panel/17.2-CONTEXT.md
@docs/superpowers/specs/2026-07-06-dashboard-news-panel-design.md
@CLAUDE.md

<interfaces>
<!-- frontend/src/lib/schemas.ts: 기존 zod 스키마 패턴(예: trackedItemSchema, collectionHealthSchema, roleGroupSchema). ★ 추가: eventSchema{title,link,startDate,endDate,thumbnail?}, noticeSchema{title,link,date,type}, newsResponseSchema{events:eventSchema[], notices:noticeSchema[], updatedAt:string|null}. boundary loud-fail(기존 패턴). -->
<!-- frontend/src/lib/queries.ts: 기존 useItems()/useCollectionHealth()/useLatestPrice() React Query 훅 + fetch 헬퍼. ★ 추가: useNews() = useQuery(['news'], fetch GET /api/news → newsResponseSchema.parse), staleTime ~5분, 공격적 폴링 없음. -->
<!-- frontend/src/features/dashboard/DashboardPage.tsx(17.1-03/quick 후 상태): <div class="space-y-6"><h1>대시보드</h1><AsyncBoundary(items)><div class="max-w-3xl space-y-8"><ItemSection 각인/재료/></div></AsyncBoundary></div>. ★ max-w-3xl 제거 → 컨텐츠 max-w-6xl + lg:grid lg:grid-cols-[minmax(0,1fr)_20rem] lg:gap-8: 좌 = 기존 items AsyncBoundary+섹션, 우 = <NewsPanel/>. 모바일(< lg) 단일 컬럼(물품 위 · 뉴스 아래). -->
<!-- frontend/src/features/dashboard/HealthCard.tsx·ItemCard.tsx: Card/AsyncBoundary/상태 매핑 패턴 참고. frontend/src/components/state/AsyncBoundary.tsx: status/isEmpty/onRetry(무변경 소비). frontend/src/components/ui/card.tsx: Card 프리미티브. frontend/src/lib/formatKst.ts: 날짜 포맷 참고(기간/날짜 표시). -->
<!-- 보존(회귀 금지): 기존 물품 섹션(각인→재료)·ItemCard(딥링크/아이콘/배지/가격/시각)·물품 AsyncBoundary는 좌 컬럼에서 그대로. 뉴스 링크는 외부(로아) → 새 탭·rel=noopener. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: newsResponseSchema(zod) + useNews() 훅</name>
  <read_first>
    - frontend/src/lib/schemas.ts (기존 zod 스키마·boundary 패턴 — 추가 지점)
    - frontend/src/lib/queries.ts (기존 useItems/useCollectionHealth 훅·fetch 헬퍼 — 추가 지점)
    - .planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md (백엔드 NewsResponse 필드 계약)
  </read_first>
  <files>frontend/src/lib/schemas.ts, frontend/src/lib/queries.ts</files>
  <action>
    schemas.ts에 `eventSchema`(title, link, startDate, endDate, thumbnail optional), `noticeSchema`(title, link, date, type), `newsResponseSchema`(events: eventSchema 배열, notices: noticeSchema 배열, updatedAt: string nullable)와 파생 타입을 추가(기존 스키마 export 패턴 준수, boundary loud-fail). queries.ts에 `useNews()` = React Query `useQuery({ queryKey: ['news'], queryFn: GET /api/news → newsResponseSchema.parse, staleTime: 5분 })`를 기존 fetch 헬퍼/훅 패턴대로 추가. 프론트는 `/api/news`(백엔드)만 호출 — 로아 API 직접 호출 없음(D-06).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - schemas.ts에 newsResponseSchema(+event/notice)와 타입이 백엔드 NewsResponse 계약대로 정의된다
    - queries.ts에 useNews() 훅이 GET /api/news를 호출·파싱(staleTime ~5분)한다
    - `cd frontend && npm run build` 그린, 로아 직접 호출 코드 없음
  </acceptance_criteria>
  <done>useNews() 훅과 newsResponseSchema가 백엔드 계약대로 추가되고 빌드가 그린이다.</done>
</task>

<task type="auto">
  <name>Task 2: NewsPanel.tsx (이벤트/공지 표 + AsyncBoundary + 새 탭 링크)</name>
  <read_first>
    - frontend/src/lib/queries.ts (Task 1 useNews — 소비)
    - frontend/src/components/state/AsyncBoundary.tsx (status/isEmpty/onRetry)
    - frontend/src/components/ui/card.tsx (Card 프리미티브)
    - frontend/src/features/dashboard/HealthCard.tsx (Card + AsyncBoundary 조립 패턴 참고)
    - frontend/src/lib/formatKst.ts (날짜/기간 포맷 참고)
  </read_first>
  <files>frontend/src/features/dashboard/NewsPanel.tsx</files>
  <action>
    `NewsPanel`을 구현한다: `useNews()` 소비, `AsyncBoundary`(status, isEmpty = events·notices 모두 0, onRetry=refetch)로 감싼다. 내부는 경량 제목의 **이벤트 섹션**(표: 제목 · 기간 startDate~endDate)과 **공지 섹션**(표: 타입 뱃지 · 제목 · 날짜) — 각 최대 6행(백엔드가 이미 제한하나 방어적으로 slice). 각 행은 로아 공식 `link`로 여는 `<a href={link} target="_blank" rel="noopener noreferrer">`. 빈 상태 카피('진행중 이벤트가 없어요'/'새 공지가 없어요'), 로딩 스켈레톤·에러 '다시 불러오기'는 AsyncBoundary가 담당(D-07). 스타일은 기존 Card/타이포/8pt 재사용(단일 Card 2섹션 또는 2 Card — Discretion). 물품 그리드와 독립 boundary(한쪽 실패가 다른 쪽 안 가림).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "useNews" src/features/dashboard/NewsPanel.tsx && grep -q "target=\"_blank\"" src/features/dashboard/NewsPanel.tsx && grep -q "AsyncBoundary" src/features/dashboard/NewsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NewsPanel이 useNews() 소비 + AsyncBoundary(isEmpty=events·notices 모두 0)로 로딩/빈/에러 표시(D-07)
    - 이벤트 표(제목·기간)·공지 표(타입·제목·날짜), 각 ≤6행
    - 각 행 링크가 target="_blank" rel="noopener noreferrer" 새 탭
    - `cd frontend && npm run build` 그린
  </acceptance_criteria>
  <done>이벤트/공지 표와 로딩/빈/에러 상태·새 탭 링크를 갖춘 NewsPanel이 빌드 그린으로 완성된다.</done>
</task>

<task type="auto">
  <name>Task 3: DashboardPage 2-column 레이아웃 통합</name>
  <read_first>
    - frontend/src/features/dashboard/DashboardPage.tsx (max-w-3xl 상태 — 2-column 재구성 대상)
    - frontend/src/features/dashboard/NewsPanel.tsx (Task 2 — 우 컬럼 배치)
    - docs/superpowers/specs/2026-07-06-dashboard-news-panel-design.md (§6 레이아웃)
  </read_first>
  <files>frontend/src/features/dashboard/DashboardPage.tsx</files>
  <action>
    DashboardPage를 lg 2-column으로 재구성한다: 컨텐츠 래퍼 `max-w-6xl`, `lg:grid lg:grid-cols-[minmax(0,1fr)_20rem] lg:gap-8`. **좌 컬럼** = 기존 물품 `AsyncBoundary` + 각인→재료 세로 섹션(그대로, 기존 단독 `max-w-3xl`은 제거 — 그리드가 폭 제어). **우 컬럼** = `<NewsPanel/>`. 모바일(< lg)은 단일 컬럼 — 물품 위, 뉴스 아래 세로 스택. h1('대시보드')·물품 섹션·ItemCard(딥링크/아이콘/배지/가격/시각)는 회귀 없이 보존. 백엔드/수집/캐시/event-impact 0줄.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "NewsPanel" src/features/dashboard/DashboardPage.tsx && grep -q "max-w-6xl" src/features/dashboard/DashboardPage.tsx && ! grep -q "max-w-3xl" src/features/dashboard/DashboardPage.tsx && grep -q "sortByRole" src/features/dashboard/DashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage가 lg 2-column(좌 물품 / 우 NewsPanel), 모바일 세로 스택으로 렌더(D-03)
    - 기존 단독 max-w-3xl 제거 → 컨텐츠 max-w-6xl 그리드가 폭 제어
    - 물품 섹션(sortByRole 각인→재료)·ItemCard 보존(회귀 없음)
    - `cd frontend && npm run build` 그린, git diff에 백엔드 파일 없음
  </acceptance_criteria>
  <done>대시보드가 좌 물품 / 우 뉴스 2-column으로 재구성되고(모바일 스택), 물품 회귀 없이 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 GET /api/news → 프론트 렌더 | 프론트는 백엔드 read DTO만 소비 — 로아 API 직접 호출 금지(상시 가드) |
| 뉴스 link → 외부 새 탭 | 로아 공식 링크로 이동 — rel=noopener로 tabnabbing 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1723-01 | Tampering | 외부 링크 새 탭 tabnabbing(window.opener) | mitigate | 모든 뉴스 링크 `rel="noopener noreferrer"` |
| T-1723-02 | Information Disclosure | 프론트가 로아 API 직접 호출로 키 노출 | mitigate | 프론트는 백엔드 /api/news만 — 로아 직접 호출 코드 없음(verify grep) |
| T-1723-03 | Tampering | title/link 렌더 XSS | accept | React 기본 이스케이프 + zod 파싱 값, dangerouslySetInnerHTML 미사용, href는 문자열 |
| T-1723-04 | Availability | 뉴스 실패가 물품 그리드까지 가림 | mitigate | NewsPanel 독립 AsyncBoundary — 물품 그리드 boundary와 분리(D-07) |
| T-1723-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 React Query/zod/Card/AsyncBoundary 재사용 |
</threat_model>

<verification>
- DashboardPage lg 2-column(좌 물품 / 우 NewsPanel), 모바일 세로 스택 — max-w-3xl 제거·max-w-6xl 그리드(D-03).
- NewsPanel: useNews() + AsyncBoundary(isEmpty=events·notices 0), 이벤트 표(제목·기간)·공지 표(타입·제목·날짜) 각 ≤6, 링크 target=_blank rel=noopener(D-04/D-07).
- 프론트는 GET /api/news만 소비 — 로아 직접 호출 없음, 물품 섹션/ItemCard 회귀 0(D-06).
- `cd frontend && npm run build` 그린; git diff에 백엔드 src/ 없음(백엔드 0줄).
</verification>

<success_criteria>
- 대시보드 우측에 이벤트·공지가 표 형식으로 표시되고 항목 클릭 시 로아 공식 페이지가 새 탭으로 열린다(NEWS-02, D-03/D-04).
- 뉴스 소싱 실패·갱신 전에도 빈 화면 없이 로딩/빈/에러가 정직히 표시된다(NEWS-03, D-07).
- 프론트 로아 직접 호출 0·백엔드 0줄, 물품 그리드 회귀 0(D-06).
</success_criteria>

<output>
Create `.planning/phases/17.2-news-panel/17.2-03-SUMMARY.md` when done
</output>
