---
phase: 18-free-deploy-security
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified:
  - deploy/lostark.service
  - docs/deploy/oracle-vm-runbook.md
autonomous: false
requirements: [DEPLOY-01, DEPLOY-03]

must_haves:
  truths:
    - "D-01/D-07: Oracle Cloud Always Free VM(ARM Ampere·Ubuntu) 한 대에 docker-compose 스택을 **수동 배포**하는 런북 — 프로비저닝 → Docker 설치 → DuckDNS → 방화벽 → `.env.prod` → `up -d --build` → systemd 부팅 기동 → 검증. CD 자동화는 범위 밖(v2)."
    - "D-05: 런북이 **방화벽 이중 개방**을 명시 — OCI 보안목록/NSG(80·443 공개, 22는 본인 IP 제한) **AND** VM 내부 iptables(80·443 ACCEPT + netfilter-persistent save). Oracle Ubuntu 기본 iptables가 80/443을 막는 대표 함정을 경고."
    - "D-02: DuckDNS 서브도메인을 VM public IP로 등록하고 `SITE_ADDRESS`로 설정 — DNS 전파 후 Caddy가 Let's Encrypt 인증서를 자동 발급(HTTPS 강제)."
    - "D-07: `deploy/lostark.service` systemd 유닛이 `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d`를 부팅 시 기동(`restart: unless-stopped`와 함께 상시 가동 — @Scheduled 수집기 24/7)."
  artifacts:
    - path: "deploy/lostark.service"
      provides: "부팅 시 compose 스택 기동 systemd 유닛 — 상시 가동 보장"
    - path: "docs/deploy/oracle-vm-runbook.md"
      provides: "Oracle VM 수동 배포 전체 절차(보안 포함) — 사용자가 실행하는 단일 진입 문서"
  key_links:
    - from: "런북 `up -d --build`"
      to: "18-03 docker-compose.prod.yml"
      via: "--env-file .env.prod로 스택 기동"
      pattern: "docker-compose.prod.yml"
    - from: "런북 방화벽/HTTPS 절차"
      to: "18-05 보안 검증 게이트(포트·HTTPS 확인)"
      via: "게이트가 런북 산출 상태를 off-box에서 검증"
      pattern: "firewall"
---

<objective>
Oracle Cloud Always Free VM 한 대에 prod 스택을 **수동 배포**하는 런북과 부팅 기동 systemd 유닛을 작성한다. 프로비저닝·Docker 설치·DuckDNS·**방화벽 이중 개방**·`.env.prod` 강시크릿·`compose up`·부팅 기동·라이브 검증을 한 문서로 정리한다.

Purpose: DEPLOY-01(공개 URL로 3화면+관리자 접근)을 상시 VM에서 실현하고, DEPLOY-03의 방화벽·HTTPS 항목을 절차로 보장한다. 상시 가동으로 `PriceCollector`가 24/7 수집(Core Value)한다.

Output: `deploy/lostark.service`·`docs/deploy/oracle-vm-runbook.md`. 실제 VM 실행은 사용자 수동(autonomous:false) — 앱 코드 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-free-deploy-security/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- deploy/lostark.service (systemd):
  [Unit] Description=Lostark Price Tracker (docker compose) / Requires=docker.service / After=docker.service network-online.target
  [Service] Type=oneshot / RemainAfterExit=yes / WorkingDirectory=/opt/lostark-price-tracker /
            ExecStart=/usr/bin/docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build /
            ExecStop=/usr/bin/docker compose -f docker-compose.prod.yml --env-file .env.prod down / TimeoutStartSec=0
  [Install] WantedBy=multi-user.target -->
<!-- docs/deploy/oracle-vm-runbook.md 섹션:
  1) VM 프로비저닝: Oracle Cloud 계정 → Always Free ARM Ampere(VM.Standard.A1.Flex, Ubuntu 22.04 LTS, 2~4GB) 생성.
     ★ "Out of capacity"면 리전/가용성도메인 변경·재시도. SSH 공개키 등록.
  2) 서버 초기화: sudo apt update && upgrade; Docker Engine + docker compose plugin 설치(공식 스크립트); `sudo usermod -aG docker $USER`(재로그인).
  3) DuckDNS: duckdns.org 로그인 → 서브도메인 생성 → 현재 IP를 VM public IP로 설정, token 기록. (선택) IP 갱신 크론(curl duckdns update url).
  4) 방화벽 이중 개방(보안 핵심):
     (a) OCI 콘솔 → VCN Security List(또는 NSG) Ingress: TCP 80·443 = 0.0.0.0/0, TCP 22 = 본인 IP/32.
     (b) VM 내부 iptables: `sudo iptables -I INPUT 6 -p tcp --dport 80 -j ACCEPT`; 443 동일; `sudo netfilter-persistent save`.
     ★ Oracle Ubuntu 이미지는 기본 iptables가 80/443을 REJECT — 이 단계 누락이 "방화벽 열었는데 접속 안 됨"의 대표 원인.
  5) 앱 배치: `sudo mkdir -p /opt/lostark-price-tracker` → git clone → `cp .env.prod.example .env.prod`;
     .env.prod에 openssl rand -hex 32로 POSTGRES_PASSWORD·REDIS_PASSWORD·ADMIN_API_SECRET 생성 입력, LOSTARK_API_KEY 실키, SITE_ADDRESS=서브.duckdns.org, ACME_EMAIL 입력. ★ .env.prod 커밋 금지 재확인.
  6) 기동: `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build`. DNS 전파 후 Caddy가 인증서 자동 발급(수 분). `docker compose logs -f caddy`로 발급 확인.
  7) 부팅 기동: deploy/lostark.service를 /etc/systemd/system/에 복사 → `sudo systemctl daemon-reload && sudo systemctl enable --now lostark`.
  8) 검증: https://서브.duckdns.org 로드·자물쇠(유효 인증서)·3화면·관리자 로그인·`docker compose logs app`에서 collection_run SUCCESS 축적.
  9) 운영: 업데이트=git pull + `up -d --build`; 로그=compose logs; (선택) 백업=`docker compose exec postgres pg_dump ...`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: systemd 유닛(deploy/lostark.service)</name>
  <read_first>
    - docker-compose.prod.yml (18-03 산출 — up 명령·--env-file .env.prod 정합)
    - .env.prod.example (18-03 산출 — env 파일명 확인)
  </read_first>
  <files>deploy/lostark.service</files>
  <action>
    `deploy/lostark.service`를 인터페이스 명세대로 만든다: `[Unit]` Requires/After docker.service·network-online.target; `[Service]` Type=oneshot·RemainAfterExit=yes·WorkingDirectory=/opt/lostark-price-tracker·ExecStart=`docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build`·ExecStop=`... down`·TimeoutStartSec=0; `[Install]` WantedBy=multi-user.target. 헤더 주석에 설치법(`/etc/systemd/system/`에 복사 → daemon-reload → enable --now) 명시.
  </action>
  <verify>
    <automated>grep -q "docker compose -f docker-compose.prod.yml" deploy/lostark.service && grep -q "WantedBy=multi-user.target" deploy/lostark.service</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/lostark.service`가 `--env-file .env.prod`로 compose up을 ExecStart — `grep -q "up -d" deploy/lostark.service` 성립
    - `WantedBy=multi-user.target`(부팅 기동) + `After=docker.service` 존재
    - RemainAfterExit=yes(oneshot 유지)
  </acceptance_criteria>
  <done>부팅 시 compose 스택을 기동하는 systemd 유닛이 작성됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 배포 런북(docs/deploy/oracle-vm-runbook.md)</name>
  <read_first>
    - .planning/phases/18-free-deploy-security/18-CONTEXT.md (D-01·D-05·D-07 결정·함정)
    - docker-compose.prod.yml (18-03 — 기동 명령)
    - .env.prod.example (18-03 — 채울 시크릿 목록)
    - deploy/lostark.service (Task 1 — 설치 절차)
  </read_first>
  <files>docs/deploy/oracle-vm-runbook.md</files>
  <action>
    `docs/deploy/oracle-vm-runbook.md`를 인터페이스의 9개 섹션대로 작성한다(한국어). 특히 **섹션 4 방화벽 이중 개방**(OCI 보안목록 + VM iptables, Oracle Ubuntu iptables 함정 경고), **섹션 5 강시크릿 생성**(openssl rand·123456789 금지·.env.prod 커밋 금지), **섹션 6 Caddy 인증서 자동 발급**, **섹션 8 라이브 검증 체크리스트**를 빠짐없이 포함. 각 명령은 복사-실행 가능한 코드 블록으로. 상단에 "선행: Phase 18-01~03 산출물(Dockerfile·Caddyfile·compose·.env.prod.example) 필요" 명시.
  </action>
  <verify>
    <automated>grep -q "iptables" docs/deploy/oracle-vm-runbook.md && grep -q "duckdns" docs/deploy/oracle-vm-runbook.md && grep -q "openssl rand" docs/deploy/oracle-vm-runbook.md && grep -q "up -d --build" docs/deploy/oracle-vm-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - 런북이 방화벽 **이중 개방**(OCI + iptables) 절차 포함 — `grep -q "iptables" docs/deploy/oracle-vm-runbook.md` 성립, OCI 보안목록도 언급
    - DuckDNS 등록 + `SITE_ADDRESS` 설정 절차 포함 — `grep -qi "duckdns" docs/deploy/oracle-vm-runbook.md` 성립
    - 강시크릿 생성(openssl rand)·`.env.prod` 커밋 금지 명시 — `grep -q "openssl rand" docs/deploy/oracle-vm-runbook.md` 성립
    - `docker compose ... up -d --build` 기동 + systemd 부팅 기동 절차 포함
    - 라이브 검증 체크리스트(HTTPS 자물쇠·3화면·관리자 로그인·collection_run SUCCESS) 포함
  </acceptance_criteria>
  <done>Oracle VM 수동 배포 전 과정이 보안 절차 포함 단일 문서로 정리돼, 사용자가 따라 배포하면 라이브가 된다.</done>
</task>

<task type="checkpoint">
  <name>Task 3: [수동] 사용자 VM 배포 실행 + 라이브 확인</name>
  <action>
    사용자가 런북을 따라 Oracle VM에 실제 배포한다(계정/VM은 실행자 접근 밖 — autonomous:false). 완료 후 `https://<도메인>` 라이브·HTTPS 자물쇠·관리자 로그인·collection_run 축적을 사용자가 확인해 보고한다. 이 체크포인트가 DEPLOY-01의 실측 근거이며 18-05 보안 게이트의 off-box 검증 대상을 만든다.
  </action>
  <verify>
    <manual>사용자가 공개 URL 접속·HTTPS 인증서 유효·3화면 렌더·관리자 로그인·수집 축적을 확인 보고</manual>
  </verify>
  <acceptance_criteria>
    - 공개 URL(HTTPS)로 대시보드·타임라인·이벤트영향 3화면 접근 가능
    - 관리자 콘솔 시크릿 로그인 동작(강시크릿)
    - `docker compose logs app`에 collection_run SUCCESS 축적(실데이터)
  </acceptance_criteria>
  <done>공개 URL로 라이브 데모가 서빙되고 실수집이 축적됨을 사용자가 확인했다(DEPLOY-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → VM | OCI 보안목록 + iptables 이중 방화벽, 80/443만 개방, 22 제한 |
| 운영자 → 시크릿 | .env.prod은 VM에만, 강시크릿(openssl rand), 커밋 금지 |
| TLS 발급 | DuckDNS 도메인 + Caddy 자동 Let's Encrypt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Info Disclosure | 열린 SSH(22)로 무차별 접근 | mitigate | OCI 보안목록에서 22를 본인 IP/32로 제한(런북 명시) |
| T-18-04-02 | Tampering | 방화벽 오구성으로 DB/Redis 노출 | mitigate | 80/443만 개방·DB/Redis는 애초에 미publish(18-03) — 게이트가 off-box 확인 |
| T-18-04-03 | Spoofing | 약한 prod 시크릿 재사용 | mitigate | openssl rand 강생성·dev 123456789 금지 명시 |
| T-18-04-04 | DoS/가용성 | 재부팅 후 스택 미기동 | mitigate | systemd enable + restart: unless-stopped |
</threat_model>

<verification>
- `deploy/lostark.service`: compose up ExecStart·WantedBy multi-user.
- `docs/deploy/oracle-vm-runbook.md`: 방화벽 이중 개방·DuckDNS·openssl rand·compose up·systemd·라이브 검증 포함.
- (수동) 사용자 배포 후 공개 URL HTTPS 라이브·관리자 로그인·collection_run 확인.
- 앱 코드·수집/캐시/event-impact diff 0줄.
</verification>

<success_criteria>
- 공개 URL로 데모 3화면과 관리자 콘솔에 접근된다(DEPLOY-01).
- 방화벽·HTTPS가 절차로 보장되고 상시 가동으로 24/7 수집된다(DEPLOY-03 선반영).
</success_criteria>

<output>
Create `.planning/phases/18-free-deploy-security/18-04-SUMMARY.md` when done
</output>
