# env / secrets
.env
.env.*
!.env.example
!.env.prod.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml

# AI 툴 로컬 설정 — 지금까진 전역 ignore 덕에 빠져 있었을 뿐이라 명시한다.
# 설계 기록은 .planning/(추적)에 남고, 여기엔 머신 로컬 설정만 들어간다.
.claude/
.playwright-mcp/

# Java / Gradle
.gradle/
build/
out/

# Logs
*.log

# Local runtime
docker-data/

# 개인 학습 노트 — 파일 첫 줄이 "커밋하지 않는 개인 노트"라고 선언하고 있는데
# untracked 상태로만 있어서 `git add .` 한 번이면 딸려 들어간다. 명시해서 막는다.
STUDY.md