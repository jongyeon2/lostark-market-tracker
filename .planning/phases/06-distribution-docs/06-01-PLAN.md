---
phase: 06-distribution-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java
  - src/main/java/com/lostark/tracker/seed/SeedDataRunner.java
  - src/main/resources/application-seed.yml
  - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java
  - src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java
autonomous: true
requirements: [DIST-02, DIST-03]
user_setup:
  - "After this plan lands, push the branch to GitHub once so the CI workflow runs and the README badge (06-02) turns green — the only step that cannot be verified locally."

must_haves:
  truths:
    - "A GitHub Actions workflow at `.github/workflows/ci.yml` named exactly `CI` runs on EVERY push AND pull_request, on ubuntu-latest with Temurin JDK 21, and executes `./gradlew build` — compile + the full JUnit5/Testcontainers suite against the runner's Docker — so the exact command a reviewer runs locally is the command CI runs (DIST-02, ROADMAP success criterion 1). The filename is stable (`ci.yml`) so the README badge URL `actions/workflows/ci.yml/badge.svg` resolves; the badge itself is added in 06-02."
    - "CI relies on the build's default `api.version=1.44` pin (NO `-PdockerApiVersion` override): ubuntu-latest's Docker engine accepts it, so Testcontainers boots Postgres 16 + Redis 7 on the runner exactly as locally. `gradlew` is made executable (`chmod +x ./gradlew`) before invocation because the repo is authored on Windows where the exec bit may be absent. The `@Disabled` Task-0 spike never runs in CI (no network call, no key)."
    - "Under the `seed` Spring profile (and ONLY seed — never dev/test/prod-default), starting the app populates synthetic demo history: for every active watchlist item, `price_snapshot` rows on a 10-minute cadence spanning 7+ days ending ~now, plus 2+ `game_event`s — so a freshly cloned reviewer sees a non-empty timeline and event-impact instead of blank screens (DIST-03, ROADMAP success criterion 2)."
    - "Demo events' `occurred_at` is placed ON the 10-minute snapshot grid inside the seeded span so BOTH the pre anchor (gap 0) and the post anchor (next tick, 10 min away) fall within the Phase 5 `STALENESS_ALLOWANCE` (30 min, inclusive) — guaranteeing event-impact returns at least one `status:\"ok\"` with a real `change_rate`, not all `insufficient_data`, so the headline feature demos with actual numbers (success criterion 2/4)."
    - "Seeding is idempotent: snapshots are inserted only when `existsByTrackedItem_IdAndCollectedAt(itemId, instant)` is false (honoring the DATA-01 unique key), and the demo events are seeded only when `game_event` is empty (`count() == 0`) — re-running the `seed` profile adds no duplicates and never clobbers admin-entered data."
    - "The synthetic generation logic lives in a plain `@Component SyntheticDemoData` with a callable `seed()` method (NO `@Profile`), and a thin `@Profile(\"seed\") ApplicationRunner` (`SeedDataRunner`, ordered AFTER `WatchlistSeeder`) invokes it at boot — so the seeding is unit-testable on Testcontainers without profile gymnastics and produces NO behavior under dev/test/prod-default. The live `@Scheduled` collector does NOT fire under seed (long `collection.initial-delay-ms` in `application-seed.yml`), so the demo needs NO `LOSTARK_API_KEY`."
    - "A Testcontainers IT proves the seed end-to-end: after saving an active item and calling `seed()`, `GET /api/items/{id}/prices` returns a non-empty `snapshots` array and `GET /api/items/{id}/event-impact?window=24` returns events including ≥1 `status:\"ok\"` with a non-null `changeRate`; a second `seed()` call leaves `priceSnapshotRepository.count()` unchanged (idempotent)."
    - "No schema/migration change (Flyway V1–V3 untouched, ddl-auto=validate stays green) and no new dependency: CI runs the existing build, and the seeder writes only through existing entities (`TrackedItem`/`PriceSnapshot`/`GameEvent`) and repositories."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI: name CI, on push + pull_request, ubuntu-latest, actions/checkout@v4 + actions/setup-java@v4 (temurin 21, gradle cache), chmod +x ./gradlew, ./gradlew build (full Testcontainers suite on the runner's Docker); permissions contents:read; no deploy/secrets/matrix"
      contains: "gradlew build"
    - path: "src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java"
      provides: "@Component (no @Profile) with seed(): for each active item, generate 10-min-cadence min_price snapshots over 7+ days (deterministic walk seeded off item id) inserting each only when !existsByTrackedItem_IdAndCollectedAt; then seed 2+ grid-aligned game_events when game_event is empty; returns a count summary. Injects the UTC Clock bean for a testable 'now'."
      contains: "existsByTrackedItem_IdAndCollectedAt"
    - path: "src/main/java/com/lostark/tracker/seed/SeedDataRunner.java"
      provides: "@Component @Profile(\"seed\") @Order(2) ApplicationRunner that calls SyntheticDemoData.seed() at startup and logs the returned counts (no request/secret logging)"
      contains: "@Profile(\"seed\")"
    - path: "src/main/resources/application-seed.yml"
      provides: "seed-profile config: large collection.initial-delay-ms (mirrors application-test.yml's 3600000) so the @Scheduled collector stays quiet and no API key is needed; datasource/redis inherit the base localhost compose defaults"
      contains: "initial-delay-ms"
    - path: "src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java"
      provides: "Adds @Order(1) so watchlist items seed BEFORE SeedDataRunner runs under the seed profile; the @Profile({dev,seed}) and the curated WATCHLIST list are otherwise UNTOUCHED"
      contains: "@Order"
    - path: "src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java"
      provides: "Testcontainers IT (RANDOM_PORT, extends PostgresRedisContainers, @ActiveProfiles(test)): seeds an item + seed(), asserts non-empty GET /prices and a GET /event-impact with >=1 status ok + non-null changeRate; second seed() leaves snapshot count unchanged"
      contains: "SyntheticDemoDataIT"
  key_links:
    - from: "SeedDataRunner"
      to: "SyntheticDemoData.seed"
      via: "@Profile(seed) ApplicationRunner invokes seed() at boot, after WatchlistSeeder"
      pattern: "syntheticDemoData|SyntheticDemoData"
    - from: "SyntheticDemoData"
      to: "PriceSnapshotRepository.existsByTrackedItem_IdAndCollectedAt"
      via: "idempotent per-tick insert guard honoring the DATA-01 unique key"
      pattern: "existsByTrackedItem_IdAndCollectedAt"
    - from: ".github/workflows/ci.yml"
      to: "./gradlew build"
      via: "CI runs the exact full build+test command a reviewer runs locally"
      pattern: "gradlew build"
---

<objective>
Deliver the **reproducibility backbone** of the portfolio: (1) a **GitHub Actions CI** workflow (DIST-02) that runs the full `./gradlew build` Testcontainers suite on every push/PR — the same command a reviewer runs locally — backing the green README badge; and (2) a **`seed` profile** (DIST-03) that fills the database with synthetic history (7+ days of 10-minute `price_snapshot`s + 2+ `game_event`s) so a freshly cloned reviewer sees a **non-empty timeline and event-impact** instead of blank screens, **without needing a `LOSTARK_API_KEY`**.

The engineering signal here is **honest reproducibility**: CI proves the tests pass in a clean environment (not just "works on my machine"), and the seed makes the headline event-impact feature demoable with **real `change_rate` numbers** — not `insufficient_data`. To get real numbers the seeder respects the Phase 5 staleness contract: it places demo events ON the 10-minute snapshot grid so both anchors land inside the **30-minute `STALENESS_ALLOWANCE`**, yielding `status:"ok"`. Seeding is **idempotent** (per-tick + count guards) and `seed`-only, the generation logic is an **injectable, unit-testable component**, and a Testcontainers IT asserts the literal success-criterion-2 demo.

Output: `.github/workflows/ci.yml`; `SyntheticDemoData` (component) + `SeedDataRunner` (`@Profile("seed")`); `application-seed.yml`; an `@Order` on the existing `WatchlistSeeder`; and `SyntheticDemoDataIT` (Testcontainers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@docs/specs/2026-06-19-project-design.md

<interfaces>
<!-- New files this plan creates: -->
<!-- com.lostark.tracker.seed.SyntheticDemoData   — @Component, callable seed() (no profile) -->
<!-- com.lostark.tracker.seed.SeedDataRunner       — @Profile("seed") ApplicationRunner -->
<!-- .github/workflows/ci.yml                       — GitHub Actions CI -->
<!-- src/main/resources/application-seed.yml        — seed-profile config -->

Locked / existing (REUSE, do NOT change semantics):
- WatchlistSeeder (com.lostark.tracker.collect): @Component @Profile({"dev","seed"}) implements ApplicationRunner. Idempotently upserts the curated 12-item WATCHLIST into tracked_item by external_item_id. ONLY add @Order(1) — do NOT change the list or the upsert logic. It is the source of active items the synthetic seeder reads.
- TrackedItemRepository extends JpaRepository<TrackedItem,Long>: findByActiveTrue():List<TrackedItem> (the active watchlist), findByExternalItemId(String). Inherited save/count.
- PriceSnapshotRepository: existsByTrackedItem_IdAndCollectedAt(Long itemId, OffsetDateTime collectedAt):boolean (the DATA-01 idempotency guard — REUSE for per-tick insert), findByTrackedItem_IdAndCollectedAtBetweenOrderByCollectedAtAsc (the read path the IT exercises). Inherited save/count/deleteAll.
- GameEventRepository: findAllByOrderByOccurredAtDesc() (the event-impact source), count() (inherited — the event idempotency guard). Inherited save/deleteAll.
- PriceSnapshot entity: constructor PriceSnapshot(TrackedItem trackedItem, OffsetDateTime collectedAt, Long minPrice, OffsetDateTime fetchedAt). collected_at is UTC; UNIQUE(tracked_item_id, collected_at).
- GameEvent entity: constructor GameEvent(EventType eventType, String title, OffsetDateTime occurredAt, String description). occurred_at UTC; created_at/updated_at via @PrePersist.
- EventType enum: LOA_ON, MAJOR_UPDATE, SEASON_END, BALANCE_PATCH.
- EventImpactService.STALENESS_ALLOWANCE = Duration.ofMinutes(30), INCLUSIVE: an anchor is fresh when |occurred_at − collected_at| ≤ 30min. Collection cadence is 10 min. Place events on the 10-min grid so anchors are fresh -> status "ok".
- CollectionConfig provides @Bean("collectionClock") Clock = Clock.systemUTC() — inject this for a testable "now".
- The @Scheduled collector reads collection.initial-delay-ms (application-test.yml sets it to 3600000 to keep the collector from auto-firing during tests). Mirror that in application-seed.yml.
- build.gradle: Java toolchain 21, Spring Boot 3.4.1. The `test` task pins systemProperty 'api.version' to (project property dockerApiVersion ?: '1.44'). `./gradlew build` runs compileJava + the full test suite. Run tests locally with `-PdockerApiVersion=1.44`.
- Test base com.lostark.tracker.support.PostgresRedisContainers (singleton Postgres 16 + Redis 7, @DynamicPropertySource). Read ITs: @SpringBootTest(webEnvironment=RANDOM_PORT), @ActiveProfiles("test"), TestRestTemplate, AssertJ; @BeforeEach cleans FK order priceSnapshotRepository.deleteAll() -> trackedItemRepository.deleteAll() -> gameEventRepository.deleteAll(). WatchlistSeeder is @Profile({dev,seed}) so it is INACTIVE under the test profile — the IT must create its own item fixtures.
- No Flyway migration, no entity/column change, no new Gradle dependency in this plan. ddl-auto=validate must stay green.
</interfaces>
</context>

## Phase Goal

**As** a hiring reviewer who just cloned the repo, **I want** CI to prove the test suite passes on every push and a `seed` profile to fill the database with realistic history, **so that** I see a green badge and a non-empty timeline / event-impact (with real `change_rate` numbers) without an API key or hand-entering any data.

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow (Testcontainers build on every push/PR)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 goal + success criterion 1: CI runs on every push with Testcontainers; README shows a green badge)
    - build.gradle (the `test` task pins api.version default 1.44; Java toolchain 21; `./gradlew build` = compile + full test suite)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (the Postgres 16 + Redis 7 containers the CI runner's Docker must start)
    - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (already @Disabled — confirm CI makes no live API call and needs no key)
    - docs/specs/2026-06-19-project-design.md (§103 Distribution Plan: GitHub Actions CI build+test badge; deploy is v2/stretch)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - On any push and any pull_request, GitHub Actions runs a single `build` job on ubuntu-latest.
    - The job sets up Temurin JDK 21, makes gradlew executable, and runs `./gradlew build`, which compiles and runs the full JUnit5 + Testcontainers suite against the runner's Docker daemon (Postgres 16 + Redis 7).
    - A green run yields a passing badge at https://github.com/jongyeon2/lostark-market-tracker/actions/workflows/ci.yml/badge.svg (badge added to README in 06-02).
    - The @Disabled Task-0 spike does not run; no network call and no LOSTARK_API_KEY are needed in CI.
  </behavior>
  <action>
    Create `.github/workflows/ci.yml` with `name: CI`; trigger on both `push:` and `pull_request:` (every push + PR, no branch filter); `permissions: { contents: read }`; one job `build` `runs-on: ubuntu-latest` with steps: (1) `uses: actions/checkout@v4`; (2) `uses: actions/setup-java@v4` with `distribution: temurin`, `java-version: '21'`, `cache: gradle`; (3) `run: chmod +x ./gradlew` (the repo is authored on Windows, so the exec bit may be absent); (4) `run: ./gradlew build --no-daemon` — do NOT pass `-PdockerApiVersion` (the build defaults `api.version` to `1.44`, which ubuntu-latest's Docker engine accepts; Testcontainers boots Postgres 16 + Redis 7 on the runner's Docker exactly as locally). Keep the workflow FILENAME exactly `ci.yml` and the `name:` exactly `CI` so the 06-02 README badge URL resolves. Do NOT add deploy steps, repository secrets, or a build matrix — a single ubuntu-latest job is the MVP (deploy is v2 per design §110).
  </action>
  <verify>
    <automated>grep -q "name: CI" .github/workflows/ci.yml && grep -Eq "push:" .github/workflows/ci.yml && grep -Eq "pull_request:" .github/workflows/ci.yml && grep -q "gradlew build" .github/workflows/ci.yml && grep -Eq "java-version: *'?21'?" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with `name: CI`; `grep -n "name: CI" .github/workflows/ci.yml` matches
    - The workflow triggers on BOTH `push` and `pull_request`; `grep -nE "push:|pull_request:" .github/workflows/ci.yml` shows both
    - It sets up Temurin JDK 21 and runs `./gradlew build`; `grep -nE "temurin|java-version|gradlew build" .github/workflows/ci.yml` matches all three directives
    - The file is named exactly `ci.yml` so the badge URL `actions/workflows/ci.yml/badge.svg` (used in 06-02) resolves
    - No deploy/secret steps (MVP scope): `grep -niE "deploy|secrets\." .github/workflows/ci.yml` returns nothing
  </acceptance_criteria>
  <done>GitHub Actions CI runs the exact `./gradlew build` (Testcontainers Postgres+Redis) a reviewer runs locally, on every push/PR; the stable `ci.yml` filename backs the README badge added in 06-02.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic demo seed — SyntheticDemoData component + SeedDataRunner (@Profile seed) + application-seed.yml + WatchlistSeeder @Order</name>
  <read_first>
    - .planning/ROADMAP.md (success criterion 2: synthetic snapshots 7d+, events 2+, non-empty timeline/event-impact)
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (the existing @Profile({dev,seed}) ApplicationRunner upsert pattern to mirror, and the file that gains @Order(1))
    - src/main/java/com/lostark/tracker/read/EventImpactService.java (STALENESS_ALLOWANCE = 30 min INCLUSIVE — anchors must be within 30 min of an event for status "ok"; a 10-min cadence + grid-aligned events guarantees fresh anchors)
    - src/main/java/com/lostark/tracker/domain/PriceSnapshot.java + TrackedItem.java + GameEvent.java + EventType.java (constructors and fields for the synthetic rows)
    - src/main/java/com/lostark/tracker/repository/PriceSnapshotRepository.java (existsByTrackedItem_IdAndCollectedAt idempotency guard) + TrackedItemRepository.java (findByActiveTrue) + GameEventRepository.java (count for the event guard)
    - src/main/java/com/lostark/tracker/collect/CollectionConfig.java (the @Bean("collectionClock") UTC Clock to inject for a testable "now")
    - src/main/resources/application-test.yml (the `collection.initial-delay-ms: 3600000` pattern that keeps the @Scheduled collector quiet — mirror it) + src/main/resources/application-dev.yml (profile-file shape to mirror)
  </read_first>
  <files>src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java, src/main/java/com/lostark/tracker/seed/SeedDataRunner.java, src/main/resources/application-seed.yml, src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</files>
  <behavior>
    - Starting the app with `--spring.profiles.active=seed` inserts, for each active item, 10-min-cadence min_price snapshots spanning 7+ days ending ~now, plus 2+ game events placed on the snapshot grid within that span.
    - event-impact for a seeded item over a reasonable window (e.g. 24h) returns ≥1 event with status "ok" (both anchors within 30 min) and a non-null change_rate.
    - Re-running seed inserts no duplicate snapshots (per-tick guard) and no duplicate events (count guard).
    - Under dev / test / prod-default profiles, NO synthetic data is generated (SeedDataRunner inactive; SyntheticDemoData.seed() is never auto-invoked).
    - The live @Scheduled collector does not fire under seed (long initial-delay), so no LOSTARK_API_KEY is required to demo.
  </behavior>
  <action>
    Create package `com.lostark.tracker.seed`. `SyntheticDemoData` is a `@Component` with NO `@Profile`, constructor-injected `TrackedItemRepository`, `PriceSnapshotRepository`, `GameEventRepository`, and the `@Bean("collectionClock")` `Clock` (UTC) for a testable "now". Add a method `SeedSummary seed()` returning a small record `SeedSummary(int snapshots, int events)`: read active items via `findByActiveTrue()`; for each item, walk from `OffsetDateTime.now(clock)` truncated to the 10-minute grid BACKWARDS over `>= 7` days on a fixed 10-minute step (e.g. 8 days = 1152 ticks), computing `min_price` as a deterministic pseudo-random walk seeded off the item id (so the demo is reproducible run-to-run), and inserting each tick only when `!priceSnapshotRepository.existsByTrackedItem_IdAndCollectedAt(itemId, instant)` (idempotent; honors the DATA-01 unique key); set `fetched_at` to the same instant. After snapshots, if `gameEventRepository.count() == 0`, insert 2+ `GameEvent`s (real `EventType`s, e.g. `LOA_ON` and `MAJOR_UPDATE`) whose `occurredAt` is set to an EXACT seeded snapshot instant a few days back (so the pre anchor gap is 0 and the next-tick post anchor is 10 min away — BOTH inside the 30-min `STALENESS_ALLOWANCE`, yielding status "ok"). Never call the live API, RestClient, or the collector. Create `SeedDataRunner` as `@Component @Profile("seed") @Order(2) implements ApplicationRunner` whose `run(...)` calls `syntheticDemoData.seed()` and logs the returned counts (log the count summary only — never a request body, header, or secret). Add `@org.springframework.core.annotation.Order(1)` to `WatchlistSeeder` (leave its `@Profile({"dev","seed"})` and the curated WATCHLIST list UNTOUCHED) so watchlist items exist before `SeedDataRunner` runs. Create `application-seed.yml`: set `collection.initial-delay-ms` to a large value (mirror application-test.yml's `3600000`) so the @Scheduled collector stays quiet and the synthetic data is the sole demo source; leave datasource/redis to inherit the base `application.yml` localhost compose defaults. Keep everything read/write through existing entities + repositories — NO schema change, NO new dependency.
  </action>
  <verify>
    <automated>./gradlew compileJava -PdockerApiVersion=1.44 && grep -q 'Profile("seed")' src/main/java/com/lostark/tracker/seed/SeedDataRunner.java && grep -q "existsByTrackedItem_IdAndCollectedAt" src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java && test "$(ls src/main/resources/db/migration | wc -l)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `SyntheticDemoData` is a `@Component` with a callable `seed()` and NO `@Profile`; `SeedDataRunner` is `@Profile("seed")` + `ApplicationRunner` and calls `seed()`; `grep -n 'Profile("seed")' src/main/java/com/lostark/tracker/seed/SeedDataRunner.java` matches
    - `SyntheticDemoData.seed()` inserts snapshots only when `existsByTrackedItem_IdAndCollectedAt` is false (idempotent) and spans ≥7 days at a 10-min cadence; `grep -n "existsByTrackedItem_IdAndCollectedAt" src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java` matches
    - Demo events' `occurredAt` lands on a seeded snapshot instant so both anchors are within the 30-min staleness window (status "ok") — the code places events on the 10-min grid
    - `WatchlistSeeder` declares `@Order(1)` and still declares `@Profile({"dev", "seed"})`; the curated WATCHLIST is unchanged
    - `application-seed.yml` sets `collection.initial-delay-ms`; no Flyway migration is added (`ls src/main/resources/db/migration` still shows only V1–V3); `./gradlew compileJava -PdockerApiVersion=1.44` exits 0
  </acceptance_criteria>
  <done>A `seed` profile populates idempotent synthetic history (7d+ of 10-min snapshots + grid-aligned events) through existing repositories, so timeline and event-impact demo non-empty with real change_rate values and without an API key; generation is an injectable component, the runner is seed-only, and watchlist items seed first via @Order.</done>
</task>

<task type="auto">
  <name>Task 3: SyntheticDemoDataIT — prove non-empty timeline + event-impact "ok" + idempotency on Testcontainers</name>
  <read_first>
    - .planning/ROADMAP.md (success criteria 2 and 4: non-empty timeline + event-impact reproduced from the seed)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (the Testcontainers base to extend)
    - src/test/java/com/lostark/tracker/read/TimelinePricesIT.java + EventImpactIT.java (RANDOM_PORT + TestRestTemplate + @BeforeEach FK-order cleanup + the response-body assertion style to mirror)
    - src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java (the seed() under test — created in Task 2)
    - src/main/java/com/lostark/tracker/domain/TrackedItem.java (constructor for the IT's item fixtures, since WatchlistSeeder is inactive under the test profile)
    - src/main/java/com/lostark/tracker/read/EventImpactService.java (the status "ok" contract the IT asserts on) + src/main/java/com/lostark/tracker/web/dto/EventImpactResponse.java + EventImpactItem.java + TimelineResponse.java (response shapes)
  </read_first>
  <files>src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java</files>
  <behavior>
    - After saving ≥1 active TrackedItem and calling `syntheticDemoData.seed()`, `GET /api/items/{id}/prices?from=&to=` over the seeded span returns a non-empty `snapshots` array.
    - `GET /api/items/{id}/event-impact?window=24` returns a non-empty `events` array including ≥1 item with `status:"ok"` and a non-null `changeRate`.
    - Calling `seed()` a second time inserts no additional snapshot rows (idempotent), asserted by an unchanged `priceSnapshotRepository.count()`.
  </behavior>
  <action>
    Create `SyntheticDemoDataIT extends PostgresRedisContainers`, `@SpringBootTest(webEnvironment = RANDOM_PORT)`, `@ActiveProfiles("test")`, autowiring `TestRestTemplate`, `SyntheticDemoData`, and the three repositories. `@BeforeEach` cleans FK order (price_snapshot → tracked_item → game_event), mirroring the read ITs. Tests: (1) **seed-then-read** — save one active `TrackedItem` via the repo, call `syntheticDemoData.seed()`, then `GET /api/items/{id}/prices?from={spanStart}&to={spanEnd}` (derive the window from the same UTC `now` the seeder uses, e.g. now−8d .. now, so it covers the synthetic rows) asserting the `snapshots` array is non-empty, AND `GET /api/items/{id}/event-impact?window=24` asserting the `events` array is non-empty with ≥1 element whose `status` is `"ok"` and `changeRate` non-null (the literal success-criterion-2 demo); (2) **idempotent** — capture `priceSnapshotRepository.count()`, call `seed()` again, assert the count is unchanged. Assert via TestRestTemplate response bodies (bind `TimelineResponse` / `EventImpactResponse`, or assert raw `String` bodies like the existing read ITs). Use `-PdockerApiVersion=1.44` semantics already wired by the build.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.lostark.tracker.seed.SyntheticDemoDataIT" -PdockerApiVersion=1.44</automated>
  </verify>
  <acceptance_criteria>
    - `SyntheticDemoDataIT` extends `PostgresRedisContainers`, runs at RANDOM_PORT under the test profile, and asserts a non-empty `snapshots` series from `GET /api/items/{id}/prices`
    - It asserts `GET /api/items/{id}/event-impact?window=24` returns ≥1 event with `status` "ok" and a non-null `changeRate` (proves the seed yields a real demo, not all insufficient_data)
    - It asserts `seed()` is idempotent: a second call leaves `priceSnapshotRepository.count()` unchanged
    - `./gradlew test --tests "com.lostark.tracker.seed.SyntheticDemoDataIT" -PdockerApiVersion=1.44` exits 0
  </acceptance_criteria>
  <done>An integration test proves the seed produces the headline demo — a non-empty timeline and an event-impact "ok" with a real change_rate on Testcontainers — and that re-seeding is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo/build | The workflow checks out and builds repo code and downloads dependencies on GitHub's runner |
| `seed` profile → DB | The seeder writes synthetic rows to Postgres; it must never call the live external API or leak a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0601-01 | Information Disclosure | CI logs / secrets | mitigate | The workflow declares `permissions: contents: read`, uses NO repository secrets, and runs only build+test; the suite needs no real `LOSTARK_API_KEY`/`ADMIN_API_SECRET` (Testcontainers supply DB/Redis; the spike is @Disabled), so nothing sensitive is exposed in CI logs. |
| T-0601-02 | Tampering (supply chain — CI actions / Gradle deps) | pinned actions | mitigate | Uses only first-party GitHub actions (`actions/checkout`, `actions/setup-java`) at major-version tags and Maven Central deps already in build.gradle; no third-party action, no new dependency, no `curl \| sh`. |
| T-0601-03 | Tampering (seed corrupts real data) | seeder | mitigate | The seeder is `@Profile("seed")`-only and idempotent (per-tick `existsBy...` + event `count()==0` guards); it never runs under dev/test/prod-default and writes through the same entities/unique key as the collector, so it cannot duplicate or overwrite real snapshots/admin events. |
| T-0601-04 | DoS (runaway seed volume) | snapshot generation | accept | 10-min cadence × ~8 days × the ~12-item watchlist ≈ low tens of thousands of rows — trivial for Postgres and bounded by a fixed span; acceptable for a demo seed (retention/rollup is v2). |
| T-0601-05 | Information Disclosure (collector hits live API without a key) | collector under seed | mitigate | `application-seed.yml` pushes the `@Scheduled` collector's initial-delay out so it never fires during a demo; the synthetic data is generated locally, so there is no outbound API call and no API key is required. |
| T-0601-SC | Tampering | dependency/action supply chain | accept | No new Gradle dependency; GitHub actions are first-party pinned to major tags. |
</threat_model>

<verification>
- `.github/workflows/ci.yml` runs `./gradlew build` on every push + PR on ubuntu-latest with Temurin 21; the exact command passes locally (`./gradlew build -PdockerApiVersion=1.44`), so a green local build = the green CI badge once the branch is pushed. The first push is the only manual step (see `user_setup`); the badge URL resolves because the file is `ci.yml`.
- `SyntheticDemoDataIT` (Testcontainers Postgres+Redis) proves the `seed` data yields a non-empty timeline AND an event-impact `"ok"` with a real `change_rate`, and that re-seeding is idempotent (snapshot count unchanged).
- `grep` confirms `SeedDataRunner` is `@Profile("seed")`, `SyntheticDemoData` has no `@Profile`, and `WatchlistSeeder` gained only `@Order` (curated list + profile unchanged); `ls src/main/resources/db/migration` still shows only V1–V3 (no schema change); the full `./gradlew build` stays green (no Phase 1–5 regression).
</verification>

<success_criteria>
- GitHub Actions CI runs the full Testcontainers `./gradlew build` on every push/PR (DIST-02, ROADMAP success criterion 1); the workflow is `ci.yml` so the README badge (06-02) goes green on first push.
- The `seed` profile populates 7+ days of 10-min synthetic snapshots + 2+ grid-aligned events so timeline and event-impact respond non-empty with a real `change_rate` (DIST-03, ROADMAP success criterion 2), requiring no `LOSTARK_API_KEY`.
- Seeding is idempotent and `seed`-only; no schema/migration change and no new dependency; the synthetic-seed IT and the full suite stay green.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-docs/06-01-SUMMARY.md` when done
</output>
</content>
</invoke>
