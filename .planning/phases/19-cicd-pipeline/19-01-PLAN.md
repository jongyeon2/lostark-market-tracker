---
phase: 19-cicd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [CICD-01, CICD-02]

must_haves:
  truths:
    - "D-19-01: VM은 **Oracle Ampere ARM64**다. 따라서 GHCR에 푸시하는 `app`·`web` 이미지는 **반드시 `linux/arm64`**로 빌드해야 한다(amd64만 빌드하면 VM에서 `exec format error`로 못 뜬다). 네이티브 `ubuntu-24.04-arm` 러너(public repo 무료)로 빌드하거나, 불가 시 QEMU `platforms: linux/arm64`로 폴백한다."
    - "D-19-02: `docker-compose.prod.yml`의 app·caddy에 **`image: ghcr.io/jongyeon2/lostark-{app,web}:${IMAGE_TAG:-latest}`를 추가**한다. `build:`는 로컬 폴백용으로 남기되, **VM은 `docker compose pull`로 완성 이미지만 받아 실행**한다(4GB ARM에서 빌드 안 함). GHCR 이미지명은 소문자."
    - "D-19-03: CI는 **백엔드(`./gradlew build`)와 프론트(`npm ci && npm run build` = `tsc -b && vite build`) 둘 다**를 게이트로 돌린다. 현재 ci.yml은 백엔드만 → 프론트 게이트가 없어 타입/빌드 회귀가 배포로 샐 수 있다(CICD-01)."
    - "D-19-04: 이미지 빌드·푸시는 **`main` push + 두 테스트 그린일 때만**(`if: github.ref=='refs/heads/main' && github.event_name=='push'`, `needs:[backend,frontend]`). GHCR 인증은 `docker/login-action`(actor + `GITHUB_TOKEN`), job에 `permissions: packages: write`. **앱 시크릿(.env.prod)은 CI에 절대 주입하지 않는다** — 이미지엔 시크릿 미포함(런타임 env-only, 18-01 원칙 유지)."
    - "이 플랜은 **수집/캐시/event-impact/서빙 로직 0줄** — compose 이미지 참조 + CI 워크플로우만. 배포 실행(Tailscale SSH pull+재기동)은 19-02, 문서/롤백은 19-03."
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "app·caddy에 GHCR `image:` 참조(태그 `${IMAGE_TAG:-latest}`) — 19-02 배포 job이 `IMAGE_TAG=sha-<커밋>`로 pin해 pull+up"
    - path: ".github/workflows/ci.yml"
      provides: "백엔드+프론트 게이트 + main push 시 app·web 이미지를 arm64로 빌드해 GHCR에 `sha-<커밋>`+`latest` 푸시하는 파이프라인"
  key_links:
    - from: ".github/workflows/ci.yml (images job) → GHCR"
      to: "docker-compose.prod.yml image: ghcr.io/jongyeon2/lostark-{app,web}"
      via: "빌드한 이미지를 VM이 pull하는 계약(이미지명·태그 일치)"
      pattern: "ghcr.io/jongyeon2/lostark-"
    - from: ".github/workflows/ci.yml (app 이미지 build) → Dockerfile"
      to: "18-01 Dockerfile (context .)"
      via: "백엔드 런타임 이미지 빌드"
      pattern: "file: Dockerfile"
    - from: ".github/workflows/ci.yml (web 이미지 build) → frontend/Dockerfile"
      to: "18-02 frontend/Dockerfile (context frontend)"
      via: "프론트+Caddy 이미지 빌드"
      pattern: "context: frontend"
---

<objective>
**CI를 CD-준비 파이프라인으로 확장**한다: (1) `docker-compose.prod.yml`의 app·caddy에 GHCR `image:` 참조를 추가해 **VM이 빌드 없이 pull**할 수 있게 하고, (2) `.github/workflows/ci.yml`에 **프론트 빌드 게이트**를 추가하며, (3) `main` push + 테스트 그린 시 **app·web 이미지를 `linux/arm64`로 빌드해 GHCR에 `sha`+`latest` 태그로 푸시**하는 job을 추가한다.

Purpose: CICD-01(백엔드+프론트 게이트)과 CICD-02(이미지→GHCR)를 세운다. 배포 실행(19-02)이 소비할 **불변 이미지**를 만들어, 4GB ARM VM에서 빌드하지 않고 pull만으로 배포·롤백할 토대를 놓는다.

Output: `docker-compose.prod.yml`에 `image:` 2줄 추가(build 유지) + `.github/workflows/ci.yml`에 frontend job·images job 추가. 앱 로직·시크릿 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- docker-compose.prod.yml — app·caddy 서비스에 image: 추가(build 유지):
  app:
    build: { context: ., dockerfile: Dockerfile }
    image: ghcr.io/jongyeon2/lostark-app:${IMAGE_TAG:-latest}   # ← 추가
    ...
  caddy:
    build: { context: frontend, dockerfile: Dockerfile }
    image: ghcr.io/jongyeon2/lostark-web:${IMAGE_TAG:-latest}   # ← 추가
    ...
  ★ VM 운영: `IMAGE_TAG=sha-xxxx docker compose -f docker-compose.prod.yml --env-file .env.prod pull && ... up -d`
    (build 키는 로컬 폴백; VM은 pull만). IMAGE_TAG 미설정 시 latest. -->

<!-- .github/workflows/ci.yml — 최종 3 job 구조:
  on: { push:, pull_request: }
  jobs:
    backend:            # (기존 build job — 이름만 backend로, 내용 동일)
      runs-on: ubuntu-latest
      steps: checkout / setup-java 21(temurin,cache gradle) / chmod +x gradlew / ./gradlew build --no-daemon
    frontend:           # (신규 게이트)
      runs-on: ubuntu-latest
      defaults: { run: { working-directory: frontend } }
      steps:
        - uses: actions/checkout@v5
        - uses: actions/setup-node@v4  with: { node-version: 20, cache: npm, cache-dependency-path: frontend/package-lock.json }
        - run: npm ci
        - run: npm run build          # tsc -b && vite build → 타입체크+빌드 게이트
    images:             # (신규 — main push + 게이트 통과 시에만)
      if: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
      needs: [backend, frontend]
      runs-on: ubuntu-24.04-arm       # 네이티브 arm64(public repo 무료). 불가 시 ubuntu-latest + setup-qemu + platforms: linux/arm64
      permissions: { contents: read, packages: write }
      steps:
        - uses: actions/checkout@v5
        - id: vars   run: echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
        - uses: docker/setup-buildx-action@v3
        - uses: docker/login-action@v3  with: { registry: ghcr.io, username: ${{ github.actor }}, password: ${{ secrets.GITHUB_TOKEN }} }
        - uses: docker/build-push-action@v6   # app
          with:
            context: .
            file: Dockerfile
            platforms: linux/arm64
            push: true
            tags: |
              ghcr.io/jongyeon2/lostark-app:sha-${{ steps.vars.outputs.sha_short }}
              ghcr.io/jongyeon2/lostark-app:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max
        - uses: docker/build-push-action@v6   # web
          with:
            context: frontend
            file: frontend/Dockerfile
            platforms: linux/arm64
            push: true
            tags: |
              ghcr.io/jongyeon2/lostark-web:sha-${{ steps.vars.outputs.sha_short }}
              ghcr.io/jongyeon2/lostark-web:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max
  ★ 기존 top-level `permissions: contents: read`는 backend/frontend엔 유지, images job이 job-level로 packages: write를 오버라이드. -->

<!-- 1회성(이 플랜 밖, 사용자/19-02): 첫 푸시 후 GHCR 패키지 2개를 public으로 전환(VM에서 docker login 불필요). arm64 미스매치가 배포의 대표 실패 원인이므로 19-02 스모크 전 `docker manifest inspect`로 arch 확인 권장. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.prod.yml에 GHCR image 참조 추가 (build→pull 전환 토대)</name>
  <read_first>
    - docker-compose.prod.yml (현재 app·caddy가 build:만 보유 — image: 추가 지점)
    - Dockerfile / frontend/Dockerfile (빌드 대상 — 이미지로 대체될 산출물)
  </read_first>
  <files>docker-compose.prod.yml</files>
  <action>
    `docker-compose.prod.yml`의 **app 서비스**에 `image: ghcr.io/jongyeon2/lostark-app:${IMAGE_TAG:-latest}`를, **caddy 서비스**에 `image: ghcr.io/jongyeon2/lostark-web:${IMAGE_TAG:-latest}`를 각각 `build:` 블록 옆에 추가한다(build는 로컬 폴백으로 유지). 다른 서비스(postgres/redis)·네트워크·볼륨·포트 정책은 무변경. 주석 1줄로 "VM은 `IMAGE_TAG=sha-xxx ... pull && up -d`로 완성 이미지만 받아 실행(빌드 안 함)"을 남긴다.
  </action>
  <verify>
    <automated>cd "C:/dev/project/test" && docker compose -f docker-compose.prod.yml --env-file .env.prod.example config >/dev/null 2>&1 && echo COMPOSE_OK; grep -q 'ghcr.io/jongyeon2/lostark-app:${IMAGE_TAG' docker-compose.prod.yml && grep -q 'ghcr.io/jongyeon2/lostark-web:${IMAGE_TAG' docker-compose.prod.yml && echo IMAGE_REFS_OK; grep -Eq "^\s*build:" docker-compose.prod.yml && echo BUILD_RETAINED</automated>
  </verify>
  <acceptance_criteria>
    - app 서비스에 `image: ghcr.io/jongyeon2/lostark-app:${IMAGE_TAG:-latest}` 존재
    - caddy 서비스에 `image: ghcr.io/jongyeon2/lostark-web:${IMAGE_TAG:-latest}` 존재
    - `build:` 블록 2개 유지(로컬 폴백) — app·caddy 모두
    - `docker compose -f docker-compose.prod.yml --env-file .env.prod.example config` 유효(구문 통과). Docker 미가용 시 수동 검증 표기
    - postgres/redis 포트 미공개·caddy만 80/443 정책 무변경(회귀 없음)
  </acceptance_criteria>
  <done>VM이 `docker compose pull`로 GHCR 완성 이미지를 받아 실행할 수 있는 참조가 생겼고, 로컬 build 폴백은 유지된다(4GB ARM에서 빌드 불필요 토대).</done>
</task>

<task type="auto">
  <name>Task 2: ci.yml에 프론트 게이트 + GHCR arm64 이미지 빌드·푸시 job 추가</name>
  <read_first>
    - .github/workflows/ci.yml (현재 단일 backend build job — job 이름·트리거·permissions 원형)
    - frontend/package.json (scripts.build = `tsc -b && vite build`, npm lock 존재 → `npm ci`)
    - Dockerfile / frontend/Dockerfile (images job 빌드 context·file)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    `.github/workflows/ci.yml`을 3 job으로 확장한다(인터페이스 명세대로): (1) **backend** — 기존 build job 유지(이름을 `backend`로), `./gradlew build --no-daemon`. (2) **frontend**(신규) — `ubuntu-latest`, `working-directory: frontend`, `setup-node@v4`(node 20·npm cache·`cache-dependency-path: frontend/package-lock.json`), `npm ci` → `npm run build`(타입체크+빌드 게이트). (3) **images**(신규) — `if: github.ref=='refs/heads/main' && github.event_name=='push'`, `needs: [backend, frontend]`, `runs-on: ubuntu-24.04-arm`(네이티브 arm64; 미가용 시 `ubuntu-latest` + `docker/setup-qemu-action@v3` + `platforms: linux/arm64`), job-level `permissions: {contents: read, packages: write}`. 스텝: checkout → short sha 산출 → `setup-buildx-action@v3` → `login-action@v3`(ghcr.io, `github.actor`, `secrets.GITHUB_TOKEN`) → `build-push-action@v6` **app**(context `.`, file `Dockerfile`, platforms `linux/arm64`, push, tags `ghcr.io/jongyeon2/lostark-app:sha-<short>`+`:latest`, gha 캐시) → **web**(context `frontend`, file `frontend/Dockerfile`, 동일 옵션, `lostark-web`). top-level `permissions: contents: read`는 유지(backend/frontend 최소 권한).
  </action>
  <verify>
    <automated>cd "C:/dev/project/test" && python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml',encoding='utf-8')); j=d['jobs']; assert set(['backend','frontend','images']).issubset(j), 'jobs missing: '+str(list(j)); im=j['images']; assert im.get('needs')==['backend','frontend'], im.get('needs'); assert im['permissions']['packages']=='write'; assert 'arm' in str(im['runs-on']); print('YAML_OK jobs=',list(j))"; grep -q "lostark-app:sha-" .github/workflows/ci.yml && grep -q "lostark-web:sha-" .github/workflows/ci.yml && grep -q "linux/arm64" .github/workflows/ci.yml && echo TAGS_ARCH_OK; grep -q "npm run build" .github/workflows/ci.yml && echo FRONT_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml`에 `backend`·`frontend`·`images` 3 job 존재(YAML 파싱 통과)
    - `frontend` job이 `npm ci` + `npm run build`로 게이트(타입체크 포함)
    - `images` job: `if`가 main push 한정, `needs: [backend, frontend]`, `permissions.packages: write`, `runs-on`에 arm64
    - app·web 두 이미지 모두 `linux/arm64`로 빌드되고 `sha-<short>`+`latest` 태그로 GHCR 푸시
    - GHCR 인증은 `GITHUB_TOKEN`만 사용(앱 시크릿/PAT 미사용) — `.env.prod`/앱 시크릿 참조 0
    - `actionlint` 가용 시 경고 0(미가용 시 python yaml 파싱으로 대체)
  </acceptance_criteria>
  <done>main push가 백엔드+프론트 게이트를 통과하면 arm64 app·web 이미지가 GHCR에 SHA·latest로 올라가, 19-02 배포 job이 pull할 불변 아티팩트가 준비된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI 러너 ↔ 앱 시크릿 | 러너는 소스만 빌드. `.env.prod`/실 API 키/관리자 시크릿 **미주입** — 이미지에 시크릿 미포함(런타임 env-only) |
| CI ↔ GHCR | 푸시는 `GITHUB_TOKEN`(packages: write)로만. 외부 PAT·장수 크리덴셜 불필요 |
| 이미지 arch ↔ VM | arm64 이미지만 VM(Ampere)에서 실행 가능 — amd64 유입 시 기동 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Info Disclosure | 이미지 레이어/CI 로그에 시크릿 유입 | mitigate | CI에 앱 시크릿 미주입, 빌드는 시크릿 불요(18-01 런타임 env 원칙), `.env.prod` 참조 0 |
| T-19-01-02 | Tampering | 미검증 코드가 이미지로 배포 | mitigate | images job이 `needs:[backend,frontend]` — 테스트 그린 게이트 통과 시에만 빌드·푸시 |
| T-19-01-03 | DoS(배포 실패) | arch 미스매치(amd64)로 VM 기동 불가 | mitigate | `platforms: linux/arm64` 강제, 19-02 스모크 전 manifest arch 확인 |
| T-19-01-04 | Elevation | 과도한 워크플로우 권한 | mitigate | top-level `contents: read`, images job만 `packages: write`로 최소 상향 |
</threat_model>

<verification>
- `docker-compose.prod.yml`: app·caddy에 `image: ghcr.io/jongyeon2/lostark-{app,web}:${IMAGE_TAG:-latest}` 추가, `build:` 유지, `config` 유효, 포트 정책 무변경.
- `.github/workflows/ci.yml`: backend·frontend·images 3 job. frontend가 `npm run build` 게이트. images는 main push 한정·`needs` 게이트·arm64·`sha`+`latest` 푸시·`GITHUB_TOKEN` 인증.
- 앱 로직(수집/캐시/event-impact/서빙) diff 0줄. CI에 앱 시크릿 참조 0.
- 라이브 확인(이 플랜 밖·19-02 전): main push 후 GHCR에 두 패키지가 arm64로 생성되는지 Actions 로그·`docker manifest inspect`로 확인.
</verification>

<success_criteria>
- CICD-01: 백엔드+프론트 테스트가 배포 전 게이트로 동작한다(둘 다 그린이어야 images job 진입).
- CICD-02: `main` push 시 app·web 이미지가 arm64로 빌드돼 GHCR에 `sha-<커밋>`+`latest`로 푸시된다 — 19-02 배포가 pull할 불변 아티팩트 확보.
- (가드) 수집/캐시/event-impact/서빙 로직 0줄, CI에 앱 시크릿 미노출.
</success_criteria>

<output>
Create `.planning/phases/19-cicd-pipeline/19-01-SUMMARY.md` when done
</output>
