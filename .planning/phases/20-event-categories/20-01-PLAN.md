---
phase: 20-event-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/lostark/tracker/domain/EventType.java
  - frontend/src/lib/schemas.ts
  - frontend/src/features/timeline/eventMarkers.ts
  - frontend/src/features/admin/EventSection.tsx
  - src/test/java/com/lostark/tracker/admin/AdminEventControllerIT.java
autonomous: true
requirements: [EVT-01]

must_haves:
  truths:
    - "이벤트 카테고리 3종 additive: NEW_CLASS(신규 캐릭터 출시)·NEW_RAID(신규 레이드 출시)·GENERAL_PATCH(일반 패치). DB 마이그레이션 불필요 — event_type VARCHAR(40)에 CHECK 제약 없고 @Enumerated(STRING)+@NotNull EventType 바인딩이 유효화(알 수 없는 값=400). enum 최장명 GENERAL_PATCH(13자)<40."
    - "타입 안전 락스텝: EVENT_MARKERS·EVENT_TYPE_LABELS가 Record<EventType,...>라 zod enum에 3종 추가 시 tsc가 누락을 컴파일 에러로 강제(프론트 게이트=npm run build). 색은 dataviz 검증기 통과 팔레트: NEW_CLASS=#3B82F6(blue)·NEW_RAID=#DC2626(red)·GENERAL_PATCH=#16A34A(green) — 기존 4색과 7색 CVD PASS(worst adjacent ΔE 15.1, light 모드; 앱은 다크모드 미구현). 마커는 dash+범례 라벨 2차 인코딩."
    - "Core Value 0줄 — 수집/캐시/event-impact 로직 무변경. event-impact는 eventType 무관(occurredAt 상관)이라 회귀 없음. 동기: 차원술사(2026-07-08) 출시로 '타격의 대가' 각인서 가격 급등→정상화를 NEW_CLASS로 상관 기록."
  artifacts:
    - path: "src/main/java/com/lostark/tracker/domain/EventType.java"
      provides: "enum +3 (NEW_CLASS·NEW_RAID·GENERAL_PATCH)"
    - path: "frontend/src/lib/schemas.ts"
      provides: "eventTypeSchema z.enum +3 → 파생 타입 확장(전 소비처 락스텝)"
    - path: "frontend/src/features/timeline/eventMarkers.ts"
      provides: "EVENT_MARKERS +3 (dataviz 검증 색 + 한글 라벨)"
    - path: "frontend/src/features/admin/EventSection.tsx"
      provides: "EVENT_TYPE_LABELS·EVENT_TYPE_ORDER +3 (관리자 폼 셀렉트·목록 표시)"
  key_links:
    - from: "EventType.java (백엔드 enum)"
      to: "schemas.ts eventTypeSchema (프론트 zod)"
      via: "동일 문자열 값 3종 추가 — 백엔드 저장/바인딩과 프론트 파스가 일치"
      pattern: "NEW_CLASS"
---

<objective>
관리자 이벤트 등록의 유형을 3종 확장한다 — 신규 캐릭터 출시(NEW_CLASS)·신규 레이드 출시(NEW_RAID)·일반 패치(GENERAL_PATCH). 순수 additive(백엔드 enum + 프론트 zod·마커색·폼 라벨), DB 마이그레이션 없음, Core Value 0줄. 차원술사 출시발 각인서 시세 변동을 상관 기록할 수 있게 한다.

Output: EventType +3, eventTypeSchema +3, EVENT_MARKERS +3(dataviz 검증색), EVENT_TYPE_LABELS/ORDER +3, AdminEventControllerIT에 NEW_CLASS 왕복 1케이스.
</objective>

<task name="백엔드 enum + IT 케이스">
<what-to-build>
`EventType.java`에 NEW_CLASS·NEW_RAID·GENERAL_PATCH 추가(BALANCE_PATCH 뒤). `AdminEventControllerIT`에 NEW_CLASS 이벤트 create 201 + 영속 eventType==NEW_CLASS 검증 1케이스 추가(기존 create 패턴 재사용).
</what-to-build>
<how-to-verify>
`./gradlew build` 그린(신규 케이스 포함). 알 수 없는 값은 여전히 400(바인딩 유효화 불변).
</how-to-verify>
</task>

<task name="프론트 zod·마커색·폼 라벨">
<what-to-build>
`schemas.ts` eventTypeSchema z.enum에 3종 추가. `eventMarkers.ts` EVENT_MARKERS에 3종 색(#3B82F6/#DC2626/#16A34A)·한글 라벨. `EventSection.tsx` EVENT_TYPE_LABELS·EVENT_TYPE_ORDER에 3종. 라벨은 신규 캐릭터 출시/신규 레이드 출시/일반 패치.
</what-to-build>
<how-to-verify>
`npm run build`(tsc -b && vite build) 그린 — Record<EventType> 누락 없음(타입 락스텝). 관리자 폼 셀렉트에 7종 노출, 타임라인 범례·마커에 신규 3색 표시.
</how-to-verify>
</task>
