---
phase: 13-backend-enrichment-seed
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/main/java/com/lostark/tracker/web/dto/TrackedItemResponse.java
  - src/main/java/com/lostark/tracker/web/dto/LatestPriceResponse.java
  - src/main/java/com/lostark/tracker/read/LatestPriceService.java
  - src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java
  - src/main/java/com/lostark/tracker/web/PricesController.java
  - src/main/java/com/lostark/tracker/web/dto/EnrichedEventImpactResponse.java
  - src/main/java/com/lostark/tracker/web/EventImpactController.java
  - src/test/java/com/lostark/tracker/web/ItemEnrichmentReadIT.java
autonomous: true
requirements: [ITEM-03, ITEM-04, SEED-03]

must_haves:
  truths:
    - "4개 read 응답 — item list(TrackedItemResponse), latest(LatestPriceResponse), timeline(TimelineResponse), event-impact(신규 EnrichedEventImpactResponse) — 가 iconUrl·itemGroup·roleGroup을 노출한다 (ITEM-03)"
    - "latest는 LatestPriceService가 cache MISS 시 TrackedItem enrichment를 채워 캐시에 베이크하므로 cache HIT 경로는 추가 DB 조회 0(zero-DB 쇼케이스 API-02 보존)이고 LatestPriceCache·CacheConfig 소스는 0줄 변경 (ITEM-03, ITEM-04)"
    - "event-impact는 컨트롤러가 EventImpactService의 EventImpactResponse를 EnrichedEventImpactResponse로 감싸 enrichment를 부착하므로 EventImpactService·EventImpactResponse 소스가 0줄 변경된다 (ITEM-04)"
    - "수집 스케줄러(PriceCollector)·RedisTokenBucket·LatestPriceCache·CacheConfig·EventImpactService 소스 diff가 0줄이고 기존 수집/캐시/event-impact 회귀 테스트가 그린이다 (ITEM-04)"
    - "seed 프로파일 기동 시 키 없이 GET /api/items 응답에 iconUrl(CDN base 접두)·itemGroup·roleGroup이 스파이크 상수와 일치해 채워진다 (SEED-03)"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/web/dto/EnrichedEventImpactResponse.java"
      provides: "event-impact 컨트롤러 wrapper — itemId/window/iconUrl/itemGroup/roleGroup + List<EventImpactItem>"
      contains: "roleGroup"
    - path: "src/main/java/com/lostark/tracker/read/LatestPriceService.java"
      provides: "MISS 시 TrackedItem enrichment를 채워 LatestPriceResponse를 캐시에 베이크(HIT zero-DB 보존)"
      contains: "iconUrl"
    - path: "src/test/java/com/lostark/tracker/web/ItemEnrichmentReadIT.java"
      provides: "4개 read 응답의 enrichment 노출 + cache HIT 보존 + event-impact/cache 0줄 회귀 단언"
      contains: "iconUrl"
  key_links:
    - from: "LatestPriceService (MISS)"
      to: "LatestPriceResponse.iconUrl/itemGroup/roleGroup"
      via: "trackedItemRepository.findById → enrichment 매핑 → cache.put(enriched)"
      pattern: "findById"
    - from: "EventImpactController"
      to: "EnrichedEventImpactResponse"
      via: "eventImpactService.eventImpact(...) 결과를 enrichment로 wrap (서비스 무변경)"
      pattern: "EnrichedEventImpactResponse"
---

<objective>
13-01이 채운 `tracked_item` enrichment를 **read-path additive 한 겹**으로 4개 read 응답에 노출한다 — 수집·캐시·event-impact 계산은 0줄 변경. 부착 규칙은 "잠긴(캐시/서비스가 빌드하는) DTO는 그대로 두고 감싸고, 자유로운 DTO는 제자리 확장":
- **item list** (`TrackedItemResponse`): 3필드 추가 + `from(TrackedItem)` 매핑 (ItemController 무변경).
- **latest** (`LatestPriceResponse`): 3필드 추가 + `LatestPriceService`가 MISS 시 enrichment를 채워 캐시에 베이크 → **HIT 시 zero-DB(API-02) 보존**, LatestPriceCache/CacheConfig 0줄.
- **timeline** (`TimelineResponse`): 3필드 추가 + `PricesController`가 `findById`로 enrich (range는 캐시 안 됨).
- **event-impact**: 신규 `EnrichedEventImpactResponse`를 컨트롤러가 조립 → `EventImpactService`·`EventImpactResponse` 0줄.

Purpose: 프론트(Phase 14)가 소비할 iconUrl·itemGroup·roleGroup 계약을 4개 응답에 확정하고, ITEM-04 불변(수집/캐시/event-impact 0줄)을 회귀로 증명한다. 키 불필요 — seed만으로 응답에 아이콘이 채워진다(SEED-03).

Output: enrichment를 노출하는 4개 read 응답 + 신규 wrapper DTO + read-path Testcontainers IT(노출·HIT 보존·0줄 회귀). 잠긴 5개 파일(PriceCollector/RedisTokenBucket/LatestPriceCache/CacheConfig/EventImpactService) diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-backend-enrichment-seed/13-01-PLAN.md
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- TrackedItemResponse(record): id/externalItemId/displayName/category/active + static from(TrackedItem). ItemController.list()가 from()으로 매핑 — from()에 enrichment 추가하면 컨트롤러 무변경. -->
<!-- LatestPriceResponse(record): itemId/minPrice/collectedAt. CacheConfig가 Jackson2JsonRedisSerializer<>(objectMapper, LatestPriceResponse.class) + ParameterNamesModule로 직렬화 → record 컴포넌트 추가는 자동 직렬화, 누락 필드는 null 역직렬화. 따라서 필드 추가해도 CacheConfig 0줄. -->
<!-- LatestPriceService.latest(id): cache.get HIT시 즉시 반환(zero-DB, API-02). MISS시 existsById(404)→findTop 스냅샷(404)→ new LatestPriceResponse(itemId, minPrice, collectedAt) → cache.put. trackedItemRepository 이미 주입됨. existsById→findById로 바꿔 enrichment 확보(읽기 1회 동일 비용). -->
<!-- LatestPriceCache.get/put/evict: RedisTemplate<String, LatestPriceResponse> 제네릭 — record 형태 무관하게 직렬화. 0줄 유지 대상. -->
<!-- TimelineResponse(record): downsampled/bucketWidth/snapshots/events. PricesController.prices()가 직접 new TimelineResponse(...) 빌드 + existsById(404) 체크 보유 + trackedItemRepository 주입됨 → existsById→findById로 enrich, 컨트롤러에서 3필드 추가. range 비캐시. -->
<!-- EventImpactResponse(record): itemId/window/events. EventImpactService.eventImpact()가 new EventImpactResponse(...)로 빌드 — 이 서비스는 ITEM-04로 0줄 잠금. 따라서 EventImpactResponse에 필드 추가 금지(서비스 생성자 호출 깨짐). 대신 컨트롤러가 신규 EnrichedEventImpactResponse로 감쌈. EventImpactController는 existsById(404) 체크 + trackedItemRepository 주입됨. -->
<!-- ITEM-04 잠금 파일(0줄): collect/PriceCollector.java(@Scheduled 수집), ratelimit/RedisTokenBucket.java, cache/LatestPriceCache.java, cache/CacheConfig.java, read/EventImpactService.java. read/WindowQueryService·DownsampleService도 무접촉. -->
</interfaces>
</context>

## Phase Goal

**As a** 프론트 소비자(Phase 14)·면접관, **I want to** 4개 read 응답이 품목의 아이콘 URL·품목군·역할군을 일관되게 노출하되 수집·캐시·event-impact 핵심 경로는 한 줄도 안 바뀌었음을, **so that** enrichment가 Core Value(수집 신뢰성) 위에 얹은 안전한 read-path additive 한 겹임이 코드와 회귀 테스트로 증명되고 키 없이 아이콘이 응답에 뜬다.

<tasks>

<task type="auto">
  <name>Task 1: item list + latest 응답에 enrichment — TrackedItemResponse.from() 매핑 + LatestPriceService 캐시 베이크(HIT zero-DB 보존)</name>
  <read_first>
    - src/main/java/com/lostark/tracker/web/dto/TrackedItemResponse.java (record + from(TrackedItem) — 3필드 추가 + 매핑)
    - src/main/java/com/lostark/tracker/web/dto/LatestPriceResponse.java (record 컴포넌트 추가 대상; javadoc의 "최소 페이로드" 의도 유지)
    - src/main/java/com/lostark/tracker/read/LatestPriceService.java (cache-aside HIT/MISS 흐름 — MISS만 수정, HIT 무변경)
    - src/main/java/com/lostark/tracker/cache/CacheConfig.java (직렬화가 record 컴포넌트 자동 처리 — 참조만, 수정 금지)
    - src/main/java/com/lostark/tracker/web/ItemController.java (list()=from() 매핑, latest()=service 위임 — 변경 불필요 확인)
  </read_first>
  <files>src/main/java/com/lostark/tracker/web/dto/TrackedItemResponse.java, src/main/java/com/lostark/tracker/web/dto/LatestPriceResponse.java, src/main/java/com/lostark/tracker/read/LatestPriceService.java</files>
  <action>
    `TrackedItemResponse`에 record 컴포넌트 `String iconUrl, String itemGroup, String roleGroup`을 추가하고(기존 id/externalItemId/displayName/category/active 뒤), `from(TrackedItem item)`이 `item.getIconUrl()`/`item.getItemGroup()`/`item.getRoleGroup()`을 전달하도록 매핑한다. ItemController.list()는 from()을 쓰므로 무변경.
    `LatestPriceResponse`에 record 컴포넌트 `String iconUrl, String itemGroup, String roleGroup`을 추가한다(itemId/minPrice/collectedAt 뒤). javadoc에 "enrichment는 정적 메타데이터라 가격과 함께 캐시에 베이크 — read 시 추가 조회 없음"을 한 줄 보강.
    `LatestPriceService.latest(itemId)`의 **MISS 분기만** 수정한다: 현재 `existsById(itemId)`(404 게이트) + `findTop...snapshot`을 유지하되, `existsById`를 `findById(itemId).orElseThrow(ItemNotFoundException)`로 바꿔 `TrackedItem` 엔티티를 확보한다(읽기 횟수 동일). 그런 다음 `new LatestPriceResponse(itemId, newest.getMinPrice(), newest.getCollectedAt(), item.getIconUrl(), item.getItemGroup(), item.getRoleGroup())`로 enrichment를 채워 빌드하고 `cache.put(itemId, response)`로 베이크한다. **HIT 분기(cache.get 반환)는 절대 수정 금지** — 캐시된 값이 이미 enrichment를 포함하므로 zero-DB 보존(API-02). `LatestPriceCache`·`CacheConfig`는 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -q "iconUrl" src/main/java/com/lostark/tracker/web/dto/TrackedItemResponse.java && grep -q "iconUrl" src/main/java/com/lostark/tracker/web/dto/LatestPriceResponse.java && grep -q "findById" src/main/java/com/lostark/tracker/read/LatestPriceService.java && test -z "$(git diff --name-only -- src/main/java/com/lostark/tracker/cache/LatestPriceCache.java src/main/java/com/lostark/tracker/cache/CacheConfig.java)" && ./gradlew compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `TrackedItemResponse`가 iconUrl/itemGroup/roleGroup 컴포넌트를 갖고 `from()`이 엔티티 getter로 매핑함
    - `LatestPriceResponse`가 iconUrl/itemGroup/roleGroup 컴포넌트를 가짐
    - `LatestPriceService` MISS 분기가 `findById`로 엔티티를 얻어 enrichment를 채운 응답을 빌드·캐시하고, HIT 분기는 무변경(cache.get 반환 그대로)
    - `git diff` 상 `cache/LatestPriceCache.java`·`cache/CacheConfig.java` 변경 0줄
    - ItemController 변경 0줄(list=from 매핑, latest=service 위임)
    - `./gradlew compileJava` 그린
  </acceptance_criteria>
  <done>item list와 latest 응답이 enrichment를 노출하고, latest는 캐시에 enrichment를 베이크해 HIT zero-DB를 보존하며 캐시 인프라는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: timeline 제자리 확장 + event-impact wrapper — EventImpactService/Response 0줄 유지</name>
  <read_first>
    - src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java (record — 3필드 추가 대상)
    - src/main/java/com/lostark/tracker/web/PricesController.java (prices(): existsById 체크 + new TimelineResponse 빌드 — enrich 지점)
    - src/main/java/com/lostark/tracker/web/dto/EventImpactResponse.java (서비스가 빌드 — 수정 금지, wrapper의 events 출처)
    - src/main/java/com/lostark/tracker/read/EventImpactService.java (ITEM-04 잠금 — 참조만, 수정 절대 금지)
    - src/main/java/com/lostark/tracker/web/EventImpactController.java (existsById 체크 + service 위임 — enrich 지점)
  </read_first>
  <files>src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java, src/main/java/com/lostark/tracker/web/PricesController.java, src/main/java/com/lostark/tracker/web/dto/EnrichedEventImpactResponse.java, src/main/java/com/lostark/tracker/web/EventImpactController.java</files>
  <action>
    **timeline:** `TimelineResponse`에 record 컴포넌트 `String iconUrl, String itemGroup, String roleGroup`을 추가한다(downsampled/bucketWidth/snapshots/events와 함께; 최상위 품목 메타데이터). `PricesController.prices()`에서 기존 `existsById(id)`(404 게이트)를 `findById(id).orElseThrow(new ItemNotFoundException(id))`로 바꿔 엔티티를 확보하고(검증 순서 보존: 400 window 체크가 먼저), 마지막 `new TimelineResponse(downsampled.downsampled(), downsampled.bucketWidth(), downsampled.snapshots(), events, item.getIconUrl(), item.getItemGroup(), item.getRoleGroup())`로 enrichment를 채운다. WindowQueryService·DownsampleService는 무접촉.
    **event-impact:** 신규 record `EnrichedEventImpactResponse(long itemId, int window, String iconUrl, String itemGroup, String roleGroup, java.util.List<EventImpactItem> events)`를 만든다(EventImpactResponse는 그대로 둠). `EventImpactController.eventImpact()`에서 기존 `existsById(id)`를 `findById(id).orElseThrow(new ItemNotFoundException(id))`로 바꾸고(400 window 체크 순서 보존), `EventImpactResponse base = eventImpactService.eventImpact(id, window);`(서비스 호출 무변경)를 호출한 뒤 `return new EnrichedEventImpactResponse(base.itemId(), base.window(), item.getIconUrl(), item.getItemGroup(), item.getRoleGroup(), base.events());`로 감싼다. **EventImpactService·EventImpactResponse 소스는 0줄 변경.**
  </action>
  <verify>
    <automated>grep -q "iconUrl" src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java && test -f src/main/java/com/lostark/tracker/web/dto/EnrichedEventImpactResponse.java && grep -q "EnrichedEventImpactResponse" src/main/java/com/lostark/tracker/web/EventImpactController.java && grep -q "findById" src/main/java/com/lostark/tracker/web/PricesController.java && ./gradlew compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `TimelineResponse`가 iconUrl/itemGroup/roleGroup 컴포넌트를 갖고 `PricesController`가 findById로 채움(400-우선-404 검증 순서 보존)
    - `EnrichedEventImpactResponse`가 존재하고 itemId/window/iconUrl/itemGroup/roleGroup/events를 가짐
    - `EventImpactController`가 service의 EventImpactResponse를 EnrichedEventImpactResponse로 감싸 반환
    - `git diff` 상 `read/EventImpactService.java`·`web/dto/EventImpactResponse.java` 변경 0줄
    - `./gradlew compileJava` 그린
  </acceptance_criteria>
  <done>timeline은 제자리 확장으로, event-impact는 컨트롤러 wrapper로 enrichment를 노출하며 EventImpactService/EventImpactResponse는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 3: read-path enrichment IT(4개 응답 노출 + HIT 보존) + ITEM-04 불변 회귀 단언(잠금 5파일 0줄)</name>
  <read_first>
    - src/test/java/com/lostark/tracker/admin/AdminItemControllerIT.java (web IT 베이스·픽스처 insert·publicExternalIds 헬퍼 패턴 계승)
    - src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java (13-01에서 만든 enrichment 단언 패턴)
    - src/main/java/com/lostark/tracker/web/ItemController.java (GET /api/items·/{id}/latest 경로)
    - src/main/java/com/lostark/tracker/web/PricesController.java (GET /{id}/prices?from=&to=)
    - src/main/java/com/lostark/tracker/web/EventImpactController.java (GET /{id}/event-impact?window=)
  </read_first>
  <files>src/test/java/com/lostark/tracker/web/ItemEnrichmentReadIT.java</files>
  <action>
    `ItemEnrichmentReadIT`를 만든다(`extends PostgresRedisContainers`, `@SpringBootTest(webEnvironment=RANDOM_PORT)` 또는 기존 web IT 스타일). test 프로파일에선 시더가 비활성이므로, enrichment를 채운 `TrackedItem` 픽스처(예: external_item_id=6861009, iconUrl="https://cdn-lostark.game.onstove.com/efui_iconatlas/use/use_8_109.png", itemGroup="강화재료", roleGroup="MATERIAL")와 최소 1개 PriceSnapshot을 저장한 뒤 4개 응답을 검증:
    (1) `GET /api/items` → 해당 품목 항목의 iconUrl/itemGroup/roleGroup이 픽스처 값과 일치(ITEM-03 item list).
    (2) `GET /api/items/{id}/latest` → iconUrl/itemGroup/roleGroup 노출. 두 번째 호출(cache HIT)도 동일 enrichment 반환(HIT 보존). (선택) 캐시 HIT가 DB를 안 치는 성질은 기존 캐시 IT가 커버하므로 여기선 값 일관성만 단언.
    (3) `GET /api/items/{id}/prices?from=&to=`(유효 window) → 최상위 iconUrl/itemGroup/roleGroup 노출(ITEM-03 timeline).
    (4) `GET /api/items/{id}/event-impact?window=24` → 최상위 iconUrl/itemGroup/roleGroup 노출(EnrichedEventImpactResponse), events 배열 형태 유지(ITEM-03 event-impact).
    **ITEM-04 불변 회귀:** (a) 기존 수집/캐시/event-impact 회귀 테스트가 여전히 그린임을 전체 빌드로 확인(행위 불변). (b) 소스 0줄 회귀는 실행 단계의 `git diff --stat`로 강제 — 잠금 5파일(PriceCollector/RedisTokenBucket/LatestPriceCache/CacheConfig/EventImpactService)이 변경 목록에 없어야 함.
    SEED-03(키 없는 재현)은 아래 <verification>의 seed 프로파일 curl 절차로 수동 확인(픽스처 IT가 패스스루를, curl이 seed 베이크를 증명).
  </action>
  <verify>
    <automated>test -f src/test/java/com/lostark/tracker/web/ItemEnrichmentReadIT.java && grep -q "MATERIAL" src/test/java/com/lostark/tracker/web/ItemEnrichmentReadIT.java && ./gradlew test --tests "*ItemEnrichmentReadIT" -q && test -z "$(git diff --name-only -- src/main/java/com/lostark/tracker/collect/PriceCollector.java src/main/java/com/lostark/tracker/ratelimit/RedisTokenBucket.java src/main/java/com/lostark/tracker/cache/LatestPriceCache.java src/main/java/com/lostark/tracker/cache/CacheConfig.java src/main/java/com/lostark/tracker/read/EventImpactService.java)"</automated>
  </verify>
  <acceptance_criteria>
    - `ItemEnrichmentReadIT`가 Testcontainers에서 그린이며 4개 응답(item list/latest/timeline/event-impact)의 iconUrl·itemGroup·roleGroup 노출을 단언
    - latest 두 번째 호출(cache HIT)이 동일 enrichment를 반환함을 단언
    - `git diff --name-only`에 PriceCollector/RedisTokenBucket/LatestPriceCache/CacheConfig/EventImpactService 0건(잠금 5파일 무변경)
    - 전체 `./gradlew build` 그린(기존 수집/캐시/event-impact 회귀 테스트 포함)
  </acceptance_criteria>
  <done>read-path IT가 4개 응답의 enrichment 노출과 HIT 보존을 증명하고, 잠금 5파일 0줄 + 기존 회귀 그린으로 ITEM-04 불변이 단언된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked_item enrichment → read 응답 | DB의 정적 메타데이터를 read 응답에 노출 — 공개 아이콘 URL·분류만(민감정보 없음) |
| read-path additive → 잠긴 핵심 경로 | enrichment 부착이 수집/캐시/event-impact 계산을 변경하면 Core Value 침해 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | enrichment 부착이 수집/캐시/event-impact 로직을 오염 | mitigate | 부착은 DTO·컨트롤러·read 서비스 MISS 분기에 한정; PriceCollector/RedisTokenBucket/LatestPriceCache/CacheConfig/EventImpactService 0줄(Task 3 git diff 단언 + 기존 회귀 그린). |
| T-13-06 | Information Disclosure | 캐시 직렬화 변경으로 의도치 않은 데이터 노출 | mitigate | CacheConfig 0줄; LatestPriceResponse에 추가되는 값은 공개 아이콘 URL·분류 문자열뿐(가격·키 아님). 누락 필드는 null 역직렬화로 안전. |
| T-13-07 | Denial of Service | latest 응답 enrichment가 HIT 경로에 DB 조회 추가(쇼케이스 저하) | mitigate | enrichment를 MISS 시 캐시에 베이크 → HIT는 추가 조회 0(API-02 보존); Task 1 AC가 HIT 분기 무변경 강제. |
| T-13-08 | Injection | read 응답에 동적 SQL/외부 입력 | accept | 추가 입력 없음 — 기존 findById/range 쿼리 재사용, enrichment는 엔티티 getter 패스스루. |
| T-13-SC | Tampering | 의존성 공급망 | accept | 신규 패키지 0 — 기존 Spring Web/Data만. |
</threat_model>

<verification>
- `./gradlew build` 전체 그린(Testcontainers): ItemEnrichmentReadIT + 기존 수집/캐시/event-impact 회귀 테스트.
- `git diff --name-only`에 collect/PriceCollector.java · ratelimit/RedisTokenBucket.java · cache/LatestPriceCache.java · cache/CacheConfig.java · read/EventImpactService.java · read/WindowQueryService.java · read/DownsampleService.java 0건 (ITEM-04 불변).
- 4개 read 응답이 iconUrl·itemGroup·roleGroup을 노출(IT 단언).
- **SEED-03 수동 확인(키 없이):** `docker compose up -d postgres redis` 후 `./gradlew bootRun --args='--spring.profiles.active=seed'` → `curl -s localhost:8080/api/items | jq '.[0]'`에 `iconUrl`/`itemGroup`/`roleGroup`이 채워지고 iconUrl이 `https://cdn-lostark.game.onstove.com/efui_iconatlas/use/`로 시작; 융화재료 표본의 값이 12-SPIKE-FINDINGS 상수와 일치. (LOSTARK_API_KEY 불필요.)
- latest 응답 DTO 계약(iconUrl/itemGroup/roleGroup, roleGroup ∈ {MATERIAL,DEALER,SUPPORT})이 Phase 14 zod 스키마 합의 대상으로 문서화됨.
</verification>

<success_criteria>
- 4개 read 응답(item list/latest/timeline/event-impact)이 iconUrl·itemGroup·roleGroup을 노출 (ITEM-03).
- 수집 스케줄러·Redis 캐시(LatestPriceCache/CacheConfig)·EventImpactService 소스 diff 0줄, 기존 회귀 그린 (ITEM-04).
- latest는 enrichment를 캐시에 베이크해 HIT zero-DB(API-02)를 보존 (ITEM-03, ITEM-04).
- seed 프로파일만으로(키 없이) 응답에 iconUrl·group이 스파이크 상수와 일치해 채워짐 (SEED-03).
</success_criteria>

<output>
Create `.planning/phases/13-backend-enrichment-seed/13-02-SUMMARY.md` when done
</output>
