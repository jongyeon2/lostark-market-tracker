---
phase: 17.3-coupon-admin
plan: 01
subsystem: api
tags: [spring-boot, jpa, postgres, flyway, testcontainers, coupon]

requires:
  - phase: 04-admin-events
    provides: GameEvent/AdminEventController CRUD pattern + X-Admin-Secret gate (SecurityConfig /api/admin/**)
provides:
  - Coupon entity (date-only LocalDate expiresAt, D-01) + V5__add_coupon.sql (no code dedup index, D-04)
  - CouponRepository (public unexpired soonest-first + admin all-by-expiry finders)
  - CouponRequest/CouponResponse DTOs (entity unbound — blocks id/timestamp mass-assignment)
  - AdminCouponService + AdminCouponController (/api/admin/coupons CRUD behind X-Admin-Secret)
  - Public CouponController (GET /api/coupons — unexpired-only, soonest-expiry-first, KST date)
affects: [17.3-02 frontend coupon data layer + admin CouponSection, 17.3-03 dashboard coupon panel]

tech-stack:
  added: []
  patterns:
    - "Admin CRUD stack mirrors GameEvent 1:1 (entity → repository → request/response DTO → service → controller)"
    - "Public read controller injects the repository directly (ItemController precedent)"
    - "Date-only expiry (LocalDate) with KST-date validity judgement — expiry day stays valid through its end (D-01)"

key-files:
  created:
    - src/main/java/com/lostark/tracker/domain/Coupon.java
    - src/main/resources/db/migration/V5__add_coupon.sql
    - src/main/java/com/lostark/tracker/repository/CouponRepository.java
    - src/main/java/com/lostark/tracker/web/dto/CouponRequest.java
    - src/main/java/com/lostark/tracker/web/dto/CouponResponse.java
    - src/main/java/com/lostark/tracker/admin/AdminCouponService.java
    - src/main/java/com/lostark/tracker/web/admin/AdminCouponController.java
    - src/main/java/com/lostark/tracker/web/CouponController.java
    - src/test/java/com/lostark/tracker/admin/AdminCouponControllerIT.java
    - src/test/java/com/lostark/tracker/web/CouponControllerIT.java
  modified: []

key-decisions:
  - "D-01: expiresAt is date-only (LocalDate / DATE column). Public validity is expiresAt >= LocalDate.now(Asia/Seoul) — the expiry day is valid through its end (23:59 KST). SPEC's 'UTC 기준' wording superseded by the date-only KST-date decision."
  - "D-04: no dedup index on code — the same coupon code may be re-registered freely (watchlist external_item_id-unique precedent NOT applied)."
  - "Admin list keeps expired coupons (findAllByOrderByExpiresAtAsc); the public endpoint filters them out. The ≤6 display cap is the dashboard's responsibility (17.3-03), so the API returns all valid coupons sorted."
  - "SecurityConfig unchanged — /api/admin/** is already authenticated (X-Admin-Secret) and /api/coupons is auto-public via anyRequest().permitAll()."

patterns-established:
  - "Coupon admin/public stack as a 1:1 GameEvent mirror — future admin-curated domains can copy the same 8-file shape"

requirements-completed: [COUPON-01, COUPON-02, COUPON-03]

duration: ~25 min
completed: 2026-07-07
---

# Phase 17.3 Plan 01: 백엔드 쿠폰 도메인·API Summary

**GameEvent를 1:1 미러링한 쿠폰 도메인 — date-only(LocalDate) 만료·code unique 없음(V5), 관리자 CRUD(/api/admin/coupons, X-Admin-Secret)와 공개 read(/api/coupons, KST 미만료·만료임박순)를 Testcontainers IT로 증명**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-07
- **Tasks:** 3
- **Files created:** 10 (8 main + 2 test)

## Accomplishments
- `Coupon` 엔티티 + `V5__add_coupon.sql`: date-only `expires_at DATE`(D-01), code dedup 인덱스 없음(D-04). JPA `ddl-auto=validate` 통과.
- 관리자 CRUD 스택(`AdminCouponService`/`AdminCouponController`) — POST 201·GET(만료 포함·임박순)·PUT full-replace·DELETE 204, `CouponRequest` 바인딩으로 id/타임스탬프 mass-assignment 차단.
- 공개 `CouponController` — `GET /api/coupons`가 `LocalDate.now(Asia/Seoul)` 기준 미만료만 만료임박순 반환, 만료 제외(당일 포함).
- IT 10케이스 그린: admin CRUD 9(201/목록/replace/삭제/404·404/400·400/시크릿없는 401) + 공개 만료필터 1. `./gradlew build` 전체 스위트 그린.

## Task Commits

1. **Task 1: Coupon 엔티티 + V5 마이그레이션 + CouponRepository** — `7f3672d` (feat)
2. **Task 2: DTO 2종 + AdminCouponService + AdminCouponController + 공개 CouponController** — `3b4b9da` (feat)
3. **Task 3: AdminCouponControllerIT + CouponControllerIT** — `b3dfa70` (test)

## Files Created/Modified
- `domain/Coupon.java` — 쿠폰 엔티티 (code/reward/expiresAt(LocalDate)/타임스탬프, @PrePersist/@PreUpdate)
- `db/migration/V5__add_coupon.sql` — coupon 테이블(expires_at DATE, dedup 인덱스 없음)
- `repository/CouponRepository.java` — 공개 미만료·임박순 + admin 전체·임박순 파생 쿼리
- `web/dto/CouponRequest.java`·`CouponResponse.java` — 엔티티 미바인딩 요청/응답 record
- `admin/AdminCouponService.java` — create/list/replace(404)/delete(404)
- `web/admin/AdminCouponController.java` — `/api/admin/coupons` CRUD
- `web/CouponController.java` — 공개 `/api/coupons`(KST 미만료·임박순)
- `test/.../AdminCouponControllerIT.java`(9)·`CouponControllerIT.java`(1)

## Decisions Made
- 계획대로 실행 — frontmatter key-decisions의 D-01/D-04, admin 만료 포함·공개 제외, SecurityConfig 무변경을 그대로 반영.

## Deviations from Plan

None - plan executed exactly as written.

(주: Task 1 수용기준의 `! grep -qi "UNIQUE"` 게이트를 통과시키기 위해 V5 주석에서 "UNIQUE" 토큰을 "dedup index"로 표현했다. DDL에는 애초에 unique 제약이 없어 D-04 의미는 동일 — 실질 변경 아님.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 17.3-02(프론트 데이터 계층 + 관리자 CouponSection)가 소비할 계약 준비 완료: 공개 `GET /api/coupons`(CouponResponse) + admin CRUD(`/api/admin/coupons`).
- CouponResponse 필드: `{id, code, reward, expiresAt("YYYY-MM-DD"), createdAt, updatedAt}` — 프론트 `couponSchema`가 이 형태를 미러링하면 된다.

---
*Phase: 17.3-coupon-admin*
*Completed: 2026-07-07*
