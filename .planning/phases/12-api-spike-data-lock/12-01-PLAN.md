---
phase: 12-api-spike-data-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java
  - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java
  - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
autonomous: false
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04, SPIKE-05]
user_setup:
  - service: lostark-developer-portal
    why: "The spike calls the real markets/options + markets/items API once to confirm CategoryCodes, the icon URL field, and tradeable item ids/iconUrls before locking the curation list"
    env_vars:
      - name: LOSTARK_API_KEY
        source: "Lostark Developer Portal -> API Key (JWT). Set in .env / shell env only (gitignored), never committed."

must_haves:
  truths:
    - "/markets/options 응답으로 유물 각인서(CategoryCode=40000 / ItemGrade=유물)와 융화재료의 leaf CategoryCode가 실측값으로 12-SPIKE-FINDINGS.md(D-07)에 기록된다 (SPIKE-01)"
    - "/markets/items 응답의 아이콘 URL 필드(필드명·CDN 도메인) 존재 여부가 findings에 확정되고, 없으면 fallback 경로가 정해진다 (SPIKE-02)"
    - "융화재료 4종(상급 오레하·최상급 오레하·아비도스·상급 아비도스, D-01) + 딜러/서포터 균형 각인서(D-02)가 거래 가능·external_item_id·iconUrl로 검증되어 role_group=DEALER/SUPPORT/MATERIAL(D-03)·품목당 6필드(D-08)·총 ~17개(D-04, 범위 12~20)로 findings에 확정된다 (SPIKE-03, SPIKE-04)"
    - "각인서 iconUrl이 서로 구별되는지 확인 결과가 findings에 명시되고, 아이콘 부재·로딩 실패 fallback 전략(역할색 배경 + lucide 글리프, 동일 시 라벨 병기)이 문서화된다 (SPIKE-05; D-05, D-06)"
    - "findings·커밋에 실 API 키·계정 식별자·가격 원문이 없다 — 공개 메타데이터(id/name/grade/category/iconUrl)만 (SPIKE-05)"
    - "스파이크는 기존 인프라 확장(D-09) + spike 프로파일 @Disabled·콘솔 출력 마스킹 캡처(D-10)로 CI에서 실행되지 않고 키는 env에서만 온다; 키 없이 ./gradlew build가 그린이고 수집/캐시/event-impact 소스는 0줄 변경"
  artifacts:
    - path: ".planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md"
      provides: "실측 CategoryCode(각인서/융화재료), Icon 필드명·CDN 도메인, 품목당 6필드 큐레이션 표(~17), 각인서 아이콘 구별 여부, fallback 전략, 키-안전 노트"
      contains: "role_group"
    - path: "src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java"
      provides: "/markets/options 조회 + CategoryCode 파라미터화된 카테고리 검색(각인서 40000/융화재료)"
      contains: "markets/options"
    - path: "src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java"
      provides: "@Disabled spike 테스트에 options + 각인서/융화재료 검색 케이스 + 각 품목 Id/Name/Grade/Icon 출력 + 각인서 아이콘 구별 확인"
      contains: "@Disabled"
  key_links:
    - from: "MarketsApiSpikeTest"
      to: "LOSTARK_API_KEY env var"
      via: "Authorization: bearer header sourced from environment (LostarkSpikeClient)"
      pattern: "LOSTARK_API_KEY"
    - from: "12-SPIKE-FINDINGS.md"
      to: "Phase 13 seed/DTO 계약"
      via: "품목당 6필드 표(external_item_id/display_name/category_code/icon_url/item_group/role_group)"
      pattern: "external_item_id"
---

<objective>
v1.0 Task 0의 spike-then-lock 패턴을 재사용해, 본인 JWT 키로 Lostark `/markets/options`·`/markets/items`를 1회 실측한다. (1) 유물 각인서(40000)·융화재료의 leaf CategoryCode를 확정하고, (2) `/markets/items` 응답의 아이콘 URL 필드(필드명·CDN 도메인) 존재 여부를 확정하며, (3) 융화재료 4종(상급/최상급 오레하 + 아비도스 + 상급 아비도스)과 딜러/서포터 유물 각인서 후보를 거래 가능·external_item_id·iconUrl로 검증해 최종 큐레이션 12~20개(목표 ~17)를 6필드로 잠그고, (4) 각인서 아이콘 구별 여부 확인 + 아이콘 부재/실패 fallback 전략을 `12-SPIKE-FINDINGS.md`에 확정한다.

Purpose: 이 게이트가 잠근 6필드 상수·fallback 전략만으로 Phase 13(백엔드 enrichment/seed)·14(프론트 아이콘)가 진행된다(런타임 키 불필요). 실측 전엔 iconUrl·CategoryCode·큐레이션이 미확정이므로 모든 후속의 하드 선행이다.

Output: 확장된 spike 클라이언트/테스트(@Disabled, CI 미실행) + 6필드 큐레이션 표·증거 발췌를 담은 findings 문서. 큐레이션·아비도스/운명 포함을 비준하는 blocking 휴먼 체크포인트 포함. 키/가격/식별자는 산출물에 미기재.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-api-spike-data-lock/12-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-task-0/TASK0-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- 기존 LostarkSpikeClient(spike 프로파일, RestClient): searchMarketItems(itemName)은 CategoryCode 50010 하드코딩 + getItemDetail(id). 키는 ${lostark.api.key} env, 'bearer ' 접두·공백 strip 후 authorization 헤더. base-url=${LOSTARK_API_BASE_URL:https://developer-lostark.game.onstove.com}. -->
<!-- 기존 MarketsApiSpikeTest: @Disabled + @SpringBootTest + @ActiveProfiles("spike") extends PostgresRedisContainers; apiKey 없으면 assumeTrue로 self-skip. -->
<!-- application-spike.yml은 이미 base-url·key를 env에서 읽음 — 설정 변경 불필요. -->
<!-- Task 0 실측: /markets/items 리스트 응답에 "Icon":"https://..." 필드가 이미 관측됨. CategoryCode는 leaf여야 함(부모는 TotalCount:0); leaf 코드는 /markets/options의 Categories[].Subs[].Code. 각인서=40000은 커뮤니티 확인, 융화재료 코드는 실측 필요. -->
</interfaces>
</context>

## Phase Goal

**As a** 데이터 모델러(로아 유저), **I want to** 실 markets API를 1회 호출해 각인서·융화재료의 CategoryCode·item id·iconUrl과 아이콘 구별 여부를 직접 확인하고, **so that** 추정이 아닌 실측값으로 큐레이션 ~17개와 fallback 전략을 잠가 Phase 13·14가 키 없이 재현되게 한다.

<tasks>

<task type="auto">
  <name>Task 1: spike 클라이언트·테스트를 /markets/options + 각인서(40000)/융화재료 카테고리 검색으로 확장 (@Disabled, env 키, build 그린)</name>
  <read_first>
    - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java (기존 searchMarketItems/getItemDetail·키 정규화·RestClient 패턴 — 그대로 재사용하고 메서드 추가)
    - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (기존 @Disabled·spike 프로파일·assumeTrue self-skip·System.out 캡처 패턴)
    - src/test/resources/application-spike.yml (base-url·key를 env에서 읽음 — 변경 불필요, 참조만)
    - .planning/phases/12-api-spike-data-lock/12-CONTEXT.md (D-08 6필드 계약; D-09 기존 인프라 확장; D-10 콘솔 출력 캡처)
    - .planning/phases/01-foundation-task-0/TASK0-FINDINGS.md (요청 포맷, leaf CategoryCode 규칙, Icon 필드 관측, 레이트리밋 헤더)
  </read_first>
  <files>src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java, src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java</files>
  <action>
    `LostarkSpikeClient`에 세 가지를 추가한다(기존 메서드·키 정규화·RestClient 빌더는 보존): (1) `ResponseEntity<String> getMarketOptions()` — GET `/markets/options`, 전체 raw body 반환(각인서/융화재료 leaf CategoryCode 출처 `Categories[].Subs[].Code` 확인용). (2) `ResponseEntity<String> searchMarketItems(int categoryCode, String itemName)` — 현재 50010 하드코딩 대신 `CategoryCode`를 파라미터로 받는 오버로드; 기존 `searchMarketItems(String)`은 `searchMarketItems(50010, itemName)`으로 위임해 호환 유지. body는 기존 포맷(Sort=CURRENT_MIN_PRICE, PageNo=1, SortCondition=ASC, ItemName 선택). 새 메서드들도 동일 authorization(env 키) 사용.
    `MarketsApiSpikeTest`에 케이스를 추가한다(기존 `@Disabled`·`@ActiveProfiles("spike")`·`extends PostgresRedisContainers`·`assumeTrue(apiKey…)` 유지): (a) `getMarketOptions()` 호출 → CategoryCode 트리(각인서 40000·융화재료 후보 코드)를 `System.out`으로 출력. (b) 각인서 카테고리(40000)와 융화재료 후보 카테고리를 `searchMarketItems(categoryCode, name)`로 검색하되, 융화재료는 `ItemName`으로 "오레하"·"아비도스" 등 후보명을 질의 — 각 응답에서 품목별 `Id`/`Name`/`Grade`/`Icon`을 한 줄씩 출력(가격 필드는 출력 생략 또는 마스킹). (c) 각인서 응답의 여러 `Icon` URL이 서로 구별되는지 비교해 distinct/identical을 `System.out`으로 출력(Pitfall 7). 모든 호출은 2xx면 통과, apiKey 없으면 self-skip. 어떤 가격 원문·계정 식별자도 소스에 하드코딩하지 않는다.
  </action>
  <verify>
    <automated>grep -q "markets/options" src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java && grep -Eq "searchMarketItems\(int" src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java && grep -q "@Disabled" src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java && grep -q "getMarketOptions" src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java && ./gradlew compileTestJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `LostarkSpikeClient` 소스가 `markets/options`(GET)와 `searchMarketItems(int` 오버로드를 포함하고, 기존 `searchMarketItems(String)`·`getItemDetail(long)`·키 정규화·RestClient 빌더가 보존됨
    - `MarketsApiSpikeTest` 소스가 `@Disabled`·`@ActiveProfiles("spike")`를 유지하고 `getMarketOptions` 호출 + 각인서/융화재료 검색 케이스를 포함하며 품목별 `Icon` 출력 + 각인서 아이콘 구별 비교 라인을 포함
    - `./gradlew build`가 `LOSTARK_API_KEY` 없이도 그린이고 스파이크 테스트는 실행되지 않음(@Disabled, 네트워크 호출 0)
    - 커밋되는 어떤 파일에도 JWT/키 리터럴 없음: `grep -rEi "bearer [A-Za-z0-9._-]{20,}" src` 결과 0건
    - 수집/캐시/event-impact 소스 무변경: `git diff --name-only`에 `Scheduler`/`TokenBucket`/`Cache`/`EventImpact` 경로 0건
  </acceptance_criteria>
  <done>spike 클라이언트·테스트가 /markets/options + 각인서(40000)/융화재료 카테고리 검색 + 아이콘 구별 확인까지 확장되었고, @Disabled로 CI 미실행·키 없이 build 그린이며 수집/캐시/event-impact는 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: 스파이크 실행(사용자 키) → 콘솔 출력 마스킹 정리 → 12-SPIKE-FINDINGS.md 작성(6필드 표·CategoryCode·Icon·fallback·키안전)</name>
  <read_first>
    - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (Task 1에서 만든 실행 대상 — 어떻게 돌리는지)
    - .planning/phases/01-foundation-task-0/TASK0-FINDINGS.md (findings 포맷·마스킹 발췌·필드 매트릭스 선례 — 동일 톤으로 작성)
    - .planning/phases/12-api-spike-data-lock/12-CONTEXT.md (D-01 융화재료 4종; D-02 각인서 딜러/서포터 균형; D-03 role_group=DEALER/SUPPORT/MATERIAL + item_group 별도; D-04 ~17개; D-05 fallback; D-06 구별 여부; D-08 6필드 스키마)
    - .planning/research/PITFALLS.md (Pitfall 1 키유출·5 비거래/잘못된 id·7 각인서 동일 아이콘 — findings가 막아야 하는 것)
  </read_first>
  <files>.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md</files>
  <action>
    로컬 env에 `LOSTARK_API_KEY`를 설정(user_setup 참조)하고 Task 1의 스파이크 테스트를 1회 수동 실행한다(예: `@Disabled`를 잠시 제거하거나 IDE/`./gradlew test --tests MarketsApiSpikeTest`로 실행; docker-compose Postgres/Redis 기동 필요). 콘솔 출력만 사용하고 raw 응답을 디스크에 덤프하지 않는다(D-10). 출력에서 `12-SPIKE-FINDINGS.md`를 작성한다(TASK0-FINDINGS 포맷 계승, 마스킹된 발췌만):
    (a) **CategoryCode 실측** — `/markets/options`에서 확인한 유물 각인서(40000) leaf 코드와 융화재료 leaf 코드를 표로(SPIKE-01).
    (b) **Icon 필드** — `/markets/items` 응답의 아이콘 URL 필드명(예: `Icon`)과 CDN 도메인을 확정 기록; 부재 시 fallback 경로 명시(SPIKE-02).
    (c) **품목당 6필드 큐레이션 표** — `external_item_id`/`display_name`/`category_code`/`icon_url`/`item_group`/`role_group`. 행: 융화재료 4(상급 오레하/최상급 오레하/아비도스/상급 아비도스, role_group=MATERIAL, item_group=강화재료) + 딜러/서포터 각인서(거래확인분, role_group=DEALER|SUPPORT, item_group=각인서). 거래확인·실측 id/iconUrl만 포함, 총 12~20개(목표 ~17). 거래 불가/식별 불가 품목은 제외하고 그 사실을 노트로(SPIKE-03, SPIKE-04; Pitfall 5). grade·정확 CategoryCode는 산문 증거로만, 가격·키 제외.
    (d) **각인서 아이콘 구별 여부** — distinct/identical을 한 줄로 명시; identical이면 Phase 14가 라벨 병기로 처리함을 노트(SPIKE-04; D-06; Pitfall 7).
    (e) **fallback 전략** — 아이콘 부재/로딩 실패 시 역할색 배경 + lucide 글리프(각인서=책류, 융화재료=플라스크/망치류), 고정 슬롯(레이아웃 시프트 없음); 동일 아이콘 시 라벨 병기(SPIKE-05; D-05).
    (f) **키-안전 노트** — 키는 env에만, findings엔 공개 메타데이터만, 커밋 전 grep 절차(SPIKE-05; Pitfall 1).
    작성 후 커밋 전 점검: `grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md`가 0건이어야 한다.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md && grep -q "external_item_id" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md && grep -q "role_group" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md && grep -Eq "icon_url|iconUrl|Icon" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md && grep -Eq "40000" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md && ! grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `12-SPIKE-FINDINGS.md`가 유물 각인서(40000)·융화재료의 leaf CategoryCode를 실측값으로 기록(SPIKE-01)
    - 아이콘 URL 필드명·CDN 도메인이 확정 기록되거나(존재 시) 부재 시 fallback 경로가 명시됨(SPIKE-02)
    - 품목당 6필드 표가 존재하고 모든 행이 실측 `external_item_id`·`icon_url`를 가짐(추정/플레이스홀더 0건), 총 12~20개(SPIKE-03, SPIKE-04)
    - 각인서 아이콘 구별 여부(distinct/identical)가 한 줄로 명시됨(SPIKE-04; D-06)
    - fallback 전략(역할색 배경 + lucide 글리프 + 동일 시 라벨 병기)이 문서화됨(SPIKE-05)
    - findings에 JWT/`bearer <token>`/가격 원문/계정 식별자 0건 — `grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}"` 결과 없음(SPIKE-05)
  </acceptance_criteria>
  <done>실 markets 응답이 캡처되어 CategoryCode·Icon 필드·6필드 큐레이션 표(~17)·각인서 구별 여부·fallback 전략이 findings에 잠겼고, 키/가격/식별자는 산출물에 없다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (checkpoint): 큐레이션 목록·아비도스/운명 포함·fallback 비준</name>
  <files>.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md</files>
  <action>
    휴먼 리뷰를 위해 PAUSE한다. 자동 진행 금지 — 이 blocking 체크포인트가 Phase 13(seed/DTO)·14(프론트)가 소비할 큐레이션 상수를 비준한다. 개발자에게 findings의 6필드 큐레이션 표·각인서 구별 여부·fallback 전략을 제시하고 아래 <what-built>/<how-to-verify>/<resume-signal>로 안내한 뒤 resume 신호를 기다린다.
  </action>
  <what-built>12-SPIKE-FINDINGS.md: 실측 CategoryCode(각인서 40000/융화재료), Icon 필드·CDN 도메인, 품목당 6필드 큐레이션 표(융화재료 4 + 각인서 ~13 = ~17), 각인서 아이콘 구별 여부, fallback 전략(역할색 + lucide 글리프 + 라벨 병기), 키-안전 노트.</what-built>
  <how-to-verify>
    1. `12-SPIKE-FINDINGS.md`를 열어 큐레이션 표의 융화재료 4종(상급 오레하/최상급 오레하/아비도스/상급 아비도스)이 전부 거래확인·실측 id/iconUrl을 가졌는지, 도메인 안목에 맞는지 확인.
    2. 운명 계열 융화재료: 거래/식별이 확인됐다면 포함할지, 아니면 보류 유지할지 최종 결정(현재 보류).
    3. 딜러/서포터 각인서 선정이 균형(딜러 다수 + 서포터 포함)에 맞고 role_group(DEALER/SUPPORT/MATERIAL) 분류가 정확한지 확인.
    4. 각인서 아이콘 구별 여부 기록을 확인 — identical이면 Phase 14 라벨 병기 결정이 반영됐는지.
    5. findings·커밋에 키/가격/식별자가 없는지 확인.
  </how-to-verify>
  <verify><human-check>개발자가 큐레이션 목록·아비도스/운명 포함·role_group·fallback을 비준한다(또는 변경 사항을 지정한다).</human-check></verify>
  <resume-signal>"비준 — 큐레이션 잠금"이라고 입력하거나, 변경 사항을 기술(예: "운명 융화재료 포함", "각인서 X 빼고 Y 추가", "role_group 재분류").</resume-signal>
  <done>개발자가 큐레이션 상수·role_group·fallback을 비준해(또는 변경 기록해) Phase 13을 언블록한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Lostark markets API | JWT 키를 실은 아웃바운드 호출; 외부 응답은 신뢰 불가 입력 |
| env/.env → spike code | 실 키는 env에서만 와야 하며 커밋 소스·CI에 진입 금지 |
| spike 응답 → findings/커밋 | 응답의 가격·계정 식별자가 공개 산출물에 새어선 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Lostark API JWT 키 | mitigate | 키는 `${LOSTARK_API_KEY}` env에서만; `.env` gitignored; 스파이크 `@Disabled`라 CI에서 키 불필요. AC: 커밋 파일에 `bearer <token>` 리터럴 0건 grep. **HIGH severity(보안 게이트 block_on=high)** → 누출 시 blocking 체크포인트가 큐레이션 잠금을 게이트. |
| T-12-02 | Information Disclosure | 응답의 가격 원문·계정 식별자 | mitigate | findings엔 공개 메타데이터(id/name/grade/category/iconUrl)만; 가격·식별자 마스킹. 커밋 전 `eyJ`/`bearer`/가격 grep 0건(Task 2 AC). raw 응답 디스크 미덤프(D-10). |
| T-12-03 | Spoofing/Repudiation | 401(만료/오타 토큰)을 데이터 발견으로 오인 | mitigate | 스파이크는 401을 유효 빈 응답과 구별해 findings에 기록; auth 에러 위에 모델을 잠그지 않음(Task 0 401 핸들링 선례). |
| T-12-04 | Tampering | 비거래/잘못된 id를 큐레이션에 잠금 | mitigate | API 응답에서 거래 가능·item id 확인된 품목만 표에 포함; 미확인은 제외+노트(Pitfall 5); blocking 체크포인트가 도메인 비준. |
| T-12-05 | Denial of Service | 레이트리밋 API 과호출 | mitigate | 수동 1회 실행, `@Disabled` 기본, 루프/CI 실행 불가; 100/min 한참 아래. |
| T-12-SC | Tampering | 의존성 공급망 | accept | 신규 패키지 설치 없음; RestClient는 기존 Spring 동봉. legitimacy 게이트 불필요. |
</threat_model>

<verification>
- `./gradlew build`가 키 없이 그린이고 네트워크 호출을 안 함(스파이크 @Disabled).
- 커밋되는 어떤 파일에도 API 키 리터럴/가격 원문/계정 식별자 없음.
- `12-SPIKE-FINDINGS.md`가 실측 CategoryCode(각인서 40000/융화재료), Icon 필드·CDN 도메인, 품목당 6필드 큐레이션 표(~17), 각인서 아이콘 구별 여부, fallback 전략을 담음.
- 수집/캐시/event-impact 소스 diff 0줄.
- blocking 휴먼 체크포인트가 큐레이션·아비도스/운명 포함·role_group·fallback을 비준.
</verification>

<success_criteria>
- 유물 각인서(40000)·융화재료 leaf CategoryCode가 실측값으로 잠김 (SPIKE-01).
- 아이콘 URL 필드(필드명·CDN 도메인) 존재 여부 확정, 부재 시 fallback 경로 확정 (SPIKE-02).
- 거래 가능·item id·iconUrl 검증된 큐레이션 12~20개(목표 ~17) 6필드 확정 (SPIKE-03, SPIKE-04).
- 각인서 아이콘 구별 여부 기록 + 아이콘 부재/실패 fallback 전략 문서화 (SPIKE-04, SPIKE-05).
- 키/가격/식별자가 findings·커밋에 없음 — 공개 메타데이터만 (SPIKE-05).
- spike 프로파일 @Disabled로 CI 미실행, 키 없이 build 그린, 수집/캐시/event-impact 무변경.
</success_criteria>

<output>
Create `.planning/phases/12-api-spike-data-lock/12-01-SUMMARY.md` when done
</output>
