---
phase: 07-frontend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/TopNav.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/state/LoadingState.tsx
  - frontend/src/components/state/EmptyState.tsx
  - frontend/src/components/state/ErrorState.tsx
  - frontend/src/components/state/AsyncBoundary.tsx
  - frontend/src/components/RootErrorBoundary.tsx
  - frontend/src/features/dashboard/DashboardPage.tsx
  - frontend/src/features/timeline/TimelinePage.tsx
  - frontend/src/features/impact/ImpactPage.tsx
autonomous: true
requirements: [FND-02, FND-03, FND-04]

must_haves:
  truths:
    - "Navigation uses React Router with real paths (D-03): routes `/dashboard`, `/timeline`, `/impact`, and `/` redirecting to `/dashboard`. A page refresh keeps the current screen, links are deep-linkable and screenshot-stable, and the browser back button works (the demo-surface feel) — in-memory tab switching was rejected. The router is structured so Phase 9-10 can add filter state as URL searchParams (D-04 forward-note) without restructuring."
    - "A top fixed navigation bar (UI-SPEC Layout, secondary surface, ~56px) carries the brand title '로스트아크 시세 트래커' and three text-label items '대시보드' / '품목 타임라인' / '이벤트 영향'; the active route's item is shown in accent blue-600 (text/underline) and the rest in slate-500, so the user always sees where they are. The content area below is max-width-constrained with xl(32px) side padding and is the primary visual focus of each route (FND-03)."
    - "Shared presentational state components render the UI-SPEC visual + copy contract (D-07): `LoadingState` (skeleton + the helper text '불러오는 중…'), `EmptyState` (heading '표시할 데이터가 아직 없어요' + the seed-backend body copy), and `ErrorState` (the '백엔드에 연결하지 못했어요…' copy + the '다시 불러오기' primary CTA). These components own the exact UI-SPEC strings — generic '데이터 없음'/'오류 발생' wording is forbidden."
    - "A thin `<AsyncBoundary>` wrapper takes a React Query result (status: pending/error/success) and an `isEmpty` predicate and renders the right state component, so each screen writes ONLY the success path — branching logic is centralized (DRY, testable) and the ErrorState's '다시 불러오기' CTA calls the query's `refetch` (D-08, closing the D-02 user-retry loop). It pairs naturally with the D-01 React Query result and the D-06 thrown parse errors."
    - "FND-02 is proven end-to-end on the Dashboard route: `DashboardPage` calls the 07-02 `useItems()` hook through `<AsyncBoundary>` and renders a minimal success view (e.g. active item count + displayName list) — so with the seed backend up, `/api/items` is fetched via the Vite proxy and rendered with no CORS error (ROADMAP success criterion 2); with the backend down, the shared ErrorState appears instead of a crash (ROADMAP success criterion 4 / verification 'backend off → error state'). Timeline and Impact are intentionally empty route skeletons (real widgets are Phase 8-10)."
    - "All three routes mount under the shared nav shell and the app stays crash-free: an unexpected render error is caught by a top-level React error boundary (so a thrown error in one screen shows a fallback, not a white screen), and `npm run build` (tsc) passes with zero type errors. No file under the Java `src/` changes."
  artifacts:
    - path: "frontend/src/components/state/AsyncBoundary.tsx"
      provides: "Thin wrapper: props { status:'pending'|'error'|'success', isEmpty?:boolean, error?, onRetry?, children }; maps pending->LoadingState, error->ErrorState(onRetry), success+isEmpty->EmptyState, success->children (D-08)"
      contains: "AsyncBoundary"
    - path: "frontend/src/components/state/ErrorState.tsx"
      provides: "ErrorState with the UI-SPEC error copy ('백엔드에 연결하지 못했어요…') and the '다시 불러오기' primary CTA (shadcn Button, accent) wired to an onRetry callback"
      contains: "다시 불러오기"
    - path: "frontend/src/components/state/EmptyState.tsx"
      provides: "EmptyState with heading '표시할 데이터가 아직 없어요' + seed-backend body copy (one body line overridable per screen)"
      contains: "표시할 데이터가 아직 없어요"
    - path: "frontend/src/components/state/LoadingState.tsx"
      provides: "LoadingState: shadcn Skeleton(s) + helper text '불러오는 중…'"
      contains: "불러오는 중"
    - path: "frontend/src/components/layout/TopNav.tsx"
      provides: "Top fixed nav: brand '로스트아크 시세 트래커' + 3 NavLink items (대시보드/품목 타임라인/이벤트 영향) with active = accent blue-600 (text/underline), inactive = slate-500 Label"
      contains: "품목 타임라인"
    - path: "frontend/src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard route: useItems() through <AsyncBoundary> rendering active item count + displayName list (the FND-02 live /api/items proof + FND-04 state demo)"
      contains: "useItems"
    - path: "frontend/src/main.tsx"
      provides: "Router wiring: createBrowserRouter / <RouterProvider> (or <BrowserRouter><Routes>) inside the existing QueryClientProvider, with the / -> /dashboard redirect and the AppLayout shell"
      contains: "dashboard"
  key_links:
    - from: "frontend/src/components/state/AsyncBoundary.tsx"
      to: "frontend/src/components/state/ErrorState.tsx"
      via: "AsyncBoundary renders ErrorState on status==='error' and passes onRetry (D-08 -> D-07)"
      pattern: "ErrorState"
    - from: "frontend/src/features/dashboard/DashboardPage.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "DashboardPage calls useItems() and feeds its status/data into AsyncBoundary (FND-02 live render)"
      pattern: "useItems"
    - from: "frontend/src/components/layout/TopNav.tsx"
      to: "react-router"
      via: "NavLink active state drives the accent-vs-slate styling for the current route (D-03)"
      pattern: "NavLink|react-router"
---

<objective>
Assemble the **app shell** that turns the scaffold + library layer into a navigable demo: **React Router** with real `/dashboard` · `/timeline` · `/impact` paths and a `/`→`/dashboard` redirect (D-03), a **top nav bar** rendering the UI-SPEC brand + three text-label items with accent active-state (FND-03), the **shared presentational state components** `LoadingState`/`EmptyState`/`ErrorState` carrying the exact UI-SPEC copy (D-07), and the thin **`<AsyncBoundary>`** that maps a React Query result to the right state so each screen writes only the success path (D-08). The **Dashboard route** wires `useItems()` through AsyncBoundary as the **FND-02 end-to-end proof** (live `/api/items` via the proxy, ErrorState when the backend is down); Timeline and Impact are intentionally empty route skeletons for Phases 9-10.

The engineering signal is **centralized, contract-driven async UX**: instead of every screen hand-rolling loading/empty/error branches, one boundary component does it, the UI-SPEC copy lives in one place, and the '다시 불러오기' CTA closes the D-02 user-retry loop by calling `refetch`. Routing uses real URLs so refresh/deep-link/back all work — the demo feels like a real app, and it is structured for the Phase 9-10 searchParams filters.

Output: `main.tsx` router wiring + `AppLayout`/`TopNav`, `src/components/state/{LoadingState,EmptyState,ErrorState,AsyncBoundary}.tsx`, a top-level `RootErrorBoundary`, and the three `features/*/*.Page.tsx` (Dashboard live-renders `useItems`; Timeline/Impact are empty skeletons).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Depends on 07-02: src/lib/{schemas,api,queries,queryClient,formatKst}.ts exist; main.tsx already wraps <App/> in QueryClientProvider. -->
<!-- Depends on 07-01: shadcn blocks button/card/skeleton/alert/navigation-menu under src/components/ui/; UI-SPEC tokens in index.css; @ alias. -->
<!-- UI-SPEC copy contract (use these EXACT strings; generic wording forbidden): -->
<!--   brand: 로스트아크 시세 트래커 ; nav: 대시보드 / 품목 타임라인 / 이벤트 영향 -->
<!--   loading helper: 불러오는 중… ; empty heading: 표시할 데이터가 아직 없어요 -->
<!--   empty body: seed 프로파일 백엔드를 기동하면 시세가 채워집니다 (SPRING_PROFILES_ACTIVE=seed). -->
<!--   error: 백엔드에 연결하지 못했어요. seed 백엔드(:8080)가 켜져 있는지 확인하고 다시 불러오세요. ; primary CTA: 다시 불러오기 -->
<!-- UI-SPEC layout: top fixed nav ~56px secondary surface; content max-w-screen-xl + xl(32px) side padding; active nav = accent blue-600 text/underline, inactive = slate-500 Label; nav items are TEXT labels (icon optional but always with text). -->
<!-- React Query result -> AsyncBoundary mapping: isPending->Loading, isError->Error(refetch), success && isEmpty->Empty, success->children. -->
<!-- D-04 forward-note: structure routes so Phase 9-10 can add filter state as URL searchParams; do NOT build filter UI now. -->
<!-- Scope: Timeline/Impact are EMPTY skeleton routes (a page title + the shell); their data widgets are Phase 8(Dashboard)/9/10. Only Dashboard live-renders useItems() as the FND-02 proof. -->
<!-- No backend src/ change; no admin/write UI (FE-V2-01); no dark mode (FE-V2-03). -->
</interfaces>
</context>

## Phase Goal

**As** a user (and a hiring reviewer), **I want** a top-nav app where I can move between Dashboard / Item Timeline / Event Impact with real URLs, and where every screen shows consistent loading / empty / error states, **so that** the demo behaves like a real app (refresh & back work) and the Dashboard actually proves the front end reaches the backend through the proxy — with a friendly retryable error when the backend is down instead of a crash.

<tasks>

<task type="auto">
  <name>Task 1: Shared state components — LoadingState / EmptyState / ErrorState (UI-SPEC copy)</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-UI-SPEC.md (Copywriting Contract: exact loading/empty/error strings + '다시 불러오기' CTA; Layout: state components centered, heading+body+CTA vertical, md(16px) gaps; Color: destructive for error, accent for CTA)
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-07 presentational components own the UI-SPEC visual + copy contract)
    - frontend/src/components/ui/button.tsx + alert.tsx + skeleton.tsx + card.tsx (the shadcn blocks from 07-01 these compose)
    - frontend/src/lib/utils.ts (cn helper for class composition)
  </read_first>
  <files>frontend/src/components/state/LoadingState.tsx, frontend/src/components/state/EmptyState.tsx, frontend/src/components/state/ErrorState.tsx</files>
  <behavior>
    - `LoadingState` shows shadcn Skeleton placeholders plus the helper text '불러오는 중…'.
    - `EmptyState` shows the heading '표시할 데이터가 아직 없어요' and the seed-backend body copy, with the body line overridable per screen (one line only).
    - `ErrorState` shows the error copy '백엔드에 연결하지 못했어요. seed 백엔드(:8080)가 켜져 있는지 확인하고 다시 불러오세요.' and a primary '다시 불러오기' Button (accent) that calls an `onRetry` prop; it uses the destructive color for the error visual.
    - All three are purely presentational (props in, no data fetching), centered in the content area, heading+body+(CTA) stacked with md(16px) gaps.
  </behavior>
  <action>
    Create the three components under `src/components/state/`. `LoadingState`: render one or more shadcn `Skeleton`s and the muted helper text '불러오는 중…' (Body/Label typography). `EmptyState`: props `{ body?: string }`; render the fixed heading '표시할 데이터가 아직 없어요' (Heading 20/600) and a body defaulting to 'seed 프로파일 백엔드를 기동하면 시세가 채워집니다 (SPRING_PROFILES_ACTIVE=seed).' (overridable per screen). `ErrorState`: props `{ onRetry: () => void; message?: string }`; render the fixed error copy as the default message using the destructive token (optionally a shadcn `Alert`), and a primary shadcn `Button` labeled '다시 불러오기' (accent/primary) wired to `onRetry`. All three: centered column, `gap` md(16px), no data fetching, no generic '데이터 없음'/'오류 발생' wording. Use the UI-SPEC tokens from index.css (no hardcoded hex beyond what tokens provide).
  </action>
  <verify>
    <automated>cd frontend && grep -q "불러오는 중" src/components/state/LoadingState.tsx && grep -q "표시할 데이터가 아직 없어요" src/components/state/EmptyState.tsx && grep -q "다시 불러오기" src/components/state/ErrorState.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `LoadingState.tsx` renders a shadcn `Skeleton` and the text '불러오는 중…'; `grep -n "불러오는 중" src/components/state/LoadingState.tsx` matches
    - `EmptyState.tsx` shows heading '표시할 데이터가 아직 없어요' with an overridable body; `grep -n "표시할 데이터가 아직 없어요" src/components/state/EmptyState.tsx` matches
    - `ErrorState.tsx` shows the UI-SPEC error copy and a '다시 불러오기' Button calling `onRetry`; `grep -nE "다시 불러오기|onRetry" src/components/state/ErrorState.tsx` matches
    - No generic '데이터 없음'/'오류 발생' strings are used; components are presentational (no fetch/useQuery import)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Three presentational state components carry the exact UI-SPEC copy and visuals (D-07): skeleton + '불러오는 중…', '표시할 데이터가 아직 없어요' + seed body, and the error copy + '다시 불러오기' CTA — reused identically across screens.</done>
</task>

<task type="auto">
  <name>Task 2: AsyncBoundary wrapper — map React Query result to the right state component</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-08 thin AsyncBoundary: status pending/error/success + isEmpty predicate → state component; screens write success path only; D-02 retry = refetch)
    - frontend/src/components/state/LoadingState.tsx + EmptyState.tsx + ErrorState.tsx (the three states it renders — Task 1)
    - frontend/src/lib/queries.ts (the React Query result shape — isPending/isError/data/refetch — from 07-02)
  </read_first>
  <files>frontend/src/components/state/AsyncBoundary.tsx</files>
  <behavior>
    - Given a query's status and an optional `isEmpty` predicate result, `AsyncBoundary` renders: pending → `LoadingState`; error → `ErrorState` (passing the query's `refetch` as `onRetry`); success && isEmpty → `EmptyState`; success && !isEmpty → its `children` (the screen's success view).
    - The component is generic/thin: it does no fetching itself and makes no assumption about the data type beyond the status flags + isEmpty boolean passed in.
    - The '다시 불러오기' retry path therefore re-runs the query (closing the D-02 user-retry loop).
  </behavior>
  <action>
    Create `src/components/state/AsyncBoundary.tsx` exporting `AsyncBoundary`. Accept props `{ status: 'pending' | 'error' | 'success'; isEmpty?: boolean; onRetry?: () => void; emptyBody?: string; children: React.ReactNode }` (or accept the React Query result object directly and derive status — pick one consistent shape and document it). Render: `status==='pending'` → `<LoadingState/>`; `status==='error'` → `<ErrorState onRetry={onRetry ?? noop} />`; `status==='success' && isEmpty` → `<EmptyState body={emptyBody} />`; else → `children`. Keep it dependency-light (no data fetching, no zod). This is the single place screens delegate their loading/empty/error branching to (D-08).
  </action>
  <verify>
    <automated>cd frontend && grep -qE "LoadingState|EmptyState|ErrorState" src/components/state/AsyncBoundary.tsx && grep -qE "pending|error|success" src/components/state/AsyncBoundary.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `AsyncBoundary.tsx` maps pending→LoadingState, error→ErrorState(onRetry), success+isEmpty→EmptyState, success→children; `grep -nE "LoadingState|EmptyState|ErrorState" src/components/state/AsyncBoundary.tsx` shows all three
    - It is thin/presentational — no `useQuery`/`fetch` inside; the data type is generic
    - The error branch forwards an `onRetry`/`refetch` so '다시 불러오기' re-runs the query (D-02 loop)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>A thin `<AsyncBoundary>` centralizes loading/empty/error branching from a React Query result + isEmpty predicate, so each screen writes only the success path and retry re-runs the query (D-08).</done>
</task>

<task type="auto">
  <name>Task 3: React Router shell — routes, /→/dashboard redirect, TopNav + AppLayout, RootErrorBoundary</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-03 React Router real paths /dashboard·/timeline·/impact + / redirect; D-04 forward-note structure for searchParams; Claude's Discretion: top-level ErrorBoundary)
    - .planning/phases/07-frontend-foundation/07-UI-SPEC.md (Layout & Visual Hierarchy: top fixed nav ~56px, brand left, content max-w + xl padding; active = accent blue-600, inactive = slate-500; Typography roles)
    - frontend/src/main.tsx (the existing QueryClientProvider wrapping <App/> from 07-02 — the router goes inside it)
    - frontend/src/components/ui/navigation-menu.tsx (the shadcn nav block from 07-01) + frontend/src/App.tsx (the 07-01 placeholder this replaces)
  </read_first>
  <files>frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/components/layout/TopNav.tsx, frontend/src/components/layout/AppLayout.tsx, frontend/src/components/RootErrorBoundary.tsx, frontend/package.json</files>
  <behavior>
    - The app has real routes `/dashboard`, `/timeline`, `/impact`, and `/` redirects to `/dashboard`; refreshing on any route keeps that screen and the browser back button navigates between them.
    - A persistent top nav bar (secondary surface, ~56px) shows the brand '로스트아크 시세 트래커' and three text NavLinks ('대시보드'/'품목 타임라인'/'이벤트 영향'); the active route's link is accent blue-600 (text/underline), others slate-500.
    - The content area below the nav is max-width constrained with xl(32px) side padding and hosts the active route.
    - An unexpected render error anywhere in a route shows a top-level fallback (RootErrorBoundary), not a blank white screen.
  </behavior>
  <action>
    Add `react-router-dom` to `frontend/package.json`. Wire routing in `src/main.tsx` (or a routes module) INSIDE the existing `QueryClientProvider`: define routes for `/dashboard`→DashboardPage, `/timeline`→TimelinePage, `/impact`→ImpactPage, and `/`→redirect to `/dashboard` (`<Navigate to="/dashboard" replace />` or a loader redirect), all nested under an `AppLayout` route so the nav persists. Create `AppLayout` rendering `<TopNav/>` + a `<main>` with `max-w-screen-xl mx-auto` and `px` xl(32px), an `<Outlet/>` for the route. Create `TopNav` using the shadcn navigation-menu (or styled NavLinks): brand '로스트아크 시세 트래커' on the left (Label/Heading weight), then three `NavLink`s to the routes using the exact labels; style the active state via NavLink's isActive → accent blue-600 text + underline, inactive → slate-500 (Label 14/600); keep items as text labels (icon optional, always with text). Create `RootErrorBoundary` (a React error boundary or React Router `errorElement`) that renders a simple fallback card on a thrown render error. Replace the 07-01 placeholder `App.tsx` usage with the router/layout. Structure routes so Phase 9-10 can read filters from `useSearchParams` later (D-04) — but add NO filter UI now. Keep StrictMode and the QueryClientProvider intact.
  </action>
  <verify>
    <automated>cd frontend && grep -qE "react-router" package.json && grep -qE "/dashboard|dashboard" src/main.tsx src/components/layout/TopNav.tsx && grep -q "로스트아크 시세 트래커" src/components/layout/TopNav.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `react-router-dom` is a dependency; routes `/dashboard`, `/timeline`, `/impact` exist and `/` redirects to `/dashboard`; `grep -nE "timeline|impact|dashboard|Navigate" src/main.tsx` (or routes module) matches
    - `TopNav.tsx` renders brand '로스트아크 시세 트래커' + the three exact nav labels with active=accent / inactive=slate styling; `grep -nE "품목 타임라인|이벤트 영향" src/components/layout/TopNav.tsx` matches
    - `AppLayout` constrains content width (max-w) with xl side padding and renders an `<Outlet/>`; the router stays inside the `QueryClientProvider`
    - A top-level error boundary/`errorElement` renders a fallback instead of a white screen on a thrown render error
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>React Router gives real `/dashboard`·`/timeline`·`/impact` URLs with a `/` redirect under a persistent top-nav shell (active-state accent), refresh/back/deep-link work, and a top-level error boundary prevents white-screen crashes (D-03, FND-03).</done>
</task>

<task type="auto">
  <name>Task 4: Route pages — Dashboard live-renders useItems() (FND-02 proof); Timeline/Impact empty skeletons</name>
  <read_first>
    - .planning/ROADMAP.md (success criterion 2: front calls /api/items via proxy → 200 rendered, no CORS; criterion 4: common state component works on ≥1 screen; verification: backend off → ErrorState, not crash)
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (phase boundary: 3 screens are empty skeletons in Phase 7; data widgets are Phase 8-10; Dashboard is the live-render proof)
    - frontend/src/lib/queries.ts (useItems hook — 07-02) + frontend/src/components/state/AsyncBoundary.tsx (Task 2)
    - frontend/src/lib/formatKst.ts (07-02, available for any timestamp display) + frontend/src/components/ui/card.tsx (07-01)
  </read_first>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/ImpactPage.tsx</files>
  <behavior>
    - `DashboardPage` calls `useItems()` and passes its status + an `isEmpty` predicate (`data.length === 0`) + `refetch` into `<AsyncBoundary>`, rendering on success a minimal view: the active item count and a list of `displayName`s (the real data widgets — health card, latest-price cards — are Phase 8).
    - With the seed backend up, visiting `/dashboard` fetches `/api/items` through the proxy and renders the list (FND-02 proven; no CORS error in the console/network tab).
    - With the backend down, the Dashboard shows the shared `ErrorState` (with '다시 불러오기'), not a crash (ROADMAP verification).
    - `TimelinePage` and `ImpactPage` render only a page title (Display 28/600, e.g. '품목 타임라인' / '이벤트 영향') and a brief placeholder — no data fetching yet (Phase 9-10).
  </behavior>
  <action>
    Create `DashboardPage` (`src/features/dashboard/DashboardPage.tsx`): call `useItems()`, derive `status` and `isEmpty = (data?.length ?? 0) === 0`, and wrap the success view in `<AsyncBoundary status={...} isEmpty={isEmpty} onRetry={refetch}>` — success renders a page title '대시보드' (Display) plus a minimal summary (e.g. '추적 중 N개 품목' and a `displayName` list, optionally in shadcn `Card`s). This is the FND-02 end-to-end proof and the FND-04 state demo on a real screen; do NOT build the Phase-8 health/latest widgets here. Create `TimelinePage` and `ImpactPage` as empty skeletons: a page title ('품목 타임라인' / '이벤트 영향', Display typography) and a one-line placeholder note that the screen lands in Phase 9 / Phase 10 — NO `useQuery`, NO filter UI, NO chart. Use `formatKst` only if you display a timestamp; keep everything inside `frontend/`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "useItems" src/features/dashboard/DashboardPage.tsx && grep -q "AsyncBoundary" src/features/dashboard/DashboardPage.tsx && test -f src/features/timeline/TimelinePage.tsx && test -f src/features/impact/ImpactPage.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardPage.tsx` calls `useItems()` and feeds status + `isEmpty` + `refetch` into `<AsyncBoundary>`, rendering item count + displayName list on success; `grep -nE "useItems|AsyncBoundary" src/features/dashboard/DashboardPage.tsx` matches
    - The Dashboard is the FND-02 live proof: with seed backend up, `/dashboard` renders the proxied `/api/items` result; with backend down, the shared ErrorState shows (no crash)
    - `TimelinePage.tsx` and `ImpactPage.tsx` are empty skeletons (title + placeholder, no `useQuery`/chart/filter); both files exist and build
    - No Phase-8 health/latest widgets are built here; no file under the Java `src/` changes
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>The Dashboard route live-renders `useItems()` through AsyncBoundary — proving FND-02 (proxied `/api/items`, no CORS) and FND-04 (shared states incl. ErrorState when backend is down) on a real screen — while Timeline/Impact are intentional empty skeletons for Phases 9-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend data → rendered DOM | Item `displayName`/category etc. from the API are rendered into the page |
| URL / route params → app state | The route path (and later searchParams) drives which screen renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0703-01 | Tampering / XSS (rendering API strings) | DashboardPage / state components | mitigate | All API strings are rendered as React text children (JSX), which React escapes by default; the plan uses NO `dangerouslySetInnerHTML` anywhere, so a malicious `displayName`/`title` cannot inject script. Data is also zod-validated upstream (07-02 D-06) before it reaches the render. |
| T-0703-02 | Denial of Service / UX crash (unhandled render error) | route tree | mitigate | A top-level RootErrorBoundary (errorElement) catches thrown render errors and shows a fallback instead of a white screen; the AsyncBoundary routes query errors (incl. backend-down) to the friendly ErrorState with a retry, so an offline backend degrades gracefully (ROADMAP verification) rather than crashing. |
| T-0703-03 | Information Disclosure (error copy leaking internals) | ErrorState | mitigate | The ErrorState shows a fixed, user-facing UI-SPEC message ('백엔드에 연결하지 못했어요…') and does NOT render raw exception/stack/response-body text, so backend internals or stack traces are not exposed in the UI. |
| T-0703-04 | Elevation of Privilege (write/admin surface) | routes | accept | This shell is read-only: there is no admin/write route, form, or `X-Admin-Secret` usage (admin UI is FE-V2-01, out of scope); the only CTA is the read-only '다시 불러오기' refetch. No privileged surface exists to attack. |
</threat_model>

<verification>
- `cd frontend && npm run build` (tsc + vite build) passes — the router, layout, state components, AsyncBoundary, and three pages type-check against the 07-02 hooks.
- Manual (ROADMAP verification, with seed backend on :8080): `npm run dev` → `/` redirects to `/dashboard`; the three nav items switch routes with real URLs; refresh keeps the route; back button works; `/api/items` is fetched via the proxy and the Dashboard renders the item list with NO CORS/404 error in the console/network tab (FND-02, FND-03).
- Stop the backend and refresh `/dashboard` → the shared ErrorState ('백엔드에 연결하지 못했어요…' + '다시 불러오기') renders instead of a crash; clicking '다시 불러오기' re-runs the query (FND-04, D-02/D-08).
- `grep` confirms the exact UI-SPEC copy in the state components and TopNav; `git status --short -- src/` is empty (backend untouched).
</verification>

<success_criteria>
- React Router gives real `/dashboard`·/timeline`·/impact` URLs with a `/`→/dashboard redirect under a persistent top-nav shell (active-state accent), with refresh/back/deep-link working (D-03, FND-03).
- Shared `LoadingState`/`EmptyState`/`ErrorState` carry the exact UI-SPEC copy and a thin `<AsyncBoundary>` centralizes the loading/empty/error branching with retry=refetch (D-07, D-08, FND-04).
- The Dashboard live-renders `useItems()` via AsyncBoundary — proving FND-02 (proxied `/api/items`, no CORS) and graceful ErrorState when the backend is down — while Timeline/Impact are empty skeletons for Phases 9-10; `npm run build` passes and the Java `src/` is unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-foundation/07-03-SUMMARY.md` when done
</output>
