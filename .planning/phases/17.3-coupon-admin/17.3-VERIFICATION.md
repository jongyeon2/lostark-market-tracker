---
phase: 17.3-coupon-admin
verified: 2026-07-07T01:00:00Z
status: passed
score: 4/4 성공 기준 검증됨
---

# Phase 17.3: 쿠폰 관리자 등록 — 대시보드 쿠폰 섹션 검증 보고서

**페이즈 목표:** 관리자가 콘솔에서 쿠폰(코드·보상·만료일)을 등록·수정·삭제하면 PostgreSQL에 영속되고, 대시보드 뉴스 패널 '쿠폰' 섹션에 미만료 쿠폰만 만료임박순(≤6, 코드 원클릭 복사)으로 표시되며, 만료 쿠폰은 공개 목록에서 자동 제외된다. 수집/가격 캐시/event-impact는 0줄(Core Value 가드).
**검증 시각:** 2026-07-07
**상태:** passed
**검증 방식:** 목표 역산(goal-backward) — ROADMAP Phase 17.3 목표 + 4개 성공 기준 + 요구사항 COUPON-01~03. 오케스트레이터가 인라인으로 수행(이 환경은 백그라운드 서브에이전트가 권한 거부됨).

## 목표 달성

### 관측 가능한 진실 (ROADMAP 성공 기준)

| # | 진실 | 상태 | 증거 |
|---|------|------|------|
| 1 | 관리자가 쿠폰(코드·보상·만료일)을 등록·수정·삭제하고 PostgreSQL에 영속한다 (X-Admin-Secret, `/api/admin/coupons`) | ✓ 검증됨 | `AdminCouponController`(POST 201/GET/PUT full-replace/DELETE 204, `/api/admin/coupons`) → `AdminCouponService`(create/list/replace(404)/delete(404)) → `Coupon` 엔티티 + `V5__add_coupon.sql`. `AdminCouponControllerIT` 9/9 그린(201+엔티티 스탬프·목록·PUT·DELETE·404·400·**시크릿 없는 POST 401**). SecurityConfig `/api/admin/**` 무변경. 프론트 `CouponSection`(AdminConsolePage 배치)이 admin 훅 소비. |
| 2 | 대시보드에 미만료 쿠폰이 만료임박순(≤6)으로 표시되고 코드를 원클릭 복사할 수 있다 | ✓ 검증됨 | `NewsPanel` 최상단 `CouponSection`이 `useCoupons()`(GET /api/coupons) 소비, 방어적 `slice(0,6)`; 백엔드가 이미 `expiresAt ASC` 정렬. 복사 버튼 `navigator.clipboard.writeText(code)` + 클릭 행 라벨 ~2초 '복사됨!' 토글(D-03). `npm run build` 그린. |
| 3 | 만료 쿠폰은 공개 `GET /api/coupons`에서 자동 제외되고, 없거나 로딩 실패해도 대시보드가 정직히 표시한다 | ✓ 검증됨 | `CouponController`가 `findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc(LocalDate.now(Asia/Seoul))`로 미만료만 반환(D-01, 당일 포함). `CouponControllerIT`가 유효 2(today/today+10) + 만료 1(어제) 시드 → 유효 2만 임박순 반환·만료 제외 증명. `CouponSection`은 `useNews`와 독립된 자체 `AsyncBoundary`(로딩/빈/에러) → COUPON-03. |
| 4 | (Core Value 가드) 수집·가격 캐시·event-impact 로직 0줄, 프론트 로아 직접 호출 0 | ✓ 검증됨 | Phase 전체 diff(`5fb23ed..HEAD`)가 쿠폰 도메인 10파일 + 프론트 6파일만 변경. `collect`/`read`/`impact`/`config` + V1–V4 마이그레이션 + SecurityConfig **diff 0줄**. 쿠폰 프론트 코드에 로아 직접 호출 없음(`/api/`만 소비). |

**점수:** 4/4 성공 기준 검증됨

### 요구사항 추적성

| 요구사항 | 상태 | 증거 |
|----------|------|------|
| COUPON-01 (관리자 CRUD·PostgreSQL 영속) | ✓ 완료 | 17.3-01(admin API) + 17.3-02(CouponSection UI). AdminCouponControllerIT 9/9. |
| COUPON-02 (대시보드 미만료·임박순·≤6·원클릭 복사) | ✓ 완료 | 17.3-03 NewsPanel 쿠폰 섹션 + clipboard 복사. |
| COUPON-03 (만료 자동 제외·정직한 표시) | ✓ 완료 | CouponControllerIT 만료 제외 + 독립 AsyncBoundary. |

### 필수 산출물

| 산출물 | 상태 | 상세 |
|--------|------|------|
| `domain/Coupon.java` | ✓ 존재 + 실질적 | date-only `LocalDate expiresAt`(D-01), @PrePersist/@PreUpdate, full-replace `replace(...)` |
| `db/migration/V5__add_coupon.sql` | ✓ 존재 + 실질적 | `coupon`(expires_at DATE), code dedup 인덱스 없음(D-04), PostgreSQL 타입 |
| `repository/CouponRepository.java` | ✓ 존재 + 실질적 | 공개 미만료·임박순 + admin 전체·임박순 파생 쿼리 |
| `web/dto/Coupon{Request,Response}.java` | ✓ 존재 + 실질적 | 엔티티 미바인딩 요청(3필드 검증) + `from(Coupon)` 응답 |
| `admin/AdminCouponService.java` | ✓ 존재 + 실질적 | create/list/replace(404)/delete(404), 타임스탬프 미할당 |
| `web/admin/AdminCouponController.java` | ✓ 존재 + 실질적 | `/api/admin/coupons` CRUD, X-Admin-Secret 게이트(무변경) |
| `web/CouponController.java` | ✓ 존재 + 실질적 | 공개 `/api/coupons` KST 미만료·임박순 |
| `lib/{schemas,api,queries}.ts` | ✓ 존재 + 실질적 | couponSchema(date-only) + getCoupons/admin CRUD fn + useCoupons·admin 훅(양쪽 키 invalidate) |
| `features/admin/CouponSection.tsx` | ✓ 존재 + 실질적 | 인라인 CRUD(type=date, D-01) + Table + 2단계 삭제 + AsyncBoundary |
| `features/dashboard/NewsPanel.tsx` | ✓ 존재 + 실질적 | 최상단 쿠폰 섹션 + 원클릭 복사(D-03) + 독립 AsyncBoundary |

## 자동 검증 결과

- **백엔드:** `./gradlew build` 그린 — 전체 스위트 통과(신규 `AdminCouponControllerIT` 9 + `CouponControllerIT` 1 포함, Testcontainers Postgres/Redis). 이전 phase IT 전체 회귀 그린 → **cross-phase 회귀 0**.
- **프론트:** `cd frontend && npm run build`(`tsc -b && vite build`) 그린 — 타입/빌드 통과.
- **스키마 드리프트:** `verify.schema-drift 17.3` → `drift_detected:false`(Flyway V5는 앱/테스트 기동 시 적용, 별도 push ORM 아님).

## 수동 검증 권장 (비블로킹)

- 실 브라우저에서 관리자 콘솔 쿠폰 등록 → 대시보드 뉴스 패널 최상단 노출 → **복사 버튼 클릭 시 clipboard 기록 + '복사됨!' ~2초 토글** 확인(보안 컨텍스트 clipboard API는 자동 빌드로 재현 불가). 실패 시에도 크래시 없음은 try/catch로 코드상 보장.

## 결론

Phase 17.3의 4개 성공 기준과 요구사항 COUPON-01~03이 코드 + IT로 모두 충족되고, Core Value 가드(수집/캐시/event-impact·V1–V4·SecurityConfig 0줄)와 프론트 로아 직접 호출 0이 diff로 확인됨. **status: passed.**

---
*Phase: 17.3-coupon-admin*
*Verified: 2026-07-07*
