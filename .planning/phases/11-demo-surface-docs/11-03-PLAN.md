---
phase: 11-demo-surface-docs
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - frontend/README.md
  - README.md
  - frontend/.env.example
autonomous: true
requirements: [DEMO-01, DEMO-02]

must_haves:
  truths:
    - "**루트 README = 짧은 포인터, `frontend/README` = 상세 일원화(D-05).** 루트 `README.md`에는 '프론트 데모' 섹션(실행 순서 3단계 요약 + 대표 스크린샷 1장 + `frontend/README` 링크)만 **추가**하고 상세는 `frontend/README`로 모은다(단일 진실 원천·중복 최소). **기존 §'데모 — curl + 샘플 JSON' 섹션은 손대지 않고 그대로 유지한다(회귀 금지 — ROADMAP 완료조건 3·검증 항목).** 루트 README는 'curl로도, 브라우저로도' 두 길을 제시한다(프론트 데모는 대체가 아니라 추가)."
    - "**`frontend/README`는 재현 핵심 + 프록시 설명까지 간결하게(D-06).** 포함: 사전조건(`SPRING_PROFILES_ACTIVE=seed`로 백엔드 기동) → `npm install && npm run dev` → 3화면 설명 + 스크린샷(상대경로 `docs/screenshots/*.png`) + **Vite 프록시 동작 1문단**(왜 백엔드 무변경으로 `/api`가 동일 출처로 보이는지 — `vite.config.ts`의 `server.proxy['/api']`→`:8080`, `VITE_API_TARGET` override). KST 표시 정직성('시각은 KST, 정렬·계산은 UTC' — Phase 7 D-09)을 한 줄 서술. 트러블슈팅·`npm run build` 상세는 최소/생략(정적 서빙 v2이므로 build 강조 불요)."
    - "**정적 서빙(DEMO-03)은 v2로 미룬 스코프 결정을 정직히 기록한다(D-01).** README/문서에 '단일 출처 정적 서빙(Spring `resources/static`)은 v2 후보'임을 **한 줄로** 남긴다 — '못 해서'가 아니라 '범위 결정'(과대 약속 회피). 데모 재현은 'seed 백엔드 + `npm run dev`'(이미 동작)로 충분함을 명시한다. ROADMAP 완료조건 5(정적 서빙)는 이번 **의도된 미충족**임을 문서가 흐리지 않는다."
    - "**DEMO-01 재현이 문서대로 정확하다(재현 검증).** `frontend/README`의 실행 명령이 `frontend/package.json`의 실제 스크립트(`dev`/`build`/`preview`)와 일치하고, 스크린샷 상대경로가 11-02의 확정 파일명(`docs/screenshots/{dashboard,item-timeline,event-impact}.png`)을 가리키며, `cd frontend && npm run build`(tsc 포함)가 통과한다. 리뷰어가 README만 따라 클린 클론에서 3화면을 비어있지 않게 재현하는 '5분' 경로의 명령·경로가 검증된다(실제 클린 클론 human walkthrough는 verify-phase/UAT)."
    - "**정직성 카피 일관(honest-data 에토스).** README 카피는 상관≠인과(이벤트가 가격을 올렸다고 단정하지 않음)와 seed 합성 데이터(실데이터 아님)임을 명시한다(PROJECT 전제). `frontend/.env.example`의 `VITE_API_TARGET` 프록시 override 예시를 `frontend/README`의 환경 변수 안내와 연결해 커밋한다(현재 워킹트리 미커밋 1줄 정리)."
  artifacts:
    - path: "frontend/README.md"
      provides: "프론트 데모 상세 단일 출처(D-06): 사전조건·실행 명령·3화면·스크린샷·Vite 프록시·KST 정직성·정적서빙 v2 한 줄(D-01)"
      contains: "npm run dev"
    - path: "README.md"
      provides: "루트 README에 '프론트 데모' 포인터 섹션 추가(D-05) — 기존 curl 섹션 보존, frontend/README 링크 + 대표 스크린샷"
      contains: "frontend/README"
    - path: "frontend/.env.example"
      provides: "VITE_API_TARGET 프록시 override 예시 — frontend/README 환경 변수 안내의 코드 근거(미커밋 1줄 정리)"
      contains: "VITE_API_TARGET"
  key_links:
    - from: "frontend/README.md"
      to: "frontend/vite.config.ts"
      via: "프록시 설명 문단이 server.proxy['/api']→:8080 / VITE_API_TARGET override를 코드 근거로 서술(백엔드 무변경 동일출처)"
      pattern: "proxy"
    - from: "frontend/README.md"
      to: "frontend/docs/screenshots/README.md"
      via: "스크린샷 상대경로(docs/screenshots/*.png)가 11-02 확정 파일명을 그대로 참조 — 슬롯/링크 일치"
      pattern: "screenshots"
    - from: "README.md"
      to: "frontend/README.md"
      via: "루트 '프론트 데모' 섹션이 상세를 frontend/README로 위임(D-05 단일 진실 원천) — 기존 curl 섹션은 무변경 보존"
      pattern: "frontend/README"
---

<objective>
포트폴리오 데모의 **문서 표면(DEMO-02)** 과 **재현 검증(DEMO-01)** 을 잠근다. 신규 `frontend/README`에 재현 핵심(seed 백엔드 → `npm run dev` → 3화면 + 스크린샷 + 프록시 설명)을 일원화하고, 루트 README에는 짧은 '프론트 데모' 포인터 섹션을 **추가**하되 **기존 curl 데모 섹션은 회귀 없이 보존**한다(D-05). 정적 서빙(DEMO-03)을 v2로 미룬 결정은 정직히 한 줄 남긴다(D-01).

엔지니어링 신호는 **정직한 재현성**이다: 문서의 실행 명령이 `package.json`과 실제로 일치하고, 스크린샷 경로가 11-02 확정 파일명을 가리키며, `npm run build`가 통과해 '5분 재현'(ROADMAP 사용자 확인 포인트)이 빈말이 아님을 검증한다. 프록시 설명은 '왜 백엔드 0줄 변경으로 `/api`가 동일 출처로 보이는지'를 코드 근거(`vite.config.ts`)로 서술한다.

Output: `frontend/README.md`(신규), 루트 `README.md`(프론트 데모 섹션 추가·curl 보존), `frontend/.env.example`(VITE_API_TARGET 정리).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-demo-surface-docs/11-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- 루트 README.md 현재 구조(무변경 보존 대상) — 섹션 헤딩:
     "한눈에 — 3단계 재현" / "아키텍처" / "설계 결정 & 트레이드오프" / "로컬 실행"
     / "데모 — curl + 샘플 JSON"(★회귀 금지) / "API 한계 / 스코프" / "데이터 보존 정책" / "기술 스택 & 문서"
     → '프론트 데모' 섹션은 curl 섹션을 건드리지 않고 추가(삽입 위치는 Claude's Discretion). -->

<!-- frontend/package.json scripts: dev / build(tsc -b && vite build) / preview / lint
     frontend/vite.config.ts: server.proxy['/api'] → http://localhost:8080, VITE_API_TARGET 로 override
     frontend/.env.example: VITE_API_TARGET 예시(워킹트리 미커밋 1줄) -->

<!-- 스크린샷 상대경로(11-02 확정): frontend/README 기준 docs/screenshots/{dashboard,item-timeline,event-impact}.png
     루트 README 기준 frontend/docs/screenshots/dashboard.png (대표 1장) -->

<!-- 라우트: Dashboard "/" · Item Timeline "/timeline" · Event Impact "/impact" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/README.md 작성 (재현 핵심 + 프록시 설명)</name>
  <files>frontend/README.md, frontend/.env.example</files>
  <read_first>frontend/package.json, frontend/vite.config.ts, frontend/.env.example, frontend/docs/screenshots/README.md, .planning/phases/07-frontend-foundation/07-CONTEXT.md, .planning/phases/11-demo-surface-docs/11-CONTEXT.md</read_first>
  <action>`frontend/README.md`를 신규 작성한다(D-06, 간결하게). 섹션 순서·헤딩·문구 톤은 Claude's Discretion이되 다음을 포함:
1) **사전조건** — `SPRING_PROFILES_ACTIVE=seed`로 백엔드 기동(루트 README의 seed 기동 명령 재사용·참조). seed = 합성 8일치 시세 + 데모 이벤트, **API 키 불필요**.
2) **실행** — `cd frontend && npm install && npm run dev` → Vite dev 서버(기본 5173). 명령은 `package.json` 실제 스크립트와 일치시킨다.
3) **3화면 설명 + 스크린샷** — Dashboard(`/`)·Item Timeline(`/timeline`)·Event Impact(`/impact`) 각 1~2줄 설명 + 상대경로 이미지 임베드 `![alt](docs/screenshots/dashboard.png)` 등(11-02 확정 파일명·alt 사용). PNG 미존재 시에도 마크다운 슬롯은 유지(사용자가 채움).
4) **Vite 프록시 동작(1문단)** — `vite.config.ts`의 `server.proxy['/api']`→`localhost:8080`로 브라우저가 `/api/*`를 동일 출처로 인식 → CORS 불필요·**백엔드 0줄 변경**. `VITE_API_TARGET`로 타깃 override 가능(`.env.example` 참조).
5) **정직성 한 줄들** — 시각은 KST 표시·정렬/계산은 UTC(Phase 7 D-09); seed는 합성 데이터(실데이터 아님); 변화율은 상관≠인과; **단일 출처 정적 서빙은 v2 후보**(D-01, '범위 결정'이라 명시).
트러블슈팅·`npm run build` 상세는 최소/생략. 마지막으로 `frontend/.env.example`의 `VITE_API_TARGET` 예시 줄을 확정(워킹트리 미커밋 1줄 정리)해 README 환경변수 안내와 일치시킨다.</action>
  <verify>
    <automated>cd frontend && test -f README.md && grep -q "npm run dev" README.md && grep -qE "SPRING_PROFILES_ACTIVE=seed|seed" README.md && grep -q "docs/screenshots/" README.md && grep -qiE "proxy|프록시" README.md && grep -q "VITE_API_TARGET" README.md && grep -q "VITE_API_TARGET" .env.example && npm run build && echo "frontend README OK"</automated>
  </verify>
  <done>frontend/README.md가 사전조건·실행 명령·3화면+스크린샷 상대경로·프록시 설명·정직성(KST/seed/상관≠인과/정적서빙 v2)을 담고, .env.example의 VITE_API_TARGET이 정리됐으며 build 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 루트 README에 '프론트 데모' 포인터 섹션 추가 (curl 섹션 보존)</name>
  <files>README.md</files>
  <read_first>README.md, frontend/README.md, frontend/docs/screenshots/README.md</read_first>
  <action>루트 `README.md`에 '프론트 데모'(또는 '브라우저 데모') 섹션을 **추가**한다(D-05, 삽입 위치는 Claude's Discretion — 예: 기존 §'데모 — curl + 샘플 JSON' 앞/뒤). 내용은 **짧은 포인터**:
1) 실행 순서 3단계 요약 — (1) seed 프로파일 백엔드 기동, (2) `cd frontend && npm install && npm run dev`, (3) 브라우저에서 Dashboard/Item Timeline/Event Impact 3화면.
2) 대표 스크린샷 1장 — `![...](frontend/docs/screenshots/dashboard.png)`(루트 기준 상대경로, 11-02 확정 파일명).
3) 상세 위임 링크 — '자세한 실행·프록시 설명은 [`frontend/README`](frontend/README.md)' (단일 진실 원천).
4) 한 줄 정직성 — 'curl로도, 브라우저로도 동일한 read API를 본다 / seed 합성 데이터'.
**기존 §'데모 — curl + 샘플 JSON'(및 그 하위 1~4·그 외 엔드포인트)·§'한눈에'·§'로컬 실행'은 절대 수정·삭제하지 않는다**(회귀 금지). 추가만 하고 기존 텍스트는 보존.</action>
  <verify>
    <automated>cd /c/dev/project/test && grep -q "데모 — curl + 샘플 JSON" README.md && grep -qE "프론트 데모|브라우저 데모" README.md && grep -q "frontend/README" README.md && grep -q "frontend/docs/screenshots/" README.md && echo "root README pointer added + curl preserved"</automated>
  </verify>
  <done>루트 README에 프론트 데모 포인터 섹션(3단계 요약 + 대표 스크린샷 + frontend/README 링크)이 추가되고, 기존 curl/샘플 JSON 섹션이 그대로 남아있다.</done>
</task>

<task type="auto">
  <name>Task 3: DEMO-01 재현 정확성 검증 (문서 dry-run)</name>
  <files>frontend/README.md, README.md</files>
  <read_first>frontend/package.json, frontend/README.md, README.md, frontend/docs/screenshots/README.md, .planning/ROADMAP.md</read_first>
  <action>문서가 실제로 따라 할 수 있는지 dry-run 검증한다(실제 클린 클론 human walkthrough는 verify-phase/UAT로 이월):
1) `frontend/README`/루트 README의 실행 명령이 `frontend/package.json`의 실제 스크립트명과 정확히 일치하는지 대조(`npm run dev`/`build`/`preview`).
2) 스크린샷 상대경로가 11-02 확정 파일명(`docs/screenshots/{dashboard,item-timeline,event-impact}.png` / 루트 기준 `frontend/...`)을 가리키는지, 깨진 경로가 없는지 확인.
3) `cd frontend && npm run build`(tsc 포함)가 통과하는지 확인(타입 회귀 차단).
4) 루트 README의 기존 curl 섹션이 무변경인지 회귀 확인(§'데모 — curl + 샘플 JSON' 존재).
5) SUMMARY에 **'5분 재현' 체크리스트**(seed 기동 명령·dev 명령·3화면 URL·스크린샷 존재여부)를 남겨 사용자 UAT walkthrough가 그대로 따라가게 한다. ROADMAP 완료조건 5(정적 서빙)는 D-01로 **의도된 미충족**임을 SUMMARY에 명시.</action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "docs/screenshots/dashboard.png" README.md && cd /c/dev/project/test && grep -q "데모 — curl + 샘플 JSON" README.md && grep -q "frontend/README" README.md && echo "DEMO-01 reproduction docs verified"</automated>
  </verify>
  <done>실행 명령이 package.json과 일치하고 스크린샷 경로가 확정 파일명을 가리키며 npm run build 통과·curl 섹션 무변경이 확인됐고, SUMMARY에 5분 재현 체크리스트 + 정적서빙 v2 미충족 기록이 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/문서 콘텐츠 → 공개 레포 | 문서에 명령·예시 환경변수가 들어간다. 실제 시크릿(LOSTARK_API_KEY·X-Admin-Secret) 값이 평문으로 들어가면 노출이 된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | `frontend/.env.example`·README 명령 예시 | mitigate | `.env.example`은 **예시 값/플레이스홀더만**(VITE_API_TARGET=http://localhost:8080 류) — 실제 키 금지. README는 'seed 프로파일은 API 키 불필요', '실제 비밀값은 절대 커밋하지 않음'(루트 README 기존 카피와 일관)을 유지. 실 시크릿 평문 미포함을 작성 시 확인. |
| T-11-03-02 | Spoofing | 과대주장 카피 | accept | honest-data 에토스에 따라 상관≠인과·seed 합성 데이터·정적서빙 v2를 정직히 명시 — 데모 표면이 백엔드 능력을 과대표현하지 않는다(보안보다 신뢰성 항목이나 문서 무결성으로 기록). |

**고(high) 심각도 위협 없음** — 문서/예시 설정 파일만 추가·정리하며 신규 실행 경로·시크릿을 도입하지 않는다(security_block_on: high 게이트 통과).
</threat_model>

<verification>
- `frontend/README.md`가 사전조건(seed 기동)·실행 명령(`npm install && npm run dev`)·3화면+스크린샷 상대경로·Vite 프록시 설명·정직성(KST/seed/상관≠인과/정적서빙 v2 한 줄, D-01)을 담는다.
- 루트 `README.md`에 프론트 데모 포인터 섹션(3단계 + 대표 스크린샷 + `frontend/README` 링크)이 추가되고, **기존 §'데모 — curl + 샘플 JSON' 섹션이 무변경 보존**된다(회귀 없음).
- 실행 명령이 `frontend/package.json` 스크립트와 일치하고 스크린샷 경로가 11-02 확정 파일명을 가리킨다. `cd frontend && npm run build`가 타입 에러 0으로 통과한다.
- `frontend/.env.example`의 `VITE_API_TARGET` 예시가 정리되고 실 시크릿 평문이 없다.
- SUMMARY에 '5분 재현' 체크리스트와 ROADMAP 완료조건 5(정적 서빙) 의도된 미충족(D-01) 기록이 있다.
- Java `src/`·`frontend/src/` 무변경(문서/설정 전용 plan).

## 회귀 안전 (기존 기능 보존)
- 루트 README는 **추가만** 하고 기존 curl/샘플 JSON·한눈에·로컬 실행 섹션을 보존한다(grep 회귀 검증). 소스 코드 무변경이라 앱 동작에 영향이 없고, `.env.example`은 예시 값만 정리해 런타임에 영향이 없다.
</verification>

<success_criteria>
- DEMO-02 문서 축이 잠긴다: frontend/README 상세 일원화 + 루트 README 포인터(D-05/D-06), curl 섹션 회귀 없음.
- DEMO-01 재현이 문서대로 정확함이 dry-run으로 검증된다(명령 일치·경로 유효·build green), 5분 재현 UAT 체크리스트 준비.
- 정적 서빙(DEMO-03) v2 연기가 정직히 기록된다(D-01) — 과대 약속 없음.
</success_criteria>

<output>
Create `.planning/phases/11-demo-surface-docs/11-03-SUMMARY.md` when done.
</output>
