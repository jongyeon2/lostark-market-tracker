---
phase: 17.3-coupon-admin
plan: 02
type: execute
wave: 2
depends_on: [17.3-01]
files_modified:
  - frontend/src/lib/schemas.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/queries.ts
  - frontend/src/features/admin/CouponSection.tsx
  - frontend/src/features/admin/AdminConsolePage.tsx
autonomous: true
requirements: [COUPON-01]

must_haves:
  truths:
    - "COUPON-01(UI): 관리자 콘솔에 `CouponSection`이 있어 목록·등록·수정·삭제가 동작 — `EventSection` 레이아웃(인라인 create/edit 폼 + Table + 인라인 2단계 삭제 확인 + 피드백 카피 + AsyncBoundary)을 미러링하고, admin mutation 패턴(adminRequest X-Admin-Secret 자동첨부·onSuccess invalidate·401 auto-logout)을 재사용한다"
    - "D-01: 쿠폰 만료일 입력은 `<Input type=\"date\">`(이벤트의 `datetime-local`과 다름) — 값은 \"YYYY-MM-DD\" 문자열 그대로 백엔드 expiresAt로 전송, KST↔UTC 변환 없음(`kstLocalToUtcIso` 미사용). 날짜만·시간대 변환 없는 정직성"
    - "쿠폰 데이터 계층은 read 3단(zod `couponSchema` → api `getCoupons`/admin CRUD fn → queries `useCoupons`/admin 훅) — 기존 5개 read DTO + 이벤트 admin 훅 패턴 준수, zod boundary loud-fail(비2xx/스키마 불일치 throw)"
    - "백엔드 0줄 — 이 플랜은 frontend/만 수정. 프론트는 백엔드 `/api/coupons`·`/api/admin/coupons`만 소비(로아 직접 호출 없음, 상시 가드). 신규 의존성 0(React Query/zod/기존 ui 프리미티브 재사용)"
  artifacts:
    - path: "frontend/src/features/admin/CouponSection.tsx"
      provides: "관리자 콘솔 쿠폰 CRUD UI(목록+등록/수정 폼+삭제, type=date) (COUPON-01)"
    - path: "frontend/src/lib/queries.ts"
      provides: "useCoupons(공개 read 훅, 17.3-03 소비) + useAdminCoupons/useCreateCoupon/useReplaceCoupon/useDeleteCoupon"
  key_links:
    - from: "getCoupons()/admin 쿠폰 fn → /api/coupons·/api/admin/coupons"
      to: "CouponController/AdminCouponController(17.3-01)"
      via: "read/write DTO 계약 소비(zod couponSchema boundary)"
      pattern: "/api/coupons"
    - from: "CouponSection admin mutation"
      to: "AdminConsolePage 배치 + ['admin-coupons']/['coupons'] invalidate"
      via: "EventSection/useCreateEvent admin mutation 패턴 재사용"
      pattern: "CouponSection"
---

<objective>
프론트 쿠폰 데이터 계층(read + admin)과 관리자 콘솔 쿠폰 섹션을 구현한다 — `schemas.ts`에 `couponSchema`/`AdminCouponRequest`, `api.ts`에 공개 `getCoupons()` + admin CRUD fn(`getAdminCoupons`/`createAdminCoupon`/`replaceAdminCoupon`/`deleteAdminCoupon`), `queries.ts`에 공개 `useCoupons()`(17.3-03 대시보드가 소비) + admin 훅을 추가하고, `EventSection`을 미러링한 `CouponSection`을 `AdminConsolePage`에 배치한다. 만료일 입력은 `type="date"`(D-01) — KST 변환 없이 "YYYY-MM-DD" 그대로 전송.

Purpose: 관리자가 콘솔에서 쿠폰을 full CRUD 하도록 하고(COUPON-01), 대시보드(17.3-03)가 소비할 공개 read 훅 `useCoupons`를 같은 데이터 계층에 함께 두어 lib 파일 충돌을 없앤다.

Output: `couponSchema`/`AdminCouponRequest` + 공개/admin api fn + `useCoupons`/admin 훅 + `CouponSection.tsx` + `AdminConsolePage` 배치. 백엔드 diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.3-coupon-admin/17.3-CONTEXT.md
@.planning/phases/17.3-coupon-admin/17.3-SPEC.md
@CLAUDE.md

<interfaces>
<!-- frontend/src/lib/schemas.ts: 기존 gameEventResponseSchema/AdminEventRequest 패턴. ★ 추가: couponSchema = z.object({ id:z.number(), code:z.string(), reward:z.string(), expiresAt:z.string()(백엔드 LocalDate "YYYY-MM-DD" — Z 오프셋 없는 날짜 문자열, formatKst 금지·slice 표시), createdAt:z.string(), updatedAt:z.string() }); couponsSchema = z.array(couponSchema); type Coupon = z.infer<...>; type AdminCouponRequest = { code:string; reward:string; expiresAt:string }(3필드만 — id/타임스탬프는 백엔드 스탬프). NewsResponse 날짜 주석처럼 "expiresAt은 date-only 문자열, 표시는 slice(0,10)"을 명시. -->
<!-- frontend/src/lib/api.ts: 공개는 request(fetch+zod), admin은 adminRequest(X-Admin-Secret 자동첨부·401 auto-logout). ★ 추가(이벤트 fn 미러): getCoupons()=couponsSchema.parse(await request('/api/coupons')); getAdminCoupons()=couponsSchema.parse(adminRequest('/api/admin/coupons',{method:'GET'})); createAdminCoupon(body:AdminCouponRequest)=couponSchema.parse(adminRequest(...,{method:'POST',body})); replaceAdminCoupon(id,body)=couponSchema.parse(adminRequest(`/api/admin/coupons/${id}`,{method:'PUT',body})); deleteAdminCoupon(id)=await adminRequest(`/api/admin/coupons/${id}`,{method:'DELETE'}) (204). -->
<!-- frontend/src/lib/queries.ts: 기존 useNews(공개 read, staleTime) + ADMIN_EVENTS_KEY/useAdminEvents/useCreateEvent/... 패턴. ★ 추가: const COUPONS_KEY=['coupons']; useCoupons()=useQuery({queryKey:COUPONS_KEY, queryFn:getCoupons, staleTime:5*60*1000})(대시보드 소비). const ADMIN_COUPONS_KEY=['admin-coupons']; useAdminCoupons()=useQuery(...getAdminCoupons); useCreateCoupon/useReplaceCoupon/useDeleteCoupon mutation — onSuccess는 ['admin-coupons']와 ['coupons']를 **둘 다** invalidate(관리자 변경이 대시보드에도 반영). ★ COUPONS_KEY는 CouponSection과 NewsPanel(17.3-03)이 공유하므로 이 플랜에서 정의. -->
<!-- frontend/src/features/admin/EventSection.tsx(원형 — 그대로 미러): 인라인 create/edit 폼 + feedback + AsyncBoundary(status/isEmpty/onRetry/emptyHeading/emptyBody/errorMessage) + Table + 인라인 2단계 삭제 확인(confirmingDeleteId). useAdminEvents/useCreateEvent/useReplaceEvent/useDeleteEvent 소비. ★ CouponSection 차이: 필드 = code(Input)·reward(Input 또는 Textarea)·expiresAt(<Input type="date">, KST 변환 없음 — D-01). 목록 컬럼 = 코드·보상·만료일(slice YYYY.MM.DD)·관리. 이벤트 유형 Select/발생시각 datetime-local 없음. -->
<!-- frontend/src/features/admin/AdminConsolePage.tsx: <main>에 <EventSection/><WatchlistSection/><CollectionSection/> 스택. ★ <CouponSection/>을 추가(배치 지점 — EventSection 옆). 셸 헤더/레이아웃 무변경. -->
<!-- 재사용(무변경 소비): frontend/src/components/ui/{button,input,card,table,textarea}.tsx, frontend/src/components/state/AsyncBoundary.tsx, adminRequest(api.ts). 신규 의존성 0. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: couponSchema/AdminCouponRequest(zod) + api fn(공개+admin) + queries 훅</name>
  <read_first>
    - frontend/src/lib/schemas.ts (gameEventResponseSchema/AdminEventRequest·NewsResponse 날짜 주석 패턴 — 추가 지점)
    - frontend/src/lib/api.ts (request/adminRequest + 이벤트 admin CRUD fn — 추가 지점)
    - frontend/src/lib/queries.ts (useNews·ADMIN_EVENTS_KEY·useCreateEvent/... 훅 패턴 — 추가 지점)
  </read_first>
  <files>frontend/src/lib/schemas.ts, frontend/src/lib/api.ts, frontend/src/lib/queries.ts</files>
  <action>
    schemas.ts에 `couponSchema`(id:number, code:string, reward:string, expiresAt:string[백엔드 LocalDate "YYYY-MM-DD" — date-only 문자열, 표시는 slice], createdAt:string, updatedAt:string), `couponsSchema=z.array(couponSchema)`, `type Coupon`, `type AdminCouponRequest={code:string;reward:string;expiresAt:string}`를 기존 export 패턴으로 추가(expiresAt이 UTC instant가 아닌 date-only 문자열임을 NewsResponse 주석 스타일로 명시). api.ts에 `getCoupons()`(request '/api/coupons' → couponsSchema.parse)와 admin CRUD `getAdminCoupons`/`createAdminCoupon`/`replaceAdminCoupon`/`deleteAdminCoupon`(adminRequest + couponSchema.parse, DELETE는 204 파싱 없음)을 이벤트 fn 미러로 추가. queries.ts에 `const COUPONS_KEY=['coupons']` + `useCoupons()`(useQuery queryFn:getCoupons, staleTime 5분 — 대시보드 소비)와 `const ADMIN_COUPONS_KEY=['admin-coupons']` + `useAdminCoupons`/`useCreateCoupon`/`useReplaceCoupon`/`useDeleteCoupon`(mutation, onSuccess는 ['admin-coupons']·['coupons'] 둘 다 invalidate)를 추가. 프론트는 백엔드 엔드포인트만 호출(로아 직접 호출 없음). 백엔드 파일은 건드리지 않는다.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - schemas.ts에 couponSchema(+couponsSchema)·Coupon·AdminCouponRequest가 date-only expiresAt으로 정의된다
    - api.ts에 getCoupons + getAdminCoupons/createAdminCoupon/replaceAdminCoupon/deleteAdminCoupon이 request/adminRequest·zod boundary로 추가된다
    - queries.ts에 useCoupons(['coupons']) + useAdminCoupons/useCreateCoupon/useReplaceCoupon/useDeleteCoupon(onSuccess 둘 다 invalidate)이 추가된다
    - `cd frontend && npm run build` 그린, 백엔드 diff 0줄
  </acceptance_criteria>
  <done>쿠폰 read/admin 데이터 계층(zod·api·queries)이 이벤트 패턴대로 추가되고 빌드가 그린이다.</done>
</task>

<task type="auto">
  <name>Task 2: CouponSection.tsx (인라인 CRUD 폼 + Table + 삭제 확인, type=date)</name>
  <read_first>
    - frontend/src/features/admin/EventSection.tsx (인라인 폼·2단계 삭제·feedback·AsyncBoundary·admin mutation 소비 원형 — 그대로 미러)
    - frontend/src/lib/queries.ts (Task 1 useAdminCoupons/useCreateCoupon/useReplaceCoupon/useDeleteCoupon — 소비)
    - frontend/src/components/state/AsyncBoundary.tsx (status/isEmpty/onRetry props)
    - frontend/src/components/ui/input.tsx (type 속성 passthrough — type="date" 확인)
  </read_first>
  <files>frontend/src/features/admin/CouponSection.tsx</files>
  <action>
    `CouponSection`을 EventSection 미러로 구현한다: `useAdminCoupons`+`useCreateCoupon`+`useReplaceCoupon`+`useDeleteCoupon` 소비. 인라인 create/edit 폼 = `code`(Input), `reward`(Input 또는 Textarea), `expiresAt`(**`<Input type="date">`** — 값 "YYYY-MM-DD" 그대로, `kstLocalToUtcIso` 미사용, D-01). 제출 시 `AdminCouponRequest={code:code.trim(), reward:reward.trim(), expiresAt}` — editingId 있으면 replace, 없으면 create; onSuccess 피드백('쿠폰을 등록/변경했어요')·resetForm, onError 피드백. `startEdit(coupon)`는 폼에 code/reward/expiresAt(그대로) 프리필. 목록 Table 컬럼 = 코드·보상·만료일(`expiresAt.slice(0,10).replace(/-/g,'.')` YYYY.MM.DD)·관리; 정렬은 만료임박순(expiresAt asc). 각 행 수정/삭제(인라인 2단계 confirmingDeleteId 확인). 로딩/빈/에러는 AsyncBoundary(emptyHeading '등록된 쿠폰이 없어요' 등, errorMessage는 EventSection의 CONNECTION_ERROR 문구 재사용). Card 헤더 제목 '쿠폰'. 신규 의존성 0.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "useAdminCoupons" src/features/admin/CouponSection.tsx && grep -q "type=\"date\"" src/features/admin/CouponSection.tsx && grep -q "AsyncBoundary" src/features/admin/CouponSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CouponSection이 useAdminCoupons/useCreateCoupon/useReplaceCoupon/useDeleteCoupon을 소비하고 인라인 create/edit + 2단계 삭제 확인 + feedback + AsyncBoundary를 갖춘다
    - 만료일 입력이 `<Input type="date">`이고 값이 KST 변환 없이 "YYYY-MM-DD"로 전송된다(D-01)
    - 목록이 코드·보상·만료일(YYYY.MM.DD)·관리 컬럼을 만료임박순으로 표시한다
    - `cd frontend && npm run build` 그린
  </acceptance_criteria>
  <done>type=date 인라인 CRUD 폼·목록·삭제 확인을 갖춘 CouponSection이 빌드 그린으로 완성된다.</done>
</task>

<task type="auto">
  <name>Task 3: AdminConsolePage에 CouponSection 배치</name>
  <read_first>
    - frontend/src/features/admin/AdminConsolePage.tsx (섹션 스택 셸 — 배치 대상)
    - frontend/src/features/admin/CouponSection.tsx (Task 2 산출 — 배치)
  </read_first>
  <files>frontend/src/features/admin/AdminConsolePage.tsx</files>
  <action>
    AdminConsolePage `<main>`의 섹션 스택에 `<CouponSection/>`을 추가한다(import 포함, EventSection 옆 배치 — 예: EventSection 다음). 셸 헤더('관리자 콘솔'/'로그아웃')·`max-w-3xl`·`space-y-8` 레이아웃은 무변경. 기존 EventSection/WatchlistSection/CollectionSection 회귀 없음.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "CouponSection" src/features/admin/AdminConsolePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AdminConsolePage가 <CouponSection/>을 import·렌더하고 기존 3개 섹션은 회귀 없이 유지된다
    - `cd frontend && npm run build` 그린, 백엔드 diff 0줄
  </acceptance_criteria>
  <done>관리자 콘솔에 쿠폰 섹션이 배치되어 등록→목록 반영·수정·삭제가 동작하고 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 콘솔 → /api/admin/coupons | adminRequest가 X-Admin-Secret을 sessionStorage에서 자동 첨부, 401 시 전역 auto-logout(D-03 15-02) |
| 백엔드 응답 → 프론트 렌더 | zod couponSchema.parse boundary — 비2xx/스키마 불일치 throw → ErrorState(silent empty 금지) |
| 사용자 입력(code/reward) → 렌더 | React 기본 이스케이프 — dangerouslySetInnerHTML 미사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1732-01 | Spoofing | 시크릿 없이 admin 쿠폰 쓰기 | mitigate | adminRequest X-Admin-Secret 자동첨부 + 백엔드 게이트(17.3-01) — 401 시 auto-logout |
| T-1732-02 | Information Disclosure | 시크릿 값이 로그/에러 메시지에 유입 | mitigate | ApiError는 status+path만, 시크릿 미기재(기존 api.ts 계약) |
| T-1732-03 | Tampering | 렌더 XSS(code/reward) | accept | React 기본 이스케이프 + zod 파싱 문자열, dangerouslySetInnerHTML 미사용 |
| T-1732-04 | Tampering | 프론트가 로아 API 직접 호출 | mitigate | 백엔드 /api/coupons·/api/admin/coupons만 소비(상시 가드) |
| T-1732-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — React Query/zod/기존 ui 프리미티브 재사용 |
</threat_model>

<verification>
- schemas.ts couponSchema/AdminCouponRequest(date-only expiresAt) + api.ts getCoupons/admin CRUD fn + queries.ts useCoupons(['coupons'])·admin 훅(onSuccess 둘 다 invalidate) 추가 — `npm run build` 그린.
- CouponSection: 인라인 create/edit(type=date, KST 변환 없음 D-01) + 목록(코드·보상·만료일·관리, 임박순) + 2단계 삭제 + AsyncBoundary(COUPON-01).
- AdminConsolePage에 CouponSection 배치, 기존 섹션 회귀 0.
- 프론트는 백엔드 엔드포인트만 소비(로아 직접 호출 0), 백엔드 diff 0줄, 신규 의존성 0.
</verification>

<success_criteria>
- 관리자 콘솔 쿠폰 섹션에서 등록→목록 반영·수정·삭제가 동작한다(COUPON-01).
- 만료일은 type=date 입력으로 KST 변환 없이 "YYYY-MM-DD" 전송(D-01), 데이터 계층은 read 3단·zod boundary 준수.
- 대시보드(17.3-03)가 소비할 useCoupons(['coupons']) 공개 read 훅이 같은 계층에 준비된다 — 백엔드 0줄·신규 의존성 0.
</success_criteria>

<output>
Create `.planning/phases/17.3-coupon-admin/17.3-02-SUMMARY.md` when done
</output>
