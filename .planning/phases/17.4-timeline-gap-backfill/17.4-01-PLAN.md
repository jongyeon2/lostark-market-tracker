---
phase: 17.4-timeline-gap-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V6__add_item_daily_stats.sql
  - src/main/java/com/lostark/tracker/domain/ItemDailyStat.java
  - src/main/java/com/lostark/tracker/domain/DailyStatSource.java
  - src/main/java/com/lostark/tracker/repository/ItemDailyStatRepository.java
  - src/test/java/com/lostark/tracker/repository/ItemDailyStatRepositoryIT.java
autonomous: true
requirements: [BACKFILL-04]

must_haves:
  truths:
    - "D-05: 백필 일평균은 별도 `item_daily_stats`(tracked_item_id, stat_date, avg_price, source) 테이블에 additive 적재한다 — `price_snapshot` 스키마·수집/캐시/event-impact 로직 0줄(Core Value 가드). Flyway 다음 순번은 **V6**(V1~V5 immutable), JPA `ddl-auto=validate`가 V6 스키마로 통과"
    - "멱등 upsert: `(tracked_item_id, stat_date, source)` UNIQUE + PostgreSQL `INSERT ... ON CONFLICT DO UPDATE`로 같은 (품목·날짜·소스) 재적재는 avg_price/updated_at만 갱신 — 서버 재기동/일 1회 재실행이 중복 행을 만들지 않는다(D-03 멱등성의 저장 계층 근거)"
    - "source는 Java enum `DailyStatSource{YDAY_AVG, DETAIL_STATS}`를 `@Enumerated(STRING)`으로 저장 — 소스 2종(D-01)을 한 테이블에서 구분해 표시 계층이 지표 출처를 정직하게 라벨링할 수 있게 한다. 알 수 없는 source 문자열은 매핑 경계에서 실패(loud)"
    - "BACKFILL-04: `PriceSnapshot`·`price_snapshot` 스키마·`PriceCollector`·`LatestPriceCache`·`EventImpactService`·`WindowQueryService` 로직 0줄 — 신규 테이블/엔티티/repository만 추가하는 순수 additive 계층"
  artifacts:
    - path: "src/main/resources/db/migration/V6__add_item_daily_stats.sql"
      provides: "item_daily_stats 테이블(tracked_item_id FK·stat_date DATE·avg_price NUMERIC·source·타임스탬프) + UNIQUE(tracked_item_id, stat_date, source) (D-05)"
    - path: "src/main/java/com/lostark/tracker/domain/ItemDailyStat.java"
      provides: "일별 평균가 엔티티(BigDecimal avgPrice·LocalDate statDate·DailyStatSource source) — price_snapshot과 독립"
    - path: "src/main/java/com/lostark/tracker/repository/ItemDailyStatRepository.java"
      provides: "멱등 upsert(`upsertDailyStat` ON CONFLICT) + 윈도우 read finder(`findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc`) — 02/03이 쓰고 04가 읽는 저장 계약"
  key_links:
    - from: "ItemDailyStatRepository.upsertDailyStat(...)"
      to: "17.4-02 YDayAvgPrice 캡처 + 17.4-03 상세 Stats 소급 러너"
      via: "멱등 upsert 계약(tracked_item_id, stat_date, source, avg_price)"
      pattern: "item_daily_stats"
    - from: "ItemDailyStatRepository.findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc(...)"
      to: "17.4-04 타임라인 read 병합(PricesController)"
      via: "윈도우 일별 평균 조회 계약"
      pattern: "findByTrackedItemIdAndStatDateBetween"
    - from: "ItemDailyStat 엔티티 + V6 마이그레이션"
      to: "JPA ddl-auto=validate"
      via: "Flyway가 스키마 소유 — 엔티티 매핑과 컬럼 일치"
      pattern: "item_daily_stats"
---

<objective>
백필 일별 평균가를 담을 **독립 저장 계층**을 신규로 구현한다 — `V6__add_item_daily_stats.sql` 마이그레이션 + `ItemDailyStat` 엔티티 + `DailyStatSource` enum + `ItemDailyStatRepository`(멱등 upsert + 윈도우 read finder)를 만든다. 별도 `item_daily_stats` 테이블에 additive 적재하며 `price_snapshot` 스키마·수집/캐시/event-impact 로직은 0줄 무변경(D-05, BACKFILL-04). `(tracked_item_id, stat_date, source)` UNIQUE + `ON CONFLICT DO UPDATE`로 서버 재기동/일 1회 재실행이 멱등이 되도록 저장 계층을 설계한다(D-03의 멱등성 근거).

Purpose: 소스 2종(D-01 — YDayAvgPrice 무료 캡처·상세 Stats 재료 소급)이 공통으로 쓸 멱등 upsert 계약과, 타임라인 read(17.4-04)가 소비할 윈도우 조회 계약을 제공한다. 백필 도메인이 `price_snapshot`·수집·캐시와 자원/스키마 경쟁 없는 독립 테이블·PostgreSQL 영속임을 스키마로 보장한다.

Output: `V6` 마이그레이션·`ItemDailyStat`·`DailyStatSource`·`ItemDailyStatRepository` + upsert/read RepositoryIT. `price_snapshot`·`PriceCollector`·`LatestPriceCache`·`EventImpactService` diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.4-timeline-gap-backfill/17.4-CONTEXT.md
@.planning/phases/17.4-timeline-gap-backfill/17.4-SPIKE-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- V6__add_item_daily_stats.sql (다음 순번 — V1~V5 immutable): CREATE TABLE item_daily_stats( id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, tracked_item_id BIGINT NOT NULL REFERENCES tracked_item (id), stat_date DATE NOT NULL, avg_price NUMERIC(18,4) NOT NULL, source VARCHAR(20) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT uq_item_daily_stats UNIQUE (tracked_item_id, stat_date, source) ); + CREATE INDEX ix_item_daily_stats_item_date ON item_daily_stats (tracked_item_id, stat_date); ★ price_snapshot의 tracked_item_id FK 컨벤션 준수(V1 참조), PostgreSQL 타입만(DATE·NUMERIC·TIMESTAMPTZ·VARCHAR). avg_price는 거래 평균가라 소수(147007.4 등) → NUMERIC(18,4). 헤더 주석에 목적·D-05·additive 가드 명시. -->
<!-- DailyStatSource(com.lostark.tracker.domain) enum { YDAY_AVG, DETAIL_STATS }: YDAY_AVG=리스트 YDayAvgPrice 무료 캡처(전 품목·전일), DETAIL_STATS=상세 Stats[] 재료 14일 소급. -->
<!-- ItemDailyStat(com.lostark.tracker.domain): @Entity @Table(name="item_daily_stats"), @Id @GeneratedValue(IDENTITY) Long id, @Column(name="tracked_item_id",nullable=false) Long trackedItemId(★ 스칼라 FK 컬럼 — @ManyToOne 지양, price_snapshot이 연관 대신 스칼라 tracked_item_id 쓰는 선례가 있으면 준수; 없으면 스칼라로 단순 유지), @Column(name="stat_date",nullable=false) LocalDate statDate, @Column(name="avg_price",nullable=false,precision=18,scale=4) BigDecimal avgPrice, @Enumerated(STRING) @Column(name="source",nullable=false,length=20) DailyStatSource source, @Column(name="created_at",nullable=false) OffsetDateTime createdAt, @Column(name="updated_at",nullable=false) OffsetDateTime updatedAt. @PrePersist/@PreUpdate 타임스탬프(GameEvent 원형). 생성자 ItemDailyStat(Long trackedItemId, LocalDate statDate, BigDecimal avgPrice, DailyStatSource source) + getter. ★ 엔티티는 조회/검증용 — 실제 적재는 repository 네이티브 upsert가 담당(중복 회피). -->
<!-- ItemDailyStatRepository(com.lostark.tracker.repository) extends JpaRepository<ItemDailyStat,Long>: (1) @Modifying(clearAutomatically=true) @Query(nativeQuery=true, value="INSERT INTO item_daily_stats (tracked_item_id, stat_date, avg_price, source, created_at, updated_at) VALUES (:trackedItemId, :statDate, :avgPrice, :source, now(), now()) ON CONFLICT (tracked_item_id, stat_date, source) DO UPDATE SET avg_price = EXCLUDED.avg_price, updated_at = now()") int upsertDailyStat(@Param("trackedItemId") long trackedItemId, @Param("statDate") LocalDate statDate, @Param("avgPrice") BigDecimal avgPrice, @Param("source") String source) ★ source는 enum.name() 문자열로 바인딩. (2) List<ItemDailyStat> findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc(Long trackedItemId, LocalDate from, LocalDate to) = 04 read 병합용 윈도우 조회. -->
<!-- IT 베이스: src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java(Testcontainers Postgres/Redis), @DataJpaTest는 Flyway/컨테이너 정합 확인 후 사용 — 프로젝트가 @SpringBootTest+@Transactional 선례면 그걸 따른다. TrackedItem 시드는 TrackedItemRepository.save(new TrackedItem(externalId, name, category)). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V6__add_item_daily_stats.sql + DailyStatSource + ItemDailyStat 엔티티</name>
  <read_first>
    - src/main/resources/db/migration/V1__init_schema.sql (price_snapshot의 tracked_item_id FK·UNIQUE·타입 컨벤션 준거)
    - src/main/resources/db/migration/V5__add_coupon.sql (직전 마이그레이션 — 다음 순번 V6 확인·헤더 주석 형식)
    - src/main/java/com/lostark/tracker/domain/GameEvent.java (@Entity·@PrePersist/@PreUpdate 타임스탬프 원형)
    - src/main/java/com/lostark/tracker/domain/TrackedItem.java (도메인 패키지 매핑·컬럼 어노테이션 스타일)
  </read_first>
  <files>src/main/resources/db/migration/V6__add_item_daily_stats.sql, src/main/java/com/lostark/tracker/domain/DailyStatSource.java, src/main/java/com/lostark/tracker/domain/ItemDailyStat.java</files>
  <action>
    `V6__add_item_daily_stats.sql`을 만든다: `CREATE TABLE item_daily_stats( id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, tracked_item_id BIGINT NOT NULL REFERENCES tracked_item (id), stat_date DATE NOT NULL, avg_price NUMERIC(18,4) NOT NULL, source VARCHAR(20) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT uq_item_daily_stats UNIQUE (tracked_item_id, stat_date, source) );` 다음 `CREATE INDEX ix_item_daily_stats_item_date ON item_daily_stats (tracked_item_id, stat_date);`. 헤더 주석에 목적(백필 일별 평균·D-05)·additive 가드(price_snapshot 무변경) 명시. PostgreSQL 타입만. `price_snapshot` 파일은 건드리지 않는다. `DailyStatSource`(`com.lostark.tracker.domain`) enum `{ YDAY_AVG, DETAIL_STATS }` — Javadoc에 각 소스 의미(YDAY_AVG=리스트 무료 캡처 전 품목·전일, DETAIL_STATS=상세 Stats 재료 14일). `ItemDailyStat`(`com.lostark.tracker.domain`)를 GameEvent 구조로 만든다: `@Entity @Table(name="item_daily_stats")`, `@Id @GeneratedValue(strategy=IDENTITY) Long id`, `@Column(name="tracked_item_id",nullable=false) Long trackedItemId`(스칼라 FK), `@Column(name="stat_date",nullable=false) LocalDate statDate`, `@Column(name="avg_price",nullable=false,precision=18,scale=4) BigDecimal avgPrice`, `@Enumerated(EnumType.STRING) @Column(name="source",nullable=false,length=20) DailyStatSource source`, `@Column(name="created_at",nullable=false) OffsetDateTime createdAt`, `@Column(name="updated_at",nullable=false) OffsetDateTime updatedAt`. `@PrePersist onCreate()`(createdAt/updatedAt=now UTC)·`@PreUpdate onUpdate()`(updatedAt=now UTC)는 GameEvent 그대로. 생성자 `ItemDailyStat(Long trackedItemId, LocalDate statDate, BigDecimal avgPrice, DailyStatSource source)` + getter. 세터 없음.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - V6__add_item_daily_stats.sql이 item_daily_stats 테이블 + UNIQUE(tracked_item_id, stat_date, source)를 생성 — `grep -qi "CREATE TABLE item_daily_stats" src/main/resources/db/migration/V6__add_item_daily_stats.sql` 성립, `grep -qi "UNIQUE (tracked_item_id, stat_date, source)" src/main/resources/db/migration/V6__add_item_daily_stats.sql` 성립
    - `price_snapshot` 관련 마이그레이션(V1)·엔티티 diff 0줄
    - DailyStatSource enum(YDAY_AVG, DETAIL_STATS) + ItemDailyStat 엔티티가 avg_price(BigDecimal)·source(@Enumerated STRING)·tracked_item_id로 정의되고 `./gradlew compileJava` 그린
    - Flyway 순번이 V6이며 V1~V5는 무변경(immutable)
  </acceptance_criteria>
  <done>item_daily_stats(V6) 스키마·엔티티·enum이 컴파일되고 tracked_item_id FK·(item,date,source) UNIQUE로 additive 계층을 정의하며 price_snapshot은 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 2: ItemDailyStatRepository (멱등 upsert + 윈도우 read finder) + RepositoryIT</name>
  <read_first>
    - src/main/java/com/lostark/tracker/repository/PriceSnapshotRepository.java (@Query·파생 쿼리·윈도우 조회 네이밍 원형)
    - src/main/java/com/lostark/tracker/domain/ItemDailyStat.java (Task 1 산출 — 엔티티 필드·source enum)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (Testcontainers 베이스)
    - src/main/java/com/lostark/tracker/domain/TrackedItem.java (IT에서 FK 만족용 tracked_item 시드)
  </read_first>
  <files>src/main/java/com/lostark/tracker/repository/ItemDailyStatRepository.java, src/test/java/com/lostark/tracker/repository/ItemDailyStatRepositoryIT.java</files>
  <action>
    `ItemDailyStatRepository`(`com.lostark.tracker.repository`) `extends JpaRepository<ItemDailyStat,Long>`에 두 메서드를 둔다: (1) 멱등 upsert — `@Modifying(clearAutomatically=true)` + `@Query(nativeQuery=true, value="INSERT INTO item_daily_stats (tracked_item_id, stat_date, avg_price, source, created_at, updated_at) VALUES (:trackedItemId, :statDate, :avgPrice, :source, now(), now()) ON CONFLICT (tracked_item_id, stat_date, source) DO UPDATE SET avg_price = EXCLUDED.avg_price, updated_at = now()")` `int upsertDailyStat(@Param("trackedItemId") long trackedItemId, @Param("statDate") LocalDate statDate, @Param("avgPrice") BigDecimal avgPrice, @Param("source") String source)` — source는 호출부에서 `DailyStatSource.name()` 문자열로 바인딩. (2) 윈도우 조회 — `List<ItemDailyStat> findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc(Long trackedItemId, LocalDate from, LocalDate to)`(04 read 병합용). `ItemDailyStatRepositoryIT`(PostgresRedisContainers 상속, @SpringBootTest, @ActiveProfiles("test"))를 작성: (a) tracked_item 1건 시드 후 같은 (trackedItemId, stat_date=오늘, source=YDAY_AVG)로 `upsertDailyStat`를 **avg_price 다르게 2회** 호출 → `findAll().size()==1` **그리고** 저장된 avg_price가 **두 번째 값**임을 검증(멱등 upsert = 중복 행 없이 갱신). (b) 같은 (item·date)라도 source가 YDAY_AVG vs DETAIL_STATS면 **2행** 공존 검증(소스 구분). (c) stat_date 3일치(today-2, today-1, today) 적재 후 `findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc(id, today-2, today-1)`가 **2건·오름차순**만 반환 검증. 실 API 미호출.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - upsertDailyStat 2회(같은 item·date·source, 값만 변경) → 행 1개·avg_price는 마지막 값(멱등 갱신) IT 통과
    - 같은 (item·date)·다른 source → 2행 공존 IT 통과(소스 구분)
    - findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc가 윈도우·오름차순 필터 IT 통과
    - `./gradlew build` 그린(Testcontainers Postgres/Redis, 실 API·키 미사용)
    - 수집/캐시/event-impact·price_snapshot diff 0줄
  </acceptance_criteria>
  <done>멱등 upsert(중복 없이 갱신·소스별 공존)와 윈도우 조회가 RepositoryIT로 증명되고 `./gradlew build`가 그린이며 Core Value 경로 0줄이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백필 적재 → item_daily_stats | 신규 독립 테이블 — price_snapshot(Core Value)과 물리 분리, tracked_item FK로만 참조 |
| 애플리케이션 → PostgreSQL | JPA/Flyway 기존 스택, 파라미터 바인딩 upsert(문자열 조립 금지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01-01 | Tampering | 백필 적재가 price_snapshot/수집 경로를 오염 | mitigate | 별도 테이블·별도 repository, price_snapshot·PriceCollector diff 0줄(BACKFILL-04) — verify가 diff 확인 |
| T-174-01-02 | Tampering | 중복 재적재로 일별 시리즈 왜곡 | mitigate | (item,date,source) UNIQUE + ON CONFLICT DO UPDATE 멱등 upsert — RepositoryIT가 재실행 시 행 1개 검증 |
| T-174-01-03 | Tampering | source 컬럼에 알 수 없는 문자열 유입 | mitigate | @Enumerated(STRING) — 조회 매핑 경계에서 미지 값 실패(loud), 저장은 enum.name()만 |
| T-174-01-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — Spring Data JPA/Flyway 기존 스택 재사용 |
</threat_model>

<verification>
- `V6__add_item_daily_stats.sql` + `ItemDailyStat`로 JPA `ddl-auto=validate` 통과 — `./gradlew build` 그린(D-05).
- `upsertDailyStat`가 (tracked_item_id, stat_date, source) UNIQUE + ON CONFLICT로 멱등(재실행 시 중복 행 0, avg_price 갱신) — ItemDailyStatRepositoryIT(D-03 저장 근거).
- 소스 2종(YDAY_AVG/DETAIL_STATS)이 한 테이블에서 (item·date) 동일해도 공존 — 소스 구분 IT(D-01).
- `price_snapshot`·`PriceCollector`·`LatestPriceCache`·`EventImpactService` diff 0줄(BACKFILL-04, Core Value 가드).
</verification>

<success_criteria>
- 백필 일별 평균가가 별도 `item_daily_stats`(tracked_item_id·stat_date·avg_price·source) 테이블에 PostgreSQL 영속하고 price_snapshot은 무변경이다(D-05, BACKFILL-04).
- 멱등 upsert로 서버 재기동/일 1회 재실행이 중복 행 없이 갱신만 한다(D-03의 저장 계층 근거).
- 소스 2종을 한 테이블에서 구분(source enum)해 표시 계층이 지표 출처를 정직하게 라벨링할 수 있다(D-01).
</success_criteria>

<output>
Create `.planning/phases/17.4-timeline-gap-backfill/17.4-01-SUMMARY.md` when done
</output>