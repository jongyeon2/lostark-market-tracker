---
phase: 17.3-coupon-admin
plan: 03
subsystem: ui
tags: [react, typescript, react-query, tailwind, dashboard, clipboard, coupon]

requires:
  - phase: 17.3-02
    provides: useCoupons(['coupons']) public read hook + Coupon type
  - phase: 17.2-dashboard-news-panel
    provides: NewsPanel (useNews AsyncBoundary, newsDate helper, SectionHeading)
provides:
  - Dashboard NewsPanel coupon section at the top (code·reward·expiry + one-click copy, own AsyncBoundary)
affects: []

tech-stack:
  added: []
  patterns:
    - "Per-widget AsyncBoundary isolation — coupon section independent of useNews so one failure never hides the other (COUPON-03)"
    - "One-click clipboard copy via navigator.clipboard.writeText with ~2s per-row label toggle, try/catch guarded (D-03)"

key-files:
  created: []
  modified:
    - frontend/src/features/dashboard/NewsPanel.tsx

key-decisions:
  - "D-02: 쿠폰 섹션을 뉴스 패널 최상단에 배치 — 최종 순서 쿠폰 → 진행중 이벤트 → 공지사항 (쿠폰이 가장 actionable)."
  - "D-03: 복사 피드백은 클릭한 행 버튼 라벨을 ~2초 '복사됨!'로 토글 후 원복(per-row copiedId). navigator.clipboard.writeText만 사용, 토스트 시스템 미도입(신규 의존성 0). 실패는 try/catch로 조용히 무시(크래시 금지)."
  - "COUPON-03: 쿠폰 섹션은 useCoupons 상태의 자체 AsyncBoundary — useNews boundary와 독립. 만료 쿠폰은 백엔드가 이미 제외(17.3-01), 방어적 ≤6 slice."

patterns-established:
  - "대시보드 위젯은 위젯별 독립 AsyncBoundary로 격리 — 한 데이터 소스 실패가 다른 위젯을 가리지 않음"

requirements-completed: [COUPON-02, COUPON-03]

duration: ~12 min
completed: 2026-07-07
---

# Phase 17.3 Plan 03: 대시보드 뉴스 패널 쿠폰 섹션 Summary

**뉴스 패널 최상단에 미만료 쿠폰(≤6·만료임박순) 섹션 추가 — code·reward·만료일 + 원클릭 클립보드 복사(~2초 '복사됨!' 토글)와 useNews와 독립된 자체 AsyncBoundary로 정직한 로딩/빈/에러 표시**

## Performance

- **Duration:** ~12 min
- **Completed:** 2026-07-07
- **Tasks:** 2 (단일 파일 상호의존 — 하나의 커밋)
- **Files:** 1 modified

## Accomplishments
- `NewsPanel`에 로컬 `CouponSection` 추가 — `useCoupons()` 소비, 방어적 ≤6 slice(백엔드가 이미 만료임박순·만료 제외).
- 각 행: code(강조·tabular)·reward·만료일(`newsDate`, date-only YYYY.MM.DD) + 원클릭 복사 `Button`.
- 복사: `navigator.clipboard.writeText(code)` → 클릭 행 라벨 ~2초 '복사됨!' 토글 후 원복(per-row copiedId), 실패는 try/catch로 조용히 무시(크래시·빈화면 금지, D-03).
- 쿠폰 섹션 **최상단** 배치(쿠폰 → 진행중 이벤트 → 공지사항, D-02) + `useNews`와 **독립된** 자체 AsyncBoundary(COUPON-03). 기존 이벤트/공지 섹션·newsDate·useNews boundary 회귀 0. `npm run build` 그린, 백엔드·lib diff 0줄.

## Task Commits

1. **Task 1+2: 쿠폰 섹션 컴포넌트(복사·자체 AsyncBoundary) + 최상단 배치** — `4407947` (feat)

_주: Task 1(컴포넌트)과 Task 2(최상단 배치)는 동일 파일 NewsPanel.tsx의 상호의존 변경(렌더 호출 없이는 컴포넌트가 빌드에서 미사용)이라 빌드 가능한 단일 원자 커밋으로 처리._

## Files Created/Modified
- `features/dashboard/NewsPanel.tsx` — 최상단 쿠폰 섹션(로컬 CouponSection: useCoupons·복사 버튼·자체 AsyncBoundary) 추가 + CardContent 재구성(쿠폰 → 이벤트 → 공지)

## Decisions Made
- 계획대로 실행 — frontmatter key-decisions(D-02 최상단, D-03 복사 토글, COUPON-03 독립 boundary) 그대로 반영. setTimeout 원복 시 최신 선택을 지우지 않도록 `setCopiedId((cur)=>cur===id?null:cur)` 함수형 가드 추가(경미한 견고화).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 17.3 3플랜(백엔드 도메인·프론트 데이터계층+관리자·대시보드 노출) 모두 완료 — 쿠폰 end-to-end(관리자 등록 → 대시보드 복사·사용) 동작.
- 검증: 관리자 콘솔에서 쿠폰 등록 → 대시보드 뉴스 패널 최상단에 만료임박순 노출 → 복사 버튼 동작(수동 UAT 권장, 실 브라우저 clipboard).

---
*Phase: 17.3-coupon-admin*
*Completed: 2026-07-07*
