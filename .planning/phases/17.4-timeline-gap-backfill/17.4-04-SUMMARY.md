---
phase: 17.4-timeline-gap-backfill
plan: 04
subsystem: ui
tags: [read-merge, recharts, zod, timeline, backfill, spring-restcontroller]

requires:
  - phase: 17.4-01
    provides: ItemDailyStatRepository.findByTrackedItemIdAndStatDateBetweenOrderByStatDateAsc window read
  - phase: 17.4-02
    provides: YDAY_AVG going-forward daily averages
  - phase: 17.4-03
    provides: DETAIL_STATS material 14-day backfill
provides:
  - "TimelineResponse.backfill: List<DailyStatPoint> — separate array from snapshots (D-02)"
  - "PricesController KST-window merge of item_daily_stats (additive, empty→[])"
  - "timelineSchema.backfill (z.enum source, .default([]) forward-compat)"
  - "PriceTimelineChart: backfill continuous line + 실측 min distinct markers, honest labels"
affects: []

tech-stack:
  added: []
  patterns:
    - "Third independent read array (backfill) kept apart from snapshots — no metric mixing (D-02)"
    - "Recharts two-dataKey line/marker split on one merged dataset (connectNulls only on the continuous series)"
    - "Empty-state gate keyed on BOTH series so a backfill-only window still renders"

key-files:
  created:
    - src/main/java/com/lostark/tracker/web/dto/DailyStatPoint.java
    - src/test/java/com/lostark/tracker/web/PricesControllerIT.java
  modified:
    - src/main/java/com/lostark/tracker/web/dto/TimelineResponse.java
    - src/main/java/com/lostark/tracker/web/PricesController.java
    - frontend/src/lib/schemas.ts
    - frontend/src/features/timeline/PriceTimelineChart.tsx
    - frontend/src/features/timeline/TimelinePage.tsx

key-decisions:
  - "PricesControllerIT created new (the plan's file did not exist; timeline read tests lived in read/TimelinePricesIT + web/ItemEnrichmentReadIT)"
  - "TimelinePage wired (not in plan files_modified): pass backfill prop + relax empty-state gate to snapshots==0 && backfill==0 — required to meet the success criterion that server-off windows render"

patterns-established:
  - "실측 최저호가(파랑 마커) vs 백필 일평균 거래가(주황 연속선) — color + label honest distinction"

requirements-completed: [BACKFILL-03]

duration: ~30min
completed: 2026-07-07
---

# Phase 17.4-04: 타임라인 read 병합 + 차트 Summary

**타임라인이 백필 일평균을 연속 라인으로, 실측 min을 구분 색 마커로 그린다 — `item_daily_stats`를 `TimelineResponse.backfill`(snapshots와 분리)로 병합, 두 지표 혼합 금지·정직 라벨, Core Value 경로 0줄.**

## Performance

- **Duration:** ~30 min
- **Completed:** 2026-07-07
- **Tasks:** 2
- **Files created:** 2, modified: 5

## Accomplishments
- `DailyStatPoint`(statDate·avgPrice·source) + `TimelineResponse.backfill` 별도 배열(snapshots와 분리, D-02).
- `PricesController`가 `item_daily_stats`를 KST 캘린더-데이 윈도우로 additive 병합 — `WindowQueryService`/`DownsampleService` 무변경, 백필 없으면 `[]`·200.
- `PricesControllerIT`(신규, 2 tests): backfill이 실측 snapshots와 분리·윈도우·오름차순·소스 태깅, 빈 윈도우 200·`[]` 증명.
- `schemas.ts`: `dailyStatSourceSchema`(z.enum)·`dailyStatPointSchema`·`timelineSchema.backfill`(`.default([])` 구버전 방어).
- `PriceTimelineChart`: 백필=연속 라인(connectNulls·주황), 실측 min=구분 색 마커(파랑·점)·별도 dataKey — **혼합 금지**, 범례/툴팁 정직 구분("일평균 거래가" vs "최저호가").
- `TimelinePage`: `backfill` 배선 + 빈 상태 게이트를 `snapshots==0 && backfill==0`으로 완화 → 서버 off(백필만) 구간도 렌더(D-04).

## Task Commits

1. **Task 1: 백엔드 read 병합 + PricesControllerIT** — `0ddb7ca` (feat)
2. **Task 2: 프론트 schema + 차트 + TimelinePage 배선** — `a6c09ca` (feat)

## Files Created/Modified
- `web/dto/DailyStatPoint.java` — 백필 read DTO(신규)
- `web/dto/TimelineResponse.java` — backfill 필드 추가
- `web/PricesController.java` — item_daily_stats KST 윈도우 병합(additive)
- `web/PricesControllerIT.java` — 병합 계약 IT(신규)
- `lib/schemas.ts` — dailyStatPoint/source 스키마 + timelineSchema.backfill
- `timeline/PriceTimelineChart.tsx` — 백필 연속선 + 실측 min 마커(2 dataKey 분리)
- `timeline/TimelinePage.tsx` — backfill 배선 + 빈 상태 게이트 완화

## Decisions Made
- **PricesControllerIT 신규 생성** — 플랜 지정 파일이 부재(타임라인 read 테스트는 `read/TimelinePricesIT`·`web/ItemEnrichmentReadIT`에 존재). files_modified대로 신규 작성.
- **TimelinePage 배선(플랜 files_modified 밖)** — 차트에 backfill을 전달하려면 호출부 수정이 불가피하고, 성공 기준("서버 off 구간도 연속으로 보이게")을 충족하려면 `snapshots==0` 조기 반환을 `snapshots==0 && backfill==0`으로 완화해야 함. 최소 additive 배선.

## Deviations from Plan

### Auto-fixed Issues

**1. [Wiring] TimelinePage.tsx 수정(플랜 files_modified 미포함)**
- **Found during:** Task 2 (차트에 backfill 전달)
- **Issue:** 차트는 호출부에서만 데이터를 받음 — backfill prop 미전달 시 백필이 렌더 안 됨. 또 `snapshots==0` 조기 반환이 백필-only(서버 off) 윈도우를 "데이터 없음"으로 가림 → 성공 기준 미충족
- **Fix:** `backfill` 구조분해·prop 전달 + 게이트를 `snapshots==0 && backfill==0`으로 완화(둘 다 빈 경우만 empty). 기존 수집-헬스 메시징·30일 CTA·RangeControls 무변경
- **Verification:** `cd frontend && npm run build` 그린(tsc)
- **Committed in:** `a6c09ca`

---

**Total deviations:** 1 auto-fixed (necessary caller wiring)
**Impact on plan:** 성공 기준 충족에 필수(백필 가시화). RangeControls/기본 30일/토글 무변경(D-04 준수). 스코프 확장 없음.

## Issues Encountered
None — 계획대로 진행.

## User Setup Required
None.

## Next Phase Readiness
- 백필 read+차트 완성 — 두 소스(YDAY_AVG going-forward·DETAIL_STATS 재료 소급)가 타임라인에 연속 라인으로 표시됨.
- **BACKFILL-04 Core Value 가드 최종 확인**: `cache/`·`WindowQueryService`·`DownsampleService`·`EventImpact*`·V1 스키마·`PriceSnapshot` diff **0줄**(phase 전체 4bf0010..HEAD 검증).
- 한계(스파이크): 14일 초과·각인서 다일 과거 gap은 미복구 → Phase 18 상시 배포로 going-forward 보완.

---
*Phase: 17.4-timeline-gap-backfill*
*Completed: 2026-07-07*
