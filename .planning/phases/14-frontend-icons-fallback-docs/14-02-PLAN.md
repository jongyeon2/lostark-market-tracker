---
phase: 14-frontend-icons-fallback-docs
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/src/features/dashboard/ItemCard.tsx
  - frontend/src/features/dashboard/DashboardPage.tsx
  - frontend/src/features/_shared/ItemSelect.tsx
  - frontend/src/features/_shared/LatestPriceCard.tsx
  - frontend/src/features/timeline/TimelinePage.tsx
  - frontend/src/features/impact/ImpactPage.tsx
autonomous: true
requirements: [ICON-02, ICON-03, ICON-04, ICON-05, ICON-06, ICON-07]

must_haves:
  truths:
    - "Dashboard ItemCard의 CardTitle 좌측에 <ItemIcon size='md'>가, 품목명 우측 inline에 RoleBadge가 표시된다 — 아이콘·배지 모두 useItems() 항목의 iconUrl/roleGroup 소비(ICON-02)"
    - "ItemSelect 각 SelectItem 좌측에 <ItemIcon size='sm'>, 옵션 텍스트 우측에 RoleBadge가 붙고, 옵션이 SelectGroup/SelectLabel 역할군 정적 헤더(딜러/서포터/융화재료)로 구분된다 — 필터 컨트롤 아님(ICON-03·D-08)"
    - "역할 배지는 품목명 옆 inline 배치로 4곳(ItemCard·ItemSelect·LatestPriceCard·ImpactPage 정체성 영역) 공통 적용돼 융화재료/딜러각인/서포터각인 역할군을 시각 구분한다(ICON-06·D-04)"
    - "LatestPriceCard가 iconUrl/roleGroup props를 받아 CardTitle 좌측 아이콘 + 품목명 우측 RoleBadge를 렌더하고, 호출처 TimelinePage·ImpactPage가 useItems()에서 찾은 선택 품목의 iconUrl/roleGroup을 전달한다(ICON-04)"
    - "Event Impact 화면은 선택 품목의 정체성 영역(ImpactPage 상단 LatestPriceCard)에 아이콘·역할 배지를 1회만 렌더하고 EventImpactCards의 이벤트 카드마다 역할 배지를 붙이지 않는다 — EventTypeBadge/ImpactStatusBadge와 충돌 회피(ICON-05·D-04)"
    - "셀렉터·대시보드 그리드가 sortByRole로 역할군(DEALER→SUPPORT→MATERIAL→null) → 이름 순 클라이언트 정렬되며, 백엔드는 0줄(D-07)"
    - "각인서 실아이콘(11종 동일 글리프)은 fallback으로 일부러 대체하지 않고 실아이콘 + 한글 라벨 + 역할 배지로 식별한다 — fallback은 null/onError 방어에 한정(D-05)"
    - "useItems()가 반환하는 큐레이션 15개(융화재료 4 + 딜러 9 + 서포터 2)가 정렬·그룹핑 후에도 하나도 누락되지 않고 Dashboard/Timeline/Event Impact 셀렉터에서 선택 가능하다 — roleGroup null은 '기타' 그룹/말미로 흡수(ICON-07)"
  artifacts:
    - path: "frontend/src/features/_shared/ItemSelect.tsx"
      provides: "SelectGroup 역할군 헤더 + 옵션별 ItemIcon/RoleBadge + sortByRole 정렬(ICON-03·D-07·D-08)"
      contains: "SelectGroup"
    - path: "frontend/src/features/_shared/LatestPriceCard.tsx"
      provides: "iconUrl/roleGroup props 추가 + CardTitle 아이콘·배지(ICON-04/05)"
      contains: "ItemIcon"
    - path: "frontend/src/features/dashboard/ItemCard.tsx"
      provides: "CardTitle 좌측 ItemIcon + 품목명 우측 RoleBadge(ICON-02)"
      contains: "RoleBadge"
    - path: "frontend/src/features/impact/ImpactPage.tsx"
      provides: "선택 품목 iconUrl/roleGroup을 LatestPriceCard에 전달(ICON-05 정체성 1회)"
      contains: "roleGroup"
  key_links:
    - from: "TimelinePage/ImpactPage (useItems().find(itemId))"
      to: "LatestPriceCard iconUrl/roleGroup props"
      via: "선택 품목 객체에서 iconUrl/roleGroup 추출 → props 전달"
      pattern: "iconUrl"
    - from: "ItemSelect (useItems() 결과)"
      to: "역할군 SelectGroup 섹션 + 옵션 ItemIcon/RoleBadge"
      via: "sortByRole → roleGroup별 group-by → SelectGroup/SelectLabel + SelectItem 내 ItemIcon/RoleBadge"
      pattern: "SelectGroup"
---

<objective>
14-01의 공유 기반(`<ItemIcon>`·`RoleBadge`·`sortByRole`·enrichment zod)을 **4개 화면에 입힌다** — 백엔드 `src/` 0줄, 신규 fetch 0. 각 화면은 기반을 import만 한다.

- **ICON-02 Dashboard ItemCard:** CardTitle 좌측 `<ItemIcon size='md'>` + 품목명 우측 `RoleBadge`(inline, D-04). 출처 = `useItems()` 항목.
- **ICON-03 ItemSelect:** 옵션마다 `<ItemIcon size='sm'>` + 텍스트 + `RoleBadge`, 그리고 `SelectGroup`/`SelectLabel`로 딜러/서포터/융화재료 정적 헤더(D-08). `sortByRole` 정렬(D-07).
- **ICON-04 LatestPriceCard(Timeline·Impact 공유):** `iconUrl`/`roleGroup` props 추가 → CardTitle 아이콘·배지. 호출처 `TimelinePage`/`ImpactPage`가 선택 품목 enrichment 전달.
- **ICON-05 Event Impact:** 선택 품목 **정체성 영역에 1회**(ImpactPage 상단 LatestPriceCard) — 이벤트 카드마다가 아님(EventTypeBadge/ImpactStatusBadge 충돌 회피, D-04). `EventImpactCards.tsx`는 건드리지 않는다.
- **ICON-06 역할 구분:** 위 4곳 inline RoleBadge로 역할군 시각 구분(필터 컨트롤 v2 제외).
- **ICON-07 추가 품목 선택:** 큐레이션 15개가 정렬/그룹핑 후에도 누락 없이 3화면 셀렉터에서 선택 가능(null role → 기타/말미 흡수).
- **D-05 각인서 동일 아이콘:** 실아이콘 + 라벨 + 역할 배지로 식별, fallback 강제 대체 금지.
- **D-07 정렬:** 셀렉터·대시보드 클라 정렬(`sortByRole`), 백엔드 0줄.

Purpose: 이미 잠긴 enrichment를 3화면+셀렉터에 일관·식별적으로 노출하고, fallback이 "미완성"이 아닌 "의도된 디자인"으로 읽히게 한다. seed만으로(키 없이) 아이콘이 뜬다.

Output: 6파일(ItemCard·DashboardPage·ItemSelect·LatestPriceCard·TimelinePage·ImpactPage) enrich. `npm run build` 그린, 백엔드 `src/` 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-icons-fallback-docs/14-01-PLAN.md
@.planning/phases/14-frontend-icons-fallback-docs/14-CONTEXT.md
@.planning/phases/14-frontend-icons-fallback-docs/14-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- 14-01 산출물(이 plan이 import): -->
<!-- @/features/_shared/ItemIcon → <ItemIcon iconUrl roleGroup size?='md'|'sm'/> 고정 슬롯 + null/onError fallback. -->
<!-- @/features/_shared/RoleBadge → <RoleBadge roleGroup/> solid 색배경 한글 배지; roleGroup null이면 null 반환. -->
<!-- @/features/_shared/roleGroup → ROLE_LABEL, sortByRole<T extends {roleGroup, displayName}>(items): T[](원본 불변, null 말미), RoleGroup 타입. -->
<!-- @/lib/schemas → TrackedItem(이제 iconUrl/itemGroup/roleGroup 포함), LatestPrice/Timeline/EventImpact(enrichment 평면). -->
<!-- 현재 소비처: -->
<!-- ItemCard({item: TrackedItem}): CardHeader>CardTitle(item.displayName)+p(category). useLatestPrice(item.id) 가격 영역. → CardTitle 좌측 ItemIcon + displayName 우측 RoleBadge(flex items-center gap-2). -->
<!-- DashboardPage: useItems() → data?.map(item => <ItemCard item={item}/>). 기존 D-02 주석 "backend already sorts by displayName, NO front-end re-sort" → D-07로 갱신: sortByRole(data) 후 map. -->
<!-- ItemSelect({value:number|null, onChange}): useItems() → data.map(item => <SelectItem value={String(item.id)}>{item.displayName}</SelectItem>) 평면. → sortByRole + roleGroup별 group-by → SelectGroup/SelectLabel(ROLE_LABEL) 섹션 + SelectItem 내 <ItemIcon size='sm'/>+이름+<RoleBadge/>. select.tsx는 SelectGroup/SelectLabel 이미 export. SelectItem children은 ItemText로 trigger에도 미러됨(허용). -->
<!-- LatestPriceCard({itemId, displayName}): CardTitle(displayName) + useLatestPrice 가격 영역(AsyncBoundary). → props에 iconUrl?:string|null, roleGroup?:RoleGroup|null 추가; CardTitle을 flex items-center gap-2 [ItemIcon md][displayName][RoleBadge]로. -->
<!-- TimelinePage/ImpactPage: const {data:items}=useItems(); displayName=items?.find(i=>i.id===itemId)?.displayName ?? ''. → 같은 find 결과(sel)에서 sel?.iconUrl ?? null, sel?.roleGroup ?? null도 뽑아 LatestPriceCard에 전달. -->
<!-- EventImpactCards.tsx: 수정 금지 — 이벤트 카드에 역할 배지 추가하면 EventTypeBadge/ImpactStatusBadge와 충돌(D-04 zone 분리). ICON-05는 ImpactPage 정체성 영역(LatestPriceCard)로 충족. -->
<!-- 정렬 안정성: items가 비어도(빈 배열) sortByRole 안전. roleGroup null 항목은 '기타' SelectGroup(또는 라벨 없는 섹션) + 말미. -->
<!-- npm run build = tsc -b && vite build. -->
</interfaces>
</context>

## Phase Goal

**As a** 로아 유저·면접관(브라우저로 3화면을 보는 사람), **I want to** 대시보드 카드·품목 셀렉터·타임라인 최신가 카드·이벤트 영향 화면에서 품목 아이콘과 역할(딜러/서포터/융화재료) 배지를 일관되게 보고, 아이콘이 없거나 막혀도 역할색 글리프로 자연스럽게 대체되며 큐레이션 품목 전부를 역할군으로 정리된 셀렉터에서 고를 수 있기를, **so that** 데이터가 시각적으로 "자산 섹터"처럼 읽히고 fallback이 미완성이 아닌 의도된 디자인으로 보인다.

<tasks>

<task type="auto">
  <name>Task 1: Dashboard — ItemCard 아이콘·역할 배지(ICON-02) + 그리드 역할 정렬(D-07)</name>
  <read_first>
    - frontend/src/features/dashboard/ItemCard.tsx (CardTitle/category 구조 — 아이콘·배지 삽입 지점)
    - frontend/src/features/dashboard/DashboardPage.tsx (useItems → map; D-02 no-resort 주석을 D-07로 갱신)
    - frontend/src/features/_shared/ItemIcon.tsx (props·size)
    - frontend/src/features/_shared/RoleBadge.tsx (props)
    - frontend/src/features/_shared/roleGroup.ts (sortByRole)
  </read_first>
  <files>frontend/src/features/dashboard/ItemCard.tsx, frontend/src/features/dashboard/DashboardPage.tsx</files>
  <action>
    `ItemCard.tsx`: `CardTitle`을 `<CardTitle className="flex items-center gap-2 text-xl">`로 바꾸고 내부를 `[<ItemIcon iconUrl={item.iconUrl} roleGroup={item.roleGroup} size="md" />][<span>{item.displayName}</span>][<RoleBadge roleGroup={item.roleGroup} />]` 순서로 구성한다(아이콘 좌측, 품목명, 역할 배지 우측 inline — D-04). 가격 영역(useLatestPrice)·category 서브타이틀·inline 상태 매핑은 그대로 둔다. `@/features/_shared/ItemIcon`·`RoleBadge` import 추가.
    `DashboardPage.tsx`: `data?.map(...)` 직전에 `const sorted = data ? sortByRole(data) : []`로 정렬하고 `sorted.map(item => <ItemCard .../>)`로 바꾼다(D-07 — 역할군→이름). 기존 "backend already sorts by displayName, NO front-end re-sort (D-02)" 주석을 "D-07: 역할군(DEALER→SUPPORT→MATERIAL→null)→이름 클라 정렬, 백엔드 0줄"로 갱신. AsyncBoundary의 isEmpty 판정은 원본 `data?.length` 유지. `@/features/_shared/roleGroup`의 `sortByRole` import.
  </action>
  <verify>
    <automated>cd frontend && grep -q "ItemIcon" src/features/dashboard/ItemCard.tsx && grep -q "RoleBadge" src/features/dashboard/ItemCard.tsx && grep -q "item.iconUrl" src/features/dashboard/ItemCard.tsx && grep -q "sortByRole" src/features/dashboard/DashboardPage.tsx && npx tsc -b 2>/dev/null || npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `ItemCard` CardTitle이 `flex items-center gap-2`로 [ItemIcon md][displayName][RoleBadge] inline 렌더(D-04)
    - 아이콘·배지 출처가 `item.iconUrl`/`item.roleGroup`(useItems 항목)
    - `DashboardPage`가 `sortByRole(data)`로 그리드를 역할군→이름 정렬(D-07), 백엔드 0줄
    - 가격 영역·category·inline 상태 매핑 무변경
    - 타입체크 그린
  </acceptance_criteria>
  <done>대시보드 카드가 아이콘·역할 배지를 inline으로 보여주고 그리드가 역할군으로 정렬된다.</done>
</task>

<task type="auto">
  <name>Task 2: ItemSelect — 역할군 그룹 헤더(D-08) + 옵션 아이콘·배지(ICON-03) + 정렬(D-07) + 누락 없는 큐레이션(ICON-07)</name>
  <read_first>
    - frontend/src/features/_shared/ItemSelect.tsx (평면 data.map → 그룹핑 대상)
    - frontend/src/components/ui/select.tsx (SelectGroup/SelectLabel/SelectItem export·스타일 — gap-2 슬롯)
    - frontend/src/features/_shared/roleGroup.ts (ROLE_LABEL, sortByRole, RoleGroup)
    - frontend/src/features/_shared/ItemIcon.tsx (size='sm')
    - frontend/src/features/_shared/RoleBadge.tsx
  </read_first>
  <files>frontend/src/features/_shared/ItemSelect.tsx</files>
  <action>
    `ItemSelect.tsx` 성공 분기의 `<SelectContent>` 내부를 역할군 그룹으로 재구성한다. pending/error inline 분기는 그대로 유지(셀렉터 실패가 화면을 blank하지 않음).
    (1) `const sorted = sortByRole(data)` 후 roleGroup별로 묶는다(순서 DEALER→SUPPORT→MATERIAL, 그다음 null). 빈 그룹은 렌더하지 않는다.
    (2) 각 역할군을 `<SelectGroup>` + `<SelectLabel>{ROLE_LABEL[role]}</SelectLabel>`(딜러/서포터/융화재료)로 감싸고, roleGroup null 묶음이 있으면 `<SelectLabel>기타</SelectLabel>` 섹션으로 **반드시 포함**(누락 금지 — ICON-07).
    (3) 각 `<SelectItem key={item.id} value={String(item.id)}>` 내부를 `<span className="flex items-center gap-2">[<ItemIcon iconUrl={item.iconUrl} roleGroup={item.roleGroup} size="sm"/>]{item.displayName}[<RoleBadge roleGroup={item.roleGroup}/>]</span>`로 구성(아이콘 좌측 sm + 텍스트 + 역할 배지 inline — D-04). 모든 item을 빠짐없이 렌더(필터링 0).
    `@/features/_shared/roleGroup`(ROLE_LABEL·sortByRole·RoleGroup)·`ItemIcon`·`RoleBadge`·`SelectGroup`·`SelectLabel` import 추가.
  </action>
  <verify>
    <automated>cd frontend && grep -q "SelectGroup" src/features/_shared/ItemSelect.tsx && grep -q "SelectLabel" src/features/_shared/ItemSelect.tsx && grep -q "sortByRole" src/features/_shared/ItemSelect.tsx && grep -q "ItemIcon" src/features/_shared/ItemSelect.tsx && grep -q "RoleBadge" src/features/_shared/ItemSelect.tsx && grep -q "기타" src/features/_shared/ItemSelect.tsx && npx tsc -b 2>/dev/null || npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `<SelectContent>`가 SelectGroup/SelectLabel로 딜러/서포터/융화재료(+null이면 기타) 정적 헤더 섹션을 가짐(D-08, 필터 아님)
    - 각 SelectItem이 [ItemIcon sm][displayName][RoleBadge] inline(D-04), 출처 = item.iconUrl/roleGroup
    - 옵션이 `sortByRole`로 역할군→이름 정렬(D-07)
    - useItems의 모든 항목이 빠짐없이 렌더(필터링 0; null role도 기타 섹션으로 포함 — ICON-07)
    - pending/error inline 분기 무변경
    - 타입체크 그린
  </acceptance_criteria>
  <done>품목 셀렉터가 역할군 헤더로 묶이고 각 옵션에 아이콘·역할 배지가 붙으며 큐레이션 품목이 누락 없이 선택 가능하다.</done>
</task>

<task type="auto">
  <name>Task 3: LatestPriceCard props 확장 + Timeline/Impact 와이어링(ICON-04·ICON-05)</name>
  <read_first>
    - frontend/src/features/_shared/LatestPriceCard.tsx (props {itemId, displayName} → iconUrl/roleGroup 추가)
    - frontend/src/features/timeline/TimelinePage.tsx (useItems find → displayName; iconUrl/roleGroup도 추출)
    - frontend/src/features/impact/ImpactPage.tsx (동일 패턴 — ICON-05 정체성 1회)
    - frontend/src/features/impact/EventImpactCards.tsx (수정 금지 확인 — 이벤트 카드에 역할 배지 미추가)
    - frontend/src/features/_shared/ItemIcon.tsx, frontend/src/features/_shared/RoleBadge.tsx
  </read_first>
  <files>frontend/src/features/_shared/LatestPriceCard.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/ImpactPage.tsx</files>
  <action>
    `LatestPriceCard.tsx`: props 시그니처를 `{ itemId: number; displayName: string; iconUrl?: string | null; roleGroup?: RoleGroup | null }`로 확장(기존 호출 호환 위해 두 신규 prop은 optional, 기본 undefined→null로 취급). `CardTitle`을 `<CardTitle className="flex items-center gap-2 text-xl">`로 바꿔 `[<ItemIcon iconUrl={iconUrl ?? null} roleGroup={roleGroup ?? null} size="md"/>][<span>{displayName}</span>][<RoleBadge roleGroup={roleGroup ?? null}/>]` 구성(D-04). useLatestPrice 가격 영역·AsyncBoundary는 그대로. `@/features/_shared/ItemIcon`·`RoleBadge`·`@/features/_shared/roleGroup`의 `RoleGroup` import.
    `TimelinePage.tsx`·`ImpactPage.tsx`: 기존 `const displayName = items?.find(i => i.id === itemId)?.displayName ?? ''`를 선택 품목 객체로 한 번 추출하도록 바꾼다 — `const selected = items?.find(i => i.id === itemId)`; `const displayName = selected?.displayName ?? ''`. `<LatestPriceCard itemId={itemId} displayName={displayName} iconUrl={selected?.iconUrl ?? null} roleGroup={selected?.roleGroup ?? null} />`로 호출을 갱신한다. ImpactPage는 이 LatestPriceCard가 **선택 품목 정체성 1회 표시**라 ICON-05를 충족(이벤트 카드 미터치). 두 페이지 모두 D-06 자동 선택·기타 로직 무변경.
    `EventImpactCards.tsx`는 수정하지 않는다(역할 배지를 이벤트 카드에 붙이지 않음 — D-04 zone 분리).
  </action>
  <verify>
    <automated>cd frontend && grep -q "iconUrl" src/features/_shared/LatestPriceCard.tsx && grep -q "ItemIcon" src/features/_shared/LatestPriceCard.tsx && grep -q "iconUrl={selected" src/features/timeline/TimelinePage.tsx && grep -q "iconUrl={selected" src/features/impact/ImpactPage.tsx && test -z "$(git diff --name-only -- src/features/impact/EventImpactCards.tsx)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `LatestPriceCard`가 optional `iconUrl`/`roleGroup` props를 받고 CardTitle에 [ItemIcon md][displayName][RoleBadge] inline 렌더(D-04)
    - `TimelinePage`·`ImpactPage`가 `useItems().find(itemId)` 결과에서 iconUrl/roleGroup을 뽑아 LatestPriceCard에 전달(ICON-04/ICON-05)
    - ICON-05는 ImpactPage 정체성 영역(LatestPriceCard) 1회로 충족, `EventImpactCards.tsx` diff 0줄(이벤트 카드 역할 배지 없음)
    - `npm run build`(tsc -b && vite build) 그린
  </acceptance_criteria>
  <done>타임라인·이벤트 영향의 최신가 카드(선택 품목 정체성)에 아이콘·역할 배지가 1회 표시되고 이벤트 카드는 충돌 없이 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 enrichment(iconUrl/roleGroup) → 4화면 렌더 | useItems()/선택 품목의 정적 메타데이터를 컴포넌트가 표시 — 신규 fetch·외부 호출 0 |
| 프론트 → 외부 API | Lostark Open API 직접 호출 금지(상시 가드) — 이 plan은 기존 useItems/useLatestPrice 재사용, 신규 네트워크 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Denial of Service | 4화면에 아이콘 추가로 추가 네트워크/DB 호출 유발 | accept | enrichment는 이미 로드된 useItems()/useLatestPrice 응답에 포함 — 신규 쿼리 0. 아이콘 `<img>`는 `loading="lazy"`(14-01). |
| T-14-04 | Tampering | 셀렉터 그룹핑/정렬이 일부 품목을 누락(ICON-07 위반) | mitigate | sortByRole는 원본 불변 + 모든 항목 포함; null role은 '기타' 섹션으로 흡수(Task 2 AC가 누락 0 강제). |
| T-14-05 | UI 혼동 | 역할 배지가 이벤트/상태 배지와 충돌(Event Impact) | mitigate | ICON-05를 정체성 영역 1회로 한정, EventImpactCards 미터치 — solid(역할) vs tinted/outline(상태/이벤트) 처리 분리(D-04 zone). |
| T-14-BE | Tampering | 프론트 변경이 백엔드 핵심 경로 오염 | mitigate | frontend/ 6파일만 수정 — 백엔드 `src/` diff 0줄(verification git status 게이트). |
</threat_model>

<verification>
- `cd frontend && npm run build`(tsc -b && vite build) 그린.
- `git diff --name-only`에 백엔드 `src/` 0건; `frontend/src/features/impact/EventImpactCards.tsx` 0건(이벤트 카드 역할 배지 미추가).
- **수동(키 없이):** `docker compose up -d postgres redis` → `./gradlew bootRun --args='--spring.profiles.active=seed'` → `cd frontend && npm run dev` → 브라우저에서 (a) Dashboard 카드·셀렉터·Timeline 최신가·Event Impact 정체성에 아이콘·역할 배지 표시, (b) 셀렉터가 딜러/서포터/융화재료 헤더로 묶이고 큐레이션 15개 전부 선택 가능, (c) devtools offline 또는 iconUrl 손상 시 역할색 글리프로 대체되고 레이아웃 유지(미완성처럼 안 보임).
- 역할 배지 inline 4곳(ItemCard·ItemSelect·LatestPriceCard·ImpactPage 정체성) 일관 적용.
</verification>

<success_criteria>
- Dashboard 품목 카드·ItemSelect·Timeline 최신가 카드·Event Impact 정체성 영역에 아이콘이 표시된다 (ICON-02/03/04/05).
- 역할 그룹이 inline 배지로 시각 구분되고(필터 v2 제외) 셀렉터는 역할군 헤더로 묶인다 (ICON-06·D-04·D-08).
- 셀렉터·대시보드가 역할군→이름 클라 정렬(D-07), 큐레이션 15개가 누락 없이 선택 가능 (ICON-07).
- 각인서 실아이콘은 fallback 강제 대체 없이 라벨·배지로 식별 (D-05).
- `npm run build` 그린, 백엔드 `src/` 0줄.
</success_criteria>

<output>
Create `.planning/phases/14-frontend-icons-fallback-docs/14-02-SUMMARY.md` when done
</output>
