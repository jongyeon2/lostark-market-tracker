---
phase: 07-frontend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/package.json
  - frontend/src/lib/schemas.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/queries.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/lib/formatKst.ts
  - frontend/src/main.tsx
autonomous: true
requirements: [FND-02, FND-05]

must_haves:
  truths:
    - "Server state is managed by TanStack Query (React Query): a single `QueryClient` is provided at the app root and each of the 5 read endpoints is exposed as its own typed query hook, so caching, loading/error/retry, and request de-duplication are provided by the library and feed directly into the FND-04 common-state layer (D-01). This mirrors the v1.0 backend Redis serving cache as a front-end 'server-state cache' — the portfolio talking point."
    - "Queries do fetch-on-mount / route-entry ONLY: the `QueryClient` defaults set `refetchOnWindowFocus: false`, a high `staleTime` (the seed data is effectively static), and NO polling/interval — real-time auto-refresh is explicitly deferred to FE-V2-02. User-initiated retry is the UI-SPEC '다시 불러오기' CTA calling the query's `refetch` (wired in 07-03, D-02)."
    - "zod is the single source of truth for the 5 DTOs: each response shape is defined as a zod schema and the TypeScript types are derived with `z.infer` — types and validation are never maintained separately (D-05). The schemas are hand-written 1:1 against the REQUIREMENTS '실제 API 계약' table (backend unchanged, no OpenAPI codegen), and FND-02's 'types match the measured DTO fields' is satisfied because the types ARE the schemas."
    - "The API client validates every response at the boundary with `.parse`: on success it returns the parsed, typed value; on a schema mismatch `.parse` throws, the error propagates to React Query, and 07-03's shared ErrorState surfaces it ('loudly fail') (D-06). Unknown keys are stripped by zod's default object behavior (forward-compatible if the backend adds fields), and nullable fields (`summaryMessage`, `bucketWidth`, `sampleCount`, and event-impact's `preAnchorAt`/`postAnchorAt`/`prePrice`/`postPrice`/`changeRate`) are modeled explicitly so insufficient-data nulls never silently propagate into a later chart — the front extension of the off-by-9h / honest-data ethos."
    - "A `formatKst()` helper renders UTC ISO-8601 instants in Asia/Seoul for DISPLAY using native `Intl.DateTimeFormat({ timeZone: 'Asia/Seoul' })` with NO timezone library, while any axis-position/sort math is done on the raw UTC epoch (ms) and only the label is KST-formatted — the off-by-9h guard carried from the backend to the UI (FND-05, D-09)."
    - "The 5 zod schemas match the measured backend records field-for-field: CollectionHealth `{ lastRunAt:string|null, startedAt:string|null, itemsAttempted:number, itemsSucceeded:number, itemsFailed:number, status:string, summaryMessage:string|null }`; TrackedItem `{ id:number, externalItemId:string, displayName:string, category:string, active:boolean }` (array); LatestPrice `{ itemId:number, minPrice:number, collectedAt:string }`; Timeline `{ downsampled:boolean, bucketWidth:string|null, snapshots:{collectedAt:string,minPrice:number,sampleCount:number|null}[], events:{occurredAt:string,eventType:EventType,title:string}[] }`; EventImpact `{ itemId:number, window:number, events:{id:number,eventType:EventType,title:string,occurredAt:string,status:'ok'|'insufficient_data',preAnchorAt:string|null,postAnchorAt:string|null,prePrice:number|null,postPrice:number|null,changeRate:number|null}[] }`, with `EventType ∈ LOA_ON|MAJOR_UPDATE|SEASON_END|BALANCE_PATCH`. `npm run build` (tsc) passes."
  artifacts:
    - path: "frontend/src/lib/schemas.ts"
      provides: "5 zod schemas (collectionHealthSchema, trackedItemSchema + trackedItemsSchema array, latestPriceSchema, timelineSchema with pricePointSchema+eventPointSchema, eventImpactSchema with eventImpactItemSchema) + eventTypeSchema enum; exports z.infer types (CollectionHealth, TrackedItem, LatestPrice, Timeline, EventImpact, ...)"
      contains: "z.infer"
    - path: "frontend/src/lib/api.ts"
      provides: "Single typed API client module (D-12): one async fn per endpoint (getCollectionHealth, getItems, getLatestPrice(id), getTimeline(id,from,to), getEventImpact(id,window)) that fetches the proxied /api path and returns schema.parse(json) — throwing on non-2xx and on zod mismatch"
      contains: ".parse"
    - path: "frontend/src/lib/queries.ts"
      provides: "React Query hooks wrapping each api.ts fn: useCollectionHealth(), useItems(), useLatestPrice(id), useTimeline(id,from,to), useEventImpact(id,window) with stable queryKeys (e.g. ['items'], ['timeline',id,from,to])"
      contains: "useQuery"
    - path: "frontend/src/lib/queryClient.ts"
      provides: "Exported QueryClient with defaultOptions.queries = { refetchOnWindowFocus:false, staleTime: high, retry: small/0, refetchOnMount default } encoding D-02 fetch-on-mount policy"
      contains: "refetchOnWindowFocus"
    - path: "frontend/src/lib/formatKst.ts"
      provides: "formatKst(isoOrEpoch): string using Intl.DateTimeFormat('ko-KR',{ timeZone:'Asia/Seoul', ... }); plus a helper to get epoch ms for axis math; NO date-fns-tz/dayjs/luxon import"
      contains: "Asia/Seoul"
    - path: "frontend/src/main.tsx"
      provides: "App root wrapped in <QueryClientProvider client={queryClient}> so every screen's hooks resolve (routing added in 07-03)"
      contains: "QueryClientProvider"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/lib/schemas.ts"
      via: "each client fn calls the matching zod schema's .parse on the response (D-06 boundary validation)"
      pattern: "schemas|Schema"
    - from: "frontend/src/lib/queries.ts"
      to: "frontend/src/lib/api.ts"
      via: "each useQuery hook's queryFn calls the typed api.ts function"
      pattern: "api\\."
    - from: "frontend/src/main.tsx"
      to: "frontend/src/lib/queryClient.ts"
      via: "QueryClientProvider receives the exported queryClient (D-01 single client)"
      pattern: "queryClient"
---

<objective>
Build the **typed, validated server-state layer** the three screens consume: **zod schemas** for the 5 read DTOs as the single source of truth (`z.infer` for the TS types, D-05), a **typed API client** (`src/lib/api.ts`, D-12) that validates every response at the boundary with `.parse` and throws on drift (D-06), **TanStack Query** hooks with a fetch-on-mount-only policy (D-01, D-02), and the **`formatKst()`** UTC→KST display helper that keeps axis math on raw UTC epochs (FND-05, D-09).

The engineering signal is **honest, drift-proof data at the edge**: the front end mirrors the backend's "honest data / off-by-9h" discipline by parsing responses against hand-written schemas that match the measured API contract 1:1, modeling every nullable field explicitly so `insufficient_data` nulls fail loudly instead of poisoning a later chart. TanStack Query gives the "server-state cache" that narratively rhymes with the v1.0 Redis serving cache. This plan delivers the **library layer only** — no screen renders it yet; 07-03 wires these hooks through the AsyncBoundary and the dashboard's live `/api/items` render.

Output: `src/lib/schemas.ts` (5 zod schemas + inferred types), `src/lib/api.ts` (typed `.parse` client), `src/lib/queries.ts` (React Query hooks), `src/lib/queryClient.ts` (D-02 policy), `src/lib/formatKst.ts`, and `main.tsx` wrapped in `QueryClientProvider`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Depends on 07-01: frontend/ scaffold + Vite /api proxy + @ alias + Tailwind/shadcn exist. -->
<!-- MEASURED backend read contract (from src/main/java/.../web/dto, authoritative for the zod schemas). -->
<!-- All timestamps are UTC ISO-8601 strings ("...Z"); Long/int -> number; BigDecimal changeRate -> number|null. -->
<!--  GET /api/health/collection -> CollectionHealthResponse: -->
<!--    lastRunAt: OffsetDateTime|null, startedAt: OffsetDateTime|null, itemsAttempted:int, itemsSucceeded:int, itemsFailed:int, status:string, summaryMessage:string|null -->
<!--  GET /api/items -> TrackedItemResponse[]: id:Long, externalItemId:string, displayName:string, category:string, active:boolean -->
<!--  GET /api/items/{id}/latest -> LatestPriceResponse: itemId:Long, minPrice:Long, collectedAt:OffsetDateTime -->
<!--  GET /api/items/{id}/prices?from=&to= -> TimelineResponse: downsampled:boolean, bucketWidth:string|null, -->
<!--    snapshots: PricePoint[] { collectedAt:OffsetDateTime, minPrice:Long, sampleCount:Integer|null }, -->
<!--    events: EventPoint[] { occurredAt:OffsetDateTime, eventType:EventType, title:string } -->
<!--  GET /api/items/{id}/event-impact?window=N -> EventImpactResponse: itemId:long, window:int, -->
<!--    events: EventImpactItem[] { id:Long, eventType:EventType, title:string, occurredAt:OffsetDateTime, -->
<!--      status:string('ok'|'insufficient_data'), preAnchorAt:OffsetDateTime|null, postAnchorAt:OffsetDateTime|null, -->
<!--      prePrice:Long|null, postPrice:Long|null, changeRate:BigDecimal|null } -->
<!--  EventType enum: LOA_ON | MAJOR_UPDATE | SEASON_END | BALANCE_PATCH -->
<!--  Error/edge: 400 (to<=from / window out of 1..168), 404 (unknown item), 200-empty (empty range). -->
<!-- D-02 policy lives in queryClient defaults; the '다시 불러오기' refetch wiring is 07-03's job, not here. -->
<!-- Do NOT render any screen here; do NOT add filter/searchParams logic (Phase 9-10, D-04 forward-note). -->
</interfaces>
</context>

## Phase Goal

**As** a developer building the three read screens, **I want** zod-validated typed query hooks for all 5 endpoints plus a KST display helper, **so that** every screen consumes already-validated, correctly-typed data (with nulls handled at the boundary) and shows KST times without the off-by-9h bug — without re-implementing fetching, caching, or type/validation drift on each screen.

<tasks>

<task type="auto">
  <name>Task 1: zod schemas for the 5 read DTOs (single source of truth) + inferred types</name>
  <read_first>
    - .planning/REQUIREMENTS.md ("실제 API 계약 (코드 실측)" table — the authoritative field list the schemas mirror 1:1; the 400/404/200-empty edge rules)
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-05 zod single source; D-06 boundary .parse, unknown-key strip, explicit nullables)
    - frontend/src/lib/utils.ts (confirms the @/lib path the new modules live beside; created in 07-01)
    - frontend/package.json (the manifest into which `zod` is added)
  </read_first>
  <files>frontend/src/lib/schemas.ts, frontend/package.json</files>
  <behavior>
    - Each of the 5 responses has a zod schema whose fields and nullability match the measured backend records exactly (see `<interfaces>`).
    - The TypeScript types are derived from the schemas via `z.infer` — there is no separate hand-written `interface` for any DTO.
    - Nullable fields are `.nullable()` (summaryMessage, lastRunAt/startedAt, bucketWidth, sampleCount, and all event-impact anchor/price/changeRate fields); `eventType` is a zod enum of the 4 EventType values; `status` on impact items is a 2-value enum/union `'ok' | 'insufficient_data'`.
    - Unknown extra keys in a response are stripped (zod default object behavior), so a future backend field does not break parsing.
  </behavior>
  <action>
    Add `zod` to `frontend/package.json` deps. Create `src/lib/schemas.ts` defining: `eventTypeSchema = z.enum(['LOA_ON','MAJOR_UPDATE','SEASON_END','BALANCE_PATCH'])`; `collectionHealthSchema` with `lastRunAt`/`startedAt` as `z.string().nullable()`, the three counts `z.number()`, `status` `z.string()`, `summaryMessage` `z.string().nullable()`; `trackedItemSchema` (`id` `z.number()`, `externalItemId`/`displayName`/`category` strings, `active` `z.boolean()`) plus `trackedItemsSchema = z.array(trackedItemSchema)`; `latestPriceSchema` (`itemId` number, `minPrice` number, `collectedAt` string); `pricePointSchema` (`collectedAt` string, `minPrice` number, `sampleCount` `z.number().nullable()`), `eventPointSchema` (`occurredAt` string, `eventType` eventTypeSchema, `title` string), `timelineSchema` (`downsampled` boolean, `bucketWidth` `z.string().nullable()`, `snapshots` array of pricePoint, `events` array of eventPoint); `eventImpactItemSchema` (`id` number, `eventType` enum, `title` string, `occurredAt` string, `status` `z.enum(['ok','insufficient_data'])`, `preAnchorAt`/`postAnchorAt` `z.string().nullable()`, `prePrice`/`postPrice`/`changeRate` `z.number().nullable()`), `eventImpactSchema` (`itemId` number, `window` number, `events` array of item). Export `z.infer` types for each (e.g. `export type Timeline = z.infer<typeof timelineSchema>`). Treat all timestamps as ISO strings (formatting is `formatKst`'s job, Task 4). Do NOT add `.strict()` (unknown keys must strip for forward-compat per D-06).
  </action>
  <verify>
    <automated>cd frontend && grep -q "z.infer" src/lib/schemas.ts && grep -qE "LOA_ON|insufficient_data" src/lib/schemas.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/schemas.ts` defines 5 response schemas matching the measured fields; `grep -nE "collectionHealth|trackedItem|latestPrice|timeline|eventImpact" frontend/src/lib/schemas.ts` shows all five
    - Types are derived via `z.infer` (no separate DTO `interface`); `grep -n "z.infer" frontend/src/lib/schemas.ts` matches
    - Nullable fields use `.nullable()` and `status`/`eventType` are zod enums; `grep -nE "nullable|z.enum" frontend/src/lib/schemas.ts` matches, including `'ok'`/`'insufficient_data'` and the 4 EventType values
    - No `.strict()` is used (unknown keys strip for forward-compat, D-06)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>5 zod schemas mirror the measured backend DTOs 1:1 with explicit nullables and enums; the TS types are `z.infer`-derived so type and validation never drift (D-05), and unknown keys strip for forward-compat (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Typed API client (src/lib/api.ts) with boundary .parse validation</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-06 boundary .parse → throw → React Query error → ErrorState; D-12 single api.ts module)
    - .planning/REQUIREMENTS.md (endpoint paths + query params + 400/404 edge rules the client must surface as thrown errors)
    - frontend/src/lib/schemas.ts (the schemas whose .parse the client calls — created in Task 1)
    - frontend/vite.config.ts (the /api proxy these relative fetch paths rely on — from 07-01)
  </read_first>
  <files>frontend/src/lib/api.ts</files>
  <behavior>
    - One async function per endpoint: `getCollectionHealth()`, `getItems()`, `getLatestPrice(id)`, `getTimeline(id, from, to)`, `getEventImpact(id, window)`.
    - Each fetches the proxied relative path (`/api/...`), and on a non-2xx HTTP status throws an Error carrying the status (so 400/404 reach the screen's ErrorState, not a silent empty render).
    - On 2xx, the JSON body is validated with the matching schema's `.parse` and the parsed/typed value is returned; a zod mismatch throws (loudly fail).
    - Query params are encoded correctly (`from`/`to` ISO strings for timeline; integer `window` for impact).
  </behavior>
  <action>
    Create `src/lib/api.ts` as the single client module (D-12). Implement one async fn per endpoint hitting the RELATIVE proxied paths: `GET /api/health/collection`, `GET /api/items`, `GET /api/items/${id}/latest`, `GET /api/items/${id}/prices?from=${from}&to=${to}`, `GET /api/items/${id}/event-impact?window=${window}` (use `encodeURIComponent`/`URLSearchParams` for params). A small shared `request()` helper does `fetch`, checks `res.ok` (throw `new Error` including `res.status` on 4xx/5xx so 400/404 surface as errors), parses JSON, and returns it. Each public fn calls `request()` then returns the matching `schema.parse(json)` from Task 1 (e.g. `collectionHealthSchema.parse(...)`, `trackedItemsSchema.parse(...)`) — `.parse` throwing on drift is the D-06 boundary. Return the `z.infer` types. Do NOT swallow errors, do NOT add retry here (React Query owns retry policy, Task 3), do NOT add auth headers (endpoints are permitAll).
  </action>
  <verify>
    <automated>cd frontend && grep -q ".parse" src/lib/api.ts && grep -qE "/api/items|/api/health/collection" src/lib/api.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` exports one fn per endpoint (`getCollectionHealth`, `getItems`, `getLatestPrice`, `getTimeline`, `getEventImpact`); `grep -nE "getCollectionHealth|getItems|getLatestPrice|getTimeline|getEventImpact" frontend/src/lib/api.ts` shows all five
    - Each fn validates the response with the matching schema's `.parse`; `grep -n ".parse" frontend/src/lib/api.ts` matches
    - Non-2xx responses throw an Error including the HTTP status (400/404 surface, not silent empty); the client adds no auth header and no retry
    - Paths are the relative proxied `/api/...` (rely on the 07-01 Vite proxy), with `from`/`to`/`window` query params encoded
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>A single typed `api.ts` validates every response at the boundary with `.parse` and throws on HTTP errors and schema drift, so bad/insufficient data fails loudly into React Query rather than silently reaching a screen (D-06, D-12).</done>
</task>

<task type="auto">
  <name>Task 3: TanStack Query — QueryClient (fetch-on-mount policy) + 5 query hooks + provider</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-01 React Query per-endpoint hooks; D-02 fetch-on-mount only — refetchOnWindowFocus:false, high staleTime, no polling; "다시 불러오기" = refetch wired in 07-03)
    - frontend/src/lib/api.ts (the typed fns each hook's queryFn calls — Task 2)
    - frontend/src/main.tsx (the root the QueryClientProvider wraps — created in 07-01)
    - .planning/REQUIREMENTS.md ("확정 결정": seed 데이터 우선 → effectively static, justifies high staleTime / no polling)
  </read_first>
  <files>frontend/src/lib/queryClient.ts, frontend/src/lib/queries.ts, frontend/src/main.tsx, frontend/package.json</files>
  <behavior>
    - A single exported `QueryClient` sets query defaults: `refetchOnWindowFocus: false`, a high `staleTime` (seed data is effectively static), and no polling/`refetchInterval`.
    - `main.tsx` wraps the app in `<QueryClientProvider client={queryClient}>` so hooks resolve anywhere in the tree.
    - Five hooks — `useCollectionHealth()`, `useItems()`, `useLatestPrice(id)`, `useTimeline(id, from, to)`, `useEventImpact(id, window)` — each call `useQuery` with a stable queryKey and the matching `api.ts` fn as `queryFn`, returning React Query's typed result.
    - No screen consumes the hooks yet (that is 07-03); the hooks compile and are importable.
  </behavior>
  <action>
    Add `@tanstack/react-query` to `frontend/package.json`. Create `src/lib/queryClient.ts` exporting `export const queryClient = new QueryClient({ defaultOptions: { queries: { refetchOnWindowFocus: false, staleTime: <a long value, e.g. 5*60*1000 or Infinity>, retry: <0 or 1> } } })` — this encodes the D-02 fetch-on-mount/route-entry policy (no interval). Create `src/lib/queries.ts` with the 5 hooks: each `useQuery({ queryKey: [...stable key...], queryFn: () => api.getX(...) })` — keys like `['collection-health']`, `['items']`, `['latest', id]`, `['timeline', id, from, to]`, `['event-impact', id, window]`; the parameterized hooks accept their args and include them in the key. In `src/main.tsx`, import `queryClient` and wrap `<App/>` in `<QueryClientProvider client={queryClient}>` (keep StrictMode). Do NOT add Devtools as a hard dependency (optional), do NOT set a global `refetchInterval`, do NOT call the hooks from any component yet.
  </action>
  <verify>
    <automated>cd frontend && grep -q "refetchOnWindowFocus" src/lib/queryClient.ts && grep -q "QueryClientProvider" src/main.tsx && grep -qE "useQuery" src/lib/queries.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/queryClient.ts` exports a `QueryClient` with `refetchOnWindowFocus: false` and a high `staleTime`, no `refetchInterval`; `grep -nE "refetchOnWindowFocus|staleTime" src/lib/queryClient.ts` matches (D-02)
    - `frontend/src/lib/queries.ts` exports the 5 hooks each using `useQuery` with a stable queryKey and an `api.ts` `queryFn`; `grep -nE "useCollectionHealth|useItems|useLatestPrice|useTimeline|useEventImpact" src/lib/queries.ts` shows all five
    - `frontend/src/main.tsx` wraps the app in `QueryClientProvider` with the exported client; `grep -n "QueryClientProvider" src/main.tsx` matches
    - No global polling/`refetchInterval` is set; no hook is invoked by a component in this plan
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>A single QueryClient encodes the fetch-on-mount-only policy (no focus refetch, high staleTime, no polling — D-02), the app root provides it, and 5 typed per-endpoint hooks (D-01) wrap the api.ts client ready for the screens to consume in 07-03.</done>
</task>

<task type="auto">
  <name>Task 4: formatKst() UTC→KST display helper (off-by-9h guard)</name>
  <read_first>
    - .planning/phases/07-frontend-foundation/07-CONTEXT.md (D-09: native Intl.DateTimeFormat Asia/Seoul for display, axis/sort math on raw UTC epoch ms, no tz library)
    - .planning/PROJECT.md (off-by-9h guard / honest-data principle the helper extends to the UI)
    - .planning/REQUIREMENTS.md ("모든 시각은 UTC ISO-8601(...Z). 화면은 KST로 표시하되 데이터는 UTC 원본 유지")
    - frontend/src/lib/schemas.ts (the ISO string fields this helper formats — Task 1)
  </read_first>
  <files>frontend/src/lib/formatKst.ts</files>
  <behavior>
    - `formatKst(iso)` takes a UTC ISO-8601 string (or epoch ms) and returns a human KST string (e.g. `2026-06-25 14:30` style) computed with `Intl.DateTimeFormat` using `timeZone: 'Asia/Seoul'`.
    - The function never shifts the underlying instant by hand (no `+9h` arithmetic) — the timezone conversion is delegated entirely to `Intl`, so DST/edge handling is correct and there is no manual off-by-9h risk.
    - A companion accessor returns the raw epoch ms (`Date.parse` / `new Date(iso).getTime()`) for chart x-axis positioning and sorting, so position math stays on the UTC instant and only the visible label is KST-formatted.
    - No `date-fns-tz`, `dayjs`, or `luxon` import appears.
  </behavior>
  <action>
    Create `src/lib/formatKst.ts` exporting `formatKst(value: string | number): string` that builds a `Date` from the ISO string / epoch and formats it via `new Intl.DateTimeFormat('ko-KR', { timeZone: 'Asia/Seoul', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', hour12:false })` (pick a consistent, readable pattern). Also export a tiny `toEpochMs(iso: string): number` (= `Date.parse(iso)` / `new Date(iso).getTime()`) for axis/sort use so callers never KST-shift before computing positions (D-09). Add a brief doc comment stating the off-by-9h contract: display = KST via Intl, math = raw UTC epoch. Do NOT import any timezone library; do NOT manually add 9 hours.
  </action>
  <verify>
    <automated>cd frontend && grep -q "Asia/Seoul" src/lib/formatKst.ts && ! grep -qE "date-fns-tz|dayjs|luxon" src/lib/formatKst.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/formatKst.ts` exports `formatKst` using `Intl.DateTimeFormat` with `timeZone: 'Asia/Seoul'`; `grep -n "Asia/Seoul" src/lib/formatKst.ts` matches
    - It exports an epoch-ms accessor for axis/sort math (raw UTC instant), keeping position math off the KST-shifted value
    - No manual `+9` hour arithmetic and no `date-fns-tz`/`dayjs`/`luxon` import; `grep -nE "date-fns-tz|dayjs|luxon" src/lib/formatKst.ts` returns nothing
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>`formatKst()` renders UTC instants in Asia/Seoul via native Intl (no tz library, no manual +9h), and a companion epoch-ms accessor keeps chart/sort math on the raw UTC instant — the off-by-9h guard extended from backend to UI (FND-05, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON response → frontend parse boundary | Untrusted-by-shape JSON crosses into typed app state; zod `.parse` is the validation gate |
| npm registry → frontend deps | `zod` + `@tanstack/react-query` are added to the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0702-01 | Tampering / Information Disclosure (malformed or drifted response reaches UI) | api.ts parse boundary | mitigate | Every response is validated with `schema.parse` at the boundary (D-06): a shape mismatch throws into React Query → ErrorState, and unknown keys strip — so a drifted/backend-changed payload cannot silently corrupt app state or leak unexpected fields into the render. Nullable fields are modeled explicitly so `insufficient_data` nulls cannot masquerade as real numbers. |
| T-0702-02 | Information Disclosure (logging response bodies) | request() helper | mitigate | The client throws errors with the HTTP status/path only — it does NOT `console.log` full response bodies or any header, so nothing sensitive is dumped. The endpoints are public read (`permitAll`) and carry no secret, so the blast radius is already minimal. |
| T-0702-03 | Tampering (supply chain — new deps) | package.json | mitigate | `zod` and `@tanstack/react-query` are first-party, widely-audited packages added at current major versions; the lockfile pins the resolved tree. No timezone library is added (D-09 uses native `Intl`), keeping the dependency surface small. |
| T-0702-04 | Spoofing (auth bypass assumptions) | api.ts | accept | The 5 endpoints are `permitAll` read-only by backend design; the client sends no credentials and no admin secret (admin write UI is FE-V2-01, out of scope). No auth surface exists to attack in this plan. |
</threat_model>

<verification>
- `cd frontend && npm run build` (tsc + vite build) passes with the new `zod` + `@tanstack/react-query` deps — proving the `z.infer` types compile and the hooks type-check (FND-02 "types match the measured DTOs").
- `grep` confirms: `schemas.ts` has 5 schemas + `z.infer` + the EventType/`insufficient_data` enums; `api.ts` calls `.parse` per endpoint and throws on non-2xx; `queryClient.ts` sets `refetchOnWindowFocus:false` + high `staleTime` (D-02); `main.tsx` wraps in `QueryClientProvider`; `formatKst.ts` uses `Intl` `Asia/Seoul` and imports no tz library (D-09).
- The 5 schema field lists match the measured backend records in `src/main/java/.../web/dto` field-for-field (CollectionHealth/TrackedItem/LatestPrice/Timeline/EventImpact), with every backend-nullable field `.nullable()`.
- No screen renders the data yet and no `src/` (Java) file changes — the layer is library-only, consumed in 07-03.
</verification>

<success_criteria>
- zod is the single source of truth for the 5 DTOs with `z.infer` types and `.parse` boundary validation, modeling every nullable field explicitly (D-05, D-06); types match the measured API contract 1:1 (FND-02).
- TanStack Query exposes one fetch-on-mount-only hook per endpoint behind a single provided QueryClient (D-01, D-02) — no polling, no focus refetch.
- `formatKst()` renders UTC→KST via native Intl while axis/sort math stays on raw UTC epochs, guarding off-by-9h (FND-05, D-09); `npm run build` passes.
</success_criteria>

<output>
Create `.planning/phases/07-frontend-foundation/07-02-SUMMARY.md` when done
</output>
