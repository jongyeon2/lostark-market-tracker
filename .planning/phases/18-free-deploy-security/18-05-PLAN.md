---
phase: 18-free-deploy-security
plan: 05
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04]
files_modified:
  - docs/deploy/security-checklist.md
autonomous: false
requirements: [DEPLOY-03]

must_haves:
  truths:
    - "D-08: 배포 직전 **보안 검증 게이트** — 6개 항목을 전수 통과해야 go-live. (1) 관리자 쓰기 보호, (2) API 키/시크릿 미노출, (3) DB/Redis 포트 비공개, (4) HTTPS 강제, (5) CORS 정책(동일 출처), (6) actuator health-only + show-details never. `docs/deploy/security-checklist.md`에 각 항목의 **검증 명령/방법 + 통과 기준**을 기록한다."
    - "자동 검증(저장소): AdminSecretFilter 상수시간 비교·SecurityConfig `/api/admin/**` 인증, 코드/문서/git 히스토리 시크릿 부재, compose 포트 미publish, Caddy 자동 HTTPS, 백엔드 permissive CORS(`@CrossOrigin`/`allowedOrigin(\"*\")`) 부재, prod yml `show-details: never`."
    - "수동 검증(라이브 VM): off-box 포트 스캔으로 5432/6379 폐쇄 확인, HTTPS 인증서 유효·HTTP→HTTPS 리다이렉트, 잘못된 X-Admin-Secret 401 확인. 18-04 배포 후 수행."
    - "DEPLOY-03 게이트 통과 = 이 마일스톤(v1.3)의 배포 게이트. Core Value 경로·앱 로직 0줄(검증 전용 문서)."
  artifacts:
    - path: "docs/deploy/security-checklist.md"
      provides: "배포 직전 보안 게이트 체크리스트(자동+수동 검증·통과 기준) — go/no-go 근거 문서"
  key_links:
    - from: "체크리스트 자동 검증"
      to: "18-01/02/03 산출물(prod yml·Caddyfile·compose)"
      via: "grep/git 기반 정적 검증"
      pattern: "security-checklist"
    - from: "체크리스트 수동 검증"
      to: "18-04 라이브 배포"
      via: "off-box 포트 스캔·HTTPS·401 확인"
      pattern: "off-box"
---

<objective>
**배포 직전 보안 검증 게이트**를 정의하고 실행한다. DEPLOY-03의 6개 항목(관리자 쓰기 보호·키 미노출·DB/Redis 비공개·HTTPS·CORS·actuator 하드닝)을 각각 **검증 명령 + 통과 기준**으로 문서화하고, 저장소 정적 검증은 지금 실행하며, 라이브 검증(off-box 포트 스캔·HTTPS·401)은 18-04 배포 후 수행한다.

Purpose: "배포 직전 보안 게이트 통과"(마일스톤 성공 기준)를 검증 가능한 게이트로 만든다. 통과 전까지 go-live 금지 — 보안이 이 페이즈의 최우선.

Output: `docs/deploy/security-checklist.md` + 자동 검증 실행 결과. 앱 로직 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-free-deploy-security/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- docs/deploy/security-checklist.md 구조: 항목별 [검증 방법] [통과 기준] [상태]. 6개 항목:
  1) 관리자 쓰기 보호
     - AdminSecretFilter: MessageDigest.isEqual 상수시간 비교 + blank secret fail-closed.
       검증: grep -q "MessageDigest.isEqual" src/main/java/.../security/AdminSecretFilter.java
     - SecurityConfig: /api/admin/** authenticated.
       검증: grep -q "/api/admin/\*\*" src/main/java/.../security/SecurityConfig.java (authenticated 규칙)
     - prod 시크릿 강함: .env.prod의 ADMIN_API_SECRET가 openssl rand(≥32B)·123456789 아님 (수동, VM에서).
     - 라이브: 잘못된 X-Admin-Secret → 401 (수동).
  2) API 키/시크릿 미노출
     - 코드/문서 하드코딩 부재: git grep -nE "eyJ[A-Za-z0-9_-]{20,}" (JWT 패턴) 결과 0(문서 예시 제외).
     - git 히스토리: git log -p -S 실키 조각 결과 0(실키는 .env에만).
     - .env* gitignore: git check-ignore .env .env.prod 매칭.
  3) DB/Redis 포트 비공개
     - 정적: docker-compose.prod.yml에 5432/6379/8080 host publish 부재(caddy만 80/443).
     - 라이브(수동): off-box에서 nc -zv <ip> 5432 / 6379 → 연결 실패(closed).
  4) HTTPS 강제
     - 정적: Caddyfile이 도메인 사이트 블록(자동 TLS) + HSTS.
     - 라이브(수동): https 자물쇠·유효 인증서, http://<도메인> → https 리다이렉트.
  5) CORS 정책(동일 출처)
     - 백엔드에 permissive CORS 부재: grep -rE "@CrossOrigin|allowedOrigin(Patterns)?\(\"\*\"\)|setAllowedOrigins" src/main → 0 또는 wildcard+credentials 조합 없음. 동일 출처라 CORS 불필요.
  6) actuator 하드닝
     - application-prod.yml: management.endpoint.health.show-details: never + exposure.include: health.
       검증: grep -q "show-details: never" src/main/resources/application-prod.yml
  헤더에 "전 항목 PASS 전까지 go-live 금지" 명시. 상태 컬럼(자동=✓ 실행결과, 수동=배포 후). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 보안 체크리스트 문서 + 저장소 정적 검증 실행</name>
  <read_first>
    - src/main/java/com/lostark/tracker/security/AdminSecretFilter.java (상수시간 비교·fail-closed 근거)
    - src/main/java/com/lostark/tracker/security/SecurityConfig.java (/api/admin/** 인증·permitAll 범위)
    - src/main/resources/application-prod.yml (18-01 — show-details never)
    - docker-compose.prod.yml (18-03 — 포트 미publish)
    - Caddyfile (18-02 — 자동 HTTPS·HSTS)
  </read_first>
  <files>docs/deploy/security-checklist.md</files>
  <action>
    `docs/deploy/security-checklist.md`를 인터페이스 구조대로 작성한다(6개 항목·검증 방법·통과 기준·상태 컬럼, 한국어). 그다음 **저장소 정적 검증을 실제 실행**해 각 항목 상태를 채운다: (1) `grep MessageDigest.isEqual`·`/api/admin/**` 인증 확인, (2) `git grep -nE "eyJ[A-Za-z0-9_-]{20,}"`로 JWT 하드코딩 부재 확인 + `git check-ignore .env .env.prod`, (3) compose 포트 미publish grep, (4) Caddyfile 자동 TLS·HSTS grep, (5) `grep -rE "@CrossOrigin|allowedOrigin.*\*|setAllowedOrigins" src/main`으로 permissive CORS 부재 확인, (6) prod yml `show-details: never` grep. 각 결과를 체크리스트 상태 컬럼에 기록. 라이브 전용 항목(off-box 포트·HTTPS·401)은 "배포 후 수동"으로 표기.
  </action>
  <verify>
    <automated>grep -q "show-details: never" src/main/resources/application-prod.yml && ! grep -rlE "@CrossOrigin" src/main/java && git grep -nE "eyJ[A-Za-z0-9_-]{30,}" -- ':!*.md' ':!.planning' | grep -v example; echo "CORS/키 정적 검증 완료"; grep -q "go-live 금지" docs/deploy/security-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - 체크리스트가 6개 항목 전부(관리자 보호·키 미노출·DB/Redis 비공개·HTTPS·CORS·actuator)를 검증 방법+통과 기준과 함께 문서화
    - 저장소 정적 검증 실행 결과 기록: 관리자 필터 상수시간 비교 확인, 코드/문서에 JWT 하드코딩 0(`git grep eyJ...` 실키 매칭 0), compose 포트 미publish 확인, prod yml `show-details: never` 확인, 백엔드 permissive CORS 부재 확인
    - `git check-ignore .env.prod` 매칭(시크릿 커밋 방지 확인)
    - 라이브 전용 항목이 "배포 후 수동"으로 명확히 구분
    - 헤더에 "전 항목 PASS 전까지 go-live 금지" 명시
  </acceptance_criteria>
  <done>DEPLOY-03 게이트가 검증 가능한 체크리스트로 문서화되고 저장소 정적 항목이 전부 PASS로 실증됐다.</done>
</task>

<task type="checkpoint">
  <name>Task 2: [수동] 라이브 보안 검증 (off-box) + go/no-go</name>
  <read_first>
    - docs/deploy/security-checklist.md (Task 1 — 라이브 항목·통과 기준)
  </read_first>
  <action>
    18-04 배포 후, 사용자가 off-box에서 라이브 보안 항목을 검증한다: (a) `nc -zv <도메인> 5432`·`6379` → 연결 실패(포트 폐쇄) 확인, (b) `https://<도메인>` 인증서 유효 + `http://<도메인>` → https 리다이렉트, (c) 잘못된 `X-Admin-Secret`로 `/api/admin/*` 요청 → 401, (d) `.env.prod`의 ADMIN_API_SECRET가 강시크릿(≠123456789). 결과를 체크리스트 상태에 기록하고, **6개 항목 전부 PASS면 go-live 승인**, 하나라도 실패면 배포 보류·수정.
  </action>
  <verify>
    <manual>off-box 포트 스캔·HTTPS 리다이렉트·401·강시크릿 확인 결과를 체크리스트에 PASS/FAIL로 기록, 전 항목 PASS 시 go-live</manual>
  </verify>
  <acceptance_criteria>
    - off-box에서 5432·6379 연결 실패(포트 비공개 실증)
    - HTTPS 인증서 유효 + HTTP→HTTPS 리다이렉트 동작
    - 잘못된 X-Admin-Secret → 401(관리자 쓰기 보호 실증)
    - 6개 항목 전부 PASS → go-live 승인 기록
  </acceptance_criteria>
  <done>라이브 보안 항목까지 전수 PASS해 DEPLOY-03 게이트를 통과하고 go-live가 승인됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 게이트 | go-live 전 6개 항목 전수 검증 — 미통과 시 배포 차단 |
| 정적 vs 라이브 | 저장소 항목은 실행자 자동 검증, 노출면 항목은 off-box 수동 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Info Disclosure | 배포 후에야 노출 발견 | mitigate | 배포 직전 게이트로 사전 검증(정적+라이브) |
| T-18-05-02 | Spoofing | 약한 관리자 시크릿 미검출 | mitigate | 강시크릿 확인 항목 + 401 라이브 테스트 |
| T-18-05-03 | Info Disclosure | 실키가 커밋/문서에 잔존 | mitigate | git grep JWT 패턴 + git check-ignore 검증 |
| T-18-05-04 | Tampering | permissive CORS로 크리덴셜 탈취 | mitigate | 백엔드 wildcard CORS 부재 확인 + 동일 출처 구조 |
</threat_model>

<verification>
- `docs/deploy/security-checklist.md`: 6개 항목·검증 방법·통과 기준·상태.
- 저장소 정적 검증 전부 PASS(관리자 보호·키 미노출·포트 미publish·HTTPS 구성·CORS 부재·actuator never).
- (수동, 배포 후) off-box 포트 폐쇄·HTTPS 리다이렉트·401·강시크릿 PASS.
- 앱 로직·수집/캐시/event-impact diff 0줄(검증 전용).
</verification>

<success_criteria>
- 배포 직전 보안 게이트가 검증 가능한 형태로 통과된다 — 관리자 쓰기 보호·API 키 미노출·DB/Redis 포트 비공개·HTTPS·CORS 정책(DEPLOY-03).
- 전 항목 PASS 전까지 go-live가 차단된다.
</success_criteria>

<output>
Create `.planning/phases/18-free-deploy-security/18-05-SUMMARY.md` when done
</output>
