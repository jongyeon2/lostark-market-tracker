---
phase: 11-demo-surface-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/dashboard/DashboardPage.tsx
  - frontend/src/features/timeline/TimelinePage.tsx
  - frontend/src/features/impact/ImpactPage.tsx
  - frontend/src/components/state/EmptyState.tsx
  - frontend/src/components/state/ErrorState.tsx
  - frontend/src/components/state/LoadingState.tsx
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "**audit-and-fix 점검·정렬 패스이지 리디자인이 아니다(D-07).** Dashboard·Item Timeline·Event Impact 3화면을 순회하며 로딩/빈/에러 상태와 간격·정렬의 **명백한 불일치·회귀만** 수정한다. 신규 컴포넌트를 만들지 않고 Phase 7~10에서 구축된 공용 상태 컴포넌트(`frontend/src/components/state/AsyncBoundary.tsx`·`LoadingState.tsx`·`EmptyState.tsx`·`ErrorState.tsx`)를 **점검·정렬**해 재사용한다. 6-pillar 체계 감사·대규모 비주얼 고도화는 v2(`/gsd-ui-review`) 영역으로 **이번 범위 밖**이다."
    - "3화면이 비동기 영역을 **`AsyncBoundary`로 감싸 로딩/빈/에러 상태를 공용 컴포넌트로 일관 렌더**한다(Phase 7 D-08 컴포넌트별 상태 격리의 연장). 각 화면의 로딩=`LoadingState`(skeleton), 빈=`EmptyState`, 에러=`ErrorState`가 동일 패턴으로 노출되며 한 화면만 자체 인라인 분기로 어긋나는 회귀가 없다. seed 프로파일 백엔드 기준 3화면이 전부 **비어있지 않게** 뜬다(DEMO-01 재현 기반)."
    - "**반응형 = 데스크톱 우선 + 좁은 화면 깨짐 없음(D-08).** 면접 데모 기준 데스크톱(노트북/모니터, ~1440px)을 제대로 마감하고, 좁은 폭(태블릿~좌우 분할, ~768px)에서 가로 스크롤·요소 겹침·레이아웃 붕괴가 없다. Phase 10 D-04의 표↔카드 전환이 이미 그 선을 다루므로 본 패스는 그 경계를 깨지 않는 선에서 간격·정렬만 정돈한다. 모바일 완성형 브레이크포인트(Recharts 차트·네비 모바일 대응)는 v2로 **추가하지 않는다**."
    - "**백엔드 무변경 — Java `src/` 0줄 변경.** 본 plan은 `frontend/src/**`의 점검 결과 불일치 항목만 수정하고 백엔드 도메인/수집 파이프라인은 건드리지 않는다(사용자 제약). 수정은 표시·정렬·상태 렌더 일관성에 한정하며 데이터 계약(스키마·API 호출)·라우팅·URL 상태 정책은 변경하지 않는다(Phase 7~10 결정 보존)."
    - "`cd frontend && npm run build`(tsc -b + vite build)가 **타입 에러 0**으로 통과한다. 점검 결과 수정할 불일치가 없는 화면은 무변경으로 두되, SUMMARY에 화면별 점검 체크리스트 결과(수정함/이미 일관)를 기록해 '점검을 실제로 했음'을 증거로 남긴다."
  artifacts:
    - path: "frontend/src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard 화면의 로딩/빈/에러 상태 + 데스크톱/좁은폭 반응형 일관 마감(불일치 발견 시 수정)"
      contains: "AsyncBoundary"
    - path: "frontend/src/features/timeline/TimelinePage.tsx"
      provides: "Item Timeline 화면의 상태 렌더·반응형 일관 마감(불일치 발견 시 수정)"
      contains: "AsyncBoundary"
    - path: "frontend/src/features/impact/ImpactPage.tsx"
      provides: "Event Impact 화면의 상태 렌더·반응형 일관 마감(불일치 발견 시 수정)"
      contains: "AsyncBoundary"
  key_links:
    - from: "frontend/src/features/dashboard/DashboardPage.tsx"
      to: "frontend/src/components/state/AsyncBoundary.tsx"
      via: "비동기 영역을 AsyncBoundary로 감싸 LoadingState/EmptyState/ErrorState를 일관 렌더 — 3화면 공통 패턴 점검 단위(D-07)"
      pattern: "AsyncBoundary"
    - from: "frontend/src/features/impact/ImpactPage.tsx"
      to: "frontend/src/features/_shared/ItemSelect.tsx"
      via: "공용 셀렉터/카드(_shared)를 3화면이 동일하게 소비하는지 점검 — 간격·정렬 일관성의 출처"
      pattern: "ItemSelect"
---

<objective>
포트폴리오 데모 표면의 **시각적 마감(ROADMAP 완료조건 4)** 을 잠그는 audit-and-fix 패스다. Dashboard / Item Timeline / Event Impact 3화면을 순회하며 (1) 로딩/빈/에러 상태가 공용 컴포넌트(`AsyncBoundary`+`LoadingState`/`EmptyState`/`ErrorState`)로 **일관 렌더**되는지, (2) 데스크톱 우선 + 좁은 폭에서 **깨지지 않는지** 점검하고, 발견된 **명백한 불일치·회귀만** 최소 수정한다.

핵심은 **리디자인이 아니라 일관성 회귀 수정**이라는 점(D-07)이다. 공용 상태 컴포넌트·표↔카드 전환은 Phase 7~10에서 이미 구축됐으므로 본 패스는 새로 만들지 않고 **점검·정렬**만 한다. 이 패스가 먼저 끝나야 11-02 스크린샷이 "잘 나온 최종 화면"을 담고, 11-03 README가 그 화면을 정직히 서술할 수 있다 — 그래서 wave 1이다.

Output: `frontend/src/**`의 점검 결과 불일치 수정(없으면 무변경) + SUMMARY에 화면별 점검 체크리스트 결과. 백엔드 `src/`는 0줄 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-demo-surface-docs/11-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-UI-SPEC.md
@.planning/phases/09-item-timeline/09-UI-SPEC.md
@.planning/phases/10-event-impact/10-UI-SPEC.md

<interfaces>
<!-- 공용 상태 컴포넌트 (무변경 원칙 — 점검 대상, 정렬 외 로직 변경 금지):
     frontend/src/components/state/AsyncBoundary.tsx — { isLoading, isError, isEmpty, loading?, error?, empty?, children } 형태로 상태 격리(Phase 7 D-08)
     LoadingState.tsx(skeleton) · EmptyState.tsx · ErrorState.tsx -->

<!-- 3화면 진입점: features/{dashboard/DashboardPage, timeline/TimelinePage, impact/ImpactPage}.tsx
     공용: features/_shared/{ItemSelect, LatestPriceCard}.tsx
     반응형 기준선: Phase 10 D-04 — 넓은 폭=표(EventImpactTable), 좁은 폭=카드(EventImpactCards) -->

<!-- D-07 범위: 명백한 불일치/회귀만 수정. D-08 범위: 데스크톱 마감 + 좁은폭(~768px) 무붕괴. 모바일 완성형 금지. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3화면 상태 렌더링 일관성 점검·정렬 (로딩/빈/에러)</name>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/ImpactPage.tsx, frontend/src/components/state/EmptyState.tsx, frontend/src/components/state/ErrorState.tsx, frontend/src/components/state/LoadingState.tsx</files>
  <read_first>frontend/src/components/state/AsyncBoundary.tsx, frontend/src/components/state/LoadingState.tsx, frontend/src/components/state/EmptyState.tsx, frontend/src/components/state/ErrorState.tsx, frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/ImpactPage.tsx, .planning/phases/07-frontend-foundation/07-UI-SPEC.md</read_first>
  <action>3화면 각각에서 비동기(쿼리) 영역이 `AsyncBoundary`(또는 동등 공용 상태 컴포넌트)로 감싸져 **로딩=`LoadingState`(skeleton)·빈=`EmptyState`·에러=`ErrorState`** 를 일관 렌더하는지 점검한다. 화면 중 하나만 자체 인라인 `if (isLoading) return ...` 분기로 공용 패턴에서 벗어났거나, 빈/에러 메시지 컴포넌트가 누락·중복된 **명백한 불일치**가 있으면 공용 컴포넌트 사용으로 정렬한다. 공용 상태 컴포넌트 자체는 **로직 변경 없이** props/문구/간격 불일치(예: EmptyState/ErrorState의 padding·아이콘·텍스트 정렬이 3화면에서 들쭉날쭉)만 정돈한다. 데이터 계약·쿼리키·라우팅은 변경하지 않는다. **수정 없이 이미 일관한 화면은 그대로 둔다** — 점검 결과(화면별 로딩/빈/에러 3상태 × 일관 여부)를 SUMMARY에 표로 기록한다.</action>
  <verify>
    <automated>cd frontend && grep -lq "AsyncBoundary" src/features/dashboard/DashboardPage.tsx && grep -lq "AsyncBoundary" src/features/timeline/TimelinePage.tsx && grep -lq "AsyncBoundary" src/features/impact/ImpactPage.tsx && npm run build && echo "state-consistency OK"</automated>
  </verify>
  <done>3화면이 모두 AsyncBoundary 기반으로 로딩/빈/에러를 공용 컴포넌트로 렌더하고(인라인 분기 회귀 없음), build 통과, SUMMARY에 화면별 3상태 점검 체크리스트가 기록됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 데스크톱 우선 반응형 + 간격/정렬 마감 (좁은폭 무붕괴)</name>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/ImpactPage.tsx</files>
  <read_first>frontend/src/components/layout/AppLayout.tsx, frontend/src/features/impact/EventImpactTable.tsx, frontend/src/features/impact/EventImpactCards.tsx, frontend/src/features/timeline/PriceTimelineChart.tsx, .planning/phases/10-event-impact/10-UI-SPEC.md</read_first>
  <action>3화면을 데스크톱(~1440px)과 좁은 폭(~768px) 두 기준에서 점검한다. **데스크톱**: 컨테이너 max-width·좌우 패딩·카드/표 간 수직 간격이 3화면에서 일관(예: 동일 `space-y-*`/`gap-*`/`px-*` 토큰)인지 확인하고, 한 화면만 어긋난 간격을 정렬한다. **좁은 폭**: 가로 스크롤·요소 겹침·표 오버플로가 없는지 확인한다 — Event Impact는 이미 표↔카드 전환(Phase 10 D-04)이 있으니 그 경계가 유지되는지만 보고, Timeline 차트(Recharts)는 `ResponsiveContainer`로 폭에 맞게 줄어드는지 확인한다. **모바일 완성형 브레이크포인트는 추가하지 않는다(D-08)** — 좁은 폭에서 "깨지지 않음"이 합격선이지 "완성형"이 아니다. 발견된 명백한 깨짐/간격 불일치만 최소 클래스 조정으로 수정하고, 없으면 무변경. 점검 결과(화면별 데스크톱/좁은폭 OK 여부)를 SUMMARY에 기록한다.</action>
  <verify>
    <automated>cd frontend && npm run build && echo "responsive-finish build OK"</automated>
  </verify>
  <done>3화면이 데스크톱에서 일관된 간격·정렬로 마감되고 좁은 폭(~768px)에서 가로 스크롤/겹침/표 오버플로가 없으며(모바일 완성형 미추가), build 통과, SUMMARY에 화면별 반응형 점검 결과가 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (read-only 프론트 마감) | 본 plan은 표시·정렬·상태 렌더 일관성만 수정한다. 새 입력 경로·인증·네트워크 경계·시크릿 취급을 추가하지 않으며 기존 read API 소비 방식도 바꾸지 않는다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information Disclosure | 에러 상태(`ErrorState`) 렌더 | accept | 에러 UI는 사용자향 고정 문구만 노출(스택/내부 메시지/시크릿 비노출) — Phase 8 DASH-02의 "시크릿 노출 없음" 원칙 보존, 본 패스는 그 경계를 약화하지 않는다. |
| T-11-01-02 | Tampering | Tailwind 클래스 조정 | accept | 간격/정렬 클래스는 빌드타임 정적 문자열(사용자 입력 아님). JIT가 정적 컴파일하므로 주입 표면 없음. |

**고(high) 심각도 위협 없음** — read-only 시각 마감 phase로 신규 공격 표면을 추가하지 않는다(security_block_on: high 게이트 통과).
</threat_model>

<verification>
- `cd frontend && npm run build`가 타입 에러 0으로 통과한다.
- 3화면(`DashboardPage`/`TimelinePage`/`ImpactPage`)이 모두 `AsyncBoundary` 기반으로 로딩/빈/에러를 공용 상태 컴포넌트로 렌더한다(인라인 분기 회귀 없음).
- 데스크톱(~1440px) 간격·정렬이 3화면에서 일관하고, 좁은 폭(~768px)에서 가로 스크롤/겹침/표 오버플로가 없다.
- 신규 컴포넌트·신규 라이브러리·모바일 완성형 브레이크포인트가 추가되지 않았다(D-07/D-08 범위 준수).
- Java `src/` 무변경(0줄). 데이터 계약·라우팅·URL 상태 정책 무변경(Phase 7~10 결정 보존).

## 회귀 안전 (기존 기능 보존)
- 수정은 표시·정렬·상태 렌더 일관성에 한정되며 쿼리키·스키마·라우팅을 건드리지 않으므로 Phase 8·9·10 화면 동작은 보존된다. `npm run build`가 전체 타입 그래프를 통과시켜 회귀를 차단한다. 점검 결과 불일치가 없으면 해당 화면은 무변경으로 두어 불필요한 diff를 만들지 않는다.
</verification>

<success_criteria>
- ROADMAP 완료조건 4(로딩/빈/에러 + 기본 반응형이 3화면에서 일관 마감)가 충족된다.
- audit-and-fix 범위(D-07)·데스크톱 우선 반응형(D-08)을 벗어나지 않고 명백한 불일치만 정정한다.
- build green, 백엔드 무변경, 다운스트림(11-02 스크린샷·11-03 README)이 "잘 나온 최종 화면"을 전제할 수 있다.
</success_criteria>

<output>
Create `.planning/phases/11-demo-surface-docs/11-01-SUMMARY.md` when done.
</output>
