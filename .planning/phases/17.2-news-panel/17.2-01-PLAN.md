---
phase: 17.2-news-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java
  - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java
  - .planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "D-05: 구현 전 로아 공식 `/news/events`·`/news/notices`의 실 응답 필드명을 라이브 1회 실측(기존 MarketsApiSpikeTest 하니스 확장, @Disabled·spike 프로파일·키 안전 계승, 공개 메타만)해 EventDTO/NoticeDTO 매핑을 잠근다 — 추정/플레이스홀더 0건(Phase 12/17.1 spike-then-lock 동형)"
    - "D-06: Core Value 가드 — 수집/가격 캐시/event-impact/WatchlistSeeder/SyntheticDemoData 로직 0줄. 변경은 spike 패키지(LostarkSpikeClient에 GET /news 메서드 추가 + MarketsApiSpikeTest 케이스) + findings에 한정"
    - "실 JWT 키·계정 식별자가 findings·커밋 어디에도 기재되지 않는다 — env LOSTARK_API_KEY만 사용, `bearer`/`eyJ...` 토큰 grep 0건"
    - "스파이크 하니스는 실측 후 @Disabled·spike 프로파일을 유지 — `./gradlew build`가 키 없이 그린(CI 미실행)"
  artifacts:
    - path: ".planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md"
      provides: "/news/events·/news/notices 실측 필드 → EventDTO/NoticeDTO 매핑 잠금표 (D-05) + 키-안전 노트"
  key_links:
    - from: "17.2-NEWS-SPIKE-FINDINGS.md (실측 필드명)"
      to: "17.2-02 NewsEvent/NewsNotice DTO + LostarkNewsClient 파싱"
      via: "spike-then-lock 전사 — 실측 필드를 백엔드 DTO 매핑에 그대로 반영"
      pattern: "EventDTO"
---

<objective>
로아 공식 `/news/events`·`/news/notices`의 **실 응답 필드명을 라이브 1회 실측**해 백엔드 DTO 매핑(17.2-02)을 잠근다(D-05). Phase 12/17.1 spike-then-lock 동형 — 기존 `MarketsApiSpikeTest`(@Disabled·spike 프로파일) 하니스를 확장하고, `LostarkSpikeClient`에 GET `/news/events`·`/news/notices` 메서드를 추가해 공개 메타(제목/링크/날짜/타입/기간)만 실측한다. 런타임 키 불필요·데이터(필드 스키마)만 잠근다.

Purpose: 백엔드 NewsEvent/NewsNotice DTO와 파싱을 실 API 필드에 맞춰 잠가, 구현 후 필드 불일치로 인한 재작업을 제거한다.

Output: `/news` 실측 필드 → EventDTO/NoticeDTO 매핑을 담은 `17.2-NEWS-SPIKE-FINDINGS.md`(공개 메타만). 하니스는 @Disabled 유지, 프로덕션 코드/read-path 변경 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-news-panel/17.2-CONTEXT.md
@docs/specs/2026-07-06-dashboard-news-panel-design.md
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- LostarkSpikeClient.java(spike 프로파일): RestClient(base-url + `authorization: bearer {token}`), 기존 getMarketOptions()/searchMarketItems() 패턴. ★ 추가: getNewsEvents() = GET /news/events, getNewsNotices() = GET /news/notices (둘 다 GET, 요청 바디 없음). toEntity(String.class)로 status+body 반환. -->
<!-- MarketsApiSpikeTest.java: @Disabled·@ActiveProfiles("spike")·PostgresRedisContainers. ★ 추가: captureNewsFields() — getNewsEvents()/getNewsNotices() 호출, 응답 body의 필드 키(예: Title/Link/StartDate/EndDate/Thumbnail/RewardDate; Title/Date/Link/Type)를 콘솔로 출력(공개 메타만, 가격/키 없음). 실측 후 @Disabled 원복. -->
<!-- 실행: spike 프로파일 + env LOSTARK_API_KEY로 단일 메서드 타겟 실행(--tests ...captureNewsFields), @Disabled 임시 해제→실행→원복. Gradle test stdout은 build/test-results XML <system-out>에서 회수(17.1-01 선례). build.gradle .env 로딩은 bootRun 전용 → 키를 값 출력 없이 셸 env로 주입 후 --no-daemon. -->
<!-- 키-안전(상시): 실 JWT는 env LOSTARK_API_KEY에서만. findings·커밋엔 공개 필드명/샘플 제목만 — 키·계정 식별자 금지. 커밋 전 grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" → 0건. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LostarkSpikeClient에 GET /news 메서드 추가 + MarketsApiSpikeTest 실측 케이스</name>
  <read_first>
    - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java (RestClient 패턴·기존 GET 메서드 — 확장 대상)
    - src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java (@Disabled/spike 하니스·printItemFields 패턴 — 확장 대상)
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md (실측 절차·키-안전 노트 원형)
  </read_first>
  <files>src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java, src/test/java/com/lostark/tracker/spike/MarketsApiSpikeTest.java</files>
  <action>
    LostarkSpikeClient에 `getNewsEvents()`(GET `/news/events`)와 `getNewsNotices()`(GET `/news/notices`) 메서드를 추가한다(기존 `getMarketOptions()`와 동일 시그니처 패턴 — `restClient.get().uri(path).retrieve().toEntity(String.class)`). MarketsApiSpikeTest에 `captureNewsFields()` @Test를 추가해 두 메서드를 호출하고, 응답 status + **필드 키/샘플(공개 메타: 제목·링크·시작/종료일·타입 등, 가격/키 없음)**를 콘솔로 출력한다(각 응답 첫 1~2개 항목의 필드명 식별 목적). 실행은 spike 프로파일 + env LOSTARK_API_KEY로 `captureNewsFields`만 타겟(@Disabled 임시 해제→실행→원복). 콘솔 출력만 사용(raw 디스크 미덤프). PriceCollector/캐시/event-impact·WatchlistSeeder·SyntheticDemoData·LostarkNewsClient(미존재) 등 프로덕션 read/collect 경로는 **일절 건드리지 않는다**(D-06).
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - LostarkSpikeClient에 getNewsEvents()·getNewsNotices() GET 메서드가 존재한다
    - MarketsApiSpikeTest에 captureNewsFields() 케이스가 있고, 실행 후 @Disabled·spike 프로파일 상태 — `./gradlew build`가 env 키 없이 그린
    - 실측 세션이 /news/events·/news/notices의 실 필드명을 콘솔로 반환(다음 태스크 findings 입력)
    - 수집/캐시/event-impact 및 WatchlistSeeder/SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>/news 두 엔드포인트의 실 필드를 실측할 수 있는 spike 케이스가 준비·실행되고, 하니스는 키 없이 그린인 @Disabled 상태로 복귀하며, 실측 필드가 콘솔로 확보된다.</done>
</task>

<task type="auto">
  <name>Task 2: 17.2-NEWS-SPIKE-FINDINGS.md 작성 — /news 필드 → DTO 매핑 잠금 + 키-안전</name>
  <read_first>
    - .planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md (표 포맷·키-안전 노트 계승)
    - docs/specs/2026-07-06-dashboard-news-panel-design.md (§5 데이터 계약 — 예상 DTO 형태)
  </read_first>
  <files>.planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md</files>
  <action>
    Task 1 실측 콘솔을 근거로 `17.2-NEWS-SPIKE-FINDINGS.md`를 작성한다. 포함: (1) `/news/events` 실 필드 표(필드명·타입·샘플, 가격/키 제외) → **EventDTO 매핑**(title/link/startDate/endDate[/thumbnail]). (2) `/news/notices` 실 필드 표 → **NoticeDTO 매핑**(title/link/date/type). (3) 각 엔드포인트 HTTP 메서드(GET)·경로·응답 컨테이너 형태(배열/객체) 명시. (4) 키-안전 노트(공개 메타만, 실키 미기재). 커밋 전 `grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/17.2-news-panel/` → 0건 확인.
  </action>
  <verify>
    <automated>test -f .planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md && grep -qiE "EventDTO|NoticeDTO" .planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md && ! grep -rEi "bearer [A-Za-z0-9._-]{20,}|eyJ[A-Za-z0-9._-]{10,}" .planning/phases/17.2-news-panel/</automated>
  </verify>
  <acceptance_criteria>
    - 17.2-NEWS-SPIKE-FINDINGS.md에 /news/events·/news/notices 실 필드가 EventDTO/NoticeDTO 매핑으로 기재된다(추정 0건)
    - 각 엔드포인트 GET 메서드·경로·응답 컨테이너 형태가 명시된다
    - findings·phase 디렉토리에 `bearer`/`eyJ...` 토큰·실키가 없다(grep 0건)
  </acceptance_criteria>
  <done>/news 실측 필드가 DTO 매핑으로 잠긴 17.2-NEWS-SPIKE-FINDINGS.md가 존재하고, 키가 새지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 셸(env) → 외부 Lostark API | 실 JWT(LOSTARK_API_KEY)로 /news 실측 — 키는 env에서만, 소스/findings/CI 진입 금지 |
| 실측 결과 → 커밋(공개 저장소) | findings는 공개 필드명/샘플 제목만, 실키·계정 식별자 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1721-01 | Information Disclosure | 실측 중 실 JWT 키가 findings·커밋에 유입 | mitigate | 키는 env LOSTARK_API_KEY에서만, findings는 공개 필드명만 — verify가 `bearer`/`eyJ...` grep 0건 강제, 콘솔 출력만(raw 미덤프) |
| T-1721-02 | Tampering | 잘못된 필드명을 잠그면 17.2-02 파싱이 실패 | mitigate | 실 API 실측값만 기재(추정 0건) |
| T-1721-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 spike 하니스만 확장 |
</threat_model>

<verification>
- `17.2-NEWS-SPIKE-FINDINGS.md`에 /news/events·/news/notices 실 필드가 EventDTO/NoticeDTO 매핑으로 잠김(추정 0건).
- `./gradlew build`가 env 키 없이 그린 — MarketsApiSpikeTest는 @Disabled·spike 유지.
- phase 디렉토리에 `bearer`/`eyJ...` 토큰 없음(grep 0건).
- 수집/캐시/event-impact·WatchlistSeeder·SyntheticDemoData diff 0줄(D-06).
</verification>

<success_criteria>
- /news 두 엔드포인트의 실 응답 필드가 findings에 DTO 매핑으로 잠긴다(D-05, 17.2-02 전제).
- 실키 미기재, 스파이크 하니스 CI-안전(@Disabled), 프로덕션 코드 회귀 0줄(D-06).
</success_criteria>

<output>
Create `.planning/phases/17.2-news-panel/17.2-01-SUMMARY.md` when done
</output>
