---
phase: 17.3-coupon-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/lostark/tracker/domain/Coupon.java
  - src/main/resources/db/migration/V5__add_coupon.sql
  - src/main/java/com/lostark/tracker/repository/CouponRepository.java
  - src/main/java/com/lostark/tracker/web/dto/CouponRequest.java
  - src/main/java/com/lostark/tracker/web/dto/CouponResponse.java
  - src/main/java/com/lostark/tracker/admin/AdminCouponService.java
  - src/main/java/com/lostark/tracker/web/admin/AdminCouponController.java
  - src/main/java/com/lostark/tracker/web/CouponController.java
  - src/test/java/com/lostark/tracker/admin/AdminCouponControllerIT.java
  - src/test/java/com/lostark/tracker/web/CouponControllerIT.java
autonomous: true
requirements: [COUPON-01, COUPON-02, COUPON-03]

must_haves:
  truths:
    - "D-01: 쿠폰 만료일은 날짜만(`LocalDate`) — 엔티티 `expiresAt`는 DATE 컬럼, 시각/시간대 변환 없음. 공개 유효 판정은 `expiresAt >= 오늘(서버 KST 날짜, `LocalDate.now(ZoneId.of(\"Asia/Seoul\"))`)` — 만료일 당일은 그날 끝(23:59 KST)까지 유효로 포함. SPEC의 '만료 비교 UTC 기준' 문구는 date-only D-01 결정(KST 날짜 비교)으로 대체됨"
    - "D-04: 쿠폰 `code`에 unique 제약 없음 — V5 마이그레이션은 code UNIQUE 인덱스를 만들지 않는다(워치리스트 external_item_id-unique 선례 미적용). 같은 코드 재등록 자유"
    - "COUPON-01: 관리자 CRUD 스택은 이벤트(`GameEvent`)를 1:1 미러 — `/api/admin/coupons` POST 201·GET 목록(만료 포함)·PUT full-replace·DELETE 204, `CouponRequest` 바인딩(엔티티 미바인딩)으로 id/createdAt/updatedAt mass-assignment 차단. X-Admin-Secret 게이트는 SecurityConfig `/api/admin/**` authenticated로 이미 적용 — SecurityConfig 무변경"
    - "COUPON-02/03: 공개 `GET /api/coupons`(permitAll)는 미만료 쿠폰만 만료임박순(expiresAt ASC)으로 반환하고 만료 쿠폰은 제외 — admin 목록에는 만료 쿠폰도 유지. 표시 ≤6 캡은 대시보드(17.3-03) 책임(뉴스 패널 slice 선례)이라 API는 유효 전체를 정렬 반환"
    - "Core Value 가드 — `PriceCollector`·`CollectionConfig`·`LatestPriceCache`·`EventImpactService`·`WatchlistSeeder`·`SyntheticDemoData` 로직 0줄. 쿠폰은 독립 도메인·독립 테이블(coupon), JPA `ddl-auto=validate`는 V5 마이그레이션이 스키마를 소유해 통과"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/web/admin/AdminCouponController.java"
      provides: "관리자 CRUD `/api/admin/coupons`(X-Admin-Secret 게이트) → CouponResponse (COUPON-01)"
    - path: "src/main/java/com/lostark/tracker/web/CouponController.java"
      provides: "공개 `GET /api/coupons` → 미만료·만료임박순 CouponResponse 목록 (COUPON-02/03)"
    - path: "src/main/resources/db/migration/V5__add_coupon.sql"
      provides: "coupon 테이블(code·reward·expires_at DATE·타임스탬프), code unique 없음 (D-01/D-04)"
  key_links:
    - from: "CouponController GET /api/coupons + AdminCouponController /api/admin/coupons"
      to: "frontend getCoupons()/admin 쿠폰 훅 (17.3-02)"
      via: "read DTO 계약(CouponResponse) + admin CRUD 계약"
      pattern: "/api/coupons"
    - from: "Coupon 엔티티 + V5 마이그레이션"
      to: "JPA ddl-auto=validate"
      via: "Flyway가 스키마 소유 — 엔티티 매핑과 컬럼 일치"
      pattern: "coupon"
---

<objective>
관리자 쿠폰 쓰기 스택과 공개 읽기 스택을 신규로 구현한다 — 이벤트(`GameEvent`/`AdminEventController`) 패턴을 1:1 미러링해 `Coupon` 엔티티 + `V5__add_coupon.sql` 마이그레이션 + `CouponRepository` + `CouponRequest`/`CouponResponse` DTO + `AdminCouponService` + 관리자 `AdminCouponController`(`/api/admin/coupons`, X-Admin-Secret 게이트) + 공개 `CouponController`(`/api/coupons`, 미만료·만료임박순)를 만든다. 만료일은 날짜만(`LocalDate`) — 유효 판정은 서버 KST 날짜 기준(D-01), code는 unique 없음(D-04).

Purpose: 프론트(17.3-02 admin UI / 17.3-03 대시보드)가 소비할 관리자 CRUD 계약과 공개 read 계약을 제공하고, 쿠폰이 수집/가격 캐시/event-impact와 자원 경쟁 없는 독립 도메인·PostgreSQL 영속임을 스키마로 보장한다.

Output: `Coupon`·`V5` 마이그레이션·`CouponRepository`·DTO 2종·`AdminCouponService`·`AdminCouponController`·`CouponController` + admin/공개 IT. 수집/가격 캐시/event-impact·WatchlistSeeder/SyntheticDemoData diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.3-coupon-admin/17.3-CONTEXT.md
@.planning/phases/17.3-coupon-admin/17.3-SPEC.md
@CLAUDE.md

<interfaces>
<!-- 엔티티 원형 Coupon.java(com.lostark.tracker.domain): GameEvent.java 구조 그대로 — @Entity @Table(name="coupon"), @Id @GeneratedValue(IDENTITY) Long id, @Column(name="code",nullable=false,length=100) String code, @Column(name="reward",nullable=false,length=300) String reward, @Column(name="expires_at",nullable=false) LocalDate expiresAt(★ OffsetDateTime 아님 — 날짜만, D-01), @Column(name="created_at",nullable=false) OffsetDateTime createdAt, @Column(name="updated_at",nullable=false) OffsetDateTime updatedAt. @PrePersist onCreate()/@PreUpdate onUpdate()로 UTC 타임스탬프 스탬핑(GameEvent 그대로). 생성자 Coupon(code,reward,expiresAt) + replace(code,reward,expiresAt) full-replace + getter. -->
<!-- V5__add_coupon.sql(다음 순번 — V1~V4 immutable): CREATE TABLE coupon( id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, code VARCHAR(100) NOT NULL, reward VARCHAR(300) NOT NULL, expires_at DATE NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ★ code UNIQUE 인덱스 없음(D-04). PostgreSQL 타입만(DATE·TIMESTAMPTZ). V1 game_event 테이블 형식 준수. -->
<!-- CouponRepository(com.lostark.tracker.repository) extends JpaRepository<Coupon,Long>: findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc(LocalDate today) = 공개 유효(미만료) 만료임박순; findAllByOrderByExpiresAtAsc() = admin 목록(만료 포함, 임박순). GameEventRepository 파생 쿼리 네이밍 준수. -->
<!-- CouponRequest(com.lostark.tracker.web.dto) record(@NotBlank String code, @NotBlank String reward, @NotNull LocalDate expiresAt): GameEventRequest처럼 엔티티 미바인딩 — id/createdAt/updatedAt mass-assignment 차단. 3필드 모두 필수(코드·보상·만료일). @Valid 위반 → 400 공유 에러 계약. CouponResponse record(Long id,String code,String reward,LocalDate expiresAt,OffsetDateTime createdAt,OffsetDateTime updatedAt) + static from(Coupon). expiresAt는 "YYYY-MM-DD"로 JSON 직렬화(LocalDate 기본). -->
<!-- AdminCouponService(com.lostark.tracker.admin): AdminEventService 미러 — create(req)=save(new Coupon(...)), list()=findAllByOrderByExpiresAtAsc(), replace(id,req)=findById.orElseThrow(ResourceNotFoundException)→entity.replace(...)→save, delete(id)=existsById 검사 후 deleteById(없으면 ResourceNotFoundException→404). 타임스탬프는 엔티티 @PrePersist/@PreUpdate가 스탬프(서비스 미할당). -->
<!-- AdminCouponController(com.lostark.tracker.web.admin) @RequestMapping("/api/admin/coupons"): AdminEventController 미러 — @PostMapping 201+본문, @GetMapping 목록, @PutMapping("/{id}") full-replace, @DeleteMapping("/{id}") 204. @Valid @RequestBody CouponRequest. X-Admin-Secret 게이트는 SecurityConfig /api/admin/** authenticated로 이미 적용(무변경). 요청 바디/헤더 미로깅. -->
<!-- CouponController(com.lostark.tracker.web, 공개) @RequestMapping("/api/coupons") @GetMapping: CouponRepository 직접 주입(ItemController가 repository 직접 호출하는 선례) → findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc(LocalDate.now(KST)).stream().map(CouponResponse::from).toList(). KST 상수 private static final ZoneId KST = ZoneId.of("Asia/Seoul"). permitAll(SecurityConfig anyRequest().permitAll() → 자동 공개, 무변경). -->
<!-- IT 베이스: src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java(Testcontainers), AdminAuth.entity(body,secret)/entity(secret)(X-Admin-Secret), web.dto.ApiErrorResponse({timestamp,status,error,message}), web.error.ResourceNotFoundException. AdminEventControllerIT/AdminAuthGateIT 패턴 그대로. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coupon 엔티티 + V5__add_coupon.sql 마이그레이션 + CouponRepository</name>
  <read_first>
    - src/main/java/com/lostark/tracker/domain/GameEvent.java (엔티티·@PrePersist/@PreUpdate 원형 — LocalDate로 치환)
    - src/main/resources/db/migration/V1__init_schema.sql (game_event 테이블 형식·PostgreSQL 타입 준거)
    - src/main/resources/db/migration/V4__add_item_enrichment.sql (마이그레이션 헤더 주석·다음 순번 V5 확인)
    - src/main/java/com/lostark/tracker/repository/GameEventRepository.java (JpaRepository 파생 쿼리 네이밍)
  </read_first>
  <files>src/main/java/com/lostark/tracker/domain/Coupon.java, src/main/resources/db/migration/V5__add_coupon.sql, src/main/java/com/lostark/tracker/repository/CouponRepository.java</files>
  <action>
    `Coupon.java`를 `com.lostark.tracker.domain`에 GameEvent 구조로 만든다: `@Entity @Table(name="coupon")`, `@Id @GeneratedValue(strategy=IDENTITY) Long id`, `@Column(name="code",nullable=false,length=100) String code`, `@Column(name="reward",nullable=false,length=300) String reward`, `@Column(name="expires_at",nullable=false) LocalDate expiresAt`(★ `java.time.LocalDate` — 날짜만, D-01), `@Column(name="created_at",nullable=false) OffsetDateTime createdAt`, `@Column(name="updated_at",nullable=false) OffsetDateTime updatedAt`. `@PrePersist onCreate()`(createdAt null이면 now UTC, updatedAt=now)·`@PreUpdate onUpdate()`(updatedAt=now UTC)는 GameEvent 그대로. 생성자 `Coupon(String code,String reward,LocalDate expiresAt)`·full-replace `replace(String code,String reward,LocalDate expiresAt)`·getter. 세터 없음(타임스탬프 불변). `V5__add_coupon.sql`을 만든다: `CREATE TABLE coupon( id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, code VARCHAR(100) NOT NULL, reward VARCHAR(300) NOT NULL, expires_at DATE NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now() );` — **code UNIQUE 인덱스 없음**(D-04), PostgreSQL 타입만, 헤더 주석에 목적·D-01/D-04 명시. `CouponRepository`(`com.lostark.tracker.repository`) `extends JpaRepository<Coupon,Long>`: `List<Coupon> findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc(LocalDate today)`(공개 미만료·임박순)·`List<Coupon> findAllByOrderByExpiresAtAsc()`(admin 만료 포함·임박순). 수집/가격 캐시/event-impact·WatchlistSeeder/SyntheticDemoData는 건드리지 않는다.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Coupon 엔티티가 code/reward/expiresAt(LocalDate)/createdAt/updatedAt + @PrePersist/@PreUpdate로 정의되고 `./gradlew compileJava` 그린
    - V5__add_coupon.sql이 coupon 테이블(expires_at DATE)을 생성하고 code UNIQUE가 없다(D-04) — `grep -qi "CREATE TABLE coupon" src/main/resources/db/migration/V5__add_coupon.sql` 성립, `! grep -qi "UNIQUE" src/main/resources/db/migration/V5__add_coupon.sql`
    - CouponRepository에 findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc·findAllByOrderByExpiresAtAsc가 존재
    - 수집/캐시/event-impact·WatchlistSeeder/SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>coupon 스키마(V5)·엔티티·repository가 컴파일되고 code unique 없이 date-only expires_at를 매핑하며, Core Value 경로 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 2: CouponRequest/CouponResponse DTO + AdminCouponService + AdminCouponController + 공개 CouponController</name>
  <read_first>
    - src/main/java/com/lostark/tracker/web/dto/GameEventRequest.java (record·@NotBlank/@NotNull·엔티티 미바인딩 원형)
    - src/main/java/com/lostark/tracker/web/dto/GameEventResponse.java (record + static from(entity) 원형)
    - src/main/java/com/lostark/tracker/admin/AdminEventService.java (create/list/replace/delete + ResourceNotFoundException 원형)
    - src/main/java/com/lostark/tracker/web/admin/AdminEventController.java (POST 201/GET/PUT/DELETE 204 원형)
    - src/main/java/com/lostark/tracker/web/ItemController.java (공개 read 컨트롤러가 repository 직접 주입하는 선례)
  </read_first>
  <files>src/main/java/com/lostark/tracker/web/dto/CouponRequest.java, src/main/java/com/lostark/tracker/web/dto/CouponResponse.java, src/main/java/com/lostark/tracker/admin/AdminCouponService.java, src/main/java/com/lostark/tracker/web/admin/AdminCouponController.java, src/main/java/com/lostark/tracker/web/CouponController.java</files>
  <action>
    `CouponRequest` record(`@NotBlank String code, @NotBlank String reward, @NotNull LocalDate expiresAt`) — GameEventRequest처럼 엔티티 미바인딩(id/createdAt/updatedAt mass-assignment 차단), 3필드 모두 필수. `CouponResponse` record(`Long id, String code, String reward, LocalDate expiresAt, OffsetDateTime createdAt, OffsetDateTime updatedAt`) + `static CouponResponse from(Coupon c)`. `AdminCouponService`(`com.lostark.tracker.admin`)는 AdminEventService 미러: `create(CouponRequest)`=save(new Coupon(...)), `list()`=`couponRepository.findAllByOrderByExpiresAtAsc()`(만료 포함·임박순), `replace(long id, CouponRequest)`=findById.orElseThrow(new ResourceNotFoundException("Coupon "+id+" not found"))→entity.replace(...)→save, `delete(long id)`=existsById 검사 후 deleteById(없으면 ResourceNotFoundException). 타임스탬프는 서비스에서 할당하지 않음(엔티티 스탬프). `AdminCouponController`(`com.lostark.tracker.web.admin`) `@RequestMapping("/api/admin/coupons")`: `@PostMapping`→201+CouponResponse, `@GetMapping`→목록, `@PutMapping("/{id}")`→full-replace 200, `@DeleteMapping("/{id}")`→204. `@Valid @RequestBody CouponRequest`. 공개 `CouponController`(`com.lostark.tracker.web`) `@RequestMapping("/api/coupons")` `@GetMapping`: `CouponRepository`를 직접 주입, `private static final ZoneId KST = ZoneId.of("Asia/Seoul");` 두고 `couponRepository.findByExpiresAtGreaterThanEqualOrderByExpiresAtAsc(LocalDate.now(KST)).stream().map(CouponResponse::from).toList()` 반환(미만료·만료임박순, 만료 제외 — D-01). SecurityConfig는 건드리지 않는다(/api/admin/** 이미 게이트, /api/coupons 자동 공개). 수집/캐시/event-impact 경로 0줄.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - CouponRequest(@NotBlank code·reward, @NotNull LocalDate expiresAt)·CouponResponse(+from)가 정의된다
    - AdminCouponService가 create/list(임박순)/replace(404)/delete(404)를 제공하고 타임스탬프를 직접 할당하지 않는다
    - AdminCouponController가 /api/admin/coupons POST 201·GET·PUT·DELETE 204를 노출(CouponRequest 바인딩)
    - CouponController가 GET /api/coupons에서 LocalDate.now(Asia/Seoul) 기준 미만료·만료임박순 CouponResponse를 반환한다
    - `./gradlew compileJava` 그린, SecurityConfig·수집/캐시/event-impact diff 0줄
  </acceptance_criteria>
  <done>관리자 CRUD(/api/admin/coupons)와 공개 read(/api/coupons 미만료·임박순)가 컴파일되고, SecurityConfig 무변경·Core Value 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 3: AdminCouponControllerIT + CouponControllerIT (admin CRUD + 401 + 공개 만료 필터)</name>
  <read_first>
    - src/test/java/com/lostark/tracker/admin/AdminEventControllerIT.java (admin CRUD 매트릭스 원형 — POST 201·GET·PUT·DELETE·404·400)
    - src/test/java/com/lostark/tracker/security/AdminAuthGateIT.java (시크릿 없는 요청 401 계약 assertUnauthorizedContract 원형)
    - src/test/java/com/lostark/tracker/support/AdminAuth.java (entity(body,secret)/entity(secret) 헬퍼)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (Testcontainers 베이스)
  </read_first>
  <files>src/test/java/com/lostark/tracker/admin/AdminCouponControllerIT.java, src/test/java/com/lostark/tracker/web/CouponControllerIT.java</files>
  <action>
    `AdminCouponControllerIT`(PostgresRedisContainers 상속, @SpringBootTest RANDOM_PORT, @ActiveProfiles("test"))를 AdminEventControllerIT 미러로 작성: X-Admin-Secret로 (1) POST 201 + 엔티티-스탬프 created_at/updated_at, (2) GET 목록 만료임박순(expiresAt ASC), (3) PUT full-replace(code·reward·expiresAt 전부 교체, updated_at 전진), (4) DELETE 204 + 목록 제거, (5) PUT/DELETE 없는 id → 404 `{timestamp,status,error,message}` 계약, (6) POST @NotBlank code 공백/@NotNull expiresAt null → 400 계약. **추가**: X-Admin-Secret **없는** POST `/api/admin/coupons` → 401 계약(AdminAuthGateIT assertUnauthorizedContract 패턴, `new HttpEntity<>(request)` 시크릿 헤더 없이) — SPEC 수용기준 '시크릿 없는 요청은 401'. `@BeforeEach`에서 couponRepository.deleteAll(). `CouponControllerIT`(동일 베이스)는 공개 만료 필터 검증: `LocalDate today = LocalDate.now(ZoneId.of("Asia/Seoul"))` 기준으로 유효 2건(today, today.plusDays(10)) + 만료 1건(today.minusDays(1)) 시드 후 `GET /api/coupons`가 **유효 2건만 만료임박순([today, today+10])**으로 반환하고 만료 1건은 부재임을 검증(만료일 당일 today는 포함 — 그날까지 유효, D-01). 시크릿 불필요(공개). 실 API 미호출.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - AdminCouponControllerIT: POST 201→GET 목록 포함→PUT 수정→DELETE 제거 동작, 404/400 계약, 시크릿 없는 요청 401 계약 통과
    - CouponControllerIT: 유효 2 + 만료 1 시드 시 GET /api/coupons가 유효 2개만 만료임박순으로 반환하고 만료 1개 제외(당일 today 포함) 검증
    - `./gradlew build` 그린(Testcontainers Postgres/Redis, 실 API·키 미사용)
    - 수집/캐시/event-impact·WatchlistSeeder/SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>admin CRUD·401·400/404 계약과 공개 만료 자동 제외·만료임박순이 IT로 증명되고 `./gradlew build`가 그린이며 Core Value 경로 0줄이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공개 클라이언트 → GET /api/coupons | 공개 read(admin 아님) — 공개 쿠폰 메타(코드·보상·만료일)만, 키/시크릿 없음 |
| 관리자 클라이언트 → /api/admin/coupons | X-Admin-Secret 게이트(SecurityConfig /api/admin/** authenticated, 재사용·무변경) |
| 요청 바디 → 엔티티 영속 | CouponRequest 바인딩(엔티티 미바인딩) — id/createdAt/updatedAt mass-assignment 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1731-01 | Spoofing | 시크릿 없이 쿠폰 CRUD 시도 | mitigate | /api/admin/** 게이트로 401(무변경) — AdminCouponControllerIT가 시크릿 없는 POST 401 검증 |
| T-1731-02 | Tampering | 요청 바디로 id/타임스탬프 mass-assignment | mitigate | CouponRequest만 바인딩(3필드), 엔티티 미바인딩 — created_at/updated_at은 @PrePersist/@PreUpdate 스탬프 |
| T-1731-03 | Tampering | 만료 쿠폰이 공개 목록에 노출 | mitigate | GET /api/coupons는 expiresAt >= KST today만 반환(IT로 만료 제외 검증) |
| T-1731-04 | Information Disclosure | 에러/로그에 시크릿·민감정보 유입 | mitigate | 요청 바디/헤더 미로깅, 쿠폰 데이터는 공개 메타만, 404/400 계약 바디에 시크릿 미포함 |
| T-1731-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — Spring Data JPA/RestController/Flyway 기존 스택 재사용 |
</threat_model>

<verification>
- `Coupon` 엔티티 + `V5__add_coupon.sql`(expires_at DATE, code unique 없음)로 JPA ddl-auto=validate 통과 — `./gradlew build` 그린(D-01/D-04).
- `/api/admin/coupons` POST/PUT/DELETE가 X-Admin-Secret 없으면 401, 있으면 CRUD 동작 + 404/400 계약 — AdminCouponControllerIT(COUPON-01).
- `GET /api/coupons`가 KST today 기준 미만료만 만료임박순 반환·만료 제외(당일 포함) — CouponControllerIT(COUPON-02/03).
- SecurityConfig 무변경, 수집/가격 캐시/event-impact·WatchlistSeeder/SyntheticDemoData diff 0줄(Core Value 가드).
</verification>

<success_criteria>
- 관리자가 쿠폰(코드·보상·만료일)을 등록·수정·삭제하고 PostgreSQL에 영속한다(COUPON-01, X-Admin-Secret 게이트).
- 공개 `GET /api/coupons`가 미만료 쿠폰만 만료임박순으로 서빙하고 만료 쿠폰은 자동 제외한다(COUPON-02/03, D-01).
- 만료일은 날짜만(LocalDate·KST 판정, D-01), code unique 없음(D-04) — Core Value 경로 0줄, PostgreSQL 영속 독립 도메인.
</success_criteria>

<output>
Create `.planning/phases/17.3-coupon-admin/17.3-01-SUMMARY.md` when done
</output>
