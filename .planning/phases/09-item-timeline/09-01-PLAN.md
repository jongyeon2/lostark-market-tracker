---
phase: 09-item-timeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ui/select.tsx
  - frontend/src/features/timeline/eventMarkers.ts
autonomous: true
requirements: [TIME-02, TIME-03, TIME-04]

must_haves:
  truths:
    - "`recharts` (^3, React 19 호환) is a real dependency in `frontend/package.json` and installs cleanly — it is the locked charting library from REQUIREMENTS (TIME-02), not a registry block. `npm install` completes and `node -e \"require.resolve('recharts')\"` resolves from `frontend/`."
    - "The shadcn `select` block exists at `frontend/src/components/ui/select.tsx`, authored BY HAND in the new-york style (per D-05 the item selector is a dropdown `select`). It uses `@radix-ui/react-select` primitives, `data-slot` attributes, `cn` from `@/lib/utils`, and lucide icons (ChevronDown/ChevronUp/Check) — matching the exact conventions in the existing `card.tsx`/`badge.tsx`. It is NOT created via `npx shadcn add select` (that CLI path misplaces the block into a literal `@/` directory and adds a stray `radix-ui` meta-package on this Windows setup)."
    - "`@radix-ui/react-select` is added to `frontend/package.json` dependencies (the single primitive the hand-authored select needs) — NOT the `radix-ui` umbrella package."
    - "`frontend/src/features/timeline/eventMarkers.ts` exports `EVENT_MARKERS`: a map from each of the four `eventType` enum values to `{ color, koLabel }`, locking the meaning→color mapping from D-01 / 09-UI-SPEC Event Marker Palette exactly — `LOA_ON`→`#7C3AED`/'로아ON', `MAJOR_UPDATE`→`#EA580C`/'대규모 업데이트', `SEASON_END`→`#DB2777`/'시즌 종료', `BALANCE_PATCH`→`#0D9488`/'밸런스 패치'. The map is keyed by the `EventType` zod-derived type so a future enum change fails at compile time."
    - "`eventMarkers.ts` also exports `bucketWidthLabel(bucketWidth: string | null): string` mapping the backend's ACTUAL `bucketWidth` strings to Korean (per D-08 the planner verified the real values against `DownsampleService.java`): `\"hour\"`→'1시간', `\"day\"`→'1일', anything else→the raw string (honest fallback). NOTE: the backend returns `\"hour\"`/`\"day\"`, NOT `\"1h\"` as the 09-UI-SPEC copy draft assumed — this helper is the single place that reconciles the two so the badge copy reads correctly."
    - "`cd frontend && npm run build` (tsc -b + vite build) passes with zero type errors. No file under the Java `src/` tree changes (read-only frontend phase)."
  artifacts:
    - path: "frontend/package.json"
      provides: "recharts ^3 + @radix-ui/react-select added to dependencies (TIME-02 charting lib + D-05 select primitive)"
      contains: "recharts"
    - path: "frontend/src/components/ui/select.tsx"
      provides: "Hand-authored shadcn new-york Select block (Select/SelectTrigger/SelectValue/SelectContent/SelectItem) over @radix-ui/react-select — D-05"
      contains: "SelectTrigger"
    - path: "frontend/src/features/timeline/eventMarkers.ts"
      provides: "EVENT_MARKERS (eventType→{color,koLabel}, D-01) + bucketWidthLabel (hour/day→ko, D-08) — single source for marker color & bucket copy"
      contains: "EVENT_MARKERS"
  key_links:
    - from: "frontend/src/components/ui/select.tsx"
      to: "@radix-ui/react-select"
      via: "the hand-authored block re-exports styled wrappers over the radix Select primitives"
      pattern: "react-select"
    - from: "frontend/src/features/timeline/eventMarkers.ts"
      to: "frontend/src/lib/schemas.ts"
      via: "EVENT_MARKERS is keyed by the EventType type derived from eventTypeSchema (4-enum), so color map and enum stay in lockstep"
      pattern: "EventType"
---

<objective>
Lay the **dependency + design-token foundation** the rest of Phase 9 hangs on, before any screen consumes it: install the locked charting library (`recharts`) and the one radix primitive the dropdown selector needs (`@radix-ui/react-select`); hand-author the shadcn `select` block; and create the single source of truth for event-marker colors and the downsample bucket label.

The engineering signal here is **reconciling the contract with the measured backend**: the 09-UI-SPEC drafted the downsample copy as "버킷 평균 · 1h", but reading `DownsampleService.java` shows the API actually returns `bucketWidth` of `"hour"` or `"day"` (D-09 of Phase-3, dynamic bucket unit). `bucketWidthLabel` is the one place that maps the real values to readable Korean, so the badge (09-04) renders truthfully instead of echoing a wrong literal. The marker palette (D-01) and the select block (D-05) are likewise locked once, here, so 09-02/09-04 consume them without re-deciding.

Output: `frontend/package.json` (+ lock) with recharts & radix-select, `frontend/src/components/ui/select.tsx`, `frontend/src/features/timeline/eventMarkers.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-item-timeline/09-CONTEXT.md
@.planning/phases/09-item-timeline/09-UI-SPEC.md

<interfaces>
<!-- From frontend/src/lib/schemas.ts — the EventType enum eventMarkers must key on -->
export const eventTypeSchema = z.enum(['LOA_ON', 'MAJOR_UPDATE', 'SEASON_END', 'BALANCE_PATCH'])
export type EventType = z.infer<typeof eventTypeSchema>

<!-- Backend ground truth (frontend/无변경) — DownsampleService.java chooseBucketUnit():
     returns the STRING "hour" while span hours <= ~500 (≈21 days), else "day".
     TimelineResponse.bucketWidth is null when not downsampled. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recharts + radix-select dependencies</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>frontend/package.json</read_first>
  <action>In `frontend/package.json` add two entries to `dependencies`: `recharts` at `^3` (the latest major; v3 is React-19 compatible and the REQUIREMENTS-locked charting library for TIME-02) and `@radix-ui/react-select` at `^2` (the single primitive the hand-authored select block in Task 2 imports — do NOT add the `radix-ui` umbrella meta-package). Then run `npm install` from the `frontend/` directory to materialize `node_modules` and update `package-lock.json`. Keep all existing dependencies (react 19.2, @tanstack/react-query, react-router-dom 7, zod 4, lucide-react) unchanged.</action>
  <verify>
    <automated>cd frontend && npm install && node -e "require.resolve('recharts'); require.resolve('@radix-ui/react-select'); console.log('deps OK')"</automated>
  </verify>
  <done>package.json dependencies include recharts (^3) and @radix-ui/react-select; npm install succeeds; both modules resolve from frontend/.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the shadcn select block</name>
  <files>frontend/src/components/ui/select.tsx</files>
  <read_first>frontend/src/components/ui/card.tsx, frontend/src/components/ui/badge.tsx, frontend/src/lib/utils.ts</read_first>
  <action>Create `frontend/src/components/ui/select.tsx` by hand in the shadcn **new-york** style (do NOT run `npx shadcn add select` — on this Windows setup it writes the file into a literal `@/` folder and injects a stray `radix-ui` dependency). Build styled wrappers over `@radix-ui/react-select` primitives and export: `Select` (= SelectPrimitive.Root), `SelectGroup`, `SelectValue`, `SelectTrigger`, `SelectContent`, `SelectLabel`, `SelectItem`, `SelectSeparator`. Match the existing convention exactly: `'use client'` is NOT needed (Vite SPA), import `cn` from `@/lib/utils`, set a `data-slot` attribute on each wrapper (e.g. `data-slot="select-trigger"`), and use lucide-react icons `ChevronDownIcon` for the trigger, `ChevronUpIcon`/`ChevronDownIcon` for scroll buttons, and `CheckIcon` for the selected item indicator. The trigger styling should match the new-york outline input look (border, `h-9`, rounded-md, focus-visible ring, `bg-transparent`) so it sits beside the existing Button/Badge consistently. SelectContent uses the radix Portal + popper positioning. No business logic — this is a presentational primitive consumed by 09-02 ItemSelect.</action>
  <verify>
    <automated>cd frontend && grep -q "SelectTrigger" src/components/ui/select.tsx && grep -q '@radix-ui/react-select' src/components/ui/select.tsx && grep -q 'data-slot="select-trigger"' src/components/ui/select.tsx && echo "select block OK"</automated>
  </verify>
  <done>select.tsx exports Select/SelectTrigger/SelectValue/SelectContent/SelectItem (and group/label/separator) over @radix-ui/react-select with data-slot attributes and cn from @/lib/utils; no literal @/ directory was created.</done>
</task>

<task type="auto">
  <name>Task 3: Event marker palette + bucket label tokens</name>
  <files>frontend/src/features/timeline/eventMarkers.ts</files>
  <read_first>frontend/src/lib/schemas.ts, frontend/src/features/timeline/eventMarkers.ts, .planning/phases/09-item-timeline/09-UI-SPEC.md</read_first>
  <action>Create `frontend/src/features/timeline/eventMarkers.ts`. Export `EVENT_MARKERS` typed as `Record<EventType, { color: string; koLabel: string }>` (import `EventType` from `@/lib/schemas`) with EXACTLY the D-01 / 09-UI-SPEC palette: `LOA_ON` → `{ color: '#7C3AED', koLabel: '로아ON' }`, `MAJOR_UPDATE` → `{ color: '#EA580C', koLabel: '대규모 업데이트' }`, `SEASON_END` → `{ color: '#DB2777', koLabel: '시즌 종료' }`, `BALANCE_PATCH` → `{ color: '#0D9488', koLabel: '밸런스 패치' }`. Also export `MARKER_DASH = '4 4'` and `MARKER_STROKE_WIDTH = 1.5` (the dashed-line style from 09-UI-SPEC, so the chart and legend share one source). Export `bucketWidthLabel(bucketWidth: string | null): string` returning '1시간' for `"hour"`, '1일' for `"day"`, and the raw input string for any other non-null value (honest fallback), and '' for null. Add a one-line comment noting the backend returns `"hour"`/`"day"` (verified in DownsampleService.java), reconciling the 09-UI-SPEC "1h" draft. Keep `EVENT_MARKERS` as a `Record<EventType, …>` so dropping/renaming an enum member is a compile error.</action>
  <verify>
    <automated>cd frontend && grep -q "EVENT_MARKERS" src/features/timeline/eventMarkers.ts && grep -q "bucketWidthLabel" src/features/timeline/eventMarkers.ts && grep -q "#7C3AED" src/features/timeline/eventMarkers.ts && npm run build</automated>
  </verify>
  <done>eventMarkers.ts exports EVENT_MARKERS (4 colors+labels per D-01), MARKER_DASH/STROKE width, and bucketWidthLabel (hour/day→ko per D-08); npm run build passes with zero type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Two new packages are installed into the frontend build (supply-chain surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | `recharts`, `@radix-ui/react-select` installs | mitigate | `recharts` is the REQUIREMENTS-locked charting library (pin `^3`, React-19 compatible, ~25M weekly downloads — verify on npmjs.com/package/recharts); `@radix-ui/react-select` is the official Radix primitive underlying shadcn's select (verify on npmjs.com/package/@radix-ui/react-select). No `radix-ui` umbrella/meta package is added. |
| T-09-01 | Information Disclosure | `eventMarkers.ts`, `select.tsx` | accept | Pure presentational tokens/primitives — no secrets, no network, no user input handled here. |
</threat_model>

<verification>
- `cd frontend && npm run build` passes (tsc -b + vite build), zero type errors.
- `recharts` and `@radix-ui/react-select` present in package.json dependencies and resolvable.
- `select.tsx` exports the new-york Select family with `data-slot` attrs over `@radix-ui/react-select`; no literal `@/` directory exists under `frontend/`.
- `eventMarkers.ts` exports `EVENT_MARKERS` (4 hex+ko labels per D-01) and `bucketWidthLabel` (hour→'1시간', day→'1일' per D-08).
- No file under Java `src/` changed.
</verification>

<success_criteria>
- recharts (^3) and @radix-ui/react-select installed; build green.
- Hand-authored select block present and conventions-matched (data-slot, cn, lucide).
- Marker palette + bucket-label tokens locked to D-01/D-08, keyed on the EventType enum.
</success_criteria>

<output>
Create `.planning/phases/09-item-timeline/09-01-SUMMARY.md` when done.
</output>
