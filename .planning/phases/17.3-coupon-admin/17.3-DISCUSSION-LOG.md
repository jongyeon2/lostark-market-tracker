# Phase 17.3: 쿠폰 관리자 등록 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-06
**Phase:** 17.3-coupon-admin
**Areas discussed:** 만료일 처리, 대시보드 표시(섹션 위치·복사 피드백), 데이터 정책(중복)

> SPEC.md가 WHAT을 잠금(4 requirements) — 아래는 HOW 구현 결정만.

---

## 만료일 처리

| Option | Description | Selected |
|--------|-------------|----------|
| 날짜만 | 관리자 날짜만 입력, 그날 끝(23:59 KST)까지 유효. LocalDate 영속. | ✓ |
| 날짜+시간 | 이벤트 폼처럼 datetime-local(KST), 시각 단위 만료. | |

**User's choice:** 날짜만
**Notes:** 쿠폰은 보통 '~까지'라 시간 불필요. admin 폼은 `type="date"`, 판정은 `expiresAt >= 오늘(서버 KST)`.

---

## 쿠폰 섹션 위치 (뉴스 패널)

| Option | Description | Selected |
|--------|-------------|----------|
| 맨 위 | 쿠폰→이벤트→공지. 쿠폰이 가장 actionable. | ✓ |
| 맨 아래 | 이벤트→공지→쿠폰. | |
| 가운데 | 이벤트→쿠폰→공지. | |

**User's choice:** 맨 위
**Notes:** 즉시 사용 성격이라 상단 노출.

---

## 코드 복사 피드백

| Option | Description | Selected |
|--------|-------------|----------|
| 버튼 일시 변경 | 복사 시 '복사됨!'/체크로 ~2초, 신규 의존성 0. | ✓ |
| 토스트 알림 | 하단 토스트(기존 시스템 없으면 신규 도입). | |
| 피드백 없음 | 조용히 복사만. | |

**User's choice:** 버튼 일시 변경
**Notes:** `navigator.clipboard.writeText`, 신규 의존성 없이 버튼 상태만 토글.

---

## 쿠폰 코드 중복 정책

| Option | Description | Selected |
|--------|-------------|----------|
| 중복 허용 | code unique 제약 없음, 같은 코드 재등록 가능. | ✓ |
| 코드 unique | 재등록 시 409 거부. | |

**User's choice:** 중복 허용
**Notes:** 단순·유연. 워치리스트 externalItemId-unique 선례는 따르지 않음.

---

## Claude's Discretion

- reward 자유 텍스트(varchar), 만료 판정 쿼리·정렬(≤6 캡) 구현, admin 폼 레이아웃(EventSection 재사용, type="date"), 쿠폰 행 표시·만료일 표기·복사 버튼 위치, zod couponSchema 세부, Flyway V5·컬럼 타입·엔티티/repository.

## Deferred Ideas

- SPEC out-of-scope(자동 수집·redemption 추적·코드 유효성 검증·사용자 통계·쿠폰 이미지·만료 D-day 뱃지). 이번 논의에서 새 스코프 이탈 없음.
