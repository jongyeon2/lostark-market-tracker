# Phase 17.4: 타임라인 gap 백필 — 일별 Stats - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-07
**Phase:** 17.4-timeline-gap-backfill
**Areas discussed:** 표시 방식, 백필 트리거, 백필 소스/범위(각인서 정정), 노출 정책

---

## 표시 방식 (min vs 백필 avg 구분)

| Option | Description | Selected |
|--------|-------------|----------|
| 백필 avg 연속라인 + 실측 min 표식 | 일평균을 14일 연속 백본 라인, 실측 수집일은 점/색 구분 | ✓ |
| 실측 min · 백필 avg 두 라인 분리 | 별개 색/점선 시리즈, 지표 혼동 0이나 복잡 | |
| 실측 라인 + 공백만 백필로 연결 | 최소 개입, min↔avg 경계 인위적 단차 | |

**User's choice:** 백필 avg 연속라인 + 실측 min 표식 (추천)
**Notes:** 연속성(사용자 목적)과 정직성 균형. 한 라인 혼합의 단차 문제를 피함.

---

## 백필 트리거

| Option | Description | Selected |
|--------|-------------|----------|
| 기동 시 + 일 1회 | 프로젝트 켤 때 즉시 14일 채움 + 상시 실행 시 매일 갱신 | ✓ |
| 서버 기동 시만 | 재시작마다 채움, 배포 후 갱신은 재시작 의존 | |
| 일 1회 스케줄만 | 배포 적합, 로컬 간헐 사용엔 부족 | |
| 관리자 수동 버튼 | 통제 명확하나 수동 개입 | |

**User's choice:** 기동 시 + 일 1회 (추천)

---

## 백필 소스/범위 (각인서 정정)

**배경:** 초기 스파이크 결론 "각인서 거래 없어 백필 불가"를 사용자가 정확히 반박("유물 각인서 활발히 거래될텐데"). 재검증 결과 각인서 실측가 145K·YDayAvgPrice 147K 확인 → 상세 Stats만 각인서를 0으로 주는 API 한계였음. 소스 2종 존재 판명.

| Option | Description | Selected |
|--------|-------------|----------|
| 둘 다 | YDayAvgPrice 무료 캡처(전 품목·전일치) + 상세 Stats(재료 14일 소급) | ✓ |
| YDayAvgPrice만 | 전 품목·무료·단순, 과거 다일 gap은 전일치만 | |
| 상세 Stats만 | 재료 14일 소급, 각인서 제외 | |

**User's choice:** 둘 다 (추천)
**Notes:** 커버리지 상보 — YDayAvgPrice로 각인서 포함 전 품목 going-forward, 상세 Stats로 재료 과거 gap 소급.

---

## 노출 정책 & 기본 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 항상 표시 | 백필 기본 포함, 기본 범위 30일 유지 | ✓ |
| 사용자 토글 (기본 켜짐) | on/off 버튼, 기본 켜짐 | |
| 사용자 토글 (기본 꺼짐) | 기본 실측만 | |

**User's choice:** 항상 표시 (추천)

---

## Claude's Discretion

- `item_daily_stats` 컬럼/인덱스/upsert 구현.
- 차트 렌더 세부(백필 라인 스타일·실측 표식·툴팁 문구).
- YDayAvgPrice를 수집 흐름에 편승시키는 방식 + MarketItem DTO 매핑.
- 상세 Stats 소급 대상(재료) 판정 기준(category vs roleGroup).
- 백필 시리즈 병합 방식(기존 prices 엔드포인트 vs 신규).

## Deferred Ideas

- event-impact이 백필 일평균을 이벤트 앵커로 사용 — 별도 phase.
- 타임라인 기본 범위 90일 확대 — 지금은 30일.
- 14일 초과 / 각인서 다일 과거 gap 복구 — Phase 18 상시 배포로만 예방.
- avg_price를 price_snapshot에 컬럼 추가(Task 0 V2) — Core Value 가드상 별도 테이블로 분리.
