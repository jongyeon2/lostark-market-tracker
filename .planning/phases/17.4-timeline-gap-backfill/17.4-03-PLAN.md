---
phase: 17.4-timeline-gap-backfill
plan: 03
type: execute
wave: 2
depends_on: [17.4-01]
files_modified:
  - src/main/java/com/lostark/tracker/collect/dto/ItemDetailResponse.java
  - src/main/java/com/lostark/tracker/collect/dto/MarketStat.java
  - src/main/java/com/lostark/tracker/collect/LostarkApiClient.java
  - src/main/java/com/lostark/tracker/repository/TrackedItemRepository.java
  - src/main/java/com/lostark/tracker/backfill/DetailStatsBackfillRunner.java
  - src/test/java/com/lostark/tracker/backfill/DetailStatsBackfillRunnerIT.java
autonomous: true
requirements: [BACKFILL-02, BACKFILL-04]

must_haves:
  truths:
    - "D-01(소스②): 상세 API `GET /markets/items/{id}`의 `Stats[].AvgPrice`(최근 **14일** 일별 평균, **재료만** 채워짐 — 각인서는 상세 Stats=0인 API 한계)로 재료의 과거 gap을 `source=DETAIL_STATS`로 소급 upsert한다. 대상 = 재료(`roleGroup=MATERIAL`, 로스트아크 category=50010 대응) — 각인서/딜러/서포터는 상세 Stats 소급 대상 아님"
    - "D-03: 백필 상세 소급 실행 = **서버 기동 시(ApplicationRunner) + 일 1회(@Scheduled)**, 멱등 upsert. 재기동마다 최근 14일 재료를 다시 채우고(중복 없이 갱신), 상시 운영 시 매일 갱신. AvgPrice=0(각인서·미채움) 엔트리는 skip"
    - "BACKFILL-02: 상세 소급은 재료만·저빈도라 레이트리밋 여유(스파이크: 1콜 후 remaining 98). 기존 `RedisTokenBucket`으로 tryAcquire 존중, 미획득/실패 시 해당 품목 skip(다음 트리거 재시도) — 품목별 fail-open, AUTH는 회차 중단"
    - "BACKFILL-04(Core Value 가드): 상세 소급은 신규 러너·신규 DTO·`LostarkApiClient`에 read-only GET 메서드 additive 추가만 — `PriceCollector`·`price_snapshot`·`LatestPriceCache`·`EventImpactService`·기존 `searchMarketItems`/수집 틱 로직 0줄. `@EnableScheduling`은 이미 앱 전역(CollectionConfig)이라 무변경"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/backfill/DetailStatsBackfillRunner.java"
      provides: "기동+일1회 재료 상세 Stats 14일 소급 upsert(DETAIL_STATS) — 멱등·레이트리밋 존중·품목별 fail-open (D-03, BACKFILL-02)"
    - path: "src/main/java/com/lostark/tracker/collect/LostarkApiClient.java"
      provides: "getItemDetail(long) → ItemDetailResponse(Stats[]) — 수집과 동일한 에러 택소노미(Auth/RateLimit/Transient/NonRetryable) read-only GET"
    - path: "src/main/java/com/lostark/tracker/collect/dto/ItemDetailResponse.java"
      provides: "상세 응답의 Stats[] {Date, AvgPrice, TradeCount} 매핑 DTO"
  key_links:
    - from: "DetailStatsBackfillRunner.backfill()"
      to: "17.4-01 ItemDailyStatRepository.upsertDailyStat(...)"
      via: "멱등 upsert 계약(tracked_item_id, statDate, avgPrice, DETAIL_STATS)"
      pattern: "upsertDailyStat"
    - from: "DetailStatsBackfillRunner"
      to: "LostarkApiClient.getItemDetail(long) + TrackedItemRepository 재료 finder"
      via: "재료 품목 상세 조회 → Stats[] 14일 소급"
      pattern: "getItemDetail"
---

<objective>
서버 기동 시 + 일 1회, 상세 API `Stats[]`로 **재료의 최근 14일 일별 평균**을 `source=DETAIL_STATS`로 소급 upsert해 과거 gap을 채운다(D-01 소스②, D-03, BACKFILL-02) — `LostarkApiClient`에 `getItemDetail(long)`(수집과 동일 에러 택소노미) + `ItemDetailResponse`/`MarketStat` DTO를 추가하고, `TrackedItemRepository`에 재료 finder를 두고, 신규 `DetailStatsBackfillRunner`(ApplicationRunner + @Scheduled)가 재료 품목을 순회해 `Stats[].AvgPrice>0` 14일치를 멱등 upsert한다.

Purpose: 재료(운명의 파괴석·아비도스 융화 재료 등)의 서버 off 구간 과거 gap을, 로스트아크가 보관하는 14일 일별 평균으로 정직하게 소급 채운다. 각인서는 상세 Stats=0(API 한계)이라 소급 대상 아님(각인서 백필은 17.4-02 YDayAvgPrice going-forward가 담당). 레이트리밋 존중·품목별 fail-open으로 수집 파이프라인과 자원 경쟁 없이 additive 실행한다(BACKFILL-04).

Output: `ItemDetailResponse`·`MarketStat`·`LostarkApiClient.getItemDetail`·`TrackedItemRepository`(재료 finder)·`DetailStatsBackfillRunner` + DetailStatsBackfillRunnerIT(실 API 미호출·stub). PriceCollector·price_snapshot·캐시·event-impact·searchMarketItems diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.4-timeline-gap-backfill/17.4-CONTEXT.md
@.planning/phases/17.4-timeline-gap-backfill/17.4-SPIKE-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- MarketStat(com.lostark.tracker.collect.dto) record @JsonIgnoreProperties(ignoreUnknown=true): @JsonProperty("Date") String date(예 "2026-07-07" 또는 시각 포함 — 앞 10자 yyyy-MM-dd만 파싱), @JsonProperty("AvgPrice") Double avgPrice, @JsonProperty("TradeCount") Long tradeCount. 스파이크 실측: 재료 14/14 AvgPrice>0, 각인서 0/14. -->
<!-- ItemDetailResponse(com.lostark.tracker.collect.dto) record @JsonIgnoreProperties(ignoreUnknown=true): @JsonProperty("Stats") List<MarketStat> stats. compact 생성자에서 stats null→List.of(). -->
<!-- LostarkApiClient.getItemDetail(long itemId): spike/LostarkSpikeClient.getItemDetail 패턴을 프로덕션 에러 택소노미로 포팅 — restClient.get().uri("/markets/items/{id}", itemId).retrieve().onStatus(401||403→AuthApiException).onStatus(429→RateLimitedApiException + Retry-After).onStatus(5xx→TransientApiException).onStatus(4xx→NonRetryableApiException).body(ItemDetailResponse.class); ResourceAccessException→TransientApiException. searchMarketItems와 동일 패턴, read-only GET. -->
<!-- TrackedItemRepository: List<TrackedItem> findByActiveTrueAndRoleGroup(String roleGroup) 추가(재료 소급 대상 = roleGroup="MATERIAL"). 기존 findByActiveTrue/findByExternalItemId 무변경. -->
<!-- DetailStatsBackfillRunner(com.lostark.tracker.backfill) @Component implements ApplicationRunner: 생성자(LostarkApiClient apiClient, TrackedItemRepository trackedItemRepo, ItemDailyStatRepository dailyStatRepo, RedisTokenBucket rateLimiter, Clock clock). private static final String MATERIAL="MATERIAL". run(ApplicationArguments)→backfill()(기동). @Scheduled(cron="${backfill.detail-cron:0 30 4 * * *}") scheduledBackfill()→backfill()(일1회). void backfill(): for (TrackedItem item : trackedItemRepo.findByActiveTrueAndRoleGroup(MATERIAL)) { if(!rateLimiter.tryAcquire()){log.info skip;continue;} try { ItemDetailResponse d = apiClient.getItemDetail(Long.parseLong(item.getExternalItemId())); for (MarketStat s : d.stats()) { if(s.avgPrice()==null || s.avgPrice()<=0) continue; LocalDate day = LocalDate.parse(s.date().substring(0,10)); dailyStatRepo.upsertDailyStat(item.getId(), day, BigDecimal.valueOf(s.avgPrice()), DailyStatSource.DETAIL_STATS.name()); } } catch(AuthApiException e){ log.warn("backfill auth stop"); break; } catch(RuntimeException e){ log.warn("detail backfill failed item {}", item.getId(), e); } } — 품목별 fail-open, AUTH는 break, 키/바디 미로깅. seed 프로파일 억제 불필요(실 재료만, 저빈도)이나 test 프로파일에선 @MockBean으로 API 대체. -->
<!-- IT: DetailStatsBackfillRunnerIT(PostgresRedisContainers, @SpringBootTest, @ActiveProfiles("test"), @MockBean LostarkApiClient + @MockBean/@SpyBean RedisTokenBucket(tryAcquire→true)). 재료 TrackedItem(roleGroup="MATERIAL") 시드 + 각인서(roleGroup=null/DEALER) 시드. getItemDetail stub이 14 Stats(13개 AvgPrice>0 + 1개 AvgPrice=0) 반환하도록. ApplicationRunner.run 또는 backfill() 직접 호출 후: 재료는 DETAIL_STATS 13행(0 skip)·멱등(2회 실행 시 13행 유지), 각인서는 getItemDetail 미호출(재료 finder가 제외) 검증. 실 API·키 미사용. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemDetailResponse/MarketStat DTO + LostarkApiClient.getItemDetail + 재료 finder</name>
  <read_first>
    - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java (getItemDetail GET 패턴 원형 — 포팅 대상)
    - src/main/java/com/lostark/tracker/collect/LostarkApiClient.java (searchMarketItems onStatus 에러 택소노미 — 동일 적용)
    - src/main/java/com/lostark/tracker/collect/dto/MarketItemsResponse.java (record + @JsonIgnoreProperties + compact null-guard 원형)
    - src/main/java/com/lostark/tracker/repository/TrackedItemRepository.java (파생 finder 네이밍)
  </read_first>
  <files>src/main/java/com/lostark/tracker/collect/dto/MarketStat.java, src/main/java/com/lostark/tracker/collect/dto/ItemDetailResponse.java, src/main/java/com/lostark/tracker/collect/LostarkApiClient.java, src/main/java/com/lostark/tracker/repository/TrackedItemRepository.java</files>
  <action>
    `MarketStat`(`com.lostark.tracker.collect.dto`) record `@JsonIgnoreProperties(ignoreUnknown=true)`: `@JsonProperty("Date") String date`, `@JsonProperty("AvgPrice") Double avgPrice`, `@JsonProperty("TradeCount") Long tradeCount`. `ItemDetailResponse`(같은 패키지) record `@JsonIgnoreProperties(ignoreUnknown=true)`: `@JsonProperty("Stats") List<MarketStat> stats`, compact 생성자에서 `stats = stats == null ? List.of() : stats`. `LostarkApiClient`에 `public ItemDetailResponse getItemDetail(long itemId)` 추가 — `spike/LostarkSpikeClient.getItemDetail`의 `restClient.get().uri("/markets/items/{id}", itemId)` 패턴에 `searchMarketItems`와 **동일한 onStatus 에러 매핑**(401/403→AuthApiException, 429→RateLimitedApiException+parseRetryAfter, 5xx→TransientApiException, 4xx→NonRetryableApiException)을 붙이고 `.body(ItemDetailResponse.class)`, `ResourceAccessException`→`TransientApiException`. read-only GET·바디 없음. `searchMarketItems`·생성자·기존 로직은 무변경. `TrackedItemRepository`에 `List<TrackedItem> findByActiveTrueAndRoleGroup(String roleGroup)`를 추가(재료 소급 대상), 기존 finder 무변경.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - MarketStat(Date/AvgPrice/TradeCount)·ItemDetailResponse(Stats[] null-guard)가 @JsonIgnoreProperties로 정의된다
    - LostarkApiClient.getItemDetail이 searchMarketItems와 동일한 4xx/429/5xx/I-O 에러 택소노미로 ItemDetailResponse를 반환 — `grep -q "getItemDetail" src/main/java/com/lostark/tracker/collect/LostarkApiClient.java`
    - TrackedItemRepository.findByActiveTrueAndRoleGroup가 추가되고 기존 finder 무변경
    - `./gradlew compileJava` 그린, searchMarketItems·수집 틱 diff 0줄
  </acceptance_criteria>
  <done>상세 조회(getItemDetail)와 Stats DTO·재료 finder가 컴파일되고 수집과 동일한 에러 택소노미를 재사용하며 기존 수집 경로 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 2: DetailStatsBackfillRunner(기동+일1회 멱등 소급) + IT</name>
  <read_first>
    - src/main/java/com/lostark/tracker/news/NewsPoller.java (@Scheduled 폴러 + 레이트리밋 예산 원형)
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (ApplicationRunner 기동 훅 원형)
    - src/main/java/com/lostark/tracker/collect/ItemFetchService.java (RedisTokenBucket tryAcquire·AuthApiException 처리 원형)
    - src/main/java/com/lostark/tracker/repository/ItemDailyStatRepository.java (17.4-01 — upsertDailyStat 시그니처)
    - src/main/java/com/lostark/tracker/domain/DailyStatSource.java (DETAIL_STATS)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (Testcontainers 베이스)
  </read_first>
  <files>src/main/java/com/lostark/tracker/backfill/DetailStatsBackfillRunner.java, src/test/java/com/lostark/tracker/backfill/DetailStatsBackfillRunnerIT.java</files>
  <action>
    `DetailStatsBackfillRunner`(`com.lostark.tracker.backfill`) `@Component implements ApplicationRunner`: 생성자로 `LostarkApiClient`·`TrackedItemRepository`·`ItemDailyStatRepository`·`RedisTokenBucket`·`Clock` 주입. `private static final String MATERIAL = "MATERIAL"`. `@Override run(ApplicationArguments args)`→`backfill()`(기동 시). `@Scheduled(cron = "${backfill.detail-cron:0 30 4 * * *}")` `scheduledBackfill()`→`backfill()`(일 1회, @EnableScheduling 앱 전역 재사용). `void backfill()`: `trackedItemRepo.findByActiveTrueAndRoleGroup(MATERIAL)` 순회 — 각 품목마다 `if (!rateLimiter.tryAcquire()) { log.info skip; continue; }`, `try { ItemDetailResponse d = apiClient.getItemDetail(Long.parseLong(item.getExternalItemId())); for (MarketStat s : d.stats()) { if (s.avgPrice() == null || s.avgPrice() <= 0) continue; LocalDate day = LocalDate.parse(s.date().substring(0,10)); dailyStatRepo.upsertDailyStat(item.getId(), day, BigDecimal.valueOf(s.avgPrice()), DailyStatSource.DETAIL_STATS.name()); } } catch (AuthApiException e) { log.warn("detail backfill auth stop"); break; } catch (RuntimeException e) { log.warn("detail backfill failed item {}", item.getId(), e); }` — 품목별 fail-open, AUTH는 회차 중단, item id 외 미로깅. `DetailStatsBackfillRunnerIT`(PostgresRedisContainers, @SpringBootTest, @ActiveProfiles("test"), `@MockBean LostarkApiClient`, RedisTokenBucket tryAcquire를 true로): 재료 TrackedItem(roleGroup="MATERIAL") 1건 + 각인서 TrackedItem(roleGroup="DEALER" 또는 null) 1건 시드, `getItemDetail`이 14 Stats(13개 AvgPrice>0 + 1개 AvgPrice=0, Date는 today..today-13)를 반환하도록 stub. `backfill()` 호출 후: (a) 재료가 DETAIL_STATS **13행**(AvgPrice=0 1개 skip); (b) `backfill()` 2회 → 여전히 **13행**(멱등); (c) 각인서는 `getItemDetail` **미호출**(재료 finder가 제외) — `verify(apiClient, never()).getItemDetail(각인서 id)`. 실 API·키 미사용.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - backfill()이 재료 상세 Stats 14일 중 AvgPrice>0 13개만 DETAIL_STATS로 upsert(0 skip) — IT (a) 통과
    - 2회 실행 시 13행 유지(멱등, 중복 없음) — IT (b) 통과
    - 각인서(roleGroup≠MATERIAL)는 getItemDetail 미호출 — IT (c) 통과
    - 기동(ApplicationRunner) + @Scheduled(cron) 두 경로가 backfill()을 호출(D-03), 레이트리밋 tryAcquire 존중·품목별 fail-open
    - `./gradlew build` 그린, PriceCollector·price_snapshot·캐시·event-impact diff 0줄(BACKFILL-04)
  </acceptance_criteria>
  <done>재료 14일 상세 Stats가 기동+일1회 멱등으로 소급 upsert되고(0/각인서 제외), 레이트리밋 존중·fail-open이며 수집 핵심 경로 0줄이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 → 로스트아크 상세 API | read-only GET /markets/items/{id}, bearer 토큰(LostarkApiClient 재사용), 재료만·저빈도 |
| 상세 소급 → 수집 파이프라인 | 별도 러너·별도 테이블, PriceCollector/레이트리밋 예산 공유(tryAcquire)만 |
| 외부 응답 Stats[] → item_daily_stats | @JsonIgnoreProperties + AvgPrice>0 필터 + Date 파싱 검증 후 파라미터 바인딩 upsert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-03-01 | Denial of Service | 상세 소급이 레이트리밋을 소진해 수집 방해 | mitigate | 기존 RedisTokenBucket tryAcquire 존중, 재료만·일1회 저빈도(스파이크 remaining 98). 미획득 시 skip |
| T-174-03-02 | Tampering | 잘못된 Date/AvgPrice로 시리즈 오염 | mitigate | AvgPrice>0만 upsert, Date는 yyyy-MM-dd 파싱(실패 시 해당 품목 fail-open), 멱등 upsert로 재실행 안전 |
| T-174-03-03 | Information Disclosure | 상세 실패 로그에 키/토큰 유입 | mitigate | AuthApiException 마커에 키 없음(LostarkApiClient 원형), 로그에 item id만 |
| T-174-03-04 | Spoofing | 각인서까지 상세 소급 시도(무의미 0 데이터) | mitigate | roleGroup=MATERIAL finder로 대상 한정 — 각인서 미호출(IT 검증), 각인서 백필은 17.4-02 YDayAvgPrice 담당 |
| T-174-03-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 RestClient/JPA/@Scheduled 스택 재사용 |
</threat_model>

<verification>
- 상세 `Stats[].AvgPrice`(재료 14일, 0 skip)를 DETAIL_STATS로 멱등 소급 upsert — DetailStatsBackfillRunnerIT (a)(b)(D-01 소스②).
- 기동(ApplicationRunner) + 일1회(@Scheduled cron) 두 트리거가 backfill 실행, 재기동 시 중복 행 0(D-03).
- 재료만 대상(각인서/딜러/서포터 미호출), 레이트리밋 tryAcquire 존중·품목별 fail-open — IT (c) + 로그(BACKFILL-02).
- PriceCollector·price_snapshot·LatestPriceCache·EventImpactService·searchMarketItems diff 0줄(BACKFILL-04).
</verification>

<success_criteria>
- 서버 기동 시 + 일 1회, 상세 API Stats[]로 재료의 최근 14일 일별 평균을 소급 upsert해 과거 gap을 채운다(BACKFILL-02, D-01 소스②, D-03).
- 멱등(재기동 시 중복 없음)·재료 한정·레이트리밋 존중으로 수집과 자원 경쟁 없이 실행된다.
- 각인서는 상세 Stats=0(API 한계)이라 소급 대상 아님 — 각인서 백필은 17.4-02 YDayAvgPrice going-forward가 담당(BACKFILL-04, Core Value 0줄).
</success_criteria>

<output>
Create `.planning/phases/17.4-timeline-gap-backfill/17.4-03-SUMMARY.md` when done
</output>