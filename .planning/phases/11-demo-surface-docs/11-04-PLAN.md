---
phase: 11-demo-surface-docs
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-03"]
mode: gap_closure
files_modified:
  - src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java
  - src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java
  - src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java
autonomous: true
requirements: [DEMO-01]
source_uat: .planning/phases/11-demo-surface-docs/11-UAT.md
debug_session: .planning/debug/seed-health-card-auth-error.md

must_haves:
  truths:
    - "**seed 데모의 수집 헬스 카드가 seed 데이터와 일관된 SUCCESS를 보여준다 (UAT Test 3 gap).** `SyntheticDemoData.seed()`가 합성 스냅샷/이벤트를 심을 때 **합성 SUCCESS `collection_run`도 함께 멱등 적재**한다: `itemsAttempted = 활성 품목 수(=12)`, `itemsSucceeded = 동일`, `itemsFailed = 0`, `status = \"SUCCESS\"`, `summaryMessage = null`(AUTH_ERROR/RATE_LIMITED 아님), `startedAt = finishedAt = gridNow`(seed가 쓰는 currentTickFloor 동일 grid). `startedAt = gridNow`라 영속 볼륨에 남은 과거 키리스 AUTH_ERROR run보다 **최신**이므로 `findTopByOrderByStartedAtDesc()`가 합성 SUCCESS run을 반환 → HealthCard가 '시도 12 · 성공 12 · 실패 0' + 마커 없음으로 렌더."
    - "**멱등성 보존 (재시드 무해).** 기존 seed의 멱등 계약(스냅샷=DATA-01 unique, 이벤트=count==0)과 동일하게, 합성 SUCCESS run도 같은 10분 그리드에서 재시드 시 **중복 적재하지 않는다**. 가드: `CollectionRunRepository.existsByStartedAtAndStatus(gridNow, \"SUCCESS\")`가 true면 skip. (다른 그리드의 재시드는 더 최신 SUCCESS run을 새로 남겨 '마지막 실행'을 갱신 — 허용·바람직.) 활성 품목이 0이면 run을 심지 않는다(스냅샷/이벤트 가드와 동일하게 빈 데모엔 run도 없음)."
    - "**기존 동작·결정 무회귀.** 라이브 `PriceCollector.collectTick()`·`CollectionHealthService`·`CollectionHealthResponse`·`HealthCard.tsx`·Flyway 스키마·`collection_run` 도메인 모델은 **무변경**(읽기 경로·DTO·프론트 0줄). seed가 심는 SUCCESS run은 실제 tick과 동일한 엔티티/생성자(`new CollectionRun(startedAt, finishedAt, attempted, succeeded, failed, status)` + `setSummaryMessage(null)`)를 사용해 D-12 시크릿 미노출 계약을 그대로 따른다. dev/test/prod-default 프로파일은 `SeedDataRunner(@Profile(\"seed\"))`만 `seed()`를 호출하므로 영향 없음."
    - "**회귀 테스트로 고정.** `SyntheticDemoDataIT`(test 프로파일, Testcontainers)에 단언 추가: (a) `seed()` 후 `CollectionRunRepository.findTopByOrderByStartedAtDesc()`가 `status=\"SUCCESS\"`, `itemsAttempted==itemsSucceeded==활성품목수`, `itemsFailed==0`, `summaryMessage==null`인 run을 반환한다. (b) **과거 AUTH_ERROR run을 먼저 적재**(`new CollectionRun(now.minusHours(1), ..., 12, 0, 12, \"FAILED\")` + `setSummaryMessage(\"AUTH_ERROR\")`)한 뒤 `seed()`를 호출하면, latest run이 합성 SUCCESS로 **덮인다**(영속 볼륨 잔존 시나리오 재현). (c) 같은 그리드 2회 `seed()`가 SUCCESS run을 **중복 생성하지 않는다**(count 불변)."
  artifacts:
    - path: "src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java"
      provides: "seed()가 합성 SUCCESS collection_run을 멱등 적재 — 헬스 카드가 seed 데이터와 일관된 12/12 SUCCESS를 표시할 근거"
      contains: "collectionRunRepository"
    - path: "src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java"
      provides: "existsByStartedAtAndStatus 멱등 가드 — 같은 그리드 재시드 시 중복 SUCCESS run 방지"
      contains: "existsByStartedAtAndStatus"
    - path: "src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java"
      provides: "seed 후 latest run=SUCCESS·과거 실패 run 덮기·멱등 회귀 단언"
      contains: "findTopByOrderByStartedAtDesc"
  key_links:
    - from: "src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java"
      to: "src/main/java/com/lostark/tracker/health/CollectionHealthService.java"
      via: "seed가 startedAt=gridNow인 SUCCESS run을 적재 → latestHealth()의 findTopByOrderByStartedAtDesc()가 이를 반환(읽기 경로 무변경)"
      pattern: "collection_run"
---

<objective>
UAT Test 3 gap을 닫는다: **seed 모드 데모의 수집 헬스 카드가 '12/12 실패 · 인증오류'(또는 NO_RUNS)로 떠 Core Value(수집 파이프라인)가 망가진 것처럼 보이는 문제**를 수정한다.

근본 원인은 `SyntheticDemoData.seed()`가 스냅샷/이벤트만 심고 `collection_run`을 심지 않는 것 — 헬스 카드(`findTopByOrderByStartedAtDesc()`)가 표현할 "수집 성공" 행이 없어, 영속 볼륨에 남은 과거 키리스 AUTH_ERROR run(또는 NO_RUNS)이 seed 데모의 얼굴이 된다.

수정: `seed()`가 timeline/impact와 **일관된** 합성 **SUCCESS** `collection_run`(시도=성공=활성품목수, 실패=0, 마커 없음, startedAt=gridNow)을 **멱등** 적재한다. 읽기 경로·DTO·프론트·스키마는 0줄 변경(원인은 seed 데이터 공급의 누락이지 읽기 로직 결함이 아님). Testcontainers 회귀 테스트로 고정.

Output: `SyntheticDemoData.java`·`CollectionRunRepository.java` 수정, `SyntheticDemoDataIT.java` 단언 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-demo-surface-docs/11-UAT.md
@.planning/debug/seed-health-card-auth-error.md

<interfaces>
<!-- CollectionRun 생성자(무변경 사용):
     new CollectionRun(OffsetDateTime startedAt, OffsetDateTime finishedAt,
                       int itemsAttempted, int itemsSucceeded, int itemsFailed, String status)
     + setSummaryMessage(String)  // null = 마커 없음
     필드: startedAt(not null), finishedAt(nullable), itemsAttempted/Succeeded/Failed(int),
           status(<=40자), summaryMessage(<=500자, nullable). -->

<!-- CollectionHealthService.latestHealth() = findTopByOrderByStartedAtDesc().map(from).orElseGet(noRuns)
     CollectionHealthResponse.from(run): lastRunAt=finishedAt, startedAt, attempted/succeeded/failed, status, summaryMessage
     HealthCard.tsx: "시도 {attempted} · 성공 {succeeded} · 실패 {failed}" + SummaryMarker(summaryMessage)
                     status==="NO_RUNS"면 "아직 수집 실행 기록이 없어요" 분기. -->

<!-- SyntheticDemoData.seed() @Transactional:
       gridNow = currentTickFloor()  // UTC 10분 그리드 floor
       items = trackedItemRepository.findByActiveTrue()
       for item: seedSnapshots(item, gridNow)
       if !items.isEmpty() && gameEventRepository.count()==0: seedEvents(gridNow)
       return new SeedSummary(snapshots, events)
     SeedSummary record(snapshots, events) — 생성자 호출 1곳(seed()), 접근자만 SeedDataRunner/IT에서 사용. -->

<!-- WatchlistSeeder WATCHLIST = 12개 품목(@Profile{dev,seed}, @Order(1)) → seed 프로파일에서 활성 품목 12개. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CollectionRunRepository 멱등 가드 메서드 추가</name>
  <files>src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java</files>
  <read_first>src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java, src/main/java/com/lostark/tracker/domain/CollectionRun.java</read_first>
  <action>`CollectionRunRepository`에 파생 쿼리 메서드를 추가한다:
`boolean existsByStartedAtAndStatus(java.time.OffsetDateTime startedAt, String status);`
import는 OffsetDateTime만 추가(기존 Optional/JpaRepository/CollectionRun 유지). 기존 `findTopByOrderByStartedAtDesc()`는 무변경.</action>
  <verify>
    <automated>grep -q "existsByStartedAtAndStatus" src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java && echo "repo guard added"</automated>
  </verify>
  <done>existsByStartedAtAndStatus(OffsetDateTime, String) 파생 메서드가 추가되고 컴파일된다.</done>
</task>

<task type="auto">
  <name>Task 2: SyntheticDemoData.seed()가 합성 SUCCESS collection_run을 멱등 적재</name>
  <files>src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java</files>
  <read_first>src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java, src/main/java/com/lostark/tracker/domain/CollectionRun.java, src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java</read_first>
  <action>`SyntheticDemoData`에 `CollectionRunRepository`를 생성자 주입으로 추가하고, `seed()`에서 스냅샷 적재 후(이벤트 시드와 같은 `!items.isEmpty()` 가드 안 또는 그 직후) **합성 SUCCESS collection_run을 멱등 적재**한다:
1) 활성 품목이 비어있지 않을 때만 수행(빈 데모엔 run도 없음).
2) 멱등 가드: `if (!collectionRunRepository.existsByStartedAtAndStatus(gridNow, "SUCCESS")) { ... }`.
3) run 생성: `int n = items.size();`
   `CollectionRun run = new CollectionRun(gridNow, gridNow, n, n, 0, "SUCCESS");`
   `run.setSummaryMessage(null);`(명시적 — 마커 없음)
   `collectionRunRepository.save(run);`
4) 반환 요약(택1, Claude's Discretion):
   - **권장:** `SeedSummary`에 `int runs` 필드를 추가(`record SeedSummary(int snapshots, int events, int runs)`)하고 `seed()`의 생성자 호출을 `new SeedSummary(snapshots, events, runsInserted)`로 갱신, `SeedDataRunner` 로그에 ", {} collection runs" 추가. (기존 IT/Runner는 snapshots()/events() 접근자만 사용 → 안전.)
   - 또는 SeedSummary를 건드리지 않고 run만 silently 적재(요약 무변경). 선택 시 IT는 repo로 직접 검증.
주석으로 의도를 1~2줄 남긴다: "seed 데이터와 일관되게 헬스 카드가 12/12 SUCCESS로 보이도록 합성 성공 run을 멱등 적재 — startedAt=gridNow가 최신이라 영속 볼륨의 과거 키리스 AUTH_ERROR run을 덮는다. 실제 tick과 동일 엔티티·D-12 마커 null."
**라이브 PriceCollector·읽기 경로·DTO·프론트·스키마는 건드리지 않는다.**</action>
  <verify>
    <automated>grep -q "existsByStartedAtAndStatus" src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java && grep -q "\"SUCCESS\"" src/main/java/com/lostark/tracker/seed/SyntheticDemoData.java && ./gradlew -q compileJava && echo "seed run insert compiles"</automated>
  </verify>
  <done>seed()가 활성 품목 존재 시 합성 SUCCESS collection_run을 멱등(existsByStartedAtAndStatus 가드)으로 적재하고, 읽기 경로/프론트/스키마는 무변경이며 compileJava 통과.</done>
</task>

<task type="auto">
  <name>Task 3: SyntheticDemoDataIT 회귀 단언 추가</name>
  <files>src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java</files>
  <read_first>src/test/java/com/lostark/tracker/seed/SyntheticDemoDataIT.java, src/main/java/com/lostark/tracker/repository/CollectionRunRepository.java, src/main/java/com/lostark/tracker/domain/CollectionRun.java</read_first>
  <action>`SyntheticDemoDataIT`에 `@Autowired CollectionRunRepository collectionRunRepository;`를 추가하고(`@BeforeEach clean()`에 `collectionRunRepository.deleteAll();`를 FK 무관이므로 적절한 위치에 추가), 신규 `@Test`를 추가한다:
**`seedWritesSuccessRunThatBeatsAStalePersistedAuthErrorRun()`**
1) 활성 품목 1개 저장(기존 패턴 `new TrackedItem("9001","데모 아이템","50010")`).
2) **과거 실패 run 선적재**(영속 볼륨 잔존 재현): `OffsetDateTime now = OffsetDateTime.now(clock);`
   `CollectionRun stale = new CollectionRun(now.minusHours(1), now.minusHours(1), 12, 0, 12, "FAILED");`
   `stale.setSummaryMessage("AUTH_ERROR"); collectionRunRepository.save(stale);`
3) `syntheticDemoData.seed();` 호출.
4) 단언: `CollectionRun latest = collectionRunRepository.findTopByOrderByStartedAtDesc().orElseThrow();`
   - `latest.getStatus()` == "SUCCESS"
   - `latest.getItemsAttempted()` == 활성 품목 수(여기선 1) && `getItemsSucceeded()` == 동일 && `getItemsFailed()` == 0
   - `latest.getSummaryMessage()` == null (AUTH_ERROR 아님)
5) **멱등 단언:** 같은 grid에서 `syntheticDemoData.seed();`를 한 번 더 호출 후 `collectionRunRepository.count()`가 (선적재 1 + SUCCESS 1) = 2로 **불변**(중복 SUCCESS 미생성).
기존 테스트(`seedYieldsNonEmptyTimelineAndAnEventImpactOk` 및 멱등 스냅샷 테스트)는 무변경 유지. `Clock`은 이미 주입돼 있으니 재사용.</action>
  <verify>
    <automated>./gradlew -q test --tests "com.lostark.tracker.seed.SyntheticDemoDataIT" && echo "seed health regression test green"</automated>
  </verify>
  <done>새 테스트가 seed 후 latest run=SUCCESS(attempted==succeeded, failed==0, summaryMessage==null)이고 과거 AUTH_ERROR run을 덮으며 멱등(중복 미생성)임을 단언하고 통과한다. 기존 테스트도 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed 합성 데이터 → collection_run(헬스 카드 노출) | seed가 새로 collection_run 행을 쓴다. 이 행의 summaryMessage가 시크릿(API 키/Authorization)을 담으면 /api/health/collection으로 노출될 수 있다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | seed가 쓰는 CollectionRun.summaryMessage | mitigate | summaryMessage를 **명시적으로 null**로 둔다(마커 없음). 카테고리컬 마커조차 쓰지 않으므로 시크릿 노출 경로 0 — 실제 tick의 D-12/D-14 계약과 동일. |
| T-11-04-02 | Tampering/Spoofing | 합성 SUCCESS run이 실제 수집 성공인 것처럼 오인 | accept | seed 프로파일은 데모 전용(SeedDataRunner @Profile("seed"))이고 합성 데이터임을 frontend/README가 정직히 명시. prod-default/dev/test는 seed()를 호출하지 않아 실데이터 헬스에 영향 없음. |

**고(high) 심각도 위협 없음** — 데모 전용 프로파일에서 시크릿 없는 카운트 행 1개를 멱등 적재할 뿐, 신규 외부 호출·인증 경로·시크릿을 도입하지 않는다.
</threat_model>

<verification>
- seed 후 `findTopByOrderByStartedAtDesc()`가 status="SUCCESS", attempted==succeeded==활성품목수, failed==0, summaryMessage==null인 run을 반환한다(IT).
- 과거 AUTH_ERROR run이 영속돼 있어도 seed의 SUCCESS run(startedAt=gridNow)이 최신이라 헬스 카드가 12/12 SUCCESS로 표시된다(IT 회귀).
- 같은 10분 그리드 재시드가 SUCCESS run을 중복 생성하지 않는다(멱등 IT).
- 읽기 경로(CollectionHealthService/Controller/DTO)·HealthCard.tsx·Flyway 스키마·CollectionRun 모델·PriceCollector는 무변경(diff에 없음).
- `./gradlew test`(또는 최소 SyntheticDemoDataIT + 기존 헬스/수집 IT) 그린.

## 회귀 안전 (기존 기능 보존)
- dev/test/prod-default는 `seed()`를 호출하지 않으므로(@Profile("seed")만 호출) 실데이터 수집 헬스·라이브 tick 동작 무영향.
- 기존 IT(`SyntheticDemoDataIT` 기존 2 테스트, `CollectionResilienceIT`, `PriceCollectionIT`, `LatestPriceCacheIT`) 무변경 통과 — seed가 추가하는 SUCCESS run은 별개 행이라 스냅샷/이벤트/캐시 단언에 영향 없음.
</verification>

<success_criteria>
- UAT Test 3 gap이 닫힌다: seed 데모의 수집 헬스 카드가 seed 데이터와 일관된 "시도 12 · 성공 12 · 실패 0"(마커 없음)으로 표시된다.
- 영속 볼륨에 과거 키리스 AUTH_ERROR run이 남아 있어도 seed가 최신 SUCCESS로 덮는다.
- 멱등성·D-12 시크릿 미노출·읽기 경로 무변경이 회귀 테스트로 고정된다.
</success_criteria>

<output>
Create `.planning/phases/11-demo-surface-docs/11-04-SUMMARY.md` when done.
</output>
