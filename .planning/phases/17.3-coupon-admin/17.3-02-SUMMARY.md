---
phase: 17.3-coupon-admin
plan: 02
subsystem: ui
tags: [react, typescript, zod, react-query, tailwind, admin-console, coupon]

requires:
  - phase: 17.3-01
    provides: GET /api/coupons (public) + /api/admin/coupons CRUD (CouponResponse contract)
  - phase: 15-admin-console
    provides: adminRequest X-Admin-Secret + auto-logout, EventSection CRUD pattern, AsyncBoundary
provides:
  - couponSchema/couponsSchema/AdminCouponRequest (zod, date-only expiresAt string)
  - api fns getCoupons + admin CRUD (getAdminCoupons/createAdminCoupon/replaceAdminCoupon/deleteAdminCoupon)
  - useCoupons(['coupons']) public read hook (consumed by 17.3-03 dashboard)
  - useAdminCoupons/useCreateCoupon/useReplaceCoupon/useDeleteCoupon (mutations invalidate both keys)
  - CouponSection admin CRUD UI (type=date) placed in AdminConsolePage
affects: [17.3-03 dashboard coupon panel — consumes useCoupons]

tech-stack:
  added: []
  patterns:
    - "CouponSection mirrors EventSection 1:1 (inline create/edit form + Table + 2-step delete + feedback + AsyncBoundary)"
    - "Date-only <Input type=\"date\"> value sent verbatim as expiresAt — no KST↔UTC conversion (D-01)"
    - "Coupon mutations invalidate BOTH ['admin-coupons'] and ['coupons'] so the dashboard reflects admin edits"

key-files:
  created:
    - frontend/src/features/admin/CouponSection.tsx
  modified:
    - frontend/src/lib/schemas.ts
    - frontend/src/lib/api.ts
    - frontend/src/lib/queries.ts
    - frontend/src/features/admin/AdminConsolePage.tsx

key-decisions:
  - "D-01: 만료일 입력은 <Input type=\"date\">; 값 \"YYYY-MM-DD\"를 그대로 expiresAt로 전송(kstLocalToUtcIso 미사용). 표시는 slice(0,10).replace('-','.') → YYYY.MM.DD."
  - "COUPONS_KEY(['coupons'])를 이 플랜에서 정의 — CouponSection과 NewsPanel(17.3-03)이 공유하므로 lib 계층에 함께 둬 파일 충돌 제거."
  - "쿠폰 admin mutation onSuccess는 ['admin-coupons']와 ['coupons']를 둘 다 invalidate — 관리자 변경이 공개 대시보드 패널에도 반영."

patterns-established:
  - "관리자 콘솔 섹션은 EventSection 미러로 확장 — 데이터 계층(zod→api→queries) 3단 + 인라인 CRUD 카드"

requirements-completed: [COUPON-01]

duration: ~15 min
completed: 2026-07-07
---

# Phase 17.3 Plan 02: 프론트 쿠폰 데이터 계층 + 관리자 CouponSection Summary

**EventSection을 미러링한 관리자 쿠폰 CRUD 콘솔 — date-only(type=date) 입력·zod boundary·React Query mutation(양쪽 키 invalidate)과 대시보드가 소비할 useCoupons(['coupons']) 공개 훅을 같은 데이터 계층에 배치**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-07
- **Tasks:** 3
- **Files:** 1 created + 4 modified

## Accomplishments
- 쿠폰 데이터 계층 3단: `couponSchema`/`AdminCouponRequest`(zod, date-only expiresAt) → api `getCoupons`+admin CRUD fn → queries `useCoupons`(공개)+admin 훅.
- `CouponSection`: 인라인 create/edit(code·reward·`<Input type="date">`) + 만료임박순 Table(코드·보상·만료일 YYYY.MM.DD·관리) + 2단계 삭제 확인 + AsyncBoundary + feedback.
- admin mutation onSuccess가 `['admin-coupons']`·`['coupons']` 둘 다 invalidate → 관리자 변경이 대시보드 패널에도 반영.
- `AdminConsolePage`에 `<CouponSection/>` 배치(EventSection 다음), 기존 3섹션 회귀 0. `npm run build` 그린, 백엔드 diff 0줄, 신규 의존성 0.

## Task Commits

1. **Task 1: couponSchema/AdminCouponRequest + api fn + queries 훅** — `2df87ac` (feat)
2. **Task 2: CouponSection.tsx (인라인 CRUD, type=date)** — `45359e1` (feat)
3. **Task 3: AdminConsolePage 배치** — `7a03492` (feat)

## Files Created/Modified
- `lib/schemas.ts` — couponSchema/couponsSchema/Coupon/AdminCouponRequest 추가(date-only 주석)
- `lib/api.ts` — getCoupons + admin CRUD fn(zod boundary, DELETE 204 무파싱)
- `lib/queries.ts` — useCoupons(['coupons'], staleTime 5분) + admin 훅(양쪽 키 invalidate)
- `features/admin/CouponSection.tsx` — 관리자 쿠폰 CRUD UI(신규)
- `features/admin/AdminConsolePage.tsx` — CouponSection 배치

## Decisions Made
- 계획대로 실행 — frontmatter key-decisions(D-01 type=date, COUPONS_KEY 공유 정의, 양쪽 키 invalidate) 그대로 반영.

## Deviations from Plan

None - plan executed exactly as written.

(주: reward 입력은 계획의 "Input 또는 Textarea" 중 Input을 채택 — 단일 라인 보상 문자열에 충분하고 code 필드와 일관.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 17.3-03(대시보드 뉴스 패널 쿠폰 섹션)이 소비할 `useCoupons()`(['coupons'], GET /api/coupons) 공개 훅 준비 완료.
- `Coupon` 타입: `{id, code, reward, expiresAt("YYYY-MM-DD"), createdAt, updatedAt}` — NewsPanel에서 code·reward·만료일 렌더 + 복사 버튼에 사용.

---
*Phase: 17.3-coupon-admin*
*Completed: 2026-07-07*
