---
phase: 17.1-demo-polish
plan: 02
type: execute
wave: 2
depends_on: [17.1-01]
files_modified:
  - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java
  - frontend/src/features/_shared/roleGroup.ts
autonomous: true
requirements: [POLISH-02, POLISH-03]

must_haves:
  truths:
    - "D-01: WatchlistSeeder.WATCHLIST를 15→22개로 교체 — 딜러 8유지+3신규-1제거(정밀 단도 65204305)=11, 서포터 2유지+5신규=7, 재료 2유지(아비도스 6861012·상급 아비도스 6861013)+2제거(상급 오레하 6861009·최상급 오레하 6861011)+2신규(운명의 파괴석/수호석 결정)=4"
    - "D-03: 각인 18종을 전부 '유물' 등급 각인서(category=40000)로 큐레이션 — 신규 8종도 17.1-SPIKE-FINDINGS.md의 유물 각인서 실 Id/Icon/CategoryCode로 그대로 전사(전사 시 추정 0건)"
    - "D-04: roleGroup.ts ROLE_LABEL.MATERIAL을 '융화재료'→'재료'로 일반화(운명의 결정=재련 재료 수용) — zod RoleGroup enum(schemas.ts)·역할 3색·sortByRole·백엔드 roleGroup enum 무변경, 단일 라인 변경"
    - "D-05: SyntheticDemoData는 findByActiveTrue()로 활성 품목을 동적 순회하므로, seed 프로파일에서도 WatchlistSeeder(@Profile dev,seed)가 22개를 시드하면 22개 합성 시세가 자동 생성된다 — 별도 코드 편집 불필요(키 없이 로컬/테스트 22개 재현, 정직성 이중 트랙 유지)"
    - "D-06: 불변 가드 — PriceCollector·캐시·event-impact 로직 0줄. 변경은 WatchlistSeeder 데이터 목록 + roleGroup 라벨 1줄에 한정(멱등 upsert·6필드 스키마·수집 정체성 회귀 없음)"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java"
      provides: "22개 재잠금 큐레이션(각인 18 + 재료 4), 멱등 upsert·6필드 SeedItem 스키마 유지 (D-01/D-03)"
    - path: "frontend/src/features/_shared/roleGroup.ts"
      provides: "ROLE_LABEL.MATERIAL '융화재료'→'재료' 일반화 (D-04)"
  key_links:
    - from: "WatchlistSeeder.WATCHLIST (신규 10개 행)"
      to: ".planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md"
      via: "spike-then-lock 전사 — findings 실측 Id/Icon/CategoryCode를 seeder에 그대로 옮김"
      pattern: "external_item_id"
    - from: "WatchlistSeeder(@Profile dev,seed @Order 1)"
      to: "SyntheticDemoData.findByActiveTrue()"
      via: "seed 프로파일 활성 품목 동적 순회 → 22개 합성 시세 자동 생성 (D-05)"
      pattern: "findByActiveTrue"
---

<objective>
17.1-01 스파이크가 잠근 실측값으로 큐레이션을 **15→22개로 재잠금**한다(POLISH-02/03). 수집 정체성(`WatchlistSeeder`) 한 곳만 교체하면 dev(실수집)·seed(합성) 양쪽이 22개로 재현되고, 프론트 라벨(`roleGroup.ts`) 한 줄로 재련 재료를 수용한다. Core Value(수집/캐시/event-impact)는 0줄(D-06).

- **D-01 — 세트 교체(15→22):** 딜러 9→11(유지 8 + 신규 3 − 제거 정밀 단도 `65204305`), 서포터 2→7(유지 2 + 신규 5), 재료 4→4(유지 아비도스 2 − 제거 오레하 2 + 신규 운명 결정 2).
- **D-03 — 유물 통일:** 각인 18종 전부 유물 각인서(category=40000). 신규 8종은 17.1-SPIKE-FINDINGS.md의 실 Id/Icon 전사.
- **D-04 — 라벨 일반화:** `ROLE_LABEL.MATERIAL` '융화재료'→'재료'(단일 라인). zod enum·3색·sortByRole·백엔드 enum 무변경.
- **D-05 — seed 자동 동기화:** `SyntheticDemoData`는 활성 품목을 동적 순회 → 코드 편집 없이 22개 합성 시세.
- **D-06 — 불변 가드:** PriceCollector/캐시/event-impact 0줄.

Purpose: 현행 로아 티어4 메타(딜러 11·서포터 7·아비도스+운명 결정)를 데이터에 반영해 "실시간 고변동 바스켓" 서사와 도메인 안목을 갱신하되, 수집 파이프라인 신뢰성(Core Value)은 건드리지 않는다.

Output: 22개로 재잠금된 `WatchlistSeeder.java`, '재료' 라벨의 `roleGroup.ts`. 백엔드 수집/캐시/event-impact·SyntheticDemoData diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-demo-polish/17.1-CONTEXT.md
@.planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@CLAUDE.md

<interfaces>
<!-- WatchlistSeeder.java: record SeedItem(String externalItemId, String displayName, String category, String iconUrl, String itemGroup, String roleGroup). static final List<SeedItem> WATCHLIST = List.of(...15...). ICON_BASE = "https://cdn-lostark.game.onstove.com/efui_iconatlas/use/". @Profile({"dev","seed"}) @Order(1). 멱등 upsert by external_item_id(findByExternalItemId().orElseGet(save)). category = 숫자 leaf CategoryCode(라벨 아님). -->
<!-- 현재 15개 중 제거 3: 65204305(유물 정밀 단도 각인서, DEALER), 6861009(상급 오레하 융화 재료, MATERIAL), 6861011(최상급 오레하 융화 재료, MATERIAL). 유지 12: 딜러 8(65200505 원한·65201005 예리한 둔기·65202805 저주받은 인형·65203905 아드레날린·65203705 타격의 대가·65203005 기습의 대가·65203305 돌격대장·65201505 결투의 대가) + 서포터 2(65203405 각성·65204105 전문의) + 재료 2(6861012 아비도스·6861013 상급 아비도스). -->
<!-- 신규 10(17.1-SPIKE-FINDINGS 전사): 딜러 각인 3(질량 증가·슈퍼 차지·바리케이드) + 서포터 각인 5(구슬동자·마나의 흐름·폭발물 전문가·분쇄의 주먹·중갑착용) + 재료 2(운명의 파괴석 결정·운명의 수호석 결정). 각 행의 external_item_id/iconUrl/category는 findings 실측값 그대로. 유물 각인서는 use_9_25.png 공유. -->
<!-- roleGroup.ts: export const ROLE_LABEL: Record<RoleGroup,string> = { DEALER:'딜러', SUPPORT:'서포터', MATERIAL:'융화재료' }. ★ MATERIAL만 '재료'로. ROLE_ORDER/sortByRole/roleRank 무변경. -->
<!-- schemas.ts: export const roleGroupSchema = z.enum(['DEALER','SUPPORT','MATERIAL']) — 무변경(D-04). 백엔드 roleGroup enum도 무변경. -->
<!-- SyntheticDemoData.java: seed()가 trackedItemRepository.findByActiveTrue() 동적 순회 → 활성 품목별 합성 스냅샷. priceBase는 externalItemId.hashCode() 유래(신규 품목도 자동 결정적 시세). ★ 편집 불필요(D-05는 구조적 자동 충족). -->
<!-- 불변(D-06): PriceCollector.java·CollectionConfig.java·캐시·EventImpactService 등 수집/캐시/event-impact 소스 0줄. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WatchlistSeeder WATCHLIST 15→22 재잠금(D-01/D-03)</name>
  <read_first>
    - src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java (현재 15개 목록·SeedItem 스키마·멱등 upsert — 교체 대상)
    - .planning/phases/17.1-demo-polish/17.1-SPIKE-FINDINGS.md (신규 10개 실측 external_item_id/icon_url/category_code — 전사 원천)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-01 세트 델타·D-03 유물 통일)
  </read_first>
  <files>src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</files>
  <action>
    `WATCHLIST` List를 22개로 교체한다. (1) **제거 3**: `65204305`(정밀 단도), `6861009`(상급 오레하), `6861011`(최상급 오레하) 행 삭제. (2) **유지 12**: 딜러 8·서포터 2·재료 2 행 그대로. (3) **신규 10 추가**: 17.1-SPIKE-FINDINGS.md의 실측 6필드를 `new SeedItem(externalItemId, displayName, category, ICON_BASE + iconFile, itemGroup, roleGroup)` 형태로 전사 — 딜러 각인 3(role_group=DEALER, item_group=각인서, category=40000), 서포터 각인 5(role_group=SUPPORT, item_group=각인서, category=40000), 재료 2(role_group=MATERIAL, category=findings의 재련 재료 실 CategoryCode, item_group은 findings/Discretion 값). displayName은 각인의 경우 '유물 {이름} 각인서'. **모든 신규 행은 findings 실측 Id/Icon만 사용**(추정 금지 — D-02 잠금 전사). SeedItem record·멱등 upsert·ICON_BASE·@Profile·@Order·클래스 Javadoc의 개수 서술(15→22, 각인 18·재료 4)만 갱신하고 그 외 로직은 무변경. PriceCollector/캐시/event-impact·SyntheticDemoData는 건드리지 않는다(D-06).
  </action>
  <verify>
    <automated>./gradlew build && grep -c "new SeedItem(" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java | grep -qx 22 && ! grep -qE "65204305|6861009|6861011" src/main/java/com/lostark/tracker/collect/WatchlistSeeder.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new SeedItem("`가 정확히 22 — 딜러 11 + 서포터 7 + 재료 4
    - 제거 3(65204305·6861009·6861011) external_item_id가 파일에 없다
    - 신규 10개 각인/재료의 external_item_id·iconUrl이 17.1-SPIKE-FINDINGS.md 실측값과 일치(추정 0건)
    - SeedItem 스키마·멱등 upsert·@Profile({"dev","seed"})·@Order(1) 무변경, `./gradlew build` 그린
    - PriceCollector/캐시/event-impact·SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>WATCHLIST가 22개(각인 18 + 재료 4)로 재잠금되고, 제거 3이 사라지며 신규 10이 findings 실측값으로 전사되고 build가 그린이다.</done>
</task>

<task type="auto">
  <name>Task 2: roleGroup.ts MATERIAL 라벨 '융화재료'→'재료'(D-04)</name>
  <read_first>
    - frontend/src/features/_shared/roleGroup.ts (ROLE_LABEL·ROLE_ORDER·sortByRole — 단일 출처)
    - frontend/src/lib/schemas.ts (roleGroupSchema z.enum — 무변경 확인용)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-04 최소 변경 근거)
  </read_first>
  <files>frontend/src/features/_shared/roleGroup.ts</files>
  <action>
    `ROLE_LABEL`의 `MATERIAL: '융화재료'`를 `MATERIAL: '재료'`로 바꾼다 — **이 한 줄만**. 운명의 파괴석/수호석 결정(재련 재료)이 MATERIAL 버킷에 들어와도 라벨이 정합하도록 일반화. `ROLE_ORDER`·`roleRank`·`sortByRole`·`RoleGroup` 타입 재노출은 무변경, `schemas.ts`의 `roleGroupSchema` enum도 무변경(백엔드 roleGroup enum 유예 — 새 roleGroup HONING은 v2). 인접 주석의 '융화재료' 언급이 있으면 '재료'로 정합만 맞춘다(선택).
  </action>
  <verify>
    <automated>grep -q "MATERIAL: '재료'" frontend/src/features/_shared/roleGroup.ts && ! grep -q "MATERIAL: '융화재료'" frontend/src/features/_shared/roleGroup.ts && grep -q "z.enum(\['DEALER', 'SUPPORT', 'MATERIAL'\])" frontend/src/lib/schemas.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ROLE_LABEL.MATERIAL`이 '재료'다('융화재료' 없음)
    - `sortByRole`·`ROLE_ORDER`·`roleRank`·zod `roleGroupSchema` enum 무변경(3색·정렬·enum 회귀 없음)
    - `cd frontend && npm run build` 그린(타입 회귀 없음)
  </acceptance_criteria>
  <done>MATERIAL 라벨이 '재료'로 일반화되고 정렬·enum·3색 체계는 무변경이며 프론트 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WatchlistSeeder(시드 데이터) → 공개 저장소 | 큐레이션 목록은 커밋되어 공개 — 공개 메타(id/name/category/icon/group)만, 실키·가격·계정 식별자 금지(SEED-04) |
| 큐레이션 데이터 → 실수집(dev) | seeder의 category(CategoryCode)/external_item_id가 틀리면 실 API 매칭 실패 → collection_run 실패. 실측값 전사로만 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 | Information Disclosure | WatchlistSeeder에 실키·가격이 유입 | mitigate | seeder는 공개 메타(id/name/category/icon/group)만 기재(SEED-04) — 키/가격 필드 자체가 스키마에 없음, `./gradlew build` + 리뷰로 확인 |
| T-172-02 | Tampering | 잘못된 external_item_id/CategoryCode 전사로 dev 수집이 신규 품목을 못 잡음 | mitigate | 신규 10개는 17.1-01 스파이크 실측값(휴먼 비준 완료)만 전사 — 추정 금지, verify가 개수·제거 id 부재 강제 |
| T-172-03 | Denial of Service | 큐레이션 규모 증가(15→22)로 수집/시드 부하 | accept | 22개는 분당 100회 레이트리밋·10분 케이던스 내 여유(수집 로직 무변경, D-06) |
| T-172-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 데이터 목록·라벨 문자열 변경만, 설치 태스크 없음 |
</threat_model>

<verification>
- `WatchlistSeeder.WATCHLIST`가 22개(딜러 11 + 서포터 7 + 재료 4) — `grep -c "new SeedItem("` == 22, 제거 3(65204305·6861009·6861011) 부재.
- 신규 10개 external_item_id·iconUrl이 17.1-SPIKE-FINDINGS.md 실측값과 일치(추정 0건, D-02/D-03).
- `roleGroup.ts` ROLE_LABEL.MATERIAL == '재료'; zod `roleGroupSchema`·sortByRole 무변경(D-04).
- `./gradlew build` + `cd frontend && npm run build` 그린.
- D-05 확인: seed 프로파일 부팅 시 `findByActiveTrue()`가 22개 → SyntheticDemoData가 22개 합성 시세 생성(코드 편집 0줄). (실행 시 seed 프로파일 부팅으로 활성 품목 22 확인.)
- D-06: PriceCollector·CollectionConfig·캐시·EventImpactService·SyntheticDemoData diff 0줄.
</verification>

<success_criteria>
- 큐레이션이 22개(각인 18 + 재료 4)로 재잠금되고 현행 티어4 메타를 반영한다(POLISH-02/03, D-01/D-03).
- 재련 재료가 MATERIAL '재료' 라벨로 정합 노출된다(D-04) — enum·3색·정렬 회귀 없음.
- seed 프로파일이 코드 편집 없이 22개 합성 시세를 재현한다(D-05).
- 수집/캐시/event-impact 핵심 경로 0줄 회귀(D-06, Core Value 가드).
</success_criteria>

<output>
Create `.planning/phases/17.1-demo-polish/17.1-02-SUMMARY.md` when done
</output>
