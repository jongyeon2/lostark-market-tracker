---
phase: 14-frontend-icons-fallback-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/schemas.ts
  - frontend/src/index.css
  - frontend/src/features/_shared/roleGroup.ts
  - frontend/src/features/_shared/RoleBadge.tsx
  - frontend/src/features/_shared/ItemIcon.tsx
autonomous: true
requirements: [ICON-01]

must_haves:
  truths:
    - "4개 read zod 스키마(trackedItemSchema·latestPriceSchema·timelineSchema·eventImpactSchema)가 iconUrl·itemGroup·roleGroup을 노출한다 — iconUrl/itemGroup은 z.string().nullable(), roleGroup은 z.enum(['DEALER','SUPPORT','MATERIAL']).nullable()로 모델링해 미상 값은 boundary에서 loud-fail한다(D-06 fail-loud 기조)"
    - "eventImpactSchema는 실제 백엔드 EnrichedEventImpactResponse 형태(itemId/window/iconUrl/itemGroup/roleGroup/events — enrichment가 최상위 평면 필드)에 정렬한다. CONTEXT/UI-SPEC의 중첩 enrichment 객체 서술은 부정확하며 백엔드 0줄 원칙상 실제 record가 진실이다"
    - "역할 3군 팔레트가 index.css에 신규 토큰(--role-dealer rose-600 #E11D48 / --role-support emerald-700 #047857 / --role-material amber-700 #B45309)으로 추가되고 @theme inline에 --color-role-* 로 노출돼 bg-role-* text-white 유틸로 소비된다 — accent(blue-600)가 아니라 semantic 색군으로 편입(D-01)"
    - "공용 RoleBadge가 solid 색배경 pill(bg-role-* text-white, rounded-full, px-2 py-0.5, text-sm font-semibold)로 한글 라벨(딜러/서포터/융화재료)을 렌더한다 — StatusBadge의 className 오버라이드 선례를 따라 ui/badge.tsx cva는 0줄 변경, roleGroup null이면 아무것도 렌더하지 않는다(D-03)"
    - "공용 <ItemIcon>이 고정 px 슬롯(md 32×32/sm 20×20)에 iconUrl을 렌더하고, iconUrl==null OR <img> onError(둘 다) 트리거 시 역할색 글리프 타일(DEALER/SUPPORT→ScrollText, MATERIAL→FlaskConical, null→Package on bg-muted)로 대체해 레이아웃 시프트가 0이다 — lucide-react만 사용, 외부 아이콘 라이브러리 0(D-02·D-06·ICON-01)"
    - "roleGroup 공유 모듈이 RoleGroup 타입·한글 ROLE_LABEL·역할 정렬 순서(DEALER→SUPPORT→MATERIAL→null)·sortByRole 헬퍼를 단일 출처로 제공해 14-02 셀렉터·대시보드 정렬/그룹핑이 재사용한다"
  artifacts:
    - path: "frontend/src/lib/schemas.ts"
      provides: "4개 read 스키마에 iconUrl/itemGroup/roleGroup 추가 — roleGroup은 z.enum 3값 nullable, eventImpact는 평면 형태"
      contains: "roleGroup"
    - path: "frontend/src/features/_shared/ItemIcon.tsx"
      provides: "고정 슬롯 + null/onError fallback 공용 아이콘 컴포넌트(ICON-01)"
      contains: "onError"
    - path: "frontend/src/features/_shared/RoleBadge.tsx"
      provides: "solid 색배경 역할 배지(딜러/서포터/융화재료) — bg-role-* text-white"
      contains: "role-"
    - path: "frontend/src/features/_shared/roleGroup.ts"
      provides: "RoleGroup 타입·ROLE_LABEL 한글 맵·sortByRole 정렬 헬퍼(D-07 재사용 출처)"
      contains: "sortByRole"
    - path: "frontend/src/index.css"
      provides: "--role-dealer/--role-support/--role-material 토큰 + @theme inline --color-role-* 노출"
      contains: "--role-dealer"
  key_links:
    - from: "ItemIcon (iconUrl==null || onError)"
      to: "역할색 글리프 타일(ScrollText/FlaskConical/Package)"
      via: "useState 에러 플래그 → img 언마운트 후 고정 슬롯 안에서 글리프 렌더(시프트 0)"
      pattern: "onError"
    - from: "RoleBadge / ItemIcon"
      to: "index.css --color-role-* 유틸(bg-role-dealer 등)"
      via: "Tailwind v4 @theme inline 토큰 → bg-role-*/text-white className"
      pattern: "role-"
---

<objective>
Phase 14의 **공유 기반 한 겹**을 만든다 — 4화면이 소비할 데이터 계약(zod)·시각 토큰·재사용 컴포넌트를 한 곳에 잠그고, 백엔드 `src/`는 0줄 건드리지 않는다.

- **zod 계약 확장:** `trackedItemSchema`·`latestPriceSchema`·`timelineSchema`·`eventImpactSchema` 4개에 `iconUrl`/`itemGroup`/`roleGroup`을 `.nullable()`로 추가(zod 단일 출처 D-05). `eventImpactSchema`는 실제 백엔드 `EnrichedEventImpactResponse`(평면 `itemId/window/iconUrl/itemGroup/roleGroup/events`)에 맞춘다 — **CONTEXT/UI-SPEC가 서술한 중첩 `enrichment` 객체 형태가 아니다**(코드가 진실).
- **역할색 토큰:** `index.css`에 역할 3군 팔레트(rose-600/emerald-700/amber-700)를 신규 토큰으로 추가하고 `@theme inline`에 `--color-role-*`로 노출(D-01). accent(blue-600)는 건드리지 않는다.
- **RoleBadge:** solid 색배경 pill + 한글 라벨 — StatusBadge의 className 오버라이드 선례를 따라 `ui/badge.tsx`는 0줄(D-03).
- **`<ItemIcon>`:** 고정 px 슬롯 + `iconUrl==null`/`onError` fallback(역할색 글리프 타일) — 레이아웃 시프트 0, `lucide-react`만 사용(D-02·D-06·ICON-01).
- **roleGroup 공유 모듈:** 타입·한글 라벨·정렬 순서·`sortByRole` — 14-02 정렬/그룹핑(D-07/D-08)의 단일 출처.

Purpose: 4개 화면(14-02)과 docs(14-03)가 이 기반만 import해 소비하도록, 데이터 계약과 시각 시스템을 검증 가능한 형태로 확정한다. seed만으로(키 없이) 아이콘이 뜨고 CDN 차단/아이콘 부재에도 무파손인 fallback 골격을 여기서 잠근다.

Output: enrichment를 노출하는 4개 zod 스키마 + 역할색 토큰 + `<ItemIcon>`/`RoleBadge`/`roleGroup` 공유 모듈. `npm run build`(tsc 포함) 그린, 백엔드 `src/` diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-icons-fallback-docs/14-CONTEXT.md
@.planning/phases/14-frontend-icons-fallback-docs/14-UI-SPEC.md
@.planning/phases/12-api-spike-data-lock/12-SPIKE-FINDINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- 실제 백엔드 DTO 형태(Phase 13, src 0줄 — 이게 진실): -->
<!-- TrackedItemResponse(record): id, externalItemId, displayName, category, active, iconUrl, itemGroup, roleGroup (3필드 최상위 추가). GET /api/items → array. -->
<!-- LatestPriceResponse(record): itemId, minPrice, collectedAt, iconUrl, itemGroup, roleGroup. -->
<!-- TimelineResponse(record): downsampled, bucketWidth, snapshots[], events[], iconUrl, itemGroup, roleGroup (최상위 메타데이터). -->
<!-- EnrichedEventImpactResponse(record): itemId, window, iconUrl, itemGroup, roleGroup, events[]. ★ 평면 — 중첩 enrichment 객체 아님. CONTEXT/UI-SPEC의 {itemId,window,enrichment,events} 서술은 부정확. -->
<!-- roleGroup 값 ∈ {DEALER, SUPPORT, MATERIAL} 또는 null(13-02 SUMMARY). itemGroup은 freeform 문자열(예: "각인서","강화재료"). iconUrl은 CDN base "https://cdn-lostark.game.onstove.com/efui_iconatlas/use/" 접두 또는 null. -->
<!-- 현재 schemas.ts: trackedItemSchema/latestPriceSchema/timelineSchema/eventImpactSchema 4개 모두 enrichment 필드 없음. eventImpactSchema는 현재 {itemId, window, events} — iconUrl/itemGroup/roleGroup 누락. No .strict()(unknown keys strip) 관습 유지. -->
<!-- ui/badge.tsx: shadcn cva(default/secondary/destructive/outline/ghost/link). 역할색 variant 없음 → RoleBadge는 <Badge className="bg-role-* text-white text-sm font-semibold">로 오버라이드(StatusBadge 선례 features/dashboard/StatusBadge.tsx). badge.tsx 0줄. -->
<!-- index.css: :root에 --up/--down/--warning/--neutral semantic 토큰 + @theme inline에 --color-up 등 노출 선례. 역할색 3종을 동일 패턴으로 추가. accent=--primary(blue-600) reserved — 건드리지 않음. -->
<!-- lucide-react 이미 의존(^1.21.0). ScrollText/FlaskConical/Package import 가능(StatusBadge가 CircleCheck 등, ImpactPage가 PackageSearch import하는 선례). -->
<!-- cn 유틸: @/lib/utils. Tailwind v4 CSS-first(tailwind.config 없음) — 유틸 클래스는 @theme inline 토큰에서 자동 생성. -->
<!-- npm run build = tsc -b && vite build (frontend/package.json). 타입체크 게이트. -->
</interfaces>
</context>

## Phase Goal

**As a** 4개 화면을 구현할 다운스트림(14-02)·면접관, **I want to** 아이콘·역할 배지·fallback의 데이터 계약과 시각 시스템이 단일 출처(zod + 토큰 + 공용 컴포넌트)로 확정되고 백엔드는 0줄 변경임을, **so that** 각 화면이 기반을 import만 하면 일관되게 소비할 수 있고, 아이콘이 없거나 CDN이 차단돼도 역할색 글리프로 흡수돼 UI가 안 깨진다.

<tasks>

<task type="auto">
  <name>Task 1: 4개 read zod 스키마 enrichment 확장 + roleGroup 공유 모듈</name>
  <read_first>
    - frontend/src/lib/schemas.ts (zod 단일 출처 — 4개 스키마에 필드 추가; eventImpactSchema는 평면 형태로 교정)
    - frontend/src/lib/api.ts (각 스키마 .parse 호출 지점 — 스키마 변경이 boundary 검증에 자동 반영됨 확인, 수정 불필요)
    - .planning/phases/13-backend-enrichment-seed/13-02-SUMMARY.md (실제 DTO 형태·roleGroup enum 값)
  </read_first>
  <files>frontend/src/lib/schemas.ts, frontend/src/features/_shared/roleGroup.ts</files>
  <action>
    `schemas.ts`에 역할군 enum을 추가한다: `export const roleGroupSchema = z.enum(['DEALER', 'SUPPORT', 'MATERIAL'])` + `export type RoleGroup = z.infer<typeof roleGroupSchema>`. 그런 다음 4개 스키마에 enrichment 3필드를 추가한다 — 전부 `iconUrl: z.string().nullable()`, `itemGroup: z.string().nullable()`, `roleGroup: roleGroupSchema.nullable()`:
    (1) `trackedItemSchema`(active 뒤), (2) `latestPriceSchema`(collectedAt 뒤), (3) `timelineSchema`(events 뒤 — 최상위 메타데이터).
    (4) `eventImpactSchema`는 **평면 형태**로 교정한다: 기존 `{ itemId, window, events }`에 `iconUrl`/`itemGroup`/`roleGroup`을 **최상위**에 추가해 `{ itemId, window, iconUrl, itemGroup, roleGroup, events }`로 만든다(실제 `EnrichedEventImpactResponse`에 정렬 — 중첩 `enrichment` 객체로 만들지 말 것). 기존 주석 블록(D-05/D-06 단일 출처·nullable·no .strict)은 유지하고, roleGroup enum이 미상 값에서 loud-fail함을 한 줄 보강.
    신규 파일 `frontend/src/features/_shared/roleGroup.ts`를 만든다: `RoleGroup` 타입을 `@/lib/schemas`에서 재노출하거나 import; `export const ROLE_LABEL: Record<RoleGroup, string> = { DEALER: '딜러', SUPPORT: '서포터', MATERIAL: '융화재료' }`; 정렬 순서 상수(예: `const ROLE_ORDER: Record<RoleGroup, number> = { DEALER: 0, SUPPORT: 1, MATERIAL: 2 }`, null→3); 제네릭 헬퍼 `export function sortByRole<T extends { roleGroup: RoleGroup | null; displayName: string }>(items: readonly T[]): T[]` — `[ROLE_ORDER 또는 3, displayName(ko-KR localeCompare)]` 기준으로 **새 배열**을 반환(원본 불변, D-07 출처). null roleGroup은 말미.
  </action>
  <verify>
    <automated>cd frontend && grep -q "roleGroupSchema" src/lib/schemas.ts && grep -cE "iconUrl: z.string\(\).nullable\(\)" src/lib/schemas.ts | grep -qx 4 && grep -q "iconUrl" src/lib/schemas.ts && grep -A6 "eventImpactSchema = z.object" src/lib/schemas.ts | grep -q "roleGroup" && grep -q "sortByRole" src/features/_shared/roleGroup.ts && grep -q "융화재료" src/features/_shared/roleGroup.ts && npx tsc -b --noEmit 2>/dev/null || npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.ts`가 `roleGroupSchema = z.enum(['DEALER','SUPPORT','MATERIAL'])`를 export하고 `iconUrl: z.string().nullable()`이 정확히 4회 등장(4개 스키마)
    - `trackedItemSchema`·`latestPriceSchema`·`timelineSchema`·`eventImpactSchema` 모두 iconUrl/itemGroup/roleGroup(roleGroup=enum nullable)을 가짐
    - `eventImpactSchema`가 평면 형태 `{ itemId, window, iconUrl, itemGroup, roleGroup, events }` — 중첩 `enrichment` 객체 없음
    - `roleGroup.ts`가 `ROLE_LABEL`(딜러/서포터/융화재료)·`sortByRole`(원본 불변 새 배열, null 말미)를 export
    - 타입체크(`tsc -b`) 그린 — api.ts의 .parse는 스키마 변경에 자동 정합
  </acceptance_criteria>
  <done>4개 read 응답의 enrichment 계약이 zod 단일 출처에 잠기고(eventImpact 평면 교정 포함), 역할 라벨·정렬이 공유 모듈로 제공된다.</done>
</task>

<task type="auto">
  <name>Task 2: 역할색 토큰(index.css) + RoleBadge 컴포넌트</name>
  <read_first>
    - frontend/src/index.css (:root semantic 토큰 + @theme inline --color-* 노출 선례 — 동일 패턴으로 역할색 추가)
    - frontend/src/components/ui/badge.tsx (Badge cva — 0줄 변경 대상, className 오버라이드로 소비)
    - frontend/src/features/dashboard/StatusBadge.tsx (className 오버라이드 + text-sm font-semibold 선례)
    - frontend/src/features/_shared/roleGroup.ts (ROLE_LABEL 소비)
  </read_first>
  <files>frontend/src/index.css, frontend/src/features/_shared/RoleBadge.tsx</files>
  <action>
    `index.css` `:root`에 역할색 토큰 3종을 semantic 색군(--up/--down 등) 옆에 추가한다: `--role-dealer: #e11d48;`(rose-600, 딜러=공격), `--role-support: #047857;`(emerald-700, 서포터=회복·버프), `--role-material: #b45309;`(amber-700, 융화재료=재화). `@theme inline`의 semantic 유틸 블록(`--color-up` 등) 옆에 `--color-role-dealer: var(--role-dealer);`·`--color-role-support: var(--role-support);`·`--color-role-material: var(--role-material);`를 추가해 `bg-role-dealer`/`text-role-*` 유틸이 생성되게 한다. **accent(--primary/--ring blue-600)는 건드리지 않는다.** 주석으로 "역할색 = semantic 색군(accent 아님), 흰 텍스트 대비 ≥4.7:1 AA"를 한 줄.
    신규 파일 `frontend/src/features/_shared/RoleBadge.tsx`: props `{ roleGroup: RoleGroup | null }`. roleGroup이 null이면 `return null`. 아니면 역할→배경 className 맵(`{ DEALER: 'bg-role-dealer', SUPPORT: 'bg-role-support', MATERIAL: 'bg-role-material' }`)을 골라 `<Badge className={cn(bgClass, 'text-white text-sm font-semibold')}>{ROLE_LABEL[roleGroup]}</Badge>`를 렌더한다(Badge 기본 rounded-full·px-2·py-0.5 사용; StatusBadge처럼 className으로 색·타이포 오버라이드). 색 단독 금지 — 항상 한글 라벨 동반. `@/components/ui/badge`의 `Badge`, `@/lib/utils`의 `cn`, `@/features/_shared/roleGroup`의 `ROLE_LABEL`·`RoleGroup`을 import.
  </action>
  <verify>
    <automated>cd frontend && grep -q -- "--role-dealer: #e11d48" src/index.css && grep -q -- "--color-role-dealer" src/index.css && test -z "$(git diff --name-only -- src/components/ui/badge.tsx)" && grep -q "bg-role-dealer" src/features/_shared/RoleBadge.tsx && grep -q "ROLE_LABEL" src/features/_shared/RoleBadge.tsx && grep -q "text-white" src/features/_shared/RoleBadge.tsx && npx tsc -b 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `index.css` `:root`에 `--role-dealer:#e11d48`·`--role-support:#047857`·`--role-material:#b45309`, `@theme inline`에 `--color-role-dealer/support/material` 존재
    - `--primary`/`--ring`(blue-600 accent) 줄 변경 0
    - `git diff`상 `components/ui/badge.tsx` 변경 0줄(RoleBadge는 className 오버라이드만)
    - `RoleBadge.tsx`가 roleGroup→bg-role-* 매핑 + 흰 텍스트 + 한글 ROLE_LABEL 렌더, roleGroup null이면 null 반환
    - 타입체크 그린
  </acceptance_criteria>
  <done>역할 3색이 토큰으로 잠기고(accent 불변), solid 역할 배지가 ui/badge.tsx 0줄 변경으로 재사용 가능하다.</done>
</task>

<task type="auto">
  <name>Task 3: 공용 &lt;ItemIcon&gt; — 고정 슬롯 + null/onError 역할색 글리프 fallback</name>
  <read_first>
    - frontend/src/features/_shared/roleGroup.ts (RoleGroup 타입)
    - frontend/src/index.css (bg-role-*/bg-muted 유틸 — fallback 타일 배경)
    - frontend/src/components/ui/skeleton.tsx (shrink-0/rounded 유틸 사용 관습 참고)
    - frontend/src/features/dashboard/StatusBadge.tsx (lucide icon import + aria-hidden 관습)
  </read_first>
  <files>frontend/src/features/_shared/ItemIcon.tsx</files>
  <action>
    신규 파일 `frontend/src/features/_shared/ItemIcon.tsx`. props `{ iconUrl: string | null; roleGroup: RoleGroup | null; size?: 'md' | 'sm' }`(default `size='md'`).
    **고정 슬롯(시프트 0, ICON-01):** size별 치수 맵 — `md`: 슬롯 `size-8`(32px), img `width={32} height={32}`, 글리프 `size-5`(20px); `sm`: 슬롯 `size-5`(20px), img `width={20} height={20}`, 글리프 `size-3`(12px). 슬롯 컨테이너는 항상 `shrink-0 rounded-md overflow-hidden inline-flex items-center justify-center` + 고정 size 클래스(성공/실패/로딩 무관 동일 px 점유).
    **fallback 트리거(둘 다, D-06):** `useState(false)` 에러 플래그. `const showFallback = iconUrl == null || errored`. `showFallback`이면 img를 렌더하지 않고 역할색 글리프 타일을 렌더(에러 후 재시도 루프 방지 — onError는 사실상 1회). 아니면 `<img src={iconUrl} width={...} height={...} loading="lazy" decoding="async" alt="" className="size-full object-cover" onError={() => setErrored(true)} />`.
    **글리프·배경 맵(D-02):** `DEALER`/`SUPPORT`(각인서) → `ScrollText` + `bg-role-dealer`/`bg-role-support`; `MATERIAL` → `FlaskConical` + `bg-role-material`; `null`/미상 → `Package` + `bg-muted`. 글리프 색은 역할색 타일이면 `text-white`, neutral이면 `text-muted-foreground`. lucide 컴포넌트는 글리프 size 클래스 + `aria-hidden="true"`.
    **alt 규칙:** `alt=""`(decorative — 항상 품목명 텍스트 인접, SR 중복 방지). lucide(`ScrollText`,`FlaskConical`,`Package`)·`cn`·`RoleGroup`을 import. recharts/외부 아이콘 라이브러리 추가 금지.
  </action>
  <verify>
    <automated>cd frontend && test -f src/features/_shared/ItemIcon.tsx && grep -q "onError" src/features/_shared/ItemIcon.tsx && grep -q "ScrollText" src/features/_shared/ItemIcon.tsx && grep -q "FlaskConical" src/features/_shared/ItemIcon.tsx && grep -q 'loading="lazy"' src/features/_shared/ItemIcon.tsx && grep -q 'alt=""' src/features/_shared/ItemIcon.tsx && grep -q "size-8" src/features/_shared/ItemIcon.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ItemIcon.tsx`가 size 'md'(32px 슬롯)/'sm'(20px 슬롯) 고정 슬롯을 `shrink-0 rounded-md overflow-hidden`으로 렌더
    - `iconUrl == null` 또는 `<img onError>` 둘 다에서 fallback 글리프 타일로 전환(useState 에러 플래그, 재시도 루프 없음)
    - fallback 글리프 맵: DEALER/SUPPORT→ScrollText, MATERIAL→FlaskConical, null→Package(bg-muted); 역할 타일은 bg-role-* + text-white
    - `<img>`가 width/height 명시 + `loading="lazy"` + `decoding="async"` + `alt=""`
    - 외부 아이콘 라이브러리 import 0(lucide-react만)
    - `npm run build`(tsc -b && vite build) 그린
  </acceptance_criteria>
  <done>고정 슬롯·null/onError fallback·역할색 글리프 타일을 갖춘 공용 &lt;ItemIcon&gt;이 완성되고, 전체 프론트 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 DTO(iconUrl 문자열) → 브라우저 `<img src>` | 백엔드가 시드 상수로 채운 CDN URL을 그대로 렌더 — 외부 사용자 입력 아님 |
| zod .parse boundary | enrichment 필드가 스키마에 추가됨 — 미상 roleGroup은 .parse에서 throw(loud-fail) |
| 프론트 → 외부 API | 프론트는 Lostark Open API 직접 호출 금지(상시 가드) — 이 plan은 schema/컴포넌트만 추가, fetch 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | iconUrl이 javascript:/data: 등 비-CDN URL이면 `<img src>` 오남용 | accept | iconUrl은 우리 백엔드가 시드 상수(CDN base 접두)로 채운 값이며 사용자 입력 아님. `<img src>`는 스크립트 실행 불가(href 아님). 깨진/비정상 URL은 onError→fallback으로 흡수(D-06). |
| T-14-02 | Information Disclosure | enrichment 노출로 민감정보 유출 | accept | 노출 값은 공개 아이콘 URL·품목 분류·역할군뿐(가격 원문·키·계정 식별자 아님 — 상시 가드 준수). |
| T-14-SC | Tampering | 의존성 공급망(신규 npm) | accept | 신규 패키지 0 — lucide-react/zod/cva 기존 의존만 사용(D-02·Registry Safety). |
| T-14-BE | Tampering | 프론트 변경이 백엔드 핵심 경로 오염 | mitigate | 이 plan은 frontend/ 5파일만 수정 — 백엔드 `src/` diff 0줄(아래 verification git status 게이트). |
</threat_model>

<verification>
- `cd frontend && npm run build`(tsc -b && vite build) 그린.
- `git status`/`git diff --name-only`에 백엔드 `src/` 변경 0건 — 변경은 `frontend/` 5파일(schemas.ts, index.css, roleGroup.ts, RoleBadge.tsx, ItemIcon.tsx)에 한정.
- `schemas.ts` 4개 스키마가 iconUrl/itemGroup/roleGroup 노출(eventImpact 평면), roleGroup=enum nullable.
- 신규 npm 의존 0(`git diff frontend/package.json` 변경 없음).
- (다운스트림 확인) 14-02가 이 plan의 `<ItemIcon>`·`RoleBadge`·`sortByRole`만 import해 4화면을 enrich할 수 있는 형태.
</verification>

<success_criteria>
- 4개 read zod 스키마가 enrichment(iconUrl/itemGroup/roleGroup)를 노출하고 eventImpact는 실제 백엔드 평면 형태에 정렬 (ICON-01 기반, D-05/D-06).
- 역할 3색 토큰이 index.css에 추가되고 accent(blue-600)는 불변 (D-01).
- 공용 `<ItemIcon>`이 고정 슬롯 + null/onError 역할색 글리프 fallback으로 레이아웃 시프트 0·CDN 차단 무파손 (ICON-01, D-02, D-06).
- RoleBadge가 ui/badge.tsx 0줄 변경으로 solid 역할 배지를 제공 (D-03).
- `npm run build` 그린, 백엔드 `src/` 0줄.
</success_criteria>

<output>
Create `.planning/phases/14-frontend-icons-fallback-docs/14-01-SUMMARY.md` when done
</output>
