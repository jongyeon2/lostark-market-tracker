---
phase: 17.1-demo-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/impact/CorrelationBanner.tsx
autonomous: true
requirements: [POLISH-05]

must_haves:
  truths:
    - "D-13: CorrelationBanner 문구를 번역투 없이 개선한다 — 제목 '해석에 주의하세요', 본문 '이벤트와 가격 변동이 비슷한 시점에 나타났다는 의미일 뿐, 이벤트가 가격을 움직였다는 근거는 아닙니다.'"
    - "신뢰성 가드 보존 — 중립 Info 아이콘(경고 삼각형 아님)·상시 노출·닫기 버튼 없음(과대해석 방지)·default(slate) variant 유지, 배치는 ImpactPage(무변경)"
  artifacts:
    - path: "frontend/src/features/impact/CorrelationBanner.tsx"
      provides: "개선된 상관≠인과 주의 문구(D-13) + 가드 4종(중립 Info·상시·닫기 없음·slate) 보존"
  key_links:
    - from: "CorrelationBanner (문구)"
      to: "ImpactPage <CorrelationBanner />"
      via: "event-impact 페이지 상단 상시 노출 배치 보존(무변경)"
      pattern: "CorrelationBanner"
---

<objective>
event-impact의 상관≠인과 주의 문구를 **번역투 없이 이해하기 쉽게** 개선한다(POLISH-05). 이 배너는 프로젝트 신뢰성의 얼굴 — 카피만 바꾸고 신뢰성 가드는 그대로 보존한다.

- **D-13 — 문구 개선:** 제목을 현재 '상관 ≠ 인과'에서 **'해석에 주의하세요'**로, 본문을 현재 '이 수치는 이벤트와 가격의 시점 상관일 뿐, 인과(이벤트가 가격을 올렸다)를 의미하지 않습니다.'에서 **'이벤트와 가격 변동이 비슷한 시점에 나타났다는 의미일 뿐, 이벤트가 가격을 움직였다는 근거는 아닙니다.'**로 개선.
- **가드 보존:** 중립 `Info` 아이콘(경고 삼각형 아님)·상시 노출·닫기 버튼 없음·`default`(slate) variant 유지. 배치(ImpactPage L135)는 무변경.

Purpose: 상관≠인과 경고를 리뷰어/사용자가 오해 없이 이해하도록 다듬어 event-impact의 과대해석 방지(신뢰성 서사)를 강화한다.

Output: 개선된 문구의 `CorrelationBanner.tsx`. 가드 4종·배치 무변경, 다른 파일 diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-demo-polish/17.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- CorrelationBanner.tsx: export function CorrelationBanner({className}) { return <Alert className={className}><Info /><AlertTitle>상관 ≠ 인과</AlertTitle><AlertDescription>이 수치는 이벤트와 가격의 시점 상관일 뿐, 인과(이벤트가 가격을 올렸다)를 의미하지 않습니다.</AlertDescription></Alert> }. lucide Info 아이콘, Alert default variant(neutral slate), 닫기 버튼 없음, state/dismiss 없음. ★ AlertTitle/AlertDescription 텍스트만 D-13으로 교체. -->
<!-- 보존(신뢰성 가드): <Info/>(경고 삼각형 금지), variant default(red 아님), 닫기 버튼/dismiss 콜백 미도입, 상시 렌더(조건부 숨김 금지). -->
<!-- ImpactPage.tsx L135: <CorrelationBanner /> (content-top, full width) — 배치 무변경. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CorrelationBanner 문구 개선(D-13)</name>
  <read_first>
    - frontend/src/features/impact/CorrelationBanner.tsx (AlertTitle/AlertDescription·Info·variant — 문구 교체 대상, 가드 보존)
    - .planning/phases/17.1-demo-polish/17.1-CONTEXT.md (D-13 확정 문구·가드 4종)
  </read_first>
  <files>frontend/src/features/impact/CorrelationBanner.tsx</files>
  <action>
    `<AlertTitle>` 텍스트를 '상관 ≠ 인과' → **'해석에 주의하세요'**로, `<AlertDescription>` 텍스트를 → **'이벤트와 가격 변동이 비슷한 시점에 나타났다는 의미일 뿐, 이벤트가 가격을 움직였다는 근거는 아닙니다.'**로 교체한다(D-13). **그 외 일절 무변경:** `import { Info } from 'lucide-react'`·`<Info />`(경고 삼각형 도입 금지)·`<Alert className={className}>` default variant(red variant 금지)·닫기 버튼/dismiss 미도입·상시 렌더(조건부 숨김 금지)·`className` prop 시그니처. 컴포넌트 상단 주석의 문구 인용이 있으면 새 문구로 정합만 맞춘다(선택, 취지 유지).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "해석에 주의하세요" src/features/impact/CorrelationBanner.tsx && grep -q "근거는 아닙니다" src/features/impact/CorrelationBanner.tsx && grep -q "import { Info }" src/features/impact/CorrelationBanner.tsx && ! grep -qiE "AlertTriangle|variant=\"destructive\"|onDismiss|onClose" src/features/impact/CorrelationBanner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AlertTitle이 '해석에 주의하세요', AlertDescription이 '이벤트와 가격 변동이 비슷한 시점에 나타났다는 의미일 뿐, 이벤트가 가격을 움직였다는 근거는 아닙니다.'(D-13)
    - Info 아이콘 유지(AlertTriangle 등 경고 아이콘 미도입), default(slate) variant 유지(destructive 미도입)
    - 닫기 버튼/dismiss 콜백·조건부 숨김 미도입(상시 노출)
    - `cd frontend && npm run build` 그린, CorrelationBanner.tsx 외 diff 0줄
  </acceptance_criteria>
  <done>배너 문구가 번역투 없이 개선되고, 중립 Info·slate·상시·닫기 없음 가드가 모두 보존되며 빌드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 정적 카피 → 사용자 렌더 | 상관≠인과 배너는 하드코딩 정적 문구 — 외부 입력·상태 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | 문구 개선 중 신뢰성 가드(중립 Info·slate·닫기 없음·상시)가 훼손 | mitigate | verify가 Info import 존재 + AlertTriangle/destructive/onDismiss/onClose 부재를 grep로 강제 — 과대해석 방지 가드 보존 |
| T-174-02 | Information Disclosure | 정적 카피에 민감정보 유입 | accept | 문구는 도메인 설명 텍스트뿐, 키/가격/식별자 없음 |
| T-174-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — 기존 lucide Info·Alert 재사용, 설치 태스크 없음 |
</threat_model>

<verification>
- CorrelationBanner 제목 '해석에 주의하세요' + 본문 '…근거는 아닙니다.' (D-13).
- 가드 보존: `Info` import 존재, `AlertTriangle`/`variant="destructive"`/`onDismiss`/`onClose` 부재, 상시 렌더.
- 배치(ImpactPage <CorrelationBanner/>) 무변경.
- `cd frontend && npm run build` 그린; `git diff --name-only`가 CorrelationBanner.tsx 1개.
</verification>

<success_criteria>
- 상관≠인과 주의 문구가 번역투 없이 이해하기 쉬운 표현으로 개선된다(POLISH-05, D-13).
- 신뢰성 가드 4종(중립 Info·상시 노출·닫기 없음·slate variant)이 보존된다.
- CorrelationBanner.tsx 외 회귀 0줄.
</success_criteria>

<output>
Create `.planning/phases/17.1-demo-polish/17.1-04-SUMMARY.md` when done
</output>
