---
phase: 17-real-data-transition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - frontend/README.md
autonomous: true
requirements: [REALDATA-02]

must_haves:
  truths:
    - "D-09: README.md '한눈에 3단계 재현'은 seed를 로컬 재현 경로로 유지(API 키 불필요·리뷰어 진입장벽 0)하되, seed 라벨이 '로컬/테스트 전용 · 합성 데이터'로 명확해지고 실데이터(dev)가 '라이브 데모의 실체'로 헤드라인 재프레임된다 — 공개 라이브 URL 추가는 Phase 18이므로 넣지 않는다"
    - "D-10: README.md와 frontend/README.md를 함께 재편해 seed↔실데이터 역할 구분(seed=로컬/테스트 합성 재현, dev=실수집 라이브 실체)을 정직히 서술하고, 두 문서가 서로 모순 없는 단일 진실 원천을 유지한다"
    - "실키·관리자 시크릿 값이 두 문서 어디에도 기재되지 않는다(상시 가드/DEPLOY-04) — env 변수 이름(LOSTARK_API_KEY·ADMIN_API_SECRET)만 참조하고 실제 토큰/시크릿 값은 미기재"
    - "포트폴리오 정직성 이중 트랙 서사가 보존된다 — seed는 '합성'임을 숨기지 않고, 실데이터(dev)를 라이브의 실체로 구분해 면접 설명 포인트로 성립(REALDATA-02)"
    - "변경은 README.md·frontend/README.md 2개 문서에 한정 — 코드(frontend/src, 백엔드 src/) diff 0줄, 배포/Dockerfile/prod 프로파일 등 Phase 18 항목 미추가"
  artifacts:
    - path: "README.md"
      provides: "seed 라벨 '로컬/테스트 전용 · 합성' 명확화 + 실데이터(dev) 라이브 실체 헤드라인 재프레임(D-09)"
    - path: "frontend/README.md"
      provides: "seed↔실데이터 역할 구분 정직 서술, 루트 README와 정합(D-10)"
  key_links:
    - from: "README.md (seed↔dev 서사)"
      to: "frontend/README.md (seed↔dev 서사)"
      via: "단일 진실 원천 — 두 문서가 같은 seed=합성/dev=실수집 구분을 모순 없이 서술(D-10)"
      pattern: "실데이터"
---

<objective>
데모 서사를 seed 합성 기준에서 **실수집(dev) 데이터 기준**으로 재편해, seed와 실데이터의 역할을 정직히 구분한다(REALDATA-02). seed는 리뷰어 진입장벽 0을 위해 존치하되 '합성'임을 숨기지 않고, 실데이터(dev)를 '라이브 데모의 실체'로 헤드라인화한다. 코드 변경 0줄 — README 2개 문서만 재편.

- **D-09 — 루트 README 재프레임:** `README.md` "한눈에 3단계 재현"은 seed를 로컬 재현 경로로 **유지**(API 키 불필요)하되, seed 라벨을 **'로컬/테스트 전용 · 합성 데이터'**로 명확히 하고, 실데이터(dev)를 **'라이브 데모의 실체'**로 헤드라인 재프레임한다. 공개 라이브 URL 추가는 **Phase 18**이므로 넣지 않는다.
- **D-10 — 두 문서 함께 재편:** `README.md`와 `frontend/README.md`를 함께 손봐 seed↔실데이터 역할 구분을 정직히 서술하고 **단일 진실 원천**(서로 모순 없음)을 유지한다.
- **정직성 이중 트랙 서사(specifics):** "합성(seed)으로 언제든 재현 + 실수집(dev)이 라이브의 실체"를 정직히 구분 — 면접 설명 포인트.

Purpose: 면접관/리뷰어가 seed(합성, 즉시 재현)와 실데이터(dev, 라이브 실체)의 차이를 문서에서 정직하게 이해하도록 만들어, 포트폴리오의 신뢰성(honest engineering)을 문서 레벨에서 확립한다.

Output: seed 라벨이 '로컬/테스트 전용 · 합성'으로 명확해지고 실데이터(dev)가 헤드라인화된 `README.md`·`frontend/README.md`. 코드 diff 0줄, 실키/시크릿 값 미기재, Phase 18 항목(배포/URL) 미추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-real-data-transition/17-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@CLAUDE.md

<interfaces>
<!-- README.md §"실행 방법"(L75~116): "한눈에 — 3단계 재현 (API 키 불필요)"는 seed 프로파일 재현. "두 가지 실행 프로파일"에 (a) dev=실시간 수집(.env LOSTARK_API_KEY), (b) seed=합성 8일치(키 불필요). ★ D-09 재편 대상 — seed 라벨 명확화 + dev 헤드라인화. 공개 URL 미추가(Phase 18). -->
<!-- frontend/README.md: L3~5 헤더가 "seed 백엔드 + npm run dev만으로 재현" + "데이터는 seed 합성 데이터입니다(실데이터 아님)". L7~15 "사전조건 — seed 백엔드 기동". L74~79 "정직성 노트"에 "데이터: seed는 합성, 실데이터 아님". ★ D-10 재편 대상 — seed=로컬/테스트 합성, dev=실수집 라이브 실체 구분 서술, 루트 README와 정합. -->
<!-- 실행 명령(사실 정합 유지): dev = `./gradlew bootRun --args='--spring.profiles.active=dev'` (.env의 LOSTARK_API_KEY 로드), seed = `--spring.profiles.active=seed` (키 불필요). bootRun이 .env 자동 로드(build.gradle). -->
<!-- 불변 제약(상시 가드): 실키/ADMIN_API_SECRET 값을 문서에 기재 금지 — env 변수 '이름'만 언급. 프론트는 Lostark API 직접 호출 금지(서사에서도 이 사실 유지). -->
<!-- Phase 18(Out of scope): 공개 배포·Dockerfile·라이브 데모 URL·prod/live 프로파일·정적 서빙 — 이 plan에서 추가 금지(경계 유지). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 루트 README seed 라벨 명확화 + 실데이터(dev) 헤드라인 재프레임(D-09)</name>
  <read_first>
    - README.md (§"실행 방법" L75~116 "한눈에 3단계"·"두 가지 실행 프로파일" — 재편 대상)
    - .planning/phases/17-real-data-transition/17-CONTEXT.md (D-09 및 specifics 이중 트랙 서사)
    - .planning/PROJECT.md (v1.3 핵심 결정: 데모=실데이터만·seed 로컬/테스트 전용)
  </read_first>
  <files>README.md</files>
  <action>
    `README.md` §"실행 방법"을 재편한다. (1) "한눈에 3단계 재현"의 seed 경로는 **유지**(docker compose up → seed bootRun → curl, API 키 불필요 — 리뷰어 진입장벽 0)하되, seed를 **'로컬/테스트 전용 · 합성 데이터'**로 명확히 라벨링한다. (2) 실데이터(dev)를 **'라이브 데모의 실체'**로 헤드라인 재프레임한다 — 예: "두 가지 실행 프로파일" 섹션에서 dev(실수집)를 라이브의 실체로 먼저/강조해 서술하고 seed는 합성 로컬 재현 트랙으로 명시. 실행 명령 사실은 정합 유지(dev=`--spring.profiles.active=dev` + .env `LOSTARK_API_KEY`, seed=`--spring.profiles.active=seed` 키 불필요). (3) `LOSTARK_API_KEY`·`ADMIN_API_SECRET`은 env 변수 **이름만** 언급하고 실제 값/토큰은 절대 기재하지 않는다. (4) 공개 라이브 URL·배포·Dockerfile·prod 프로파일 등 **Phase 18 항목은 추가하지 않는다**(경계 유지). 문안·섹션 배치는 재량(D-09 discretion).
  </action>
  <verify>
    <automated>grep -q "실데이터" README.md && grep -qE "로컬/테스트|합성" README.md && grep -q "LOSTARK_API_KEY" README.md && grep -q "spring.profiles.active=dev" README.md && grep -q "spring.profiles.active=seed" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md가 seed를 '로컬/테스트 전용 · 합성 데이터'로 명확히 라벨링한다(D-09)
    - 실데이터(dev)가 '라이브 데모의 실체'로 헤드라인 재프레임된다(dev 프로파일 서술이 라이브 실체로 강조됨)
    - seed 3단계 재현 경로(키 불필요)가 보존된다(리뷰어 진입장벽 0)
    - `LOSTARK_API_KEY`·`ADMIN_API_SECRET`은 이름만 언급, 실제 값/토큰 미기재; dev/seed bootRun 명령이 사실과 정합
    - 공개 라이브 URL·배포·Dockerfile·prod 프로파일 등 Phase 18 항목이 새로 추가되지 않는다
  </acceptance_criteria>
  <done>루트 README에서 seed는 '합성 로컬/테스트 재현'으로, 실데이터(dev)는 '라이브의 실체'로 정직히 구분되며, 진입장벽 0 seed 경로와 명령 사실 정합은 유지되고 Phase 18 항목은 넘어오지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: frontend/README seed↔실데이터 역할 구분 서술 + 루트 정합(D-10)</name>
  <read_first>
    - frontend/README.md (헤더 L3~5·"사전조건 — seed 백엔드 기동" L7~15·"정직성 노트" L74~79 — 재편 대상)
    - README.md (Task 1 재편 결과 — 두 문서 정합 유지)
    - .planning/phases/17-real-data-transition/17-CONTEXT.md (D-10 및 이중 트랙 서사)
  </read_first>
  <files>frontend/README.md</files>
  <action>
    `frontend/README.md`를 루트 README(Task 1)와 **정합**하게 재편한다. 현재 헤더/정직성 노트가 seed를 사실상 유일한 데모로 프레이밍("데이터는 seed 합성 데이터입니다(실데이터 아님)")하는 것을, seed↔실데이터 **역할 구분**으로 바꾼다: seed = 로컬/테스트 전용 합성 재현(키 불필요, `npm run dev`로 즉시 화면 확인), 실데이터(dev) = 실수집 라이브의 실체(dev 프로파일 백엔드 + 실키). 프론트 자체는 백엔드 DTO만 소비하므로 **어느 백엔드 프로파일이든 동일한 read API를 본다**는 사실(Vite 프록시·백엔드 0줄)은 유지하고, 데이터 출처가 seed(합성)냐 dev(실수집)냐를 정직히 구분해 서술한다. "정직성 노트"의 데이터 항목을 이 구분으로 업데이트한다. 실키/시크릿 값은 기재하지 않는다. Phase 18 항목(정적 서빙 v2 후보 언급은 기존 유지 가능하되 배포 URL/prod 신규 추가 금지). 문안은 재량(D-10 discretion).
  </action>
  <verify>
    <automated>grep -q "실데이터" frontend/README.md && grep -qE "로컬/테스트|합성" frontend/README.md && ! grep -qiE "eyJ[A-Za-z0-9_-]{10,}" frontend/README.md && ! grep -qiE "eyJ[A-Za-z0-9_-]{10,}" README.md</automated>
  </verify>
  <acceptance_criteria>
    - frontend/README.md가 seed(로컬/테스트 합성 재현)와 실데이터(dev, 라이브 실체)를 역할로 구분해 서술한다(D-10)
    - "정직성 노트" 데이터 항목이 seed↔실데이터 구분으로 업데이트된다(seed=합성임을 숨기지 않음)
    - 루트 README(Task 1)와 seed=합성/dev=실수집 서사가 모순 없이 정합한다(단일 진실 원천)
    - 프론트가 Lostark API 직접 호출 금지·백엔드 0줄(Vite 프록시) 사실은 유지된다
    - 두 문서 어디에도 JWT 형태 실키/시크릿 값이 기재되지 않는다(grep로 `eyJ...` 토큰 부재 확인)
  </acceptance_criteria>
  <done>frontend/README가 seed↔실데이터 역할을 정직히 구분하고 루트 README와 정합하며, 실키/시크릿 값은 어디에도 없고 프론트-백엔드 무변경 사실은 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서(README) → 공개 저장소 | README/frontend/README는 커밋되어 공개된다 — 실키·관리자 시크릿 값이 절대 들어가면 안 됨(DEPLOY-04/상시 가드) |
| 서사 ↔ 실행 사실 | 문서의 실행 명령·프로파일 설명이 실제 application-{dev,seed}.yml·bootRun 동작과 정합해야 함(오정보 방지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17D-01 | Information Disclosure | 재편 중 실 LOSTARK_API_KEY / ADMIN_API_SECRET 값이 문서에 유입 | mitigate | env 변수 '이름'만 참조하고 값/토큰은 미기재 — verify가 `eyJ...`(JWT) 토큰 패턴 부재를 grep로 강제. dev 서술은 ".env의 LOSTARK_API_KEY 로드"까지만 |
| T-17D-02 | Repudiation/Info | 오정보(잘못된 프로파일·명령) 서술로 리뷰어 오도 | mitigate | 실행 명령을 application-{dev,seed}.yml·기존 README 사실과 대조해 정합 유지(verify가 dev/seed bootRun 명령 존재 확인) |
| T-17D-SC | Tampering | 패키지/공급망 | accept | 문서 전용 변경 — 코드/의존성 0. 설치 태스크 없음 |
</threat_model>

<verification>
- `git diff --name-only`가 README.md·frontend/README.md 2개 문서만 — 코드(frontend/src·백엔드 src/) diff 0줄.
- 두 문서가 seed='로컬/테스트 전용 · 합성', 실데이터(dev)='라이브 실체'로 정직히 구분·정합(D-09/D-10).
- 실키/ADMIN_API_SECRET 값 미기재(`eyJ...` 토큰 부재), env 변수 이름만 참조.
- seed 3단계 재현 경로(키 불필요) 보존; dev/seed bootRun 명령 사실 정합.
- Phase 18 항목(공개 URL·배포·Dockerfile·prod 프로파일) 미추가.
</verification>

<success_criteria>
- 데모 서사가 seed↔실데이터 역할을 정직히 구분하고 실데이터(dev)를 라이브 실체로 재프레임한다(REALDATA-02, D-09/D-10).
- seed는 '합성 로컬/테스트 재현'으로 존치되 '합성'임을 숨기지 않는다(정직성 이중 트랙).
- 두 README가 단일 진실 원천으로 모순 없이 정합한다.
- 실키/시크릿 값 미기재, Phase 18 항목 미추가, 코드 diff 0줄.
</success_criteria>

<output>
Create `.planning/phases/17-real-data-transition/17-02-SUMMARY.md` when done
</output>
