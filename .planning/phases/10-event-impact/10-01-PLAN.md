---
phase: 10-event-impact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/table.tsx
  - frontend/src/features/impact/useImpactParams.ts
  - frontend/src/features/impact/impactFormat.ts
autonomous: true
requirements: [IMPCT-01, IMPCT-02, IMPCT-03]

must_haves:
  truths:
    - "`frontend/src/components/ui/table.tsx`가 shadcn **new-york** 스타일 `table` 블록을 **손으로 작성**해 `Table`·`TableHeader`·`TableBody`·`TableFooter`·`TableHead`·`TableRow`·`TableCell`·`TableCaption`을 export한다. 각 래퍼는 `data-slot` 속성(`data-slot=\"table\"` 등)과 `@/lib/utils`의 `cn`을 쓰며 기존 `card.tsx`/`badge.tsx` 컨벤션과 일치한다. Radix 의존 없는 순수 styled `<table>` 래퍼다. **`npx shadcn add table`은 사용하지 않는다** — 이 Windows 환경에서 블록을 literal `@/` 디렉터리에 잘못 배치하고 불필요한 radix 의존을 추가하는 알려진 버그가 있다(10-UI-SPEC Registry Safety)."
    - "`frontend/src/features/impact/useImpactParams.ts`가 `useTimelineParams`를 미러한 URL-as-state 훅 `useImpactParams()`를 export한다 — `react-router-dom`의 `useSearchParams`로 `?item=&window=`를 단일 상태 소스로 삼아 `{ itemId: number|null, window: number, setItem: (id:number)=>void, setWindow: (hours:number)=>void }`를 반환한다(D-01: window는 URL searchParams에 인코딩 — Phase 7 D-04 / Phase 9 D-04 정책의 연장, 딥링크·스크린샷 재현성). `setItem`/`setWindow`는 함수형 업데이터로 기존 params에 머지해 item·window가 독립적으로 공유 가능하다."
    - "기본값 동작이 D-02를 만족한다: URL에 `window`가 없으면 `useImpactParams`가 **24**를 반환하되 **eager write하지 않는다**(bare `/impact`는 깨끗하게 유지, 명시적 변경만 URL에 기록 — Phase 9 D-03 정책과 동일). 잘못된 `?window=`(숫자 아님)도 24로 폴백한다. `itemId`는 `?item=` 부재/비정상 시 `null`(timeline과 동일 규칙)."
    - "`frontend/src/features/impact/impactFormat.ts`의 `formatChangeRate(rate: number): string`가 백엔드 비율 델타를 퍼센트로 변환한다(D-06): 백엔드 `changeRate`는 `post/pre - 1`의 **비율**(4-dp, 예 `0.1230`)이므로 — `EventImpactService.java`/`EventImpactItem.java` 실측 — 반드시 **×100**해 부호+퍼센트+소수1자리로 포맷한다(`0.123`→`+12.3%`, `-0.04`→`-4.0%`, `0`→`0.0%`). 양수는 `+` 접두, 음수는 `-`(toFixed 포함), 0(반올림 후)은 부호 없는 `0.0%`이며 `-0.0%`는 발생하지 않는다."
    - "`impactFormat.ts`의 `changeRateColorClass(rate: number): string`가 changeRate 방향색을 **한국 관례**로 매핑한다(D-05): `rate > 0`→상승=빨강 `text-[#DC2626]`(red-600), `rate < 0`→하락=파랑 `text-[#1D4ED8]`(blue-700, accent blue-600 `#2563EB`과 의도적으로 구분), `rate === 0`→중립 `text-[#64748B]`(slate-500). **index.css의 `--up`(green-600)·`--down`(red-600) 토큰을 쓰지 않는다** — 그 토큰은 다른 의미축(가격 등락 일반)이고 본 화면 changeRate는 한국 거래소 관례(상승=빨강·하락=파랑)이므로 정반대다. 코드에 그 이유를 주석으로 남긴다."
    - "`impactFormat.ts`의 `formatPrice(n: number): string`가 `n.toLocaleString('ko-KR') + ' G'`로 천단위 구분 가격을 만든다(D-06, `LatestPriceCard` 관례 재사용). `STATUS_BADGE_META`(또는 동등 export)가 status를 라벨+색으로 매핑한다(D-07): `ok`→`{ label: '비교 가능', className: 'bg-up/10 text-up' }`(green-600), `insufficient_data`→`{ label: '데이터 부족', className: 'bg-warning/10 text-warning' }`(amber-600, Phase 7 `insufficient_data→warning` 의미색의 직접 연장)."
    - "`impactFormat.ts`의 `insufficientReason(item: EventImpactItem)`가 희소 vs stale를 **앵커 null 여부만으로** 도출한다(D-08, **30분 임계 하드코딩 금지** — 백엔드 결합 회피): `status !== 'insufficient_data'`면 `null`; `preAnchorAt == null || postAnchorAt == null`면 **희소**(`{ kind:'sparse', inlineLabel:'데이터 부족 · 희소', body:'이벤트 전후 윈도우에 수집된 시세가 없어 비교 기준을 잡지 못했어요.' }`); 양쪽 앵커가 모두 non-null인데 insufficient면 **stale**(`{ kind:'stale', inlineLabel:'데이터 부족 · 오래됨', body:'기준 시각이 이벤트에서 너무 떨어져 비교를 신뢰할 수 없어요.' }`). 이 규칙은 `EventImpactService.toImpactItem` 실측과 정합한다(한쪽 앵커 null=윈도우 내 스냅샷 0개=희소; 양쪽 앵커 있는데 insufficient=staleness 판정=stale)."
    - "`cd frontend && npm run build`(tsc -b + vite build)가 타입 에러 0으로 통과한다. Java `src/` 트리는 한 줄도 변경하지 않는다(read-only 프론트엔드 phase). 본 plan은 표시·인터랙션 컴포넌트를 만들지 않고 **재사용 빌딩블록**(table 블록·URL 훅·순수 포맷 함수)만 만든다."
  artifacts:
    - path: "frontend/src/components/ui/table.tsx"
      provides: "손으로 작성한 shadcn new-york table 블록(Table/TableHeader/TableBody/TableHead/TableRow/TableCell 등) — 결과 표(D-04)의 빌딩블록, Windows shadcn add 버그 회피"
      contains: "TableHeader"
    - path: "frontend/src/features/impact/useImpactParams.ts"
      provides: "useImpactParams() — ?item=&window= URL 상태 훅(useTimelineParams 미러, D-01) + 기본 window=24 비-eager(D-02)"
      contains: "useImpactParams"
    - path: "frontend/src/features/impact/impactFormat.ts"
      provides: "순수 도메인 포맷: formatChangeRate(비율×100 %, D-06)·changeRateColorClass(상승빨강/하락파랑, D-05)·formatPrice·STATUS_BADGE_META(D-07)·insufficientReason(희소/stale, D-08)"
      contains: "formatChangeRate"
  key_links:
    - from: "frontend/src/features/impact/impactFormat.ts"
      to: "frontend/src/lib/schemas.ts"
      via: "insufficientReason/STATUS_BADGE_META가 EventImpactItem·EventImpactStatus(z.infer 파생 타입)를 인자/키로 받아 스키마와 lockstep 유지"
      pattern: "EventImpactItem"
    - from: "frontend/src/features/impact/useImpactParams.ts"
      to: "frontend/src/features/timeline/useTimelineParams.ts"
      via: "동일한 useSearchParams 함수형 업데이터 패턴을 미러(복사 아닌 미러) — item은 공유, window 정수 1개 추가"
      pattern: "useSearchParams"
    - from: "frontend/src/features/impact/impactFormat.ts"
      to: "frontend/src/index.css"
      via: "STATUS_BADGE_META는 bg-up/text-up·bg-warning/text-warning 시맨틱 유틸을 쓰되, changeRateColorClass는 의도적으로 토큰이 아닌 explicit hex(#DC2626/#1D4ED8/#64748B)를 쓴다(의미축 분리)"
      pattern: "text-warning"
---

<objective>
Phase 10의 나머지가 올라탈 **재사용 빌딩블록**을 화면 조립 전에 먼저 잠근다: (1) 결과 표가 쓸 shadcn `table` 블록을 손으로 작성하고, (2) `?item=&window=` URL 상태 훅 `useImpactParams`를 `useTimelineParams` 미러로 만들며, (3) changeRate 포맷·방향색·가격·status 배지 메타·희소/stale 도출을 **순수 함수**로 한곳에 모은다.

엔지니어링 신호는 **계약을 측정된 백엔드와 정합시키는 것**이다: `changeRate`는 화면이 보여줄 `%`가 아니라 `post/pre - 1`의 **비율**(4-dp, `EventImpactService.java` 실측)이라, `formatChangeRate`가 `×100`하는 단 한 곳이 된다 — 이 변환을 빠뜨리면 `+12.3%`가 `+0.1%`로 표시되는 조용한 데이터 왜곡이 난다. 마찬가지로 희소/stale은 백엔드 `toImpactItem`이 "윈도우 내 스냅샷 0개면 앵커 null, 있는데 오래되면 시각 보고+insufficient"로 산출하므로, 프론트는 **앵커 null 여부만** 보고 도출해 30분 임계와 결합하지 않는다(가장 정직). 색·포맷·도출을 여기서 한 번 잠그면 10-02/10-03이 다시 결정하지 않고 소비한다.

Output: `frontend/src/components/ui/table.tsx`, `frontend/src/features/impact/useImpactParams.ts`, `frontend/src/features/impact/impactFormat.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-event-impact/10-CONTEXT.md
@.planning/phases/10-event-impact/10-UI-SPEC.md

<interfaces>
<!-- frontend/src/lib/schemas.ts (무변경) — impactFormat가 다루는 타입 -->
export const eventImpactStatusSchema = z.enum(['ok', 'insufficient_data'])
export type EventImpactStatus = z.infer<typeof eventImpactStatusSchema>
export type EventImpactItem = {  // z.infer 파생
  id: number; eventType: EventType; title: string; occurredAt: string; status: EventImpactStatus;
  preAnchorAt: string | null; postAnchorAt: string | null;
  prePrice: number | null; postPrice: number | null; changeRate: number | null;
}

<!-- 백엔드 실측 (frontend 무변경) — EventImpactService.java:
     changeRate = BigDecimal(post.minPrice)/BigDecimal(pre.minPrice) scale 4 HALF_UP - 1  → 비율(예 0.1230), NOT 퍼센트.
     status='ok'는 양쪽 앵커가 present+priced+fresh(±30min)일 때만; 그 외 'insufficient_data'+changeRate null.
     insufficient시 preAnchorAt/postAnchorAt = 발견된 윈도우 내 앵커 시각, 윈도우 내 스냅샷 0개인 side만 null. -->

<!-- 미러 소스 — frontend/src/features/timeline/useTimelineParams.ts:
     useSearchParams + 함수형 setter(prev.set(...)) + 기본값 반환-only(no eager write) 패턴 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn table 블록 손수 작성</name>
  <files>frontend/src/components/ui/table.tsx</files>
  <read_first>frontend/src/components/ui/card.tsx, frontend/src/components/ui/badge.tsx, frontend/src/lib/utils.ts</read_first>
  <action>`frontend/src/components/ui/table.tsx`를 shadcn **new-york** 스타일 공식 `table` 블록으로 손수 작성한다(**`npx shadcn add table` 금지** — 이 Windows 환경에서 블록을 literal `@/` 디렉터리에 배치하고 불필요한 radix 의존을 추가하는 버그). export: `Table`(루트 `<table>`를 `data-slot="table-container"` overflow-x-auto `<div>`로 감쌈, `w-full caption-bottom text-sm`)·`TableHeader`(`thead`, `[&_tr]:border-b`)·`TableBody`(`tbody`, `[&_tr:last-child]:border-0`)·`TableFooter`(`tfoot`)·`TableRow`(`tr`, `hover:bg-muted/50 border-b transition-colors`)·`TableHead`(`th`, `text-foreground h-10 px-2 text-left align-middle font-medium whitespace-nowrap`)·`TableCell`(`td`, `p-2 align-middle whitespace-nowrap`)·`TableCaption`(`caption`, `text-muted-foreground mt-4 text-sm`). 각 컴포넌트는 `React.ComponentProps<'table'|'thead'|...>`를 받고 `data-slot` 속성과 `cn(..., className)`을 쓴다(기존 `card.tsx`/`badge.tsx`와 동일 컨벤션). 비즈니스 로직 없음 — 순수 presentational 프리미티브.</action>
  <verify>
    <automated>cd frontend && grep -q "TableHeader" src/components/ui/table.tsx && grep -q "TableCell" src/components/ui/table.tsx && grep -q 'data-slot="table"' src/components/ui/table.tsx && grep -q '@/lib/utils' src/components/ui/table.tsx && test ! -d "frontend/@" && echo "table block OK"</automated>
  </verify>
  <done>table.tsx가 Table/TableHeader/TableBody/TableHead/TableRow/TableCell/TableCaption을 data-slot + cn으로 export하고, literal @/ 디렉터리가 생기지 않았다.</done>
</task>

<task type="auto">
  <name>Task 2: useImpactParams — ?item=&window= URL 상태 훅</name>
  <files>frontend/src/features/impact/useImpactParams.ts</files>
  <read_first>frontend/src/features/timeline/useTimelineParams.ts, frontend/src/lib/queries.ts</read_first>
  <action>`frontend/src/features/impact/useImpactParams.ts`를 `useTimelineParams`를 미러해 작성한다. `useSearchParams`(react-router-dom)로 `?item=`·`?window=`를 읽어 `{ itemId, window, setItem, setWindow }`를 반환하는 `useImpactParams()`를 export한다. `itemId`: `?item=`이 숫자면 그 값, 없거나 NaN이면 `null`(timeline 규칙 동일). `window`: `?window=`가 양의 정수로 파싱되면 그 값, **없거나 잘못되면 24**(D-02 기본값 — 반환만, eager write 안 함). `setItem(id)`/`setWindow(hours)`는 `setSearchParams((prev)=>{ prev.set('item'|'window', String(...)); return prev })` 함수형 업데이터로 머지(item·window 독립 공유). **클라이언트 클램프 없음** — `setWindow`는 0·169 같은 범위밖 값도 그대로 URL에 기록해 10-04의 400 UI 시연을 가능케 한다(D-03은 10-02/10-04에서 소비). 인터페이스 주석에 D-01(window=URL searchParams)·D-02(기본 24 비-eager)를 명시한다.</action>
  <verify>
    <automated>cd frontend && grep -q "useImpactParams" src/features/impact/useImpactParams.ts && grep -q "useSearchParams" src/features/impact/useImpactParams.ts && grep -q "window" src/features/impact/useImpactParams.ts && grep -q "24" src/features/impact/useImpactParams.ts && npm run build</automated>
  </verify>
  <done>useImpactParams가 itemId/window/setItem/setWindow를 반환하고, ?window= 부재 시 24 폴백(eager write 없음)·범위밖 값 클램프 없음; build 통과.</done>
</task>

<task type="auto">
  <name>Task 3: impactFormat — changeRate 포맷·방향색·status 메타·희소/stale 도출</name>
  <files>frontend/src/features/impact/impactFormat.ts</files>
  <read_first>frontend/src/lib/schemas.ts, frontend/src/features/_shared/LatestPriceCard.tsx, frontend/src/features/dashboard/StatusBadge.tsx, frontend/src/index.css, .planning/phases/10-event-impact/10-UI-SPEC.md</read_first>
  <action>`frontend/src/features/impact/impactFormat.ts`를 순수 함수 모음으로 작성한다(`@/lib/schemas`의 `EventImpactItem`·`EventImpactStatus` 타입 import).
1) `formatChangeRate(rate: number): string` — **백엔드 changeRate는 비율(`post/pre - 1`)이므로 ×100 후 %**. `const pct = Math.round(rate * 1000) / 10;`로 소수1자리 반올림 → `pct === 0`이면 `'0.0%'`(부호 없음, `-0.0%` 방지), `pct > 0`이면 `` `+${pct.toFixed(1)}%` ``, 그 외 `` `${pct.toFixed(1)}%` ``(음수 부호 포함). 함수 위에 "백엔드는 비율, 화면은 %라 ×100" 주석.
2) `changeRateColorClass(rate: number): string` — `rate > 0` → `'text-[#DC2626]'`(상승=빨강 red-600, 한국 관례 D-05), `rate < 0` → `'text-[#1D4ED8]'`(하락=파랑 blue-700, accent blue-600과 구분), `rate === 0` → `'text-[#64748B]'`(보합 slate-500). 주석으로 "index.css --up(green)/--down(red) 토큰을 쓰지 않는 이유 = 본 화면은 한국 관례라 의미축이 반대"를 남긴다.
3) `formatPrice(n: number): string` → `n.toLocaleString('ko-KR') + ' G'`(LatestPriceCard 관례).
4) `STATUS_BADGE_META: Record<EventImpactStatus, { label: string; className: string }>` → `ok: { label: '비교 가능', className: 'bg-up/10 text-up' }`, `insufficient_data: { label: '데이터 부족', className: 'bg-warning/10 text-warning' }`(D-07; Phase 7 insufficient→warning 의미색 연장).
5) `insufficientReason(item: EventImpactItem): { kind: 'sparse' | 'stale'; inlineLabel: string; body: string } | null` — `item.status !== 'insufficient_data'`면 `null`; `item.preAnchorAt == null || item.postAnchorAt == null`면 `{ kind: 'sparse', inlineLabel: '데이터 부족 · 희소', body: '이벤트 전후 윈도우에 수집된 시세가 없어 비교 기준을 잡지 못했어요.' }`; 둘 다 non-null이면 `{ kind: 'stale', inlineLabel: '데이터 부족 · 오래됨', body: '기준 시각이 이벤트에서 너무 떨어져 비교를 신뢰할 수 없어요.' }`. **30분 같은 임계 상수 하드코딩 금지**(D-08) — 오직 앵커 null 여부만 본다. 카피는 10-UI-SPEC Copywriting Contract와 축자 일치.</action>
  <verify>
    <automated>cd frontend && grep -q "formatChangeRate" src/features/impact/impactFormat.ts && grep -q "1000" src/features/impact/impactFormat.ts && grep -q "text-\[#DC2626\]" src/features/impact/impactFormat.ts && grep -q "text-\[#1D4ED8\]" src/features/impact/impactFormat.ts && grep -q "데이터 부족 · 희소" src/features/impact/impactFormat.ts && grep -q "데이터 부족 · 오래됨" src/features/impact/impactFormat.ts && ! grep -qE "STALENESS|Duration|minutes" src/features/impact/impactFormat.ts && npm run build</automated>
  </verify>
  <done>impactFormat가 formatChangeRate(비율×100 %)·changeRateColorClass(상승#DC2626/하락#1D4ED8/보합#64748B)·formatPrice·STATUS_BADGE_META·insufficientReason(앵커 null로 희소/stale, 30분 하드코딩 없음)을 export; build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 응답 → 포맷 함수 | impactFormat가 zod로 이미 검증된 EventImpactItem(비율 changeRate·nullable 앵커)을 표시 문자열/클래스로 변환한다. 네트워크·인증 경계 없음(순수 함수). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | `changeRateColorClass` arbitrary Tailwind `text-[#...]` | accept | 색 hex는 빌드타임 상수(사용자 입력 아님). Tailwind JIT가 정적 클래스로 컴파일하므로 인젝션 표면 없음. |
| T-10-01-02 | Information Disclosure | `insufficientReason`/`formatChangeRate` | accept | 출력은 고정 한국어 카피와 숫자 포맷뿐 — 예외 메시지·스택·내부 임계값 노출 없음(30분 임계는 의도적으로 미참조). |
| T-10-01-03 | Tampering | `?window=` 범위밖 값(클램프 없음) | mitigate | useImpactParams는 값을 URL에만 기록; 실제 요청 검증은 백엔드(400)가 수행하고 10-04가 400 UI로 처리. 클라이언트는 신뢰 경계가 아님. |
</threat_model>

<verification>
- `cd frontend && npm run build`가 타입 에러 0으로 통과한다.
- `table.tsx`가 new-york table 패밀리를 `data-slot`+`cn`으로 export하고 `frontend/@/` literal 디렉터리가 없다.
- `useImpactParams`가 `?item=&window=`를 읽고 window 부재 시 24 반환(eager write 없음)·범위밖 클램프 없음.
- `impactFormat`: `formatChangeRate`가 비율을 ×100해 `+12.3%`/`-4.0%`/`0.0%`로 포맷(`-0.0%` 없음), `changeRateColorClass`가 상승=`#DC2626`·하락=`#1D4ED8`·보합=`#64748B`, `insufficientReason`이 앵커 null만으로 희소/stale 구분(30분 미참조), `STATUS_BADGE_META`가 ok=비교 가능(green)/insufficient=데이터 부족(amber).
- Java `src/` 무변경.

## 회귀 안전 (기존 기능 보존)
- 신규 파일 3개만 추가하고 기존 `timeline`/`_shared`/`ui` 모듈을 수정하지 않으므로 Phase 8·9 화면은 영향 없음. `npm run build`가 전체 타입 그래프를 통과시켜 회귀를 차단한다.
</verification>

<success_criteria>
- 결과 표 빌딩블록(table)·URL 상태(useImpactParams)·도메인 포맷(impactFormat)이 D-01/D-02/D-05/D-06/D-07/D-08에 정확히 잠긴다.
- changeRate ×100 변환과 한국 관례 방향색이 단일 소스로 확정돼 10-03이 재결정 없이 소비한다.
- build green, 백엔드 무변경.
</success_criteria>

<output>
Create `.planning/phases/10-event-impact/10-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
