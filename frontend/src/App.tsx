// Placeholder root — proves the toolchain renders. The real app shell (router, nav,
// state components) arrives in 07-03; the typed API/query layer in 07-02.
function App() {
  return (
    <div>
      <h1>로스트아크 시세 트래커</h1>
    </div>
  )
}

export default App
