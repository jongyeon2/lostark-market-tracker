---
phase: 17.2-news-panel
plan: 02
type: execute
wave: 2
depends_on: [17.2-01]
files_modified:
  - src/main/java/com/lostark/tracker/news/NewsDtos.java
  - src/main/java/com/lostark/tracker/news/LostarkNewsClient.java
  - src/main/java/com/lostark/tracker/news/NewsService.java
  - src/main/java/com/lostark/tracker/news/NewsPoller.java
  - src/main/java/com/lostark/tracker/news/NewsController.java
  - src/test/java/com/lostark/tracker/news/NewsServiceIT.java
  - src/test/java/com/lostark/tracker/news/NewsControllerIT.java
autonomous: true
requirements: [NEWS-01, NEWS-03]

must_haves:
  truths:
    - "D-01: 로아 공식 `/news/events`+`/news/notices` → 저빈도 @Scheduled 폴러(기본 6h + 부팅 1회) → Redis 캐시(키 `news:latest`, TTL 12h) → `GET /api/news` read 엔드포인트로 서빙한다(Approach A)"
    - "D-02: 신규 패키지 `com.lostark.tracker.news` — LostarkNewsClient(RestClient, 기존 base-url/key 재사용) · NewsService(getLatest/refresh) · NewsPoller(@Scheduled) · NewsController(GET /api/news). Redis 캐시-only, PostgreSQL 미적재"
    - "D-06: Core Value 가드 — PriceCollector·CollectionConfig·가격 캐시·EventImpactService·WatchlistSeeder·SyntheticDemoData 로직 0줄. 뉴스는 독립 패키지·독립 스케줄·독립 Redis 키(6h당 2요청 → 마켓 예산 무영향). 프론트 직접 호출 금지·키 서버 env only"
    - "D-07: 정직성 — 폴 실패 시 마지막 캐시 유지 + updatedAt 미갱신, read 엔드포인트는 마지막 스냅샷 서빙. 캐시 비면 빈 배열 + updatedAt=null"
  artifacts:
    - path: "src/main/java/com/lostark/tracker/news/NewsController.java"
      provides: "GET /api/news → NewsResponse{events,notices,updatedAt} (공개 read, admin 게이트 없음)"
    - path: "src/main/java/com/lostark/tracker/news/NewsService.java"
      provides: "Redis 캐시 getLatest/refresh + 폴 실패 시 캐시 유지 (D-01/D-07)"
  key_links:
    - from: "17.2-NEWS-SPIKE-FINDINGS.md (실측 필드)"
      to: "NewsDtos(NewsEvent/NewsNotice) + LostarkNewsClient 파싱"
      via: "spike-then-lock 전사"
      pattern: "NewsEvent"
    - from: "NewsController GET /api/news"
      to: "frontend useNews() (17.2-03)"
      via: "read DTO 계약(NewsResponse)"
      pattern: "/api/news"
---

<objective>
로아 공식 `/news/events`·`/news/notices`를 저빈도 폴러 + Redis 캐시로 서빙하는 백엔드 뉴스 파이프라인을 신규 패키지 `com.lostark.tracker.news`에 구현한다(D-01/D-02). 프로젝트의 수집→캐시→서빙 패턴을 재사용하되 **완전 독립 read-path** — 수집/가격 캐시/event-impact는 0줄(D-06). 폴 실패 시 마지막 캐시를 유지해 정직히 서빙한다(D-07).

Purpose: 대시보드 뉴스 패널(17.2-03)이 소비할 `GET /api/news` 계약을 제공하고, 뉴스 폴링이 마켓 수집과 자원 경쟁 없이 독립임을 구조로 보장한다.

Output: `LostarkNewsClient`·`NewsService`·`NewsPoller`·`NewsController` + DTO + IT. 수집/캐시/event-impact 및 WatchlistSeeder/SyntheticDemoData diff 0줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-news-panel/17.2-CONTEXT.md
@.planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md
@docs/superpowers/specs/2026-07-06-dashboard-news-panel-design.md
@CLAUDE.md

<interfaces>
<!-- 재사용 패턴: src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java(RestClient + authorization bearer), src/main/java/com/lostark/tracker/collect/PriceCollector.java + CollectionConfig.java(@Scheduled·@Profile 실수집 게이팅 — NewsPoller 동일 프로파일로 게이트, 무변경). 기존 Redis 설정/RedisTemplate 사용처(가격 캐시) — 캐시 패턴 참고(뉴스는 독립 키 news:latest, 기존 캐시 로직 무변경). -->
<!-- 기존 read 컨트롤러(/api/health, /api/items 등) + SecurityConfig — /api/news는 공개 read(admin 게이트 없음, /api/admin/* 만 X-Admin-Secret). NewsController를 기존 read 컨트롤러 패턴으로. -->
<!-- DTO(NewsDtos.java): record NewsEvent(title, link, startDate, endDate, thumbnail), record NewsNotice(title, link, date, type), record NewsResponse(List<NewsEvent> events, List<NewsNotice> notices, String updatedAt). 필드는 17.2-NEWS-SPIKE-FINDINGS 실측 매핑 그대로. -->
<!-- LostarkNewsClient(@Profile 실수집): fetchEvents()/fetchNotices() = GET /news/events·/news/notices → Jackson 파싱 → List<NewsEvent>/List<NewsNotice>. base-url/key는 기존 lostark.api.* 재사용. -->
<!-- NewsService: refresh()=client fetch → NewsResponse(updatedAt=now) → RedisTemplate SET news:latest(JSON, TTL 12h). 실패 시 캐시 유지·미갱신(경고 로그). getLatest()=Redis GET → 없으면 empty NewsResponse(빈 리스트, updatedAt=null). -->
<!-- NewsPoller(@Scheduled fixedDelay 6h + initialDelay 부팅 직후, @Profile 실수집): NewsService.refresh() 호출. 주기 config news.poll-interval-ms(기본 21600000). -->
<!-- 키-안전: 실키는 env only. 응답/로그에 키·계정 미기재. 커밋 전 grep bearer/eyJ 0건. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NewsDtos + LostarkNewsClient (GET /news 파싱, findings 전사)</name>
  <read_first>
    - .planning/phases/17.2-news-panel/17.2-NEWS-SPIKE-FINDINGS.md (실측 필드 → DTO 매핑 — 전사 원천)
    - src/main/java/com/lostark/tracker/spike/LostarkSpikeClient.java (RestClient·bearer·GET 패턴)
    - src/main/java/com/lostark/tracker/collect/PriceCollector.java (@Profile 실수집 게이팅 확인용)
  </read_first>
  <files>src/main/java/com/lostark/tracker/news/NewsDtos.java, src/main/java/com/lostark/tracker/news/LostarkNewsClient.java</files>
  <action>
    `NewsDtos.java`에 record `NewsEvent(String title, String link, String startDate, String endDate, String thumbnail)`, `NewsNotice(String title, String link, String date, String type)`, `NewsResponse(List<NewsEvent> events, List<NewsNotice> notices, String updatedAt)`를 정의(필드는 17.2-NEWS-SPIKE-FINDINGS 실측 매핑 그대로 — 실측 필드명 불일치 시 findings 값 우선). `LostarkNewsClient`를 PriceCollector와 동일 `@Profile`(실수집)로 만들고 RestClient(base-url=`lostark.api.base-url`, `authorization: bearer {lostark.api.key}` — LostarkSpikeClient 정규화 로직 계승)로 `fetchEvents()`=GET `/news/events`, `fetchNotices()`=GET `/news/notices` 구현, Jackson으로 응답을 List<NewsEvent>/List<NewsNotice>로 파싱(이벤트 종료임박순·공지 최신순, 각 최대 6개로 제한). 4xx/5xx/파싱 예외는 상위(NewsService)가 캐시 유지로 처리하도록 전파. PriceCollector/캐시/event-impact는 건드리지 않는다(D-06).
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - NewsDtos에 NewsEvent·NewsNotice·NewsResponse record가 17.2-NEWS-SPIKE-FINDINGS 필드로 정의된다
    - LostarkNewsClient가 GET /news/events·/news/notices를 호출·파싱하고 실수집 @Profile로 게이트된다
    - 이벤트 종료임박순·공지 최신순, 각 ≤6개 제한
    - `./gradlew compileJava` 그린, 수집/캐시/event-impact diff 0줄
  </acceptance_criteria>
  <done>실측 필드 기반 DTO와 /news를 파싱하는 LostarkNewsClient가 컴파일되고, Core Value 경로 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 2: NewsService(Redis 캐시·keep-on-failure) + NewsPoller(@Scheduled) + config</name>
  <read_first>
    - src/main/java/com/lostark/tracker/news/LostarkNewsClient.java (Task 1 산출 — 주입 대상)
    - 기존 Redis 설정/RedisTemplate 사용처(가격 캐시 계층 — 뉴스는 독립 키, 무변경)
    - src/main/java/com/lostark/tracker/collect/CollectionConfig.java (@Scheduled·@Profile 패턴)
    - src/main/resources/application.yml (또는 profile yml — news.poll-interval 추가 지점)
  </read_first>
  <files>src/main/java/com/lostark/tracker/news/NewsService.java, src/main/java/com/lostark/tracker/news/NewsPoller.java, src/main/resources/application.yml</files>
  <action>
    `NewsService`: `refresh()`가 LostarkNewsClient.fetchEvents/fetchNotices 호출 → `NewsResponse(events, notices, updatedAt=Instant.now())` → RedisTemplate/StringRedisTemplate로 `news:latest`에 JSON SET(TTL 12h). client 예외 시 **캐시 유지·updatedAt 미갱신 + 경고 로그**(D-07). `getLatest()`는 Redis GET → 값 없으면 `new NewsResponse(List.of(), List.of(), null)`. Jackson ObjectMapper로 직렬화. `NewsPoller`는 실수집 `@Profile` + `@Scheduled(fixedDelayString = "${news.poll-interval-ms:21600000}", initialDelayString="...")`로 `NewsService.refresh()` 호출(부팅 직후 1회 + 6h). application.yml에 `news.poll-interval-ms` 기본값 추가. 마켓 수집(PriceCollector) 스케줄과 독립 — 공유 자원/락 없음(D-06). 기존 캐시 로직·CollectionConfig 무변경.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - NewsService.refresh()가 news:latest에 TTL 12h로 캐시하고, client 예외 시 캐시를 유지한다(D-07)
    - getLatest()가 캐시 미스 시 빈 NewsResponse(updatedAt=null)를 반환한다
    - NewsPoller가 실수집 @Profile·@Scheduled(기본 6h + 부팅 1회)로 refresh 호출, news.poll-interval-ms config 존재
    - PriceCollector/CollectionConfig/가격 캐시 diff 0줄
  </acceptance_criteria>
  <done>Redis 캐시·keep-on-failure를 갖춘 NewsService와 독립 스케줄 NewsPoller가 컴파일되고, 마켓 수집 경로 0줄이다.</done>
</task>

<task type="auto">
  <name>Task 3: NewsController(GET /api/news 공개 read) + IT</name>
  <read_first>
    - src/main/java/com/lostark/tracker/news/NewsService.java (Task 2 산출 — getLatest 주입)
    - 기존 read 컨트롤러(/api/health 등) + SecurityConfig (/api/news 공개 확인)
    - src/test/java/com/lostark/tracker/support/PostgresRedisContainers.java (IT 베이스)
    - src/test/java/com/lostark/tracker/collect/WatchlistSeederIT.java (IT 패턴 참고)
  </read_first>
  <files>src/main/java/com/lostark/tracker/news/NewsController.java, src/test/java/com/lostark/tracker/news/NewsServiceIT.java, src/test/java/com/lostark/tracker/news/NewsControllerIT.java</files>
  <action>
    `NewsController` `@RestController` `GET /api/news` → `NewsService.getLatest()`를 `NewsResponse`로 반환(기존 read 컨트롤러 패턴, admin 게이트 없음 — /api/news는 공개; SecurityConfig가 /api/admin/* 외 read를 허용하는지 확인, 필요 시 /api/news permit). `NewsServiceIT`(PostgresRedisContainers 상속, Testcontainers Redis): LostarkNewsClient를 **mock/stub**(@MockBean)으로 주입해 fixture events/notices 반환 → refresh() 후 getLatest()가 캐시에서 반환 검증 + **client 예외 시 이전 캐시 유지** 검증(D-07). `NewsControllerIT`: 캐시 시드 후 `GET /api/news`가 200 + events/notices/updatedAt JSON 반환, 캐시 미스 시 빈 배열 검증. 실 API 미호출(mock) — 키 불필요·CI 안전.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/news가 NewsResponse{events,notices,updatedAt}를 반환하고 admin 게이트 없이 공개 접근된다
    - NewsServiceIT가 캐시 set/get + 폴 실패 시 캐시 유지(D-07)를 Testcontainers Redis로 증명(client는 mock — 실 API·키 미사용)
    - NewsControllerIT가 GET /api/news 200 + 캐시 미스 시 빈 배열을 검증
    - `./gradlew build` 그린(키 없이 CI 안전), 수집/캐시/event-impact·WatchlistSeeder/SyntheticDemoData diff 0줄
  </acceptance_criteria>
  <done>GET /api/news 공개 read와 캐시·정직성 IT가 통과하고, build가 키 없이 그린이며 Core Value 경로 0줄이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버(env) → 외부 Lostark /news API | 실 JWT로 뉴스 실호출 — 키 서버 env only, 응답/로그 미기재 |
| GET /api/news → 공개 클라이언트 | 공개 read(admin 아님) — 공개 뉴스 메타만, 키/시크릿 없음 |
| 뉴스 폴러 → 마켓 수집 | 독립 스케줄·독립 Redis 키 — 레이트리밋 예산 경쟁 최소(6h당 2요청) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1722-01 | Information Disclosure | 뉴스 응답/로그에 키·시크릿 유입 | mitigate | 키 env only, DTO는 공개 뉴스 메타만, 로그에 키 미기재 |
| T-1722-02 | Denial of Service | 뉴스 폴링이 마켓 수집 레이트리밋 예산 잠식 | mitigate | 6h 저빈도·독립 스케줄(2요청/6h), 마켓 100/min·10분 케이던스에 무영향(D-06) |
| T-1722-03 | Tampering | /api/news가 admin 게이트에 잘못 묶여 공개 접근 불가 or 반대로 admin 경로 노출 | mitigate | /api/news는 명시적 공개 read, /api/admin/* 게이트 무변경 — IT로 200 확인 |
| T-1722-04 | Availability | 외부 /news 장애 시 패널 깨짐 | mitigate | keep-on-failure — 마지막 캐시 서빙, 캐시 미스 시 빈 응답(D-07) |
| T-1722-SC | Tampering | 패키지/공급망 | accept | 신규 의존성 0 — Spring RestClient/RedisTemplate/Jackson 기존 스택 재사용 |
</threat_model>

<verification>
- `GET /api/news`가 `NewsResponse{events,notices,updatedAt}` 공개 반환(admin 게이트 없음) — NewsControllerIT 200.
- NewsService가 news:latest Redis 캐시(TTL 12h) set/get + 폴 실패 시 캐시 유지 — NewsServiceIT(Testcontainers, client mock).
- NewsPoller가 실수집 @Profile·@Scheduled(기본 6h + 부팅 1회)로 독립 폴 — 마켓 수집과 공유 자원 없음.
- 이벤트/공지 각 ≤6개, 필드는 17.2-NEWS-SPIKE-FINDINGS 실측 매핑.
- `./gradlew build` 그린(키 없이 CI 안전). PriceCollector·CollectionConfig·가격 캐시·EventImpactService·WatchlistSeeder·SyntheticDemoData diff 0줄(D-06).
- phase/소스에 `bearer`/`eyJ...` 토큰 없음(grep 0건).
</verification>

<success_criteria>
- 로아 공식 /news 이벤트·공지를 저빈도 폴러 + Redis 캐시 + GET /api/news로 서빙한다(NEWS-01, D-01/D-02).
- 폴 실패·갱신 전에도 마지막 캐시/빈 응답을 정직히 서빙한다(NEWS-03, D-07).
- Core Value(수집/캐시/event-impact) 0줄 회귀, 뉴스는 독립 패키지·스케줄·Redis 키(D-06).
</success_criteria>

<output>
Create `.planning/phases/17.2-news-panel/17.2-02-SUMMARY.md` when done
</output>
