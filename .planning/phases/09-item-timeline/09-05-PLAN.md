---
phase: 09-item-timeline
plan: 05
type: execute
wave: 3
depends_on: [09-02, 09-03, 09-04]
files_modified:
  - frontend/src/features/timeline/TimelinePage.tsx
autonomous: false
requirements: [TIME-01, TIME-05]

must_haves:
  truths:
    - "`TimelinePage` replaces the Phase-7 empty skeleton and composes the full screen top-to-bottom per 09-UI-SPEC Layout: a control bar `[ItemSelect] [7일][30일][90일] [시작일][종료일]`, then the `LatestPriceCard`, then the chart area (legend + 360px chart + downsample badge). It wires `useTimelineParams` (09-03) to `ItemSelect.onChange`→`setItem`, `RangeControls.onRangeChange`→`setRange`, and feeds `from`/`to` into the timeline query."
    - "Entry default selection follows D-06: if the URL `?item=` is present it wins; otherwise the FIRST item from `useItems()` is auto-selected (and written via `setItem` once items load) — the chart is never an empty '품목을 선택하세요' prompt on entry (demo-friendly). Combined with the D-03 last-30-days default range (containing the seed 8-day window), entry shows a non-empty chart with the 2 demo markers."
    - "The latest-price card and the chart each sit in their OWN AsyncBoundary/error scope (Phase-7 D-08 / Phase-8 D-07): a chart error never blanks the card and vice-versa."
    - "The chart area distinguishes the three TIME-05 outcomes by `ApiError.status` (D-09) — NOT one generic error: the timeline query's error is inspected (`error instanceof ApiError`), and `400` (to≤from) renders the heading '조회 기간을 다시 확인해 주세요' + body '종료일이 시작일보다 같거나 빠릅니다. 시작일 이후의 종료일을 선택하고 다시 불러오세요.' (destructive); `404` (unknown item) renders '존재하지 않는 품목이에요' + '선택한 품목을 찾을 수 없습니다. 위 목록에서 다른 품목을 선택해 주세요.'; a network/other error falls back to the inherited ErrorState copy. (Copy verbatim from 09-UI-SPEC Copywriting Contract.)"
    - "A successful-but-empty window (200 with `snapshots.length === 0`) renders the EMPTY state '이 기간에는 시세 데이터가 없어요' + '선택한 기간에 수집된 가격이 없습니다. 기간을 넓히거나 아래 \"최근 30일\"을 눌러보세요.' with a primary CTA button '최근 30일 보기' that resets the range to the last-30-days window via `setRange` (D-09, 09-UI-SPEC). This is distinct from the 400/404 error copy."
    - "On chart success the page renders `EventMarkerLegend` + `PriceTimelineChart` (passing `snapshots`/`events`/`downsampled`/`bucketWidth` from the timeline query) + `DownsampleBadge` (top-right). `cd frontend && npm run build` passes with zero type errors; no Java `src/` file changes; existing /timeline route and AppLayout are unchanged except this page body."
  artifacts:
    - path: "frontend/src/features/timeline/TimelinePage.tsx"
      provides: "Composed timeline screen: selector+card+controls+chart wired to useTimelineParams; D-06 default selection; D-09 status-branched 400/404/200-empty handling (TIME-01, TIME-05)"
      contains: "ApiError"
  key_links:
    - from: "frontend/src/features/timeline/TimelinePage.tsx"
      to: "frontend/src/features/timeline/useTimelineParams.ts"
      via: "page reads itemId/from/to and calls setItem/setRange (URL is the source of truth, D-04)"
      pattern: "useTimelineParams"
    - from: "frontend/src/features/timeline/TimelinePage.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useTimeline(itemId, from, to) drives the chart; useItems() supplies the D-06 default first item"
      pattern: "useTimeline"
    - from: "frontend/src/features/timeline/TimelinePage.tsx"
      to: "frontend/src/lib/api.ts"
      via: "chart-area error branch reads ApiError.status to pick 400 vs 404 vs other copy (D-09)"
      pattern: "ApiError"
---

<objective>
Compose the timeline screen and make it **honest about every outcome**. This plan wires the shared selector + latest card (09-02), the range controls + URL state (09-03), and the chart + markers + badge (09-04) into `TimelinePage`, delivering the "select an item → card + chart update" of TIME-01 and the empty/400/404 handling of TIME-05.

Two engineering signals: (1) **demo-friendly determinism** — D-06 auto-selects the first item and D-03 defaults to the last 30 days, so the headline chart is never blank on entry; (2) **status-honest errors** — instead of one generic "something broke", the chart area reads `ApiError.status` and tells the user exactly which of the three reachable outcomes happened (bad range / missing item / empty window), each with its own next action. The latest card and chart keep independent error scopes so one failing never hides the other.

Output: `frontend/src/features/timeline/TimelinePage.tsx` (replacing the Phase-7 skeleton).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-item-timeline/09-CONTEXT.md
@.planning/phases/09-item-timeline/09-UI-SPEC.md

<interfaces>
<!-- 09-02 -->
import { ItemSelect } from '@/features/_shared/ItemSelect'          // ({ value: number|null, onChange: (id:number)=>void })
import { LatestPriceCard } from '@/features/_shared/LatestPriceCard' // ({ itemId: number, displayName: string })
<!-- 09-03 -->
import { useTimelineParams } from '@/features/timeline/useTimelineParams' // -> { itemId, from, to, setItem, setRange }
import { RangeControls } from '@/features/timeline/RangeControls'         // ({ from, to, onRangeChange })
<!-- 09-04 -->
import { PriceTimelineChart } from '@/features/timeline/PriceTimelineChart' // ({ snapshots, events, downsampled, bucketWidth })
import { EventMarkerLegend } from '@/features/timeline/EventMarkerLegend'
import { DownsampleBadge } from '@/features/timeline/DownsampleBadge'      // ({ downsampled, bucketWidth })
<!-- data + errors -->
import { useItems, useTimeline } from '@/lib/queries'
import { ApiError } from '@/lib/api'              // ApiError.status: number
import { ErrorState } from '@/components/state/ErrorState'   // ({ onRetry, message? }) — heading fixed '연결 오류'
import { EmptyState } from '@/components/state/EmptyState'    // ({ body? }) — heading fixed
<!-- NOTE: ErrorState/EmptyState have FIXED headings; the D-09 cases need DISTINCT headings,
     so render the 400/404/empty branches as small inline blocks (Alert + heading + body + CTA)
     rather than forcing them through the fixed-heading shared components. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose page + wire URL state + default selection (D-06)</name>
  <files>frontend/src/features/timeline/TimelinePage.tsx</files>
  <read_first>frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/timeline/useTimelineParams.ts, .planning/phases/09-item-timeline/09-UI-SPEC.md</read_first>
  <action>Replace the skeleton body of `TimelinePage`. Call `useTimelineParams()` for `{ itemId, from, to, setItem, setRange }` and `useItems()` for the option list + the D-06 default. Default selection: if `itemId` is null and items have loaded, `setItem(items[0].id)` in an effect (URL `?item=` wins because when present `itemId` is non-null and the effect is a no-op). Resolve the selected item's `displayName` from the items list for `LatestPriceCard`. Render the route title 'h1 품목 타임라인' (Display 28/600), then a control bar (flex, wrap, gap `sm`–`md`) containing `<ItemSelect value={itemId} onChange={setItem} />` and `<RangeControls from={from} to={to} onRangeChange={setRange} />`; then `<LatestPriceCard itemId={itemId} displayName={displayName} />` (render only once itemId is resolved). Leave a clearly-marked placeholder where the chart area goes (filled in Task 2). Match the existing page conventions (DashboardPage spacing `space-y-*`, max-width inherited from AppLayout).</action>
  <verify>
    <automated>cd frontend && grep -q "useTimelineParams" src/features/timeline/TimelinePage.tsx && grep -q "ItemSelect" src/features/timeline/TimelinePage.tsx && grep -q "RangeControls" src/features/timeline/TimelinePage.tsx && grep -q "items\[0\]\|\[0\]\.id" src/features/timeline/TimelinePage.tsx && npm run build</automated>
  </verify>
  <done>TimelinePage composes title + control bar (ItemSelect + RangeControls) + LatestPriceCard, wired to useTimelineParams; D-06 auto-selects first item when ?item= absent; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Chart area with status-branched 400/404/200-empty (D-09)</name>
  <files>frontend/src/features/timeline/TimelinePage.tsx</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/lib/queries.ts, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/button.tsx, .planning/phases/09-item-timeline/09-UI-SPEC.md</read_first>
  <action>In `TimelinePage`, call `const timeline = useTimeline(itemId ?? 0, from, to)` (guarded so it only fetches when `itemId != null` — e.g. pass `enabled` is not available via the existing hook signature, so render the chart area only once `itemId != null`). Render the chart area in its own scope: while `timeline.status === 'pending'` show a chart-sized Skeleton + '불러오는 중…'; on `timeline.status === 'error'` branch on the error — `const err = timeline.error; const status = err instanceof ApiError ? err.status : null;` — `status === 400` → an Alert (destructive) with heading '조회 기간을 다시 확인해 주세요' + body '종료일이 시작일보다 같거나 빠릅니다. 시작일 이후의 종료일을 선택하고 다시 불러오세요.'; `status === 404` → Alert heading '존재하지 않는 품목이에요' + body '선택한 품목을 찾을 수 없습니다. 위 목록에서 다른 품목을 선택해 주세요.'; otherwise → `<ErrorState onRetry={() => timeline.refetch()} />` (inherited network copy). On success, if `timeline.data.snapshots.length === 0` → an EMPTY block heading '이 기간에는 시세 데이터가 없어요' + body '선택한 기간에 수집된 가격이 없습니다. 기간을 넓히거나 아래 "최근 30일"을 눌러보세요.' + a primary Button '최근 30일 보기' whose onClick resets via `setRange(new Date(Date.now()-30*86400000).toISOString(), new Date().toISOString())`. On success with data → render `<EventMarkerLegend />` + `<PriceTimelineChart snapshots={...} events={...} downsampled={...} bucketWidth={...} />` + `<DownsampleBadge downsampled={...} bucketWidth={...} />` (badge top-right). All copy verbatim from 09-UI-SPEC. Keep this chart scope independent from the LatestPriceCard's boundary.</action>
  <verify>
    <automated>cd frontend && grep -q "ApiError" src/features/timeline/TimelinePage.tsx && grep -q "useTimeline" src/features/timeline/TimelinePage.tsx && grep -q "존재하지 않는 품목" src/features/timeline/TimelinePage.tsx && grep -q "이 기간에는 시세 데이터가 없어요" src/features/timeline/TimelinePage.tsx && grep -q "최근 30일 보기" src/features/timeline/TimelinePage.tsx && npm run build</automated>
  </verify>
  <done>Chart area shows distinct 400 / 404 / 200-empty(+'최근 30일 보기' CTA) / network states by ApiError.status (D-09), and on success renders legend+chart+badge; build passes; no Java src change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Verify the timeline narrative + honesty + KST alignment</name>
  <files>frontend/src/features/timeline/TimelinePage.tsx</files>
  <action>PAUSE for human review — this is a blocking checkpoint covering the ROADMAP 사용자 확인 포인트 (marker narrative, downsample honesty) and the off-by-9h verification, none of which a build can prove. Claude first ensures the app is runnable (seed backend on :8080 + `cd frontend && npm run dev`), then presents the developer the <what-built>/<how-to-verify>/<resume-signal> below and waits for the resume signal. Do NOT auto-advance.</action>
  <what-built>The full timeline screen: item selector + latest-price card + range controls (presets/date inputs) + price line chart with per-eventType event markers, downsample badge, and status-branched empty/400/404 states. Backend must run with the seed profile (SPRING_PROFILES_ACTIVE=seed) on :8080 and the Vite dev server (cd frontend && npm run dev).</what-built>
  <how-to-verify>
    1. Open /timeline — confirm a non-empty chart loads immediately (first item auto-selected, last-30-days window) with the price line and the 2 seed event markers visible (ROADMAP 완료조건 #1/#2).
    2. Confirm event markers are color-distinguished per eventType, the 4-color legend is readable, and hovering a marker shows the event title + KST datetime (#4). Judge whether the marker-over-price "narrative" reads (사용자 확인 포인트).
    3. Switch the item in the selector — confirm the latest-price card AND chart update to that item (#1).
    4. Click a large preset / set a wide date range to trigger downsample — confirm the '버킷 평균 · 1시간'(or '1일') badge appears, the line loses its dots, and the network response's downsampled/bucketWidth match the screen (#3, honest-data 정직성 확인).
    5. Set 종료일 ≤ 시작일 → confirm the 400 '조회 기간을 다시 확인해 주세요' copy; pick a far-future empty window → confirm the 200-empty '이 기간에는 시세 데이터가 없어요' + '최근 30일 보기' CTA; (optionally hit an unknown item id in the URL) → confirm the 404 copy (#5).
    6. Confirm x-axis labels are KST and align with the seed event times with NO 9-hour drift (검증 방법: 데모 이벤트 시각과 대조).
  </how-to-verify>
  <verify><human-check>Developer confirms the marker narrative reads, the downsample badge is honest, KST labels have no 9-hour drift, and the 400/404/200-empty states each show the right copy.</human-check></verify>
  <resume-signal>Type "approved" or describe issues (marker narrative, downsample honesty, KST alignment, error copy)</resume-signal>
  <done>Developer has verified the timeline narrative, downsample honesty, KST alignment, and the three TIME-05 outcomes (or recorded the required fixes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → screen | useItems/useTimeline responses drive selection, chart, and error copy (zod-validated upstream; ApiError carries only the HTTP status). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | chart-area error branch | mitigate | The 400/404/empty branches render fixed Korean copy keyed only on `ApiError.status` (a number) — no exception message, stack, URL, or response body is shown to the user (extends the Phase-7 T-0703-03 no-raw-error rule). |
| T-09-05-02 | Tampering | `?item=` / `?from=` / `?to=` from a hand-edited URL | accept | A forged item id → 404 copy; a bad range → 400 copy; both handled gracefully by the read-only backend + this status branch. No write/auth path exists to abuse. |
| T-09-05-03 | Denial of Service | rapid selector/range changes refiring queries | accept | React Query dedupes/caches by queryKey (['timeline', id, from, to]); the backend downsamples large ranges server-side. No client-driven unbounded load. |
</threat_model>

<verification>
- `cd frontend && npm run build` passes (tsc -b + vite build), zero type errors.
- /timeline composes selector + latest card + controls + chart per 09-UI-SPEC layout; D-06 auto-selects first item (?item= wins when present); D-03 default 30-day window.
- Chart area shows DISTINCT 400 / 404 / 200-empty(+CTA) / network states via `ApiError.status` (D-09), verbatim 09-UI-SPEC copy.
- Latest card and chart keep independent error scopes.
- Human checkpoint confirms marker narrative, downsample honesty, KST/off-by-9h alignment, and the three TIME-05 outcomes.
- No Java `src/` file changed; route tree unchanged.
</verification>

<success_criteria>
- TIME-01 (select→card+chart update) and TIME-05 (range change + empty/400/404) fully delivered.
- Headline screen is demo-non-empty on entry and honest about every failure mode; build green + human-verified.
</success_criteria>

<output>
Create `.planning/phases/09-item-timeline/09-05-SUMMARY.md` when done.
</output>
