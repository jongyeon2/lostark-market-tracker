---
quick_id: 260630-lu5
slug: readme-jscode
date: 2026-06-30
mode: quick
---

# Quick 260630-lu5: README JSCODE 스타일 재구성 Plan

## 목표

루트 `README.md`를 JSCODE-EDU(github.com/JSCODE-EDU/README) 포트폴리오 템플릿
방향으로 재구성한다. 현재 README는 곧장 엔지니어링 논문처럼 들어가 "이게 어떤
프로젝트이고 주제가 무엇인지"가 비개발자에게 약하게 읽힌다. **친근한 프로젝트
소개·주제·동기·데모·주요 기능을 상단에** 두고, **기존 깊은 엔지니어링 내용
(curl/JSON·설계 결정·아키텍처)은 하단에 정리해 유지**한다(면접관용 깊이 보존).

## 확정 사항 (사용자 AskUserQuestion)

1. 구성 방향 = **인트로 강화 + 깊이 유지** (가벼운 전환 아님, 깊은 내용 보존)
2. GitHub `jongyeon2` / 저장소 `lostark-market-tracker`, 표시명 `jongyeon`,
   https://github.com/jongyeon2
3. 기간 = **2026.06 ~ 2026.07 (~1개월, 단일 개발자)**
4. 배포 = **라이브 배포 URL 없음** — seed 모드 로컬 실행 데모 + 3화면 스크린샷만
   (과대 표기 없음, 실제 코드 상태와 일치)

## 작업

### Task 1: README.md 재구성 (단일 파일)

- **상단(친근한 인트로 — JSCODE 순서):**
  - 제목 + 이모지 + 뱃지(CI·Java 21·Spring Boot 3.4·PostgreSQL 16·Redis 7·React 19)
  - 한 줄 소개(blockquote) + 대표 이미지(dashboard.png)
  - 📖 프로젝트 소개(주제·동기·왜 만들었나, 비개발자도 읽힘)
  - 🎯 핵심 가치 / 게임=금융 파이프라인 비유 + 상관≠인과
  - ⭐ 주요 기능(수집/저장/서빙/event-impact/관리자/시각 enrichment)
  - 🖼 데모(3화면 스크린샷) · 🔧 기술 스택(backend+frontend) · 💻 실행 방법(3단계+dev/seed)
- **구분선 후 하단(엔지니어링 깊이 — 보존):**
  - 🏗 아키텍처(기존 mermaid + data flow 보존)
  - 🧠 설계 결정 & 트레이드오프(기존 보존)
  - 🎨 시각 enrichment(아이콘·역할 배지·fallback, 기존 보존)
  - 📚 API 데모(curl + JSON 4개 엔드포인트 + 표, **그대로 보존**)
  - 🗂 프로젝트 구조(신규 — 백엔드 com.lostark.tracker 패키지 + frontend + .planning 트리)
  - ⚙️ 한계/스코프 · 데이터 보존(기존 보존) · 기술 스택&문서 링크(기존 보존)
  - 👤 Developer / 프로젝트 정보(기간·작성자, 신규)
- **보존 불변:** 모든 엔드포인트·curl·JSON 샘플·스크린샷 경로·설계 결정·mermaid·
  3단계 재현·dev/seed 안내·ADMIN_API_SECRET fail-closed·데이터 보존 정책·설계 문서
  링크·.planning 링크는 사실 그대로 유지. 과대(배포 등) 신규 주장 금지.

**files:** README.md
**verify:** 마크다운 구조 확인 — 상단 친근 인트로 5섹션 + 하단 깊이 섹션 + 4개
curl/JSON 블록 보존 + dashboard/item-timeline/event-impact 스크린샷 경로 3개 유지.
**done:** README가 JSCODE 순서(제목→뱃지→한줄→이미지→소개→기능→데모→스택→실행)로
시작하고, 기존 엔지니어링 깊이가 하단에 보존됨. 백엔드/프론트 `src/` 0줄.

## 범위 밖

- frontend/README.md 변경(이번 작업은 루트 README만)
- 코드(`src/`·`frontend/src/`) 변경 — docs 전용
- 새 스크린샷 캡처(직전 260630-h16 후속 verify-work에서 이미 교체 완료)
