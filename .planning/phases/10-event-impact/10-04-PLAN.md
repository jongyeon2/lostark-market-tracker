---
phase: 10-event-impact
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - frontend/src/features/impact/ImpactPage.tsx
autonomous: false
requirements: [IMPCT-01, IMPCT-02, IMPCT-03, IMPCT-04]

must_haves:
  truths:
    - "`ImpactPage`가 Phase 7 빈 골격을 대체하고 10-UI-SPEC Layout대로 위→아래로 조립한다: (1) **상관 ≠ 인과 배너**(`CorrelationBanner`)를 콘텐츠 영역 최상단·전체 폭에 상시 배치(D-10·IMPCT-04 — 표를 보기 전 맥락 먼저), (2) 컨트롤 바 `[ItemSelect][WindowControls]`(한 줄, 좁으면 wrap), (3) `LatestPriceCard`(D-12), (4) 결과 영역(표/카드). 라우트 제목은 `이벤트 영향`(Display 28/600, 내비 라벨과 동일)."
    - "URL searchParams가 단일 상태 소스다(D-01): `useImpactParams()`로 `{ itemId, window, setItem, setWindow }`를 얻어 `<ItemSelect value={itemId} onChange={setItem} />`·`<WindowControls window={window} onWindowChange={setWindow} />`에 배선한다. 진입 기본 선택은 Phase 9 D-06 상속: `?item=`이 있으면 그 값이 이기고, 없으면 items 로드 후 첫 품목을 `setItem(items[0].id)`로 자동 선택(effect, itemId가 이미 있으면 no-op) — 진입 시 빈 '품목을 선택하세요'가 되지 않는다(데모 친화)."
    - "결과 영역이 **자기 비동기/에러 스코프**를 가지며 `LatestPriceCard`의 `<AsyncBoundary>`와 독립이다(D-12: 최신가 실패가 결과 표를 가리지 않음, Phase 7 D-08 격리). `useEventImpact(itemId, window)`는 `itemId != null`로 해소된 뒤에만 fetch한다(결과 영역을 itemId 해소 후 렌더)."
    - "결과 영역이 `ApiError.status`로 분기한다(Phase 9 D-09 패턴 상속 — 일반 '오류 발생' 금지): `400`(window≤0 또는 >168) → 제목 \"윈도우 값을 다시 확인해 주세요\" + 본문 \"윈도우는 1~168시간 사이의 정수여야 합니다. 프리셋 버튼을 누르거나 범위 안의 값을 입력해 다시 불러오세요.\" + Primary CTA \"24시간으로 보기\"(`setWindow(24)`로 기본 프리셋 리셋, D-03 복구 경로); `404`(없는 품목) → 제목 \"존재하지 않는 품목이에요\" + 본문 \"선택한 품목을 찾을 수 없습니다. 위 목록에서 다른 품목을 선택해 주세요.\"; 그 외(네트워크) → 상속 `ErrorState`(`onRetry`=refetch). 모든 카피는 10-UI-SPEC Copywriting Contract와 축자 일치."
    - "성공 응답에서 `events.length === 0`이면 `EmptyState`로 \"등록된 이벤트가 없어요\" + \"이 품목에 연결된 게임 이벤트가 아직 없습니다. 다른 품목을 선택하거나 관리자가 이벤트를 등록하면 표시됩니다.\"를 보여준다(D-11 — 관리자가 이벤트 0개로 둔 경우, per-row insufficient와 별개). `events`가 있으면 `<EventImpactTable events={...} />` + `<EventImpactCards events={...} />`를 함께 렌더해 반응형으로 ok·insufficient 혼합 목록을 보인다(D-09). pending에는 표 자리 Skeleton + \"불러오는 중…\"."
    - "`cd frontend && npm run build`가 타입 에러 0으로 통과한다. 기존 `/impact` 라우트·`AppLayout`·내비는 이 페이지 본문 외에는 변경되지 않으며 Java `src/`는 한 줄도 바뀌지 않는다(read-only, Vite 프록시 `/api`→:8080 소비). seed 프로파일 백엔드 기동 시 데모 이벤트로 non-zero changeRate가 보인다(ROADMAP 검증)."
  artifacts:
    - path: "frontend/src/features/impact/ImpactPage.tsx"
      provides: "조립된 이벤트 영향 화면: 배너(D-10)+컨트롤(D-01)+최신가(D-12)+결과 표/카드, useImpactParams 배선·D-06 기본선택·ApiError 400/404/200-empty 분기(D-03/D-09/D-11) — IMPCT-01..04"
      contains: "useImpactParams"
  key_links:
    - from: "frontend/src/features/impact/ImpactPage.tsx"
      to: "frontend/src/features/impact/useImpactParams.ts"
      via: "itemId/window를 읽고 setItem/setWindow로 ItemSelect/WindowControls 배선(URL 단일 소스, D-01)"
      pattern: "useImpactParams"
    - from: "frontend/src/features/impact/ImpactPage.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useEventImpact(itemId, window)가 결과를 구동; useItems()가 D-06 기본 첫 품목 공급"
      pattern: "useEventImpact"
    - from: "frontend/src/features/impact/ImpactPage.tsx"
      to: "frontend/src/lib/api.ts"
      via: "결과 영역 에러 분기가 ApiError.status로 400 vs 404 vs 기타 카피 선택(D-03/D-09)"
      pattern: "ApiError"
    - from: "frontend/src/features/impact/ImpactPage.tsx"
      to: "frontend/src/features/_shared/LatestPriceCard.tsx"
      via: "공용 LatestPriceCard를 자기 AsyncBoundary 격리로 표시(D-12)"
      pattern: "LatestPriceCard"
---

<objective>
이벤트 영향 화면을 조립하고 **모든 결과에 정직하게** 만든다. 이 plan은 10-02의 배너·window 컨트롤, 10-03의 결과 표/카드, 10-01의 URL 상태·포맷을 `ImpactPage`로 배선해 IMPCT-01~04를 완성한다 — 상관≠인과 배너를 최상단 상시 노출하고(D-10), `?item=&window=`를 단일 상태 소스로 묶고(D-01), 최신가 카드와 결과 영역을 독립 비동기 스코프로 격리하며(D-12), 400/404/200-empty를 각각 구분된 카피로 처리한다(D-03/D-09/D-11).

두 엔지니어링 신호: (1) **데모 친화 결정성** — D-06 첫 품목 자동 선택 + 기본 window 24h(D-02)로 진입 화면이 비지 않는다; (2) **상태 정직 에러** — 하나의 '문제 발생' 대신 `ApiError.status`로 "윈도우 범위 오류 / 없는 품목 / 이벤트 0개"를 각자의 다음 행동과 함께 알려준다. 마지막으로 이 화면은 프로젝트 신뢰성의 얼굴이라(상관≠인과 가시성, insufficient "왜" 납득성), 빌드가 증명할 수 없는 부분은 **휴먼 체크포인트**로 검증한다.

Output: `frontend/src/features/impact/ImpactPage.tsx`(Phase 7 골격 대체).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-event-impact/10-CONTEXT.md
@.planning/phases/10-event-impact/10-UI-SPEC.md

<interfaces>
<!-- 10-01 -->
import { useImpactParams } from '@/features/impact/useImpactParams'  // -> { itemId, window, setItem, setWindow }
<!-- 10-02 -->
import { WindowControls } from '@/features/impact/WindowControls'     // ({ window, onWindowChange })
import { CorrelationBanner } from '@/features/impact/CorrelationBanner' // ()
<!-- 10-03 -->
import { EventImpactTable } from '@/features/impact/EventImpactTable'  // ({ events })
import { EventImpactCards } from '@/features/impact/EventImpactCards'  // ({ events })
<!-- 공용 (무변경) -->
import { ItemSelect } from '@/features/_shared/ItemSelect'            // ({ value, onChange })
import { LatestPriceCard } from '@/features/_shared/LatestPriceCard'  // ({ itemId, displayName })
import { useItems, useEventImpact } from '@/lib/queries'
import { ApiError } from '@/lib/api'                                  // .status: number
import { EmptyState } from '@/components/state/EmptyState'            // ({ body? }) — 제목 고정
import { ErrorState } from '@/components/state/ErrorState'            // ({ onRetry })
import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert'
import { Button } from '@/components/ui/button'
import { Skeleton } from '@/components/ui/skeleton'
<!-- 패턴 소스 — frontend/src/features/timeline/TimelinePage.tsx:
     ChartArea의 ApiError.status 분기(400/404/기타) + 200-empty 블록 + D-06 첫 품목 자동선택 effect.
     주의: EmptyState/ErrorState는 제목이 고정이라, 400/404/200-empty의 구분된 제목은 inline Alert+제목+본문+CTA로 렌더. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 페이지 조립 + URL 배선 + 기본 선택(D-06) + 배너/컨트롤/최신가</name>
  <files>frontend/src/features/impact/ImpactPage.tsx</files>
  <read_first>frontend/src/features/impact/ImpactPage.tsx, frontend/src/features/timeline/TimelinePage.tsx, frontend/src/features/impact/useImpactParams.ts, frontend/src/features/impact/WindowControls.tsx, frontend/src/features/impact/CorrelationBanner.tsx, .planning/phases/10-event-impact/10-UI-SPEC.md</read_first>
  <action>`ImpactPage`의 골격 본문을 교체한다. `useImpactParams()`로 `{ itemId, window, setItem, setWindow }`, `useItems()`로 옵션 리스트 + D-06 기본값을 얻는다. 기본 선택: `itemId == null` && items 로드 완료면 effect에서 `setItem(items[0].id)`(`?item=` 있으면 itemId가 non-null이라 no-op). 선택 품목 `displayName`은 items에서 resolve해 `LatestPriceCard`에 전달. 레이아웃 루트는 `div.space-y-6`: 맨 위 `<CorrelationBanner />`(전체 폭·상시, D-10), 라우트 제목 `<h1 className="text-[28px] leading-tight font-semibold">이벤트 영향</h1>`, 컨트롤 바 `<div className="flex flex-wrap items-end gap-4"><ItemSelect value={itemId} onChange={setItem} /><WindowControls window={window} onWindowChange={setWindow} /></div>`, 그다음 `itemId != null`일 때만 `<LatestPriceCard itemId={itemId} displayName={displayName} />`(자기 AsyncBoundary 격리, D-12). 결과 영역 자리는 Task 2가 채울 명확한 placeholder로 둔다. DashboardPage/TimelinePage의 space-y 컨벤션·AppLayout max-width 상속을 따른다.</action>
  <verify>
    <automated>cd frontend && grep -q "useImpactParams" src/features/impact/ImpactPage.tsx && grep -q "CorrelationBanner" src/features/impact/ImpactPage.tsx && grep -q "WindowControls" src/features/impact/ImpactPage.tsx && grep -q "ItemSelect" src/features/impact/ImpactPage.tsx && grep -q "LatestPriceCard" src/features/impact/ImpactPage.tsx && grep -q "이벤트 영향" src/features/impact/ImpactPage.tsx && grep -q "items\[0\]" src/features/impact/ImpactPage.tsx && npm run build</automated>
  </verify>
  <done>ImpactPage가 배너+제목+컨트롤 바(ItemSelect+WindowControls)+LatestPriceCard를 useImpactParams에 배선해 조립하고, D-06으로 ?item= 부재 시 첫 품목 자동 선택; build 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 결과 영역 — useEventImpact + ApiError 400/404/200-empty 분기(D-03/D-09/D-11)</name>
  <files>frontend/src/features/impact/ImpactPage.tsx</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/lib/queries.ts, frontend/src/features/impact/EventImpactTable.tsx, frontend/src/features/impact/EventImpactCards.tsx, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/state/EmptyState.tsx, .planning/phases/10-event-impact/10-UI-SPEC.md</read_first>
  <action>`ImpactPage`에 결과 영역을 자체 스코프 컴포넌트(예: 같은 파일 내부의 `ImpactResults({ itemId, window, onResetWindow })`)로 추가하고 `itemId != null`일 때만 렌더한다(최신가 카드 boundary와 독립, D-12). 내부에서 `const impact = useEventImpact(itemId, window)`. `impact.status === 'pending'` → 표 자리 `Skeleton`(예: `h-[320px] w-full`) + "불러오는 중…". `impact.status === 'error'` → `const status = impact.error instanceof ApiError ? impact.error.status : null`: `400` → `<Alert variant="destructive">` 제목 "윈도우 값을 다시 확인해 주세요" + 본문 "윈도우는 1~168시간 사이의 정수여야 합니다. 프리셋 버튼을 누르거나 범위 안의 값을 입력해 다시 불러오세요." + `<Button onClick={onResetWindow}>24시간으로 보기</Button>`(D-03 복구); `404` → `<Alert>`(default) 제목 "존재하지 않는 품목이에요" + 본문 "선택한 품목을 찾을 수 없습니다. 위 목록에서 다른 품목을 선택해 주세요."; 그 외 → `<ErrorState onRetry={() => impact.refetch()} />`. 성공 시 `impact.data.events.length === 0` → `<EmptyState body="이 품목에 연결된 게임 이벤트가 아직 없습니다. 다른 품목을 선택하거나 관리자가 이벤트를 등록하면 표시됩니다." />`로 빈 상태(D-11; EmptyState 제목이 고정이라 필요 시 inline 제목 "등록된 이벤트가 없어요"를 함께 렌더). events가 있으면 `<EventImpactTable events={impact.data.events} />` + `<EventImpactCards events={impact.data.events} />`를 함께 렌더(반응형, D-09). 상위 ImpactPage에서 `onResetWindow={() => setWindow(24)}`로 전달(IMPCT-01 기본 window=24h 리셋). 모든 카피는 10-UI-SPEC와 축자 일치, 카드/표 정렬은 백엔드 occurred_at desc 유지.</action>
  <verify>
    <automated>cd frontend && grep -q "useEventImpact" src/features/impact/ImpactPage.tsx && grep -q "ApiError" src/features/impact/ImpactPage.tsx && grep -q "윈도우 값을 다시 확인해 주세요" src/features/impact/ImpactPage.tsx && grep -q "24시간으로 보기" src/features/impact/ImpactPage.tsx && grep -q "존재하지 않는 품목이에요" src/features/impact/ImpactPage.tsx && grep -q "관리자가 이벤트를 등록하면" src/features/impact/ImpactPage.tsx && grep -q "EventImpactTable" src/features/impact/ImpactPage.tsx && grep -q "EventImpactCards" src/features/impact/ImpactPage.tsx && npm run build</automated>
  </verify>
  <done>결과 영역이 ApiError.status로 400(+"24시간으로 보기" CTA)/404/200-empty(D-11)/네트워크를 구분 카피로 처리하고, 성공 시 표+카드(D-09)를 렌더; 최신가 boundary와 독립; build 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): 상관≠인과 가시성 · insufficient "왜" 납득성 · KST 정합 직접 확인</name>
  <files>frontend/src/features/impact/ImpactPage.tsx</files>
  <action>휴먼 리뷰를 위해 PAUSE — ROADMAP 사용자 확인 포인트(상관≠인과 고지가 과대해석을 막을 만큼 눈에 띄는지, insufficient의 "왜"(희소/stale)가 납득되게 쓰였는지)와 changeRate %·부호·KST 정합은 빌드가 증명할 수 없다. Claude는 먼저 앱이 구동 가능한지 보장(seed 프로파일 백엔드 :8080 + `cd frontend && npm run dev`)한 뒤 아래 <what-built>/<how-to-verify>/<resume-signal>을 제시하고 재개 신호를 기다린다. 자동 진행 금지.</action>
  <what-built>이벤트 영향 화면 전체: 상관≠인과 상시 배너 + 품목 셀렉터 + window 프리셋(6/24/72h)·숫자 입력 + 최신가 카드 + 이벤트별 전후 변화율 표/카드(ok/insufficient 혼합, 희소/stale 이유·앵커 노출) + window 400 / 없는 품목 404 / 이벤트 0개 빈 상태. 백엔드는 seed 프로파일(SPRING_PROFILES_ACTIVE=seed) :8080, 프론트는 `cd frontend && npm run dev`.</what-built>
  <how-to-verify>
    1. `/impact` 진입 — 첫 품목 자동 선택 + 기본 window 24h로 결과가 즉시 보이고, 상단 "상관 ≠ 인과" 배너가 표보다 먼저 눈에 들어오는지 확인(ROADMAP 사용자 확인 포인트 — 과대해석 차단).
    2. seed 데모 이벤트의 ok 행이 0이 아닌 changeRate를 상승=빨강/하락=파랑으로 보이는지, `curl 'http://localhost:8080/api/items/{id}/event-impact?window=24'` raw 응답의 changeRate(비율)와 화면 %(×100)가 부합하는지 확인(검증 방법 — % 부합).
    3. window를 좁혀 anchor가 비도록 만들어 insufficient(희소)가, 오래된 구간으로 stale이 뜨는지, 각 이유 카피와 앵커 시각(KST)이 응답 JSON status/anchor와 일치하는지 확인 — insufficient "왜"가 납득되는지 판단(사용자 확인 포인트).
    4. window 입력에 0과 200을 넣어 각각 400 "윈도우 값을 다시 확인해 주세요" + "24시간으로 보기" CTA가, URL에 없는 품목 id를 넣어 404가 뜨는지 확인.
    5. 발생 시각·앵커 시각 KST 라벨이 데모 이벤트 시각과 9시간 드리프트 없이 정합하는지 확인.
    6. 브라우저 폭을 좁혀(<768px) 표→카드 전환(D-04)이 자연스러운지 확인.
  </how-to-verify>
  <verify><human-check>개발자가 상관≠인과 가시성, insufficient 희소/stale 납득성, changeRate % 부합, KST 9시간 드리프트 없음, 400/404/빈 상태 카피, 반응형 전환을 확인.</human-check></verify>
  <resume-signal>"approved" 입력 또는 이슈 기술(배너 가시성 / insufficient 카피 / % 부합 / KST 정합 / 에러 카피 / 반응형)</resume-signal>
  <done>개발자가 헤드라인 화면의 정직성(상관≠인과·희소/stale)·% 부합·KST 정합·에러 카피·반응형을 검증(또는 필요한 수정 기록).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 API → 화면 | useItems/useEventImpact 응답이 선택·결과·에러 카피를 구동(zod 검증 upstream; ApiError는 HTTP status만 운반). |
| 손편집 URL → 상태 | `?item=`/`?window=`가 화면 상태를 결정 — 위조 값은 백엔드 404/400으로 귀결. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | 결과 영역 에러 분기 | mitigate | 400/404/네트워크 분기는 `ApiError.status`(숫자)에만 의존해 고정 한국어 카피를 렌더 — 예외 메시지·스택·URL·응답 본문을 노출하지 않는다(Phase 7/9 no-raw-error 규칙 연장). |
| T-10-04-02 | Tampering | `?item=`/`?window=` 손편집 | accept | 위조 item id → 404 카피, 범위밖 window → 400 카피로 우아하게 처리. read-only 백엔드라 쓰기/인증 악용 경로 없음. |
| T-10-04-03 | Denial of Service | 셀렉터/window 급변경으로 쿼리 재발 | accept | React Query가 queryKey(['event-impact', id, window])로 dedupe/cache; 백엔드는 windowHours를 168h로 캡하고 스냅샷을 단일 범위 읽기로 처리(N+1 회피). 클라이언트발 무한 부하 없음. |
</threat_model>

<verification>
- `cd frontend && npm run build`가 타입 에러 0으로 통과한다.
- `/impact`가 배너(D-10)+컨트롤(ItemSelect+WindowControls, D-01)+최신가(D-12)+결과 영역을 10-UI-SPEC Layout대로 조립; D-06 첫 품목 자동선택(?item= 우선)·기본 window 24h.
- 결과 영역이 `ApiError.status`로 400(+"24시간으로 보기")/404/200-empty(D-11)/네트워크를 구분 카피로 처리(D-03/D-09), 카피는 10-UI-SPEC 축자.
- 최신가 카드와 결과 영역이 독립 비동기 스코프(D-12).
- 성공 시 `EventImpactTable`+`EventImpactCards`가 ok·insufficient 혼합 목록을 반응형으로 렌더(D-09).
- 휴먼 체크포인트가 상관≠인과 가시성·insufficient 납득성·% 부합·KST 정합·반응형을 확인.
- Java `src/` 무변경; 라우트 트리 불변.

## 회귀 안전 (기존 기능 보존)
- `ImpactPage.tsx` 본문만 교체하고 라우트·`AppLayout`·내비·공용 컴포넌트(`ItemSelect`/`LatestPriceCard`)는 import-only로 소비 → Phase 8·9 화면(/, /timeline) 무영향. build가 전체 타입 그래프로 회귀를 차단하고, 휴먼 체크포인트가 런타임 회귀(드리프트·카피)를 잡는다.
</verification>

<success_criteria>
- IMPCT-01~04가 한 화면에서 완성: window 조회(01)·전후 변화율 표시(02)·ok/insufficient 희소·stale 구분(03)·상관≠인과 고지(04).
- 진입 비-empty·상태 정직 에러·헤드라인 정직성(상관≠인과·honest-data)이 빌드+휴먼으로 검증된다.
- build green, 백엔드 무변경.
</success_criteria>

<output>
Create `.planning/phases/10-event-impact/10-04-SUMMARY.md` when done.
</output>
</content>
